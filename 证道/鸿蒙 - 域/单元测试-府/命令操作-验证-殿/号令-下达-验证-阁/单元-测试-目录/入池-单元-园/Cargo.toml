[package]
name = "mingling_test_fu"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

# 四轴 mock features 分轴开关：依赖故障-园按需启用，避免多 worker 共享构建产物并发污染。
[features]
default = []
mock-daoshu = []
mock-shihai = []
mock-tianting = []
mock-rizhi = []

[dependencies]
mingling_fu = { path = "../../../../../../../乾坤/呈现-域/命令操作-府" }
serial_test = "3.0"