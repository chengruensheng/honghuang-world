# 上下文 · 识海承载-府

> 定位：**心智模型（世界之脑）**——给项目造记忆体，而不是数据库。
> 落位：`鸿蒙 / 基础设施-域 / 识海承载-府`（lib = `shihai_fu`）。
> 本质：**代码记事实，LLM 补语义，人兜底填根。**
> 状态：最终命名定稿 · 活文档（后续新增格位/检索规则并入对应小节）。

---

## 一、职责

三个环节一条链：

```
编码（谁来记） → 存储（全都在） → 检索（按需回想）
```

| 殿 | 环节 | 内容（实际落地） |
|:--|:--|:--|
| **铭记-殿** | 编码 | 代码扫描（事实）/ 语义播种（LLM 按种子）/ 人类录入（根）/ **地道-精炼**（执行期增量收尾：教训归因、机制提炼、波及追溯） |
| **纳藏-殿** | 存储 | 36 格位（72 全量）+ 依赖图索引 + 会话记录 + **回滚垫**（写操作前快照，任务失败单文件撤销） |
| **回想-殿** | 检索 | 顺序三档投影（首因+近因）+ 角色权限过滤 + 三级缓存 + 预算裁剪 |

**实际阁园结构**：

- **铭记-殿**：人类-录入-阁（录入-落根）· 代码-扫描-阁（增量-检测 / 扫描-落格位 / 批量-登记）· 地道-精炼-阁（教训-归因 / 机制-提炼 / 波及-追溯）· 语义-播种-阁（播种-归纳 / 播种-提示-模板 / 符号-补解释 / **临时代偿**：人类格位未录时 LLM 探索项目生成临时内容，人类可覆盖为经）
- **纳藏-殿**：会话-记录-阁（会话-归档）· 变更-存档-阁（回滚-垫）· 事件-流-阁（追加-落流：append-only 事实源，进程级互斥锁防并发行交错）· 坐标-索引-阁（依赖-边 / 网格-定位）· 模型-存储-阁（格位-清单 / 模型-落盘 / 类型-声明）
- **回想-殿**：投影-拼装-阁（三档-拼装）· 权限-过滤-阁（角色-过滤 + 消息可见性）· 预算-缓存-阁（分级-缓存）

> 回滚垫 = 写操作事务备份（`.上下文/回滚垫/{任务id}/`，成功即清理），与格位领域数据完全独立，零耦合；消费者在道术施展-府写工具，借本府工作区定位设施落位。

**世界格位的扫描与拼装**（事实层，供执行者 64→512 定位）：

- **环境·依赖**：扫所有府的 `Cargo.toml` 依赖段，按「府 → 依赖列表」聚合落格位；执行者据此只引用本府已声明的依赖，不越界引外部库。
- **结构**：府级快照（域/府路径）落格位；殿/阁/园细节存依赖图结构树，执行时按需下探。
- **符号语义**：依赖图符号的解释先取 `///` 注释；空解释符号经「记忆 补语义」唤起 LLM 读源码补一句语义写回依赖图，重扫时保留（不覆盖）。
- **文件级占位档案**：无 `pub` 符号的文件（如纯测试文件）在依赖图中补一条文件级占位档案（符号名 = 文件名，代码 = 全文），保证「查涉及文件」按文件路径能命中，不落空回退兜底接线文件。
- **现状拼装**（执行层读事实）= 结构下探（府→殿→阁→园）+ 相关文件的符号摘要（`pub` 符号 + 解释）+ 文件全文。

## 二、存储：36 格位（六范畴）

| 范畴 | 格位（例） | 谁写 |
|:--|:--|:--|
| 目标 | 初心·使命 / 任务 / 目标 / 验收标准 | 人类 + LLM |
| 规则 | 铁律·总纲 / 标准 / 架构 / 细则·解读 | 人类（经） |
| 自我 | 价值观·原则 / 身份 / 底线 / 能力 / 权限 | 人类（经） |
| 程序 | 环节规则 / 例外·临时 | LLM |
| 世界 | 环境·依赖 / 结构 / 文件 / 数据 / 调用 / 状态 | 代码 |
| 经历 | 事件 / 变更 / 产物 / 教训 / 理解·记忆 | 代码 + LLM |

每条记录必带**证据**，来源可信度：**代码 > 人类 > LLM**。

## 三、会话记录（工作记忆）

- 颗粒度 = **会话级**：一次任务执行 = 一个会话。
- 会话记录完整保留（要求书 / 设计方案 / 执行产物 / 验收回执），不截断、不重拼。
- 会话终点 → 归档进经历格位 → 清空会话缓存。
- 并发多任务 = 多会话并行，各会话独立，互不污染。

## 四、预算切分

- **最低预算输出**：工作区前缀 + 最前档经格位（首因）+ 最后档当前（近因）+ 会话记录。
- **上限动态提升**：需要更多上下文时，用索引定位补中间档，上限随实际需求增长，不预设硬顶。
- **会话独立预算**：会话记录（工作记忆）设独立预算，与 36 格位（长时记忆）分开计量、互不挤占；大小由会话内容自然界定，终点归档释放。
- 三级缓存：永久缓存（经格位）/ 版本缓存（权格位）/ 会话缓存（会话记录）。

## 五、被谁依赖

## 八、接单门维度 5 改查 + 批量修正（2026-08-25 推翻原 §8 方案）

> 动机：接单门维度 5「可验证性」靠关键词匹配是弱信号，需真实知道测试覆盖。
> 定位：**测试覆盖是接单门的闭包查询逻辑，不入格位**（AGENTS §17 + 多智能体 §21.3「格位边界例外」）。
> 状态：本节是设计纲要，落码待下次会话做。
> **推翻原 §8 方案**（commit fea3ada "合并进「文件」格位 + 自建扫描器"）——原方案违反 §21.3 格位边界 + §14.20 reducer 思想（机械判定归代码 + LLM 补语义）+ §22 抗模型更新（应接口式而非硬编码）。

### 8.1 判别机制（4 层瀑布）

接单门调闭包查"涉及文件是否被测覆盖"，按优先级：

| 层 | 动作 | IO |
|:--|:--|:--|
| ① 路径前缀过筛 | 命中 `证道/` / `/验证殿/` / `/验证阁/` / `/验证园/` / 文件名 `测试.rs`/`tests.rs`/`*测试*.rs` → 天然过 | 零 |
| ② 代码内测试符号扫描 | 扫 `#[test]` / `#[cfg(test)]` / `mod 测试` / `mod tests` 等测试独属符号；完全命中 → 过；部分命中（模糊）→ ③ | 零（依赖图已读全文）|
| ③ LLM 现场确认（仅模糊）| LLM 看代码上下文（测试宏/helper/示例/fixture）→ 判定：是测试 / 不是测试 / 边缘 | 单次 LLM |
| ④ 反向匹配（仅生产文件）| 证道域路径含涉及文件基名 → 有覆盖 → 过；无 → 拒 | 零 |

### 8.2 LLM 判定缓存

结果缓存到 `符号档案::测试覆盖判定: Option<测试覆盖判定>` 字段（`#[serde(default)]` 兼容旧档案）：

```
enum 测试覆盖判定 { 未判定, 是测试, 不是测试, 边缘 }
```

依赖图扫描已读全文 → 零额外 IO 算字段值；LLM 判定写回；下次扫描保留（不覆盖人工修正）。

### 8.3 误判代码批量修正流程

LLM 判定后**不当场改代码**——而是录入"待修正名单"，由巡世驱动批量执行：

- **队列位置**：`.上下文/状态/待修正-测试标识.jsonl`（§21.3「格位边界例外」+ §14.20 append-only reducer）
- **每条格式**：`{文件路径, 动作:加#[test]/去#[test]/加注释审, LLM摘录, 时间戳, 实体键=路径}`
- **触发**：巡世每轮扫一次 + 名单 ≥10 条立即触发
- **执行者**：巡世驱动末尾新增阶段（不开新线程，§6.4）
- **每条事务**：① 回滚垫备份 ② edit 修改 ③ cargo check 验证 ④ 失败回滚
- **粒度**：每条独立回滚（一损不损其他）
- **全部跑完**：清空名单 / 归档

### 8.4 接单门签名扩展

`评估接单` 加 `Option<F>` 闭包参数（F: `Fn(&str) -> bool`）：

- `None` → 旧关键词逻辑（兼容现有 11 个 `#[test]`）
- `Some(闭包)` → 走 4 层瀑布判定
- 调用方（调度驱动）实现闭包：路径前缀 + 依赖图 + 证道域 + LLM

### 8.5 触发条件

- 本会话**入稿** + commit
- 下次会话**落码**：
  1. 路径前缀 helper `是_测试文件(路径)` → `调度-接单-园/`
  2. 依赖图档案加 `测试覆盖判定` 字段（`#[serde(default)]` 兼容）
  3. `扫描依赖边` 第一遍算测试符号命中（已有 `#[test]`/`#[cfg(test)]` 检测可复用）
  4. `评估接单` 扩签名 + 4 层瀑布实现
  5. 误判名单格式 + 巡世驱动新增阶段
  6. 加单测：路径前缀 / 符号扫描 / 闭包逻辑 / 批量回滚

- **范围守则**：① 闭包接单门签名扩展（不破坏现有 11 测试）② 依赖图字段加 `#[serde(default)]` 兼容旧档案 ③ 名单走 `.上下文/状态/`（§21.3 例外）

**依据**：本会话用户拍板"按 C-最终版走" + AGENTS §17（不新立格位）+ 多智能体 §21.3（格位边界例外）+ §14.20（reducer 思想）+ §22（抗模型更新）+ §6.1（代码记事实）+ §6.4（热路径不调 LLM）

- 天庭治理-府（组织）：读投影 / 回填记忆。
- 道术施展-府（执行）：每个角色执行时读投影（任务 + 规则 + 世界）。

---

## 十、观测落点修正（2026-08-25）

### 10.1 问题（脏落点漂移）

`观测探针-府` 的 `观测目录()` 函数在 `WORLD_WORKSPACE_ROOT` 已设置时，落到
`<WORLD_WORKSPACE_ROOT>/观测/`（即项目根目录），与设计稿规定的 `.上下文/观测/`
不一致。注释 `入口.rs:204` 写的是 `.上下文/观测/记录.jsonl`，但实际代码把
WORLD_WORKSPACE_ROOT 当根。

后果：
- 根目录出现 `观测/记录.jsonl`（untracked，污染源码树，违反 AGENTS §10）
- 历史观测数据分散在两处（`.上下文/观测/` 与 `观测/`），无法合并检索

### 10.2 修正（统一格式）

`观测探针-府` 的 `观测目录()` 改为：
```rust
fn 观测目录() -> PathBuf {
    let 根 = std::env::var("WORLD_WORKSPACE_ROOT")
        .map(PathBuf::from)
        .unwrap_or_else(|_| PathBuf::from("."));
    根.join(".上下文").join("观测")
}
```

要点：
- 设了 `WORLD_WORKSPACE_ROOT`：落 `<root>/.上下文/观测`
- 未设：落 `./.上下文/观测`
- 与 `shihai_fu::工作区::定位().上下文目录()` 行为对齐，但因 `shihai_fu` 已依赖 `jiance_fu`
  （反向依赖会循环），`jiance_fu` 不引入 `shihai_fu` 依赖，硬编码 `.上下文` 字符串。

### 10.3 数据迁移

`观测/记录.jsonl` 的历史记录 append 到 `.上下文/观测/记录.jsonl`，保留审计追溯。
`观测/` 目录删除。`.gitignore` 加 `观测/` 防止再生。

### 10.4 测试

`观测探针-府` 加 2 测试：
- `观测目录_设了_world_workspace_root_落在上下文目录下` —— 设了变量验证路径
- `观测目录_未设_world_workspace_root_落在当前目录下上下文` —— 未设变量回落

并发保护：`测试设施::观测根环境锁`（与 `mingling_fu::工作区测试锁` 模式一致）。

依据：AGENTS §8（先入稿、再落码）+ AGENTS §10（不污染源码树）+ §6.4 代码记事实。

---

## 十一、工具 manifest 契约（2026-08-25 入稿）

**背景**：道术施展-府 下的「手脚-施展-殿」现有 9 个工具升级为「15+ 工具统一乐高桶」。每个工具有完整的 JSON-Schema 描述（输入/输出/副作用/可并行），供编排器（LLM）看 schema 决定怎么用，**不用写硬编码调度逻辑**。

### 11.1 manifest.json 格式契约

每个工具包目录里放 `manifest.json` + 一个或多个实现文件。daoshu_fu 启动时扫描注册。

```json
{
  "name": "tool:grep_search",
  "description": "在文件内容中搜索正则表达式，返回匹配行+文件名+行号",
  "input_schema": {
    "type": "object",
    "properties": {
      "pattern": { "type": "string", "description": "正则表达式" },
      "path": { "type": "string", "description": "相对工作区根的搜索路径" },
      "case_sensitive": { "type": "boolean", "default": true },
      "max_results": { "type": "integer", "default": 100 }
    },
    "required": ["pattern", "path"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "matches": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "file": { "type": "string" },
            "line": { "type": "integer" },
            "content": { "type": "string" }
          }
        }
      },
      "truncated": { "type": "boolean" }
    }
  },
  "tags": ["探查", "可并行", "无副作用"],
  "side_effects": "none",
  "rate_limit": null
}
```

### 11.2 字段语义

| 字段 | 类型 | 必填 | 说明 |
|:--|:--|:--|:--|
| `name` | string | ✓ | 工具唯一标识 `tool:{功能}` 命名空间 |
| `description` | string | ✓ | LLM 看这句决定是否调此工具（一句话讲清用途+输入+输出） |
| `input_schema` | JSON-Schema | ✓ | 输入参数 schema，LLM 据此构造调用参数 |
| `output_schema` | JSON-Schema | ✓ | 输出 schema，LLM 据此解析返回值 |
| `tags` | string[] | ✓ | 标签：探查/编辑/执行/版本/编排、可并行、只读 |
| `side_effects` | enum | ✓ | `none`（无副作用）/ `mutating`（修改文件或状态）/ `external`（调用外部） |
| `rate_limit` | int? | ✗ | 每分钟最大调用次数，null=不限 |

### 11.3 可并行性判定

**`tags` 含「可并行」且 `side_effects: none` 才允许并发**：
- ✅ 探查类（read / grep / glob / list / git_log）天然可并行
- ❌ 编辑类（write / edit / apply_patch）有副作用，禁止并行
- ❌ 执行类（run_command / run_test / run_clippy / run_fmt）状态不确定，禁止并行
- ❌ 版本类（git_diff / git_commit）有副作用，禁止并行

编排器实现：`tokio::join!` 同一并行组内的多个探查调用，结果按调用顺序收集进 shijian_fu 总线。

### 11.4 注册流程

```
daoshu_fu 启动 →
  扫描 tools/manifest/*.json →
  解析每个 manifest →
  校验 schema 合法性 + 名称唯一性 →
  注册到 in-memory 注册表 →
  暴露 daoshu_fu::工具清单::清单() 给编排器
```

落位：`鸿蒙/基础设施-域/道术施展-府/tools/manifest/` 目录（新增）+ 现有 手脚-施展-殿 9 个工具改 manifest 驱动。

### 11.5 工具清单（15+ 工具统一乐高桶）

| 类别 | 工具 | 可并行 | 副作用 |
|:--|:--|:--|:--|
| 探查 | `tool:read_file_lines` | ✓ | 无 |
| 探查 | `tool:grep_search` | ✓ | 无 |
| 探查 | `tool:glob_search` | ✓ | 无 |
| 探查 | `tool:list_dir` | ✓ | 无 |
| 探查 | `tool:read_memory` | ✓ | 无 |
| 编辑 | `tool:write_file` | ✗ | 有 |
| 编辑 | `tool:edit_file` | ✗ | 有 |
| 编辑 | `tool:apply_patch` | ✗ | 有 |
| 执行 | `tool:run_command` | ✗ | 有 |
| 执行 | `tool:run_test` | ✗ | 有 |
| 执行 | `tool:run_clippy` | ✗ | 有 |
| 执行 | `tool:run_fmt` | ✗ | 有 |
| 版本 | `tool:git_diff` | ✗ | 有 |
| 版本 | `tool:git_log` | ✓ | 无 |
| 版本 | `tool:git_commit` | ✗ | 有 |
| 编排 | `tool:plan_task` | ✓ | 无 |
| 编排 | `tool:query_llm` | ✓ | 无 |

### 11.6 参照 deepseek-harness tool-catalog

本设计参照 `E:\trae\deepseek-harness\docs\tool-catalog.md`：
- 每个工具 boot 时读 `ctx.tools.schemas()`（运行时注册，非静态编译期）；
- manifest 完整生成器校验 `packages/*/tool-*` 完整性（新增工具不能静默漏文档）；
- 我们的实现：daoshu_fu 启动时扫 `tools/manifest/*.json` 注册到内存表。

### 11.7 不变量

- **工具名称全局唯一**：`tool:{功能}` 命名空间，禁止重复；
- **manifest 启动期校验**：启动时扫描，缺字段或非法 JSON 直接报错（fail-fast）；
- **审计不可绕过**：每次工具调用结果都进 shijian_fu 总线（与现有事件总线契约一致）；
- **热重载暂不支持**：v1 期间启动期注册，v2 再做热重载。

### 11.8 落位

- 工具清单契约：`鸿蒙/基础设施-域/道术施展-府/tools/manifest/*.json`
- 注册逻辑：`鸿蒙/基础设施-域/道术施展-府/手脚-施展-殿/` 改造 + `daoshu_fu::工具清单` 模块
- 暴露接口：`daoshu_fu::工具清单::清单() -> Vec<工具清单项>`
- 编排器调用：`道术施展-府/任务-调度-殿` 改造点见 智能体.md §五 + 多智能体 §1.5.6

### 11.9 依据

- AGENTS §8（先入稿再落码）+ §3 命名规范（中文 + 判据 A/B）
- 上下文 §六.5 可操作规则格位注入（规则从格位读，不硬编码）
- 多智能体 §19.7 接单门 LLM 辅助确认机制（已有 LLM 现场确认模式可复用）
- 上下文 §8 测试覆盖 4 层瀑布（已用 manifest 思维）
- 多智能体 §1.5.6 工具乐高化（设计纲要）
- 智能体 §五 工具乐高化与编排演化（设计纲要）
