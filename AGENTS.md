1.执行任何任务开始前请先详细阅读并了解文档后再执行任务,文档路径为
【
1.D:\洪荒 - 世界\层级结构-设计.md
2.D:\洪荒 - 世界\多智能体架构设计.md
3.D:\洪荒 - 世界\项目心智模型-设计稿.md
4.D:\洪荒 - 世界\融合蓝图-设计稿.md
5.D:\洪荒 - 世界\上下文.md
6.D:\洪荒 - 世界\智能体.md

】,请确保严格按照文档中的要求执行任务。
2.临时测试或者临时脚本，文档等，需要在根目录创建临时文件夹，文件夹名称为：临时文件夹,在其中执行任务。
3.在执行任务时，关于命名于注释，严禁过度使用AI语气。避免AI味的命名和注释。
4.本项目为新项目，所有任务都需要从头开始执行。严禁直接使用老项目，老项目唯一作用仅参考，以及当作目标项目用于测试，当作验证域将复杂验证脚本与结果存储在老项目，避免污染本项目
5.日志已统一落地，勿重复实现：技术日志走 `鸿蒙/基础设施 - 域/日志记录-府`（lib=rizhi_fu，tracing 定制封装），各府经 lib 根调用；业务流水是领域数据，走识海承载-府，两者勿混。
6.后续开发新东西（新建府殿阁园、新增代码文件、新增业务逻辑）时，必须主动将日志埋点跟上，统一经日志记录-府 lib 根调用，不得遗漏。
7.排查问题或检查数据时，先看实际产出内容（完整字段、完整提示词、落盘记录原文、中间产物），而非代码定义、文件位置或摘要概括；看到真实内容再下结论。
8.先入稿、再落码：新增设计（新府殿阁园、新业务逻辑、新机制）必须先并入对应设计稿对应章节，再实现；实现须能在设计稿溯源，溯不到源视为补丁。设计稿随世界生长，不另立补丁文档。
9.临时文件夹只装临时产物（一次性脚本、临时验证、临时监控、临时比对记录、调试日志、备份 .diff/.head、过期设计草案等）；任务结束或版本定档后须清空。被进程锁定的文件（如正在运行的监控日志）须先结束对应进程再删，不要绕过去。临时文件夹不进入 git，已在 .gitignore 排除。
10.本项目内严禁出现 src/、tests/、scripts/ 这类空目录或源码，无论根目录或子目录：源码（含测试、基准、性能）按「维度/域/府/殿/阁/园」六层落点，不在源码区放平铺目录；如发现，立即删除并补 .gitignore 防再生。工具链自动生成的产物（如 cargo test 二进制）放 `道果树/构建物-域/`，不在源码区。
11.根目录严禁出现 IDE/工具本地配置目录（.arts/、.codeartsdoer/、.vscode/、.idea/ 等）：它们属于工具不属项目，已在 .gitignore 排除，但发现盘面仍存时直接删除。
12.`.env` 只放当前代码实际读取的配置项；过期/无效/指向错误路径的条目直接删，不要保留「以防万一」的死配置。
13.文档收割门：任何 `.md` 设计稿被 AI（含自动优化）迭代后，必须与迭代前对比；出现 `[object Object]`、内容断崖式缩水（<50%）、首尾丢失、章节级损坏，一律视为「文档污染事故」——立即回滚该文件并停止当轮自动优化。设计稿是宪法，宪法不许被吃掉。
14.甲阶段冻结令（v1 存档前）：`太初/混沌/道韵/量劫` 四根冻结，不新增代码、不写实现；呈现层（UI）权重最低——在没有达到 DeepSeek Harness 级「使用体验质量」之前，不考虑做 UI，界主明确拍板：核心（脑/身/心）优先于门面。
15.完成度刻度：带「占位」的入口 = 未完成；质量唯一标尺 = `cargo test --workspace` 全绿 + `cargo clippy --workspace -- -D warnings` 零警告。不确定「质量是否一致」时，不看文档、不看目录，只问这两条门禁过没过。
16.智能接单门约束：天道在候选池空时尝试自优化任务，必须过五维评估（候选池/世界状态/影响范围/可逆性/可验证性），且绝不触碰红线（`.git/`、`.md` 设计稿、`AGENTS` 铁律、`Cargo.toml` 核心字段、`.env` 凭据、核心算法逻辑、`.上下文/状态/*.jsonl` 历史、版本快照、LLM 凭据、`.github/workflows`、`Cargo.lock` 手写、甲阶段美化、工具链约定目录不可改名共 13 条）。补救（S0~S5）永远优先于预防（S6~S10）——S0 兜底防止全项目崩溃。依据：多智能体架构设计.md §19 本质驱动调度 v2 + §19.4 红线 13。
17.格位稀缺原则：36 格位是上限，不可新立。增加粒度只能合并旧格位腾位置（进退守恒）。高频使用格位不可被低频替换。依据：多智能体架构设计.md §21 格位设计原则。

## 命令清单

一键全验（10 项全过才退出 0）：
```
bash 一键全验.sh
```

单项验证（道果树/质量门禁-域/门禁-府/）：
```
pwsh 道果树/质量门禁-域/门禁-府/验证-格式.ps1      # cargo fmt --check
pwsh 道果树/质量门禁-域/门禁-府/验证-警告.ps1      # cargo clippy -D warnings
pwsh 道果树/质量门禁-域/门禁-府/验证-测试.ps1      # cargo test --workspace --lib
pwsh 道果树/质量门禁-域/门禁-府/验证-编译.ps1      # cargo check --all-targets
pwsh 道果树/质量门禁-域/门禁-府/验证-文档.ps1      # cargo doc --no-deps
pwsh 道果树/质量门禁-域/门禁-府/验证-审计.ps1      # cargo audit
pwsh 道果树/质量门禁-域/门禁-府/验证-依赖.ps1      # cargo deny check
pwsh 道果树/质量门禁-域/门禁-府/验证-无src目录.ps1  # AGENTS 第 10 条
pwsh 道果树/质量门禁-域/门禁-府/验证-无空目录.ps1
pwsh 道果树/质量门禁-域/门禁-府/验证-临时目录.ps1  # AGENTS 第 9 条
```

既有脚本（保留）：
```
pwsh 道果树/质量门禁-域/门禁-府/检查-全部.ps1      # 6 项综合检查
pwsh 道果树/质量门禁-域/门禁-府/检查-无-IDE-残留.ps1
pwsh 道果树/质量门禁-域/门禁-府/检查-AGENTS-一致性.ps1
pwsh 道果树/质量门禁-域/门禁-府/查重复.ps1
pwsh 道果树/质量门禁-域/门禁-府/生成依赖图.ps1
```
