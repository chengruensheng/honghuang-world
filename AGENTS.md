严格遵守以下目录中的规则（规则单源：约束条文一律以 rules/ 为唯一权威源，禁止在其他位置另建规则副本）：
   1.F:\洪荒 - 世界\rules 文件夹中的所有 .md 规则文档

规则三源边界（各归其位，不可混存）：
- 约束条文（人工维护，frontmatter 标注层级 大道/天道/临时）：rules/*.md —— 智能体认知格位的唯一种子源；修改或新增规则只改这里
- 运行时事实规则（五行引擎自动提炼）：规则.toml（dy_rule RuleSet 持久化）—— 条件→结论型，仅供五行联动评估消费，不得手写条文进去
- 任务级临时规则：看板任务 临时规则 字段 —— 任务执行期注入智能体上下文，终态自动清除，不落 rules/
