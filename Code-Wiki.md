# 洪荒 · 世界 — Code Wiki

> 本文档是「洪荒 · 世界」代码仓库的结构化索引：整体架构、模块职责、关键类与函数、依赖关系、运行方式。
> 依据：7 份设计文档 + 全部 crate 的 `Cargo.toml` / `入口.rs` / 关键实现文件实读。
> 随世界生长更新。

---

## 0. 快速导航

| 想了解 | 看这里 |
|:--|:--|
| 这是什么 | [§1 项目本质](#1-项目本质) |
| 整体怎么搭 | [§2 整体架构](#2-整体架构) |
| 目录长什么样 | [§3 目录结构](#3-目录结构) |
| 有哪些 crate | [§4 Workspace 与 crate 总览](#4-workspace-与-crate-总览) |
| 每个 crate 干啥 / 关键类型函数 | [§5 各府详解](#5-各府详解) |
| 插件化怎么做 | [§6 插件化架构](#6-插件化架构) |
| 谁依赖谁 | [§7 依赖关系](#7-依赖关系) |
| 一次迭代怎么跑 | [§8 关键数据流](#8-关键数据流) |
| 怎么构建/运行/测试 | [§9 项目运行方式](#9-项目运行方式) |
| 命令清单 | [§10 命令清单](#10-命令清单) |
| 核心机制 | [§11 核心机制清单](#11-核心机制清单) |
| 设计铁律 | [§12 设计约束与铁律](#12-设计约束与铁律) |
| 技术栈 | [§13 技术栈](#13-技术栈) |

---

## 1. 项目本质

用洪荒神话世界观建模的、会自己进化的多智能体软件开发系统。

- 界主（人·天意）发布想法 → 鸿钧化要求 → 圣人设计 → 大罗金仙实现 → 准圣六维验收 → 鸿钧终裁 → 版本存档 → 天道巡世进化。
- **世界自己写代码、自己验收、自己存档、自己进化。**

两阶段生长：
- **甲阶段（v1 实现期）**：功能优先，关闭自动优化（巡世/进化）。
- **乙阶段（自动优化期）**：v1 存档后永久进入，天道巡世 + 进化环全开，自主进化。当前已进入乙阶段。

一次完整迭代：**想法 → 要求书 → 设计 → 实现 → 验收 → 版本存档**。

---

## 2. 整体架构

### 2.1 六层结构（维度 → 域 → 府 → 殿 → 阁 → 园）

所有代码按六层切分，层层递进，**能力只在园落地**：

| 层 | 回答 | 承载 |
|:--|:--|:--|
| 维度 | 世界由什么构成 | 本体范畴 + 工程需求（9 根） |
| 域 | 靠什么活着 | 纯目录，无文件 |
| 府 | 什么能力 | 唯一编译单位（lib crate），引用止步点 |
| 殿 | 哪些模块 | `模块.rs` 桥接（只 `pub mod` + `pub use`） |
| 阁 | 什么功能 | `模块.rs` 桥接 |
| 园 | 怎么实现 | 实现代码 + `模块.rs` 桥接 |

文件落点三态：
- **域** = 纯目录，无文件。
- **府** = `入口.rs`（`Cargo.toml` 的 `lib.path` 指向它，只 `pub mod` + `pub use` 透出符号 + Service Definition）。
- **殿/阁/园** = 各一个 `模块.rs` 桥接，不写实现、不写编排。

### 2.2 九根维度

| 类别 | 维度 | 对应 | 状态 |
|:--|:--|:--|:--|
| 本体五根（创世链） | 太初 | 存在 | 骨架立·未启用 |
| | 混沌 | 可能性 | 骨架立·未启用 |
| | 乾坤 | 空间（容器） | 呈现侧已启用 |
| | 道韵 | 法则 | 未启用 |
| | 量劫 | 时间（劫数） | 未启用 |
| 工程四根（保障链） | 鸿蒙 | 地基（盘古） | **已启用（核心）** |
| | 证道 | 测试 | **已启用（单元测试-府）** |
| | 道果树 | 构建 | 构建产物目录在用 |
| | 传承殿 | 传承 | 骨架立（承载理解类文档） |

**鸿蒙 = 盘古 = 唯一地基**，承载所有通用能力（文件/网络/日志/密钥/缓存/事件总线/状态共享/模型连接等）。测试（证道）/ 构建（道果树）/ 文档（传承殿）是工程配套维度，不进运行时、不归鸿蒙。

### 2.3 四层职业（多智能体组织）

| 位阶 | 角色 | 职司（道） | 职责 | 铁律 |
|:--|:--|:--|:--|:--|
| 顶层 | 界主（人·天意） | — | 发布想法，不干预实现 | 只管想 |
| 天层 | 天道 | 巡世之神 | 长期巡逻世界（乙阶段） | 只发现不指挥 |
| 天层 | 鸿钧 | 道祖·主政 | 化要求/派遣/验收终裁/定档 | 不亲执行 |
| 中层 | 女娲 + 四圣 | 设计/评审 | 初稿设计 + 分角度评审 | 不担任执行者 |
| 底层 | 大罗金仙 | 代码/前端/数据库/监控 | 实现（写代码） | 按道分工不跨道 |
| 底层 | 六准圣 | 六维审验 | 各审一维，回呈鸿钧 | 只审验不终裁 |

一句话：**圣人定道，大罗行术，准圣验术，鸿钧终裁。**

### 2.4 三府身脑融合

心智模型（脑）与多智能体架构（身）融合为三个府：

```
                天庭治理-府 (tianting_fu)  组织编排 · 管组织
                      │ 调度执行          │ 读投影/回填
                      ▼                  ▼
                道术施展-府 (daoshu_fu)   识海承载-府 (shihai_fu)
                任务执行 · 管角色执行      脑 · 记忆
                      └───── 读投影 ──────┘
```

| 府 | 定位 | 一句话 |
|:--|:--|:--|
| 识海承载-府 | 世界之脑 | 代码记事实，LLM 补语义，人兜底填根 |
| 天庭治理-府 | 世界之身 | 想法 → 要求 → 设计 → 验收 → 版本 → 进化 |
| 道术施展-府 | 世界之身 | 每个角色按自己的道术跑 L1-L4 工作流 |

---

## 3. 目录结构

```
洪荒 - 世界/
├── 鸿蒙/                              # 地基维度
│   ├── 世界配置 - 域/
│   │   └── 配置管理-府/               # peizhi_fu
│   ├── 基础设施 - 域/
│   │   ├── 识海承载-府/               # shihai_fu
│   │   ├── 天庭治理-府/               # tianting_fu
│   │   ├── 道术施展-府/               # daoshu_fu
│   │   ├── 模型连接-府/               # moxing_fu
│   │   ├── 日志记录-府/               # rizhi_fu
│   │   ├── 事件总线-府/               # shijian_fu
│   │   ├── 插件承载-府/               # chajian_fu
│   │   └── 状态共享-府/               # zhuangtai_fu
│   └── 观测探针-府/                   # jiance_fu（直接落鸿蒙根，非域内）
├── 乾坤/                              # 空间维度
│   └── 呈现-域/
│       ├── 命令操作-府/               # mingling_fu（bin = 号令）
│       ├── 监控-域/                   # 临时 Python 监控（端口 3082）
│       └── 监控界面-府/               # 第十府 Python 直播界面（端口 8080）
├── 证道/                              # 测试维度
│   └── 鸿蒙 - 域/
│       └── 单元测试-府/               # zhengdao_fu（八殿镜像被测八府）
├── 太初/ 混沌/ 道韵/ 量劫/             # 骨架已立，暂空未启用
├── 道果树/                            # 构建维度（cargo 产物目录）
├── 传承殿/                            # 传承维度（承载理解/记录类文档）
├── .cargo/config.toml                 # 共享构建目录指向 道果树/构建物-域
├── Cargo.toml                         # workspace 根
├── README.md / 项目全景-理解.md        # 概览与全景理解
└── 层级结构-设计.md 等设计稿           # 6 份设计文档
```

运行时状态目录（工作区根下，环境变量 `WORLD_WORKSPACE_ROOT` 优先，回退当前目录）：
- `.上下文/格位/` — 识海记忆存储（每格位独立 .jsonl）
- `.上下文/状态/` — 世界状态、想法池、要求队列、验收历史
- `.上下文/装配.json` — 世界装配配置（阶段/扩展/角色册路径/合法产物扩展名）
- `.上下文/观测/记录.jsonl` — 白箱观测记录
- `.上下文/事件流.jsonl` — 业务事件流（append-only 事实源）
- `.上下文/依赖图.json` — 依赖图落盘

---

## 4. Workspace 与 crate 总览

`Cargo.toml`（workspace 根，`resolver = "2"`）共 12 个成员：

| 府 | lib 名 | 所在 | 类型 | 职责 |
|:--|:--|:--|:--|:--|
| 识海承载-府 | `shihai_fu` | 鸿蒙/基础设施-域 | lib | 记忆：编码/存储（36 格位）/检索（三档投影） |
| 天庭治理-府 | `tianting_fu` | 鸿蒙/基础设施-域 | lib | 组织编排：天层双神+队列+版本存档+进化环 |
| 道术施展-府 | `daoshu_fu` | 鸿蒙/基础设施-域 | lib | 任务执行：角色工作流引擎+手脚工具集 |
| 模型连接-府 | `moxing_fu` | 鸿蒙/基础设施-域 | lib | LLM 调用（OpenAI 兼容 function calling），配置可注入 |
| 日志记录-府 | `rizhi_fu` | 鸿蒙/基础设施-域 | lib | 技术日志（tracing 定制封装），各府经 lib 根调用 |
| 事件总线-府 | `shijian_fu` | 鸿蒙/基础设施-域 | lib | 进程内类型化事件分发（实时扩展点） |
| 插件承载-府 | `chajian_fu` | 鸿蒙/基础设施-域 | lib | 府插件接口 + 插件注册表（Service Definition 机制） |
| 状态共享-府 | `zhuangtai_fu` | 鸿蒙/基础设施-域 | lib | 跨府运行时状态共享（按类型 ID 索引） |
| 观测探针-府 | `jiance_fu` | 鸿蒙 | lib | 白箱可观测性（统一 `观测记录` + 交接处埋点） |
| 配置管理-府 | `peizhi_fu` | 鸿蒙/世界配置-域 | lib | 密钥/模型配置注入 + 世界装配读取 |
| 命令操作-府 | `mingling_fu` | 乾坤/呈现-域 | lib + bin(`号令`) | 操作台 CLI：命令解析/权限/观览/号令下达/输出 |
| 单元测试-府 | `zhengdao_fu` | 证道/鸿蒙-域 | lib | 测试承载：八殿镜像被测八府，黑盒验证 |

> 另有 `乾坤/呈现-域/监控界面-府`（Python，非 Rust crate）与 `监控-域`，为临时只读监控工具。

---

## 5. 各府详解

> 每府结构：定位 → 殿/阁/园职责 → 关键类型（struct/enum）→ 关键函数/Service Definition。

### 5.1 识海承载-府 `shihai_fu` — 世界之脑 · 记忆

**本质**：给项目造记忆体，不是数据库。代码记事实，LLM 补语义，人兜底填根。三殿：铭记（编码）· 纳藏（存储）· 回想（检索）。

#### 殿/阁/园

| 殿 | 阁 | 园 | 作用 |
|:--|:--|:--|:--|
| 铭记-殿（编码） | 代码-扫描-阁 | 扫描-执行-园 / 收集-园 / 符号-解析-园 / 批量-登记-园 / 增量-检测-园 | 六类扫描、源文件收集、符号提取、变更登记、增量检测 |
| | 地道-精炼-阁 | 教训-归因-园 / 机制-提炼-园 / 波及-追溯-园 | 失败归因→教训、复杂变更归纳机制、依赖图反查波及 |
| | 人类-录入-阁 | 录入-落根-园 | 人类录入根记录（格位须存在且固化度=经） |
| | 语义-播种-阁 | 播种-归纳-园 / 播种-提示-模板 / 符号-补解释-园 | 种子降级链归纳语义、LLM 补符号解释（「阴」守护进程） |
| 纳藏-殿（存储） | 变更-存档-阁 | 回滚-垫-园 | 写前快照，任务失败单文件撤销 |
| | 会话-记录-阁 | 会话-归档-园 | 会话缓存读写 + 归档到事件格位 |
| | 模型-存储-阁 | 格位-清单-园 / 类型-声明-园 / 模型-落盘-园 / 依赖-边-园 | 36/72 格位定义、类型契约、工作区定位+模型存储、依赖图落盘 |
| 回想-殿（检索） | 投影-拼装-阁 | 三档-拼装-园 | 元数据层化（首屏 3000 字符）+ 拼装投影（最前→最后→中间） |
| | 权限-过滤-阁 | 角色-过滤-园 | 角色范畴白名单 + 可见工具（私有仅界主） |
| | 预算-缓存-阁 | 分级-缓存-园 | 三级缓存（永久/版本/会话 + 投影指纹） |

#### 关键类型

- **格位**（心智模型基本单元）：名字 / 范畴 / 种子提示词 / 来源 / 固化度 / 共享度 / 顺序档位 / token 上限 / 推荐位置 / 存储。
- **记录**（格位里一条内容，必带证据）：格位名 / 内容 / 证据 / 时间戳 / 来源 / 前记录 / 坐标 / 失效 / 实体键（同实体成链，链头=最新）。
- **坐标**：层（项目/模块/文件/符号/代码）× 对象 × 属性。
- **会话记录**：会话 id / 内容 / 时间戳。
- **工具清单**：10 个工具名。
- **依赖图**：档案们（全项目符号集合）+ 结构树（完整目录层级）。
- 枚举：`范畴`（目标/规则/自我/程序/世界/经历）、`固化度`（经/权）、`共享度`（共享/私有）、`顺序档位`（最前/中间/最后）、`来源`（代码>人类>LLM）、`规则级别`（含临时天道）、`调用方层级`。

#### 关键函数 / Service Definition

```rust
// Service Definition（lib 根暴露）
pub trait 识海服务: Send + Sync {
    fn 回想(&self, 角色: &str, 格位们: &[格位], 预算字符: usize, 调用方: 调用方层级) -> Result<String, String>;
    fn 铐记(&self, 格位名: &str, 内容: &str, 来源: &str, 录入者: &str) -> Result<(), String>;
}
```
- `工作区::定位()` — 环境变量 `WORLD_WORKSPACE_ROOT` → 向上探测锚点（.上下文/Cargo.toml/AGENTS.md）→ 当前目录。
- `模型存储::在工作区(&工作区)` / `模型存储::打开(路径)` — 打开格位存储。
- `模型存储.写记录(&记录)` / `读格位(...)` / `读链头集(...)` — 记忆读写。
- `记录::新(格位名, 内容, 来源, 录入者)` / `记录::新带级别(...)` — 构造记录。
- `元数据层化(存储, 角色, 格位们, 预算, 调用方)` — 三档投影拼装。
- 插件：`识海插件` 实现 `chajian_fu::府插件`，`应用()` 注册 `Arc<dyn 识海服务>`。

#### 36 格位（日常主用，六范畴 × 顺序档位）

| 档位 | 格位名（节选） |
|:--|:--|
| 最前 | 初心·使命、铁律·总纲、价值观·原则、底线、身份、标准、架构、任务、状态 |
| 中间 | 方向、需求、期望·意向、能力、权限、自省·觉察、细则·解读、世界观、传承·决策、环节规则、例外·临时、环境·依赖、结构、文件、数据、调用、事件、变更、产物、教训、理解·记忆 |
| 最后 | 目标、验收标准、阶段·进度、阻塞、验收结果、承诺 |

> 复杂全量 72 格位 = 36 的细分（目标 13 / 规则 13 / 自我 9 / 程序 7 / 世界 16 / 经历 14）。

---

### 5.2 天庭治理-府 `tianting_fu` — 世界之身 · 组织编排

**本质**：想法 → 要求 → 设计 → 验收 → 版本 → 进化。八殿。

#### 殿/阁/园

| 殿 | 阁/园 | 作用 |
|:--|:--|:--|
| 类型-定义-殿 | 声明-集中-园 | 全部类型契约（14 枚举 + 16 结构） |
| 天层-主控-殿 | 鸿钧-主政-阁（需求-拆分-园 / 要求-提审-模板 / 主政-落笔-园[要求化/验收/终裁/模块树]）+ 鸿钧-对话-阁（对话-循环/意图-判别/汇报-拼装）+ 天道-巡世-阁（巡世-扫描-园） | 鸿钧回路 + 界主对话循环 + 天道巡世 |
| 智慧-主控-殿 | 设计-落笔-阁（模板-生成-园） | 中层分级设计（简单单轮/复杂多轮） |
| 队列-调度-殿 | 队列-存取-阁（落盘-取队-园） | jsonl 队列 + 八态推进 |
| 团队-调度-殿 | 执行-派遣-阁（派发-落单-园） | 拆解项 → 道术施展-府执行任务 |
| 版本-存档-殿 | 存档-落定-阁（快照-落库-园） | 增量快照 + 版本记录 + 回退 |
| 进化-主控-殿 | 进化-归因-阁（归因-留痕-园） | 失败归因 → 调整级别 |
| 驱动-入口-殿 | 世界-运行-阁（循环-驱动-园） | 世界运行主循环 |

#### 关键类型

- **枚举（14）**：阶段（甲/乙）、进入路径、要求来源、要求类别（功能/性能/美观/优化/维护/新能力/补基础）、**要求状态（八态）**、验收结论（通过/打回）、缺陷层（设计层/实现层）、调整级别（①-⑤）、优先级、成熟度、想法状态、构建状态、准圣维度（六维）、优先级。
- **结构（16）**：想法 / 约束 / 要求书 / 拆解项 / 设计方案 / 产物条目（含变化类型）/ 验收回执 / 版本记录 / 巡世候选 / 法则违逆 / 巡世报告 / 失败条目 / 进化记录 / 规模统计 / 项目档案 / **世界状态（12 字段）** / 终裁回执 / 准圣意见。
- **八态状态机**：待领 → 设计中 → 待确认 → 已确认 → 待实现 → 实现中 → 已验收 → 已存档（打回可回退）。
- **世界状态 12 字段**：阶段 / v1已存档 / 进入路径 / 长期记忆 / 界主想法池 / 在途要求 / 验收历史 / 失败模式 / 版本历史 / 巡世候选池 / 项目档案 / 天道报告库。
- **任务线 / 任务线状态**：多任务线并发调度。

#### 关键函数 / Service Definition

```rust
pub trait 天庭服务: Send + Sync {
    fn 调度要求(&self, 想法: &想法) -> Result<主政回执, String>;  // 调主政一轮
    fn 验收产物(&self, 要求id: &str) -> Result<(), String>;       // 阶段一占位
    fn 归档版本(&self, 要求id: &str) -> Result<(), String>;       // 阶段一占位
}
```
- `主政一轮(想法, 配置, 存储)` — 想法流转主流程（需求拆分 → 逐子要求串行全链）。
- `运行一轮(...)` — 元数据层化 → 依赖图加载 → 人类格位临时代偿 → 解析想法入池 → 模型设计 → 现状装配 → 图谱契约投影 → 教训注入 → 并发派遣 → 终裁 → 定档/打回重投。
- `临时天道守卫`（Drop 守卫）— 想法含【任务规则】段时写入「铁律·总纲」（级别=临时天道），任务结束写失效标记。
- `落点护栏(方向, 涉及路径)` — 涉及路径为空且非审验/核查类 → 拒绝放行。
- 插件：`天庭插件`，`注入() = ["识海承载-府", "道术施展-府"]`。

---

### 5.3 道术施展-府 `daoshu_fu` — 世界之身 · 任务执行

**本质**：角色工作流引擎 + 手脚工具集。每个角色按道术跑 L1-L4 工作流，只执行、不越级组织。五殿。

#### 殿/阁/园

| 殿 | 阁/园 | 作用 |
|:--|:--|:--|
| 类型-定义-殿 | 声明-集中-园 | 执行域契约 |
| 角色-卡册-殿 | 登记-落册-园 | 角色卡 HashMap（身份→执行角色），保存/加载 json |
| 工作流-编排-殿 | 分步-驱动-园 | L1-L4 四档执行步骤定义 |
| 任务-调度-殿 | 工具-定义-园 / 工具-执行-园 / 工具-循环-园 / 执行-提示-模板 / 派发-落单-园 / 文本-解析-园 | 10 工具 schema、执行分发+落盘护栏、function calling 主循环、派遣执行+构建自证、`<<<文件:路径>>>` 标记协议解析 |
| 手脚-施展-殿 | 文件-读写-阁（原子-读取/写入、增量-改写、批量-删除）/ 目录-浏览-阁（批量-列目、通配-找档）/ 内容-检索-阁（流式-检索）/ 命令-执行-阁（命令-沙箱、进程-驱动） | 智能体手脚架工具集 |

#### 关键类型

- **执行角色**：身份 / 道 / 职司 / 模型池 / 契约。
- **工作流级别**：问答 `L1_qa` / 脚本 `L2_script` / 程序 `L3_program` / 复杂 `L4_complex`。
- **执行任务**：目标 / 工作流 / 角色们。
- **执行状态**：运行中 / 成功 / 失败。
- **执行回执**：状态 / 产物们 / 说明 / 用量 / 轮数。
- **产物条目**：路径 / 类别 / 字节数 / 变化类型（新增/修改/未变）。

#### 关键函数 / Service Definition

```rust
pub trait 道术服务: Send + Sync {
    fn 执行任务(&self, 任务id: &str, 任务: &执行任务, 背景: &str, 现状: &str,
              涉及路径: &[String], 设计方案: &str, 验收标准: &str) -> Result<执行回执, String>;
}
```
- `任务调度::新(配置, 工作区根)` — 构造调度器。
- `调度.派遣执行(...)` — 提示渲染 → 工具循环 → 构建自证（build+test）→ 失败重试。
- `读文件` / `写文件` / `改文件` / `删文件` / `列举目录` / `寻找文件` / `搜索内容` / `运行命令` / `读格位` / `查格位历史` — 10 个工具。
- 落盘护栏三层：空内容拒写 / 超长拒写 / 路径越界拒写。
- 工具循环预算双轨：32 轮 / 90 万 token，任一耗尽熔断。
- 命令沙箱：禁 kill / 禁 cargo run 号令 / 超时 10 分钟 / 路径越界。
- 插件：`道术插件`，`注入() = ["识海承载-府"]`。

---

### 5.4 模型连接-府 `moxing_fu` — LLM 调用

**本质**：把对话消息发往可配置模型地址，取回文本。OpenAI 兼容 function calling。二殿：类型-定义-殿 · 模型-调用-殿。

#### 关键类型

- `对话消息` / `工具定义` / `工具调用` / `模型回复` / `用量`。
- `模型配置`（re-export 自 `peizhi_fu`）：密钥 / 地址 / 模型。
- `全局提供者注册表`（键名如 `"http"`）。

#### 关键常量与函数

```rust
pub const 落盘上限: u32 = 65536;   // 写文件任务
pub const 常规上限: u32 = 16384;   // 结构化 JSON 输出
pub const 精简上限: u32 = 4096;    // 短输出
pub const 请求超时: Duration = 120s;
pub const 最大重试次数: u32 = 3;
```
- `预检地址(地址)` — DNS 预检，快速失败。
- `是瞬时故障(状态码)` — 5xx / 429 / 529 可重试。
- `退避间隔(重试次数)` — 1s/2s/4s，超出 8s 兜底。
- `发送并重试(发请求, 模型)` — 指数退避重试。
- 三层护栏：工具参数兜底 / think 保留 / 空输出兜底。
- 调用点埋点：`jiance_fu::记请求` / `记回复`（自动带线程本地观测上下文的角色/任务线/要求/轮次）。

---

### 5.5 日志记录-府 `rizhi_fu` — 技术日志

**本质**：tracing 定制封装。**技术日志走 `rizhi_fu`，业务流水走识海承载-府，两者勿混。** 各府经 lib 根调用。

- `初始化默认()` — 初始化日志订阅（bin 入口首调）。
- 宏：`info!` / `warn!` / `error!` / `debug!`（经 `pub use` 透出）。
- 依赖：`tracing` + `tracing-subscriber`（fmt + registry）+ `time`（local-offset）。

---

### 5.6 配置管理-府 `peizhi_fu` — 密钥/模型配置/装配

**本质**：`.env` → 结构化配置；`.上下文/装配.json` → 世界装配。三殿：类型-定义-殿 · 密钥-注入-殿 · 装配-配置-殿。

#### 关键类型与函数

- `模型配置`：密钥 / 地址 / 模型。
- `装配配置`：阶段 / 启用扩展（巡世/进化/观测）/ 模型提供者 / 角色册路径 / 合法产物扩展名（13 种）。
- `读模型配置(.env 路径)` — 读密钥（GIT/DEEP SEEK/MINIMAX M3）。
- `读装配()` — 读 `.上下文/装配.json`，缺失/解析失败兜底 `default()`。
- `启用扩展(&装配, &扩展开关)` — 判定某扩展点是否启用。

---

### 5.7 插件承载-府 `chajian_fu` — 府插件接口与注册表

**本质**：鸿蒙作为插件基础设施提供者——插件注册表（本府）+ 事件总线 + 状态共享。跨府引用经本府插件上下文查找目标府的 Service Definition。二殿：插件-定义-殿 · 插件-注册-殿。

#### 关键类型与函数

```rust
pub trait 府插件: Send + Sync {
    fn 名称(&self) -> &str;            // 府名
    fn 注入(&self) -> Vec<&str>;        // 依赖的其他插件名
    fn 应用(&self, ctx: &mut 插件上下文) -> Result<(), Box<dyn Error + Send + Sync>>;
}

pub struct 插件上下文 {
    注册表: RwLock<HashMap<String, Box<dyn 府插件>>>,           // 府名 → 府插件
    服务表: RwLock<HashMap<TypeId, Box<dyn Any + Send + Sync>>>, // 类型 ID → 服务实例
}
```
- `插件上下文::新()` / `注册(插件)` — 注册前检查依赖是否已注册。
- `查找(名称)` / `已注册()` — 查插件。
- `注册服务::<T>(服务)` — 注册 `Arc<dyn 服务trait>`（按 `TypeId` 索引，重复注册报错）。
- `查找服务::<T>()` — 返回 `Option<T>`（克隆服务实例）。

> Service Definition 渐进式改造：本阶段只加注册表机制，不删现有 `use shihai_fu::` 跨府引用；后续逐步改为经插件上下文查找服务实例。

---

### 5.8 事件总线-府 `shijian_fu` — 类型化事件分发

**本质**：进程内实时扩展点——事件注册表 + 分发，让横切关注点（护栏/审批/观测/重试）以监听器挂入而非内嵌。**与识海承载-府事件流（append-only 持久事实源）分工不混淆。** 二殿：类型-定义-殿（分发模式/守卫裁决/工具流水线契约）· 分发-执行-殿（注册表与分发）。

- `全局总线` — 全局事件总线实例。
- 分发模式：同步分发 / 并发分发（`并发分发`）。
- 用途：跨维事件中转（原「跨维经鸿蒙」的落地机制之一）。

---

### 5.9 状态共享-府 `zhuangtai_fu` — 跨府运行时状态

**本质**：进程内运行时状态容器，按类型 ID 索引。各府经本府读取/写入共享状态，不直接传参。二殿：类型-定义-殿（状态键/状态值）· 状态-存取-殿（状态共享/读取/写入）。

- 与 Service Definition 区别：服务接口是静态（启动时注册一次），状态共享是动态（随时可写可读）。
- 与事件总线分工：状态共享是「当前状态快照」（读最新值），事件总线是「事件通知」（通知变化发生）。

---

### 5.10 观测探针-府 `jiance_fu` — 白箱可观测性

**本质**：**一个积和类型 `观测记录`** + 一个统一入口 `落`，在「命令↔角色↔LLM↔工具」交接点发出，按 `关联` 贯穿还原任意任务执行链。零上级依赖（任何生产 crate 只调一行 `jiance_fu::落(..)`）。

#### 关键类型

```rust
pub enum 观测域 { 提示词, 回复思考, 工具调用, 工具返回, 状态流转, 产物判定 }
pub enum 观测角色 { 界主, 鸿钧, 执行, 设计, 验收, 归因, 未知 }
pub struct 关联 { 任务线: Option<String>, 要求: Option<String>, 轮次: Option<usize>, 标识: Option<String> }
pub struct 载荷 { 内容: String, 附加: Option<serde_json::Value> }
pub struct 观测记录 { 时间戳, 域, 接口, 角色, 载荷, 关联 }
```

#### 关键函数

- `落(记录)` — append 到 `.上下文/观测/记录.jsonl`（落盘失败静默，不阻断业务）。
- 便捷：`记请求` / `记回复` / `记工具调用` / `记工具返回` / `记状态` / `记构建`。
- `进入观测(角色, 任务线, 要求, 轮次)` — push 线程本地观测上下文栈（可嵌套），返回 Drop 守卫在离开时 pop。
- `当前观测()` / `当前关联()` — 读栈顶，无上下文则 (未知, 空关联)。
- 单条正文封顶 200_000 字符（截头保尾）。

> 字段名/枚举名即对外 API：消费端（乾坤监控域 Python）按字面字段名解析，改动需同步消费端。

---

### 5.11 命令操作-府 `mingling_fu`（bin = `号令`）— 操作台 CLI

**本质**：世界的操作台。五殿：命令-解析 · 权限-校验 · 观览-查询 · 号令-下达 · 输出-呈现。

#### 殿/阁/园

| 殿 | 作用 |
|:--|:--|
| 命令-解析-殿 | 命令-入口-阁（兜底-入口-园：`号令.rs`/`入口执行.rs`）/ 参数-解析-阁（流式-解析-园）/ 命令-分发-阁（兜底-分发-园） |
| 权限-校验-殿 | 令牌-校验-阁（原子-校验-园）：写命令需 `WORLD_AI_TOKEN` 或 `-t`，界主（人）免令牌 |
| 观览-查询-殿 | 世界-观览-阁（流式 历法/时刻/时辰/检索/直播/纪元/缓存读取）/ 事项-查询-阁（并行-检索）/ 流水-观览-阁（流式-回放）/ 记忆-回想-阁（增量-投影） |
| 号令-下达-殿 | 世界-驱动-阁（常驻-轮询/规模-统计）/ 对话-发言-阁（即时-应答）/ 巡世-执行-阁（批量-扫描/驱动-消费）/ 想法-投递-阁（原子-入池）/ 版本-定档-阁（增量-快照）/ 要求-提审-阁（并行-提审）/ 记忆-回填-阁（增量-写入）/ 设计-审定-阁（分块-审定）/ 验收-裁决-阁（批量-审验） |
| 输出-呈现-殿 | 序列-输出-阁（原子-序列化-园）/ 文本-输出-阁（分块-呈现-园） |

#### 关键函数

- `执行()` — bin 入口流程：解析 → 校验 → 分发 → 输出，返回退出码。
- `解析调用(输入)` / `校验调用(&调用, 令牌)` / `分发调用(&调用)` / `呈现文本` / `呈现JSON`。
- `工作区根()` / `读模型配置()` / `打开存储()`。

#### bin 入口（`号令.rs`）

```rust
fn main() {
    rizhi_fu::初始化默认();
    std::process::exit(执行());
}
```

---

### 5.12 单元测试-府 `zhengdao_fu` — 测试承载

**本质**：证道维度的测试承载，八殿镜像被测八府组织，仅经被测府 lib 根 `pub` 符号测试（黑盒），不深链、不碰私有实现。

- 八验证殿：命令操作 / 天庭治理 / 插件承载 / 日志记录 / 模型连接 / 识海承载 / 道术施展 / 配置管理。
- 每殿下分阁/园，与被测府组织镜像。
- 依赖全部被测府 + `tracing`/`tracing-subscriber`。

---

### 5.13 监控界面-府（Python，非 crate）

`乾坤/呈现-域/监控界面-府`：世界执行期间的直播 + 白箱 + 让用户信任。仅依赖 Python 标准库。

- 启动：`python server.py 8080`（默认端口 8080，浏览器访问 `http://127.0.0.1:8080`）。
- 纯读 `.上下文/观测/记录.jsonl`、`.上下文/事件流.jsonl`、`临时文件夹/模型流水-观测.log` 三源，SSE 推送。
- 7 个端点：`/` `/static/*` `/api/snapshot` `/api/stream`(SSE) `/api/replay` `/api/rooms` `/api/settings` `/api/health`。
- 事件六字段契约：`ts / 源 / 动作 / 影响[] / token / 耗时ms / 证据`。
- 另有 `监控-域`（`监控服务器.py`，端口 3082）为更早期临时监控。

---

## 6. 插件化架构

### 6.1 模型

> 「九是维度，府是插件」（2026-08-20 界主拍板）。九根是维度分类，府是可挂载可替换的**插件单元**。府暴露 Service Definition（插件接口 = 服务 trait），跨府引用经插件注册表查找，鸿蒙是插件注册表 + 事件总线 + 状态共享的提供者。

```
启动时：
  插件上下文::新()
    → 府插件.应用(ctx) → ctx.注册服务(Arc<dyn 服务trait>)   （按 TypeId 入服务表）
运行时：
  跨府调用方：ctx.查找服务::<Arc<dyn 服务trait>>() → Option<Arc<dyn 服务trait>>
           → 调 trait 方法
```

### 6.2 已实现的 Service Definition（lib 根暴露的服务 trait）

| 府 | Service Definition（trait） | 方法 |
|:--|:--|:--|
| 识海承载-府 | `识海服务` | `回想` / `铐记` |
| 天庭治理-府 | `天庭服务` | `调度要求` / `验收产物` / `归档版本` |
| 道术施展-府 | `道术服务` | `执行任务` |

### 6.3 渐进式改造

本阶段只加 Service Definition 机制（插件上下文加服务注册表 + 注册/查找方法），不删现有 `use shihai_fu::` 跨府引用；后续逐步把跨府引用改为经插件上下文查找服务实例。当前跨府仍多走 `use {府lib}::符号` 止步 lib 根。

---

## 7. 依赖关系

### 7.1 跨府依赖图（单向，止步 lib 根；rizhi_fu 横切各府）

```
mingling_fu ──► tianting_fu ──► daoshu_fu ──► moxing_fu ──► peizhi_fu
      │   │   │       │              │
      │   │   │       └────► shihai_fu ──► moxing_fu
      │   │   └──► shihai_fu
      │   └──► moxing_fu / peizhi_fu   （操作台直连模型与配置）
      │
zhengdao_fu ──► {shihai_fu, tianting_fu, daoshu_fu, moxing_fu, peizhi_fu, rizhi_fu, mingling_fu, chajian_fu}
```

横切依赖：
- `rizhi_fu`（日志）— 被几乎所有生产 crate 依赖。
- `jiance_fu`（观测）— 被 shihai_fu / tianting_fu / daoshu_fu / moxing_fu 依赖，零上级依赖。
- `chajian_fu`（插件）— 被 shihai_fu / tianting_fu / daoshu_fu / mingling_fu / zhengdao_fu 依赖。
- `shijian_fu`（事件总线）— 被 daoshu_fu / tianting_fu / mingling_fu 依赖。
- `zhuangtai_fu`（状态共享）— 被 tianting_fu / mingling_fu 依赖。

### 7.2 Cargo 依赖矩阵

| crate | 内部依赖 | 外部依赖 |
|:--|:--|:--|
| `rizhi_fu` | — | tracing, tracing-subscriber, time |
| `zhuangtai_fu` | — | serde_json |
| `chajian_fu` | rizhi_fu | — |
| `shijian_fu` | rizhi_fu | serde, serde_json |
| `peizhi_fu` | rizhi_fu | serde, serde_json |
| `moxing_fu` | peizhi_fu, rizhi_fu, jiance_fu | serde, serde_json, ureq |
| `shihai_fu` | moxing_fu, rizhi_fu, jiance_fu, chajian_fu | serde, serde_json, thiserror |
| `daoshu_fu` | moxing_fu, rizhi_fu, shihai_fu, shijian_fu, jiance_fu, chajian_fu, peizhi_fu | serde, serde_json, json5, thiserror |
| `tianting_fu` | daoshu_fu, shihai_fu, shijian_fu, moxing_fu, peizhi_fu, rizhi_fu, jiance_fu, chajian_fu, zhuangtai_fu | serde, serde_json, thiserror |
| `jiance_fu` | — | serde, serde_json（注：入口.rs 直接 use） |
| `mingling_fu` | shihai_fu, tianting_fu, daoshu_fu, moxing_fu, peizhi_fu, shijian_fu, rizhi_fu, chajian_fu, zhuangtai_fu | serde_json |
| `zhengdao_fu` | daoshu_fu, shihai_fu, tianting_fu, rizhi_fu, moxing_fu, peizhi_fu, mingling_fu, chajian_fu | serde, serde_json, tracing, tracing-subscriber |

### 7.3 三条铁律

1. **跨府止步 lib 根（Service Definition）**：`use {府lib}::符号`，禁深链到殿/阁/园；路径含 `殿/阁/园` 即深链违规。
2. **跨维经鸿蒙**：9 根之间一切调用/事件/状态经鸿蒙（插件注册表 + 事件总线 + 状态共享），禁跨维直调。
3. **单向向下**：维度 → 域 → 府 → 殿 → 阁 → 园，下层永不反向引用上层。

---

## 8. 关键数据流

### 8.1 一次完整迭代（世界运行主循环）

```
界主投递想法
   │
   ▼
主政一轮（天庭治理-府 tianting_fu::主政一轮）
   │  LLM 需求拆分决策：不拆走单要求；拆则逐子要求串行全链
   ▼
运行一轮（世界运行.rs）：
   1. 元数据层化（首屏 3000 字符）→ 依赖图加载 → 结构树补全提审背景
   2. 人类格位临时代偿（人类格位未录时 LLM 探索项目生成临时内容）
   3. 解析想法 → 入池（待领）→ 依赖图滞后防御（涉及路径无匹配则重建）
   4. 模型设计（模板兜底：类别→工作流级别）→ 状态机推进（设计中→待确认→已确认→待实现）
   5. 现状装配：查相关文件 + 补全同阁 + 结构下探（封顶 8000 字符）
   6. 图谱契约投影（库根导出签名 / 核心类型契约 / 测试样例参照）
   7. 教训注入（上轮失败经验）
   8. 打回自动重投循环（最大 2 次）：
        并发派遣（std::thread::scope 多任务线程，大罗并行）
        → 地道整理并行（白嫖等待期）
        → 产物去重 + 字节数回填
        → 失败归因 + 失败沉淀
        → 终裁（机械门槛 → 六准圣 → 鸿钧终裁）
        → 通过则定档已存档；打回则撤销产物 + 重投
   ▼
版本存档（快照-落库-园：增量快照 + 版本记录 + 世界状态原子写）
   │
   ▼（乙阶段）天道巡世扫描 → 候选池 → 进化归因
```

### 8.2 CLI 执行流（号令 bin）

```
号令 main()
  → rizhi_fu::初始化默认()
  → 执行()
      → 解析调用(命令行参数)        // 命令-解析-殿
      → 空域？打印全流程总览，返回 0
      → 校验调用(&调用, WORLD_AI_TOKEN)  // 权限-校验-殿（写命令需令牌）
      → 分发调用(&调用)            // 命令-分发-殿 → 号令-下达-殿各阁
      → 结果::成功 → 呈现文本/呈现JSON（输出-呈现-殿）
      → 结果::失败 → eprintln，返回 1
  → std::process::exit(退出码)
```

### 8.3 角色执行链（道术施展-府）

```
派发-落单-园（天庭团队-调度-殿 交付拆解项）
   → 任务调度.派遣执行()
        → 执行基线指纹（任务前拍全量基线）
        → 提示渲染（执行-提示-模板：先读后写/构建验证/压缩三件套）
        → 工具循环（function calling）
             预算双轨：32 轮 / 90 万 token
             会话内压缩：历史>15000 摘要 + 工具结果微压缩 + 单条微压缩
             每轮：模型调用(moxing_fu) → 工具执行(手脚-施展-殿) → 文本解析(<<<文件:路径>>>)
        → 构建自证（build + test 双绿）
        → 失败续跑
   → 返回执行回执
```

---

## 9. 项目运行方式

### 9.1 构建

```bash
cargo build
```

构建产物统一落入 `道果树/构建物-域`（由 `.cargo/config.toml` 的 `[build] target-dir` 配置，不污染源码树）。

### 9.2 运行操作台

```bash
cargo run -p mingling_fu --bin 号令 -- <域> <动作> [参数]
```

- 无参数打印全流程总览。
- 写命令需环境变量 `WORLD_AI_TOKEN` 或传 `-t <令牌>`；读命令免令牌。
- 工作区根：环境变量 `WORLD_WORKSPACE_ROOT` 优先，回退当前目录。
- 模型配置：读工作区根 `.env`（GIT/DEEP SEEK/MINIMAX M3 密钥）；模型 = MiniMax-M3（thinking adaptive），OpenAI 兼容接口。

### 9.3 测试

```bash
cargo test
```

- 测试承载于 `证道/鸿蒙-域/单元测试-府`（zhengdao_fu），八殿镜像被测八府，黑盒经 lib 根验证。
- 全量约 300 用例全绿；`cargo check --workspace --all-targets` 零警告。
- crate 级测试共享锁 `工作区测试锁`：防并行测试竞态改写 `WORLD_WORKSPACE_ROOT` 进程级环境变量。

### 9.4 监控界面（Python）

```bash
cd 乾坤/呈现-域/监控界面-府
python server.py 8080
```

浏览器访问 `http://127.0.0.1:8080`。仅依赖 Python 标准库，零第三方依赖。纯读三源文件（`.上下文/观测/记录.jsonl`、`.上下文/事件流.jsonl`、`临时文件夹/模型流水-观测.log`）。

### 9.5 配置文件

| 文件 | 作用 | 读取入口 |
|:--|:--|:--|
| `.env` | 密钥与模型参数（GIT/DEEP SEEK/MINIMAX M3） | `peizhi_fu::读模型配置` |
| `.上下文/装配.json` | 世界装配（阶段/启用扩展/模型提供者/角色册路径/合法产物扩展名） | `peizhi_fu::读装配` |
| `.cargo/config.toml` | 共享构建目录 `道果树/构建物-域` | cargo |
| 环境变量 `WORLD_WORKSPACE_ROOT` | 工作区根定位 | 各府 `工作区::定位()` |
| 环境变量 `WORLD_AI_TOKEN` | 写命令令牌（界主免） | 命令操作-府 权限-校验-殿 |

### 9.6 运行时状态产物

- `.上下文/格位/*.jsonl` — 识海记忆（每格位独立文件）
- `.上下文/状态/世界状态.json` — 世界状态（12 字段）
- `.上下文/状态/想法池.jsonl` / `要求队列.jsonl` / `验收历史.jsonl`
- `.上下文/版本/` — 源码快照 + 版本记录（v1 → v14 真实自进化历史）
- `.上下文/观测/记录.jsonl` — 白箱观测记录
- `.上下文/事件流.jsonl` — 业务事件流
- `.上下文/依赖图.json` — 依赖图

---

## 10. 命令清单

### 写命令（11 条，需令牌 `WORLD_AI_TOKEN` 或 `-t`）

| 命令 | 作用 |
|:--|:--|
| `想法 投递` | 投递想法，驱动主政一轮 |
| `要求 化为` | 想法解析为要求书入队 |
| `设计 上呈` | 模板生成设计方案入队 |
| `设计 确认` | 确认设计（当前仅校验输出） |
| `验收 裁决` | 构造验收回执入历史 |
| `版本 存档` | 版本递增 + 增量快照 + 记录 |
| `版本 回退` | 恢复到指定版本 |
| `记忆 回填` | 写记录进格位 |
| `记忆 播种` | 扫描工作区源码写入识海 |
| `记忆 补语义` | LLM 补依赖图空解释符号 |
| `巡世 扫描` | 天道巡逻产出候选与违逆 |

### 读命令（21 条，免令牌）

想法列表/详情、要求列表/详情、设计列表/详情、验收历史/详情、版本列表/详情、记忆投影、世界状态、世界时间（十二时辰）、世界历法、世界检索、队列水位、流水列表/总览/跟踪/跟随、巡世报告、装配检查。

### 帮助（1 条）

---

## 11. 核心机制清单

| 机制 | 作用 |
|:--|:--|
| 两阶段制 | 甲=功能优先（关进化），乙=自动优化（开进化），切换点唯一=v1 存档 |
| 八态状态机 | 要求书状态流转（待领→…→已存档），非法迁移告警 |
| 需求拆分 | LLM 判断拆/不拆，拆则子要求逐个串行 |
| 打回撤销产物 | 按最近版本快照恢复涉及文件 |
| 打回自动重投 | 打回自动带意见重投（上限 2 次） |
| 机械前置全绿 | build + test 双绿才算过 |
| 现状装配 | 结构下探 + 符号摘要 + 文件全文，封顶 8000 字符 |
| 图谱契约投影 | 库根导出签名 + 核心类型契约 + 测试样例参照 |
| 人类格位临时代偿 | 人类格位未录时 LLM 探索项目生成临时内容，人类可覆盖为经 |
| 背景渐次披露 | 元数据层化（首屏 3000 字符）+ 按需读格位 |
| 工具循环预算双轨 | 轮数（32）+ token（90 万），任一耗尽熔断 |
| 会话内压缩 | 历史>15000 摘要 + 工具结果微压缩 + 单条微压缩 |
| 回滚垫 | 写操作前快照，任务失败单文件撤销 |
| 执行基线指纹 | 任务前拍全量基线，结束 diff 补齐产物 |
| 失败模式沉淀 | 失败按要求 id + 阶段去重累加 |
| 版本存档 | 增量快照 + 版本记录 + 回退 |
| 进化环 | 采集 → 归因 → 分级调整（⑤级需界主确认） |
| 巡世环 | 天道扫描 → 候选池 → 自主消费 → 闭环 |
| 模型调用护栏 | 工具参数兜底 / think 保留 / 空输出兜底 / 退避重试 / DNS 预检 / 120s 超时 |
| 命令沙箱 | 禁 kill / 禁 cargo run 号令 / 超时 10 分钟 / 路径越界 |
| 临时天道守卫 | 想法含【任务规则】段 → 任务级规则注入 + Drop 清理 |
| 白箱观测 | 统一 `观测记录` + 交接处埋点 + 线程本地上下文栈贯穿还原 |
| 插件化 | 府 = 插件单元，Service Definition 经插件上下文注册/查找 |
| 并发登记单调 id | `前缀-{毫秒}-{进程内原子序号}` 防并发撞 id |

---

## 12. 设计约束与铁律

### 12.1 架构铁律

1. **跨府止步 lib 根（Service Definition）**：`use {府lib}::符号`，禁深链到殿/阁/园。
2. **跨维经鸿蒙**：经插件注册表 + 事件总线 + 状态共享，禁跨维直调。
3. **单向向下**：维度 → 域 → 府 → 殿 → 阁 → 园，下层永不反向引用上层。
4. **同府跨园经共同上级**：经共同 `模块.rs` 透出的符号中转，不深链对方园文件。

### 12.2 工程纪律（AGENTS.md）

- **先入稿、再落码**：新增设计（新府殿阁园、新业务逻辑、新机制）必须先并入对应设计稿对应章节，再实现；溯不到源视为补丁。
- **日志统一落地**：技术日志走 `rizhi_fu`（tracing 定制封装），各府经 lib 根调用；业务流水走识海承载-府，两者勿混。新增府殿阁园/代码/业务逻辑必须主动埋点。
- **看实际产出再下结论**：排查问题先看完整字段、完整提示词、落盘记录原文、中间产物，而非代码定义/摘要。
- **临时文件夹**：只装临时产物（一次性脚本/临时验证/临时比对/调试日志/过期草案），任务结束或版本定档后清空；不进 git。
- **根目录禁平铺目录**：禁 `src/`、`tests/`、`scripts/` 空目录，源码按六层落点；禁 IDE 配置目录。
- **`.env` 只放当前代码实际读取的配置项**，过期/无效条目直接删。
- **命名禁 AI 味**：避免 AI 语的命名与注释。

### 12.3 命名规范

- 全中文标识符（类型 UpperCamelCase，函数/变量 snake_case）。
- 目录名连字符（`文件-读写-殿`），Rust 模块名下划线（`文件_读写_殿`），由 `#[path]` 桥接。
- 域/府 2 段；殿/阁/园 3 段；园 = 2 段名 + 类型后缀（`-园`/`-数据`/`-配置`/`-模板`/`-脚本`/`-资源`）。
- 代码园名须含「实现手段词」（流式/原子/批量/并行/缓存/降级/兜底/分块/增量…），且不得出现在上层名中。

### 12.4 园分类

| 类型 | 后缀 | 装什么 |
|:--|:--|:--|
| 代码园 | `-园` | 实现代码（.rs 等） |
| 存储园 | `-数据` | 数据文件（.json/.jsonl/.db 等） |
| 配置园 | `-配置` | 配置文件（.toml/.yaml 等） |
| 模板园 | `-模板` | 生成样板（.hbs/.j2 等） |
| 脚本园 | `-脚本` | 自动化脚本（.sh/.ps1 等） |
| 资源园 | `-资源` | 静态资源（图片/字体等） |

---

## 13. 技术栈

- **语言**：Rust（workspace，中文标识符，edition 2021，resolver 2）
- **序列化**：serde / serde_json（json5 用于角色卡册等）
- **错误**：thiserror
- **日志**：tracing / tracing-subscriber（fmt + registry）
- **HTTP**：ureq（模型调用，OpenAI 兼容 function calling）
- **时间**：time（local-offset，世界时间输出十二时辰）
- **监控界面**：Python 标准库（http.server + ThreadingHTTPServer + SSE）
- **模型**：MiniMax-M3（thinking adaptive），OpenAI 兼容接口

---

## 14. 关键设计文档索引

| 文档 | 内容 |
|:--|:--|
| [README.md](file:///workspace/README.md) | 项目总览、六层结构、九根维度、快速开始 |
| [层级结构-设计.md](file:///workspace/层级结构-设计.md) | 六层规范、边界模型（插件化）、命名规范 |
| [项目心智模型-设计稿.md](file:///workspace/项目心智模型-设计稿.md) | 世界之脑：36/72 格位、编码三源、检索三档 |
| [多智能体架构设计.md](file:///workspace/多智能体架构设计.md) | 世界之身：四层组织、两阶段、接口契约 |
| [融合蓝图-设计稿.md](file:///workspace/融合蓝图-设计稿.md) | 身脑融合、三府落位、已拍板决策 |
| [智能体.md](file:///workspace/智能体.md) | 天庭治理-府 + 道术施展-府 分工 |
| [上下文.md](file:///workspace/上下文.md) | 识海承载-府 记忆设计 |
| [项目全景-理解.md](file:///workspace/项目全景-理解.md) | 全景理解总索引（随世界生长更新） |
| [AGENTS.md](file:///workspace/AGENTS.md) | 工作区铁律与执行纪律 |

---

## 15. 当前状态

- 12 Rust crate + 1 Python 监控府；约 380 rs 文件；`cargo check --workspace --all-targets` 零警告，全量测试（约 300 用例）全绿。
- 版本存档 v1 → v14（真实自进化历史）。
- 已进入乙阶段（自动优化期）。
- 白箱可观测已落地：`鸿蒙/观测探针-府`（jiance_fu）统一 `观测记录`，埋点覆盖提示词/回复思考/工具调用/工具返回/状态流转/产物判定六域，按 `关联.要求` 完整还原执行链。

---

*Code Wiki · 依据代码与设计稿实读 · 随世界生长更新*
