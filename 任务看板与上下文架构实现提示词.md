# 洪荒世界・任务看板与上下文架构实现提示词

> **执行目标**
>
> ：在 
>
> `F:\洪荒 - 世界`
>
>  Rust 项目中，实现「上下文独立 + 任务看板协作」的洪荒五层架构。
> **执行方式**
>
> ：直接在现有代码基础上扩展，不破坏现有 API，新增模块和数据结构。
> **质量要求**
>
> ：编译通过、测试通过、文档完整、可直接用于生产。



***

## 一、项目现状（必须先读取确认）

### 1.1 项目结构

项目是 Rust 工作空间，中文命名，九根六层结构（包→域→府→殿→阁→园→文件）。

关键已有模块：



```
鸿蒙/基础能力-域/项目认知-府/

├── 世界模型-殿/图谱构建-阁/图谱-数据-园/图谱数据.rs    # 已有：图谱数据

├── 心智导航-殿/格位管理-阁/格位-数据-园/格位数据.rs    # 已有：格位数据

└── 执行语境-殿/语境管理-阁/语境-数据-园/语境数据.rs    # 已有：语境数据（上下文基础）

太初/任务管理-域/任务核心-府/

├── 任务模型-殿/

│   ├── 任务定义-阁/任务-数据-园/任务数据.rs            # 已有：Task 基础结构（id/title/description/status/created\_at）

│   └── 状态定义-阁/状态-枚举-园/状态枚举.rs            # 已有：TaskStatus（待受理/进行中/已完成/已取消）

└── 任务仓库-殿/仓库实现-阁/仓库-模块-园/仓库模块.rs    # 已有：任务仓库基础
```

### 1.2 现有数据结构（必须先读取原文）

**Task**（`太初/任务管理-域/任务核心-府/任务模型-殿/任务定义-阁/任务-数据-园/任务数据.rs`）：



```
pub struct Task {

&#x20;   pub id: u64,

&#x20;   pub title: String,

&#x20;   pub description: String,

&#x20;   pub status: TaskStatus,

&#x20;   pub created\_at: u64,

}
```

**TaskStatus**（`太初/任务管理-域/任务核心-府/任务模型-殿/状态定义-阁/状态-枚举-园/状态枚举.rs`）：



```
pub enum TaskStatus {

&#x20;   待受理,

&#x20;   进行中,

&#x20;   已完成,

&#x20;   已取消,

}
```



***

## 二、架构设计（核心概念）

### 2.1 核心理念



| 概念       | 定义                              | 是否共享    |
| -------- | ------------------------------- | ------- |
| **层级**   | 资源权限边界（格位维度、工具权限、图谱访问范围）        | 共享规则    |
| **个体**   | 独立的执行上下文（每个 Agent 的思考过程、工具调用记录） | **不共享** |
| **格位**   | 方法论 + 事实索引（项目本质认知）              | 全员共享    |
| **图谱**   | 项目当前现状（节点、边、坐标）                 | 全员共享    |
| **任务看板** | 层级间协作的唯一通道，任务包含所有元信息            | 全员共享    |

**核心原则**：



* 上下文是每个人单独的执行过程，**不共享**

* 格位 + 图谱是共享认知底座，**全员共享**

* 层级间协作**只通过任务看板**，不通过上下文传话

* 任务包含所有元信息，承接人只需看任务就能完整理解

### 2.2 洪荒五层架构



```
道祖（决策者）──发布需求──→ 任务看板 ──承接──→ 圣人（设计者）

&#x20;                                                     │

道祖（终审者）←──终审──── 任务看板 ←──提交──── 准圣（验证者）

&#x20;                             ↑                        │

&#x20;                             │                        │

&#x20;                             └──提交── 大罗金仙（执行者）←──承接──┘
```



| 层级       | 角色  | 职责          | 格位维度                 | 工具权限                      | 图谱范围 |
| -------- | --- | ----------- | -------------------- | ------------------------- | ---- |
| **道祖**   | 决策者 | 发布极致需求、最终验收 | 目标・方向 / 经历・历史 / 内部・我 | 无                         | 全局视图 |
| **圣人**   | 设计者 | 边界契约设计、任务拆分 | 外在・项目 / 执行・行动 / 内部・我 | read\_file                | 结构视图 |
| **大罗金仙** | 执行者 | 代码实现、工具调用   | 规则・约束 / 执行・行动 / 内部・我 | read\_file / query\_graph | 坐标视图 |
| **准圣**   | 验证者 | 验收测试、反馈修复   | 规则・约束 / 经历・历史 / 内部・我 | read\_file                | 结构视图 |



***

## 三、详细实现规范

### 3.1 任务状态机扩展

**在现有&#x20;**`TaskStatus`**&#x20;基础上扩展**，不删除现有状态，新增洪荒五层专用状态：



```
/// 任务状态：洪荒五层流转

/// 待受理 → 待圣人设计 → 圣人设计中 → 待大罗金仙实现 → 大罗金仙实现中

/// → 待准圣验收 → 准圣验收中 → 待修复（循环）→ 待道祖终审 → 道祖终审中 → 已完成

\#\[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]

pub enum TaskStatus {

&#x20;   // 保留原有状态（兼容）

&#x20;   待受理,

&#x20;   进行中,

&#x20;   已完成,

&#x20;   已取消,

&#x20;  &#x20;

&#x20;   // 新增：洪荒五层流转状态

&#x20;   待圣人设计,      // 道祖已发布需求，等待圣人承接

&#x20;   圣人设计中,      // 圣人正在进行边界契约设计

&#x20;   待大罗金仙实现,  // 圣人已完成设计，等待大罗金仙承接

&#x20;   大罗金仙实现中,  // 大罗金仙正在代码实现

&#x20;   待准圣验收,      // 大罗金仙已完成实现，等待准圣承接

&#x20;   准圣验收中,      // 准圣正在验收测试

&#x20;   待修复,          // 准圣验收不通过，等待大罗金仙修复

&#x20;   待道祖终审,      // 准圣验收通过，等待道祖承接

&#x20;   道祖终审中,      // 道祖正在最终验收

}
```

**状态流转规则**（实现 `可流转到` 方法）：



```
待受理 ──→ 待圣人设计（道祖发布）

待圣人设计 ──→ 圣人设计中（圣人承接）

圣人设计中 ──→ 待大罗金仙实现（圣人完成设计）

待大罗金仙实现 ──→ 大罗金仙实现中（大罗金仙承接）

大罗金仙实现中 ──→ 待准圣验收（大罗金仙完成实现）

待准圣验收 ──→ 准圣验收中（准圣承接）

准圣验收中 ──→ 待修复（验收不通过）

准圣验收中 ──→ 待道祖终审（验收通过）

待修复 ──→ 大罗金仙实现中（大罗金仙承接修复）

待道祖终审 ──→ 道祖终审中（道祖承接）

道祖终审中 ──→ 已完成（终审通过）

道祖终审中 ──→ 待修复（终审不通过，反馈修复）
```

### 3.2 任务数据结构扩展

**在现有&#x20;**`Task`**&#x20;基础上扩展**，保留原有字段，新增洪荒五层元信息：



```
/// 任务：太初之木，从无到有的存在

/// 扩展：包含洪荒五层流转的完整元信息

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct Task {

&#x20;   // 保留原有字段

&#x20;   pub id: u64,

&#x20;   pub title: String,

&#x20;   pub description: String,

&#x20;   pub status: TaskStatus,

&#x20;   pub created\_at: u64,

&#x20;  &#x20;

&#x20;   // 新增：洪荒五层元信息

&#x20;   pub scene: TaskScene,              // 场景类型

&#x20;   pub priority: TaskPriority,        // 优先级

&#x20;   pub creator: AgentRole,            // 发起人角色

&#x20;   pub current\_owner: AgentRole,      // 当前承接人角色

&#x20;   pub assignee\_history: Vec\<AgentRole>, // 承接历史

&#x20;  &#x20;

&#x20;   // 新增：各阶段产物

&#x20;   pub requirement: Option\<RequirementDoc>,  // 道祖需求文档

&#x20;   pub design: Option\<DesignDoc>,            // 圣人设计文档

&#x20;   pub implementation: Option\<ImplementationDoc>, // 大罗金仙实现文档

&#x20;   pub verification: Option\<VerificationDoc>,     // 准圣验收文档

&#x20;   pub final\_acceptance: Option\<FinalAcceptanceDoc>, // 道祖终审文档

&#x20;  &#x20;

&#x20;   // 新增：元数据

&#x20;   pub status\_history: Vec\<StatusChange>,   // 状态变更历史

&#x20;   pub total\_tokens: u64,                   // 总token消耗

&#x20;   pub fix\_rounds: u32,                     // 修复轮次

&#x20;   pub updated\_at: u64,                      // 最后更新时间

}
```

### 3.3 支撑数据结构

**AgentRole（层级角色）**：



```
\#\[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]

pub enum AgentRole {

&#x20;   道祖,

&#x20;   圣人,

&#x20;   大罗金仙,

&#x20;   准圣,

}
```

**TaskScene（场景类型）**：



```
\#\[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]

pub enum TaskScene {

&#x20;   理解,

&#x20;   设计,

&#x20;   修改,

&#x20;   调试,

&#x20;   重构,

}
```

**TaskPriority（优先级）**：



```
\#\[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]

pub enum TaskPriority {

&#x20;   P0,  // 最高

&#x20;   P1,

&#x20;   P2,

&#x20;   P3,  // 最低

}
```

**StatusChange（状态变更记录）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct StatusChange {

&#x20;   pub from: TaskStatus,

&#x20;   pub to: TaskStatus,

&#x20;   pub operator: AgentRole,

&#x20;   pub time: u64,

&#x20;   pub comment: Option\<String>,

}
```

**RequirementDoc（道祖需求文档）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct RequirementDoc {

&#x20;   pub goal: String,                    // 目标

&#x20;   pub background: String,              // 背景

&#x20;   pub constraints: Vec\<String>,        // 约束条件

&#x20;   pub functional\_requirements: Vec\<String>,  // 功能需求

&#x20;   pub non\_functional\_requirements: Vec\<String>, // 非功能需求

&#x20;   pub acceptance\_criteria: Vec\<String>, // 验收标准

&#x20;   pub priority: TaskPriority,           // 优先级

&#x20;   pub created\_at: u64,

}
```

**DesignDoc（圣人设计文档）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct DesignDoc {

&#x20;   pub boundaries: std::collections::HashMap\<String, String>, // 边界定义（模块→职责）

&#x20;   pub safe\_zones: Vec\<String>,       // 内部安全区域（不可外部访问）

&#x20;   pub contracts: Vec\<ContractDef>,   // 共识契约（接口定义）

&#x20;   pub files\_to\_modify: Vec\<String>,  // 涉及文件清单

&#x20;   pub files\_to\_create: Vec\<String>,  // 新建文件清单

&#x20;   pub dependencies: Vec\<DependencyDef>, // 依赖关系（必须单向）

&#x20;   pub created\_at: u64,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct ContractDef {

&#x20;   pub trait\_name: String,

&#x20;   pub methods: Vec\<MethodDef>,

&#x20;   pub description: String,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct MethodDef {

&#x20;   pub name: String,

&#x20;   pub signature: String,

&#x20;   pub description: String,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct DependencyDef {

&#x20;   pub from\_module: String,

&#x20;   pub to\_module: String,

&#x20;   pub description: String,

}
```

**ImplementationDoc（大罗金仙实现文档）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct ImplementationDoc {

&#x20;   pub code\_changes: Vec\<CodeChange>,    // 代码变更清单

&#x20;   pub tool\_calls: Vec\<ToolCallRecord>,  // 工具调用记录

&#x20;   pub self\_check: SelfCheckResult,      // 自检结果

&#x20;   pub created\_at: u64,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct CodeChange {

&#x20;   pub file\_path: String,

&#x20;   pub change\_type: String,  // 修改/新增/删除

&#x20;   pub summary: String,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct ToolCallRecord {

&#x20;   pub tool\_name: String,    // read\_file / query\_graph

&#x20;   pub arguments: String,

&#x20;   pub result\_summary: String,

&#x20;   pub timestamp: u64,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct SelfCheckResult {

&#x20;   pub passed: bool,

&#x20;   pub boundary\_compliance: bool,  // 是否遵循边界

&#x20;   pub contract\_compliance: bool,  // 是否遵循契约

&#x20;   pub issues: Vec\<String>,

}
```

**VerificationDoc（准圣验收文档）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct VerificationDoc {

&#x20;   pub rounds: Vec\<VerificationRound>, // 验收轮次

&#x20;   pub final\_result: bool,              // 最终结果（通过/不通过）

&#x20;   pub created\_at: u64,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct VerificationRound {

&#x20;   pub round: u32,

&#x20;   pub passed: bool,

&#x20;   pub boundary\_check: bool,    // 边界检查

&#x20;   pub contract\_check: bool,    // 契约检查

&#x20;   pub safety\_check: bool,      // 安全区域检查

&#x20;   pub fact\_check: bool,        // 事实准确性检查

&#x20;   pub completeness\_check: bool, // 完整性检查

&#x20;   pub issues: Vec\<String>,

&#x20;   pub suggestions: String,     // 修改建议（不通过时）

}
```

**FinalAcceptanceDoc（道祖终审文档）**：



```
\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct FinalAcceptanceDoc {

&#x20;   pub passed: bool,

&#x20;   pub requirement\_satisfaction: u8,  // 需求满足度（0-100）

&#x20;   pub maintainability: u8,            // 长程可维护性（0-100）

&#x20;   pub code\_quality: u8,               // 代码整洁度（0-100）

&#x20;   pub risk\_assessment: String,        // 风险评估

&#x20;   pub comments: String,                // 终审评语

&#x20;   pub created\_at: u64,

}
```

### 3.4 任务看板（TaskBoard）

**新增模块**：`太初/任务管理-域/任务核心-府/任务看板-殿/`



```
任务看板-殿/

├── 看板管理-阁/

│   ├── 看板-数据-园/

│   │   ├── 看板数据.rs      # TaskBoard 核心结构

│   │   └── 模块.rs

│   └── 模块.rs

├── 任务流转-阁/

│   ├── 流转-逻辑-园/

│   │   ├── 流转逻辑.rs      # 状态流转、任务承接、任务提交

│   │   └── 模块.rs

│   └── 模块.rs

└── 模块.rs
```

**TaskBoard 核心结构**：



```
/// 任务看板：洪荒五层协作的唯一通道

pub struct TaskBoard {

&#x20;   tasks: std::collections::HashMap\<u64, Task>,  // 所有任务

&#x20;   storage\_path: std::path::PathBuf,               // 存储路径

}

impl TaskBoard {

&#x20;   /// 初始化看板（从文件加载）

&#x20;   pub fn 新建(storage\_path: \&std::path::Path) -> Result\<Self, TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 发布任务（道祖发布需求）

&#x20;   pub fn 发布任务(\&mut self, task: Task) -> Result\<u64, TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 承接任务（圣人/大罗金仙/准圣/道祖承接）

&#x20;   pub fn 承接任务(\&mut self, task\_id: u64, role: AgentRole) -> Result<(), TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 提交任务（完成当前阶段，流转到下一状态）

&#x20;   pub fn 提交任务(\&mut self, task\_id: u64, role: AgentRole, next\_status: TaskStatus) -> Result<(), TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 查询任务（按状态筛选）

&#x20;   pub fn 查询任务(\&self, status: Option\<TaskStatus>, role: Option\<AgentRole>) -> Vec<\&Task>;

&#x20;  &#x20;

&#x20;   /// 获取任务详情

&#x20;   pub fn 获取任务(\&self, task\_id: u64) -> Option<\&Task>;

&#x20;  &#x20;

&#x20;   /// 更新任务产物（需求/设计/实现/验收/终审文档）

&#x20;   pub fn 更新需求文档(\&mut self, task\_id: u64, doc: RequirementDoc) -> Result<(), TaskBoardError>;

&#x20;   pub fn 更新设计文档(\&mut self, task\_id: u64, doc: DesignDoc) -> Result<(), TaskBoardError>;

&#x20;   pub fn 更新实现文档(\&mut self, task\_id: u64, doc: ImplementationDoc) -> Result<(), TaskBoardError>;

&#x20;   pub fn 更新验收文档(\&mut self, task\_id: u64, doc: VerificationDoc) -> Result<(), TaskBoardError>;

&#x20;   pub fn 更新终审文档(\&mut self, task\_id: u64, doc: FinalAcceptanceDoc) -> Result<(), TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 持久化到文件

&#x20;   pub fn 保存(\&self) -> Result<(), TaskBoardError>;

&#x20;  &#x20;

&#x20;   /// 从文件加载

&#x20;   pub fn 加载(\&mut self) -> Result<(), TaskBoardError>;

}
```

**存储方案**：JSONL 文件



* 路径：`项目根目录/.task_board/tasks.jsonl`

* 每行一个任务 JSON

* 支持增量写入和全量加载

### 3.5 上下文管理（执行语境扩展）

**在现有&#x20;**`执行语境-殿/语境管理-阁`**&#x20;基础上扩展**，实现独立上下文管理：



```
执行语境-殿/语境管理-阁/

├── 语境-数据-园/

│   ├── 语境数据.rs      # 扩展：Context 结构

│   └── 模块.rs

├── 上下文-管理-园/      # 新增：上下文管理

│   ├── 上下文管理.rs    # ContextManager

│   └── 模块.rs

└── 模块.rs
```

**Context（上下文）结构**：



```
/// 执行上下文：每个Agent独立的执行过程，不共享

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct Context {

&#x20;   pub context\_id: String,           // 上下文唯一ID

&#x20;   pub agent\_role: AgentRole,        // 所属层级角色

&#x20;   pub task\_id: Option\<u64>,         // 当前处理的任务ID

&#x20;   pub messages: Vec\<ContextMessage>, // 消息历史（思考过程、工具调用）

&#x20;   pub tool\_calls: Vec\<ToolCallRecord>, // 工具调用记录

&#x20;   pub created\_at: u64,

&#x20;   pub updated\_at: u64,

}

\#\[derive(Debug, Clone, Serialize, Deserialize)]

pub struct ContextMessage {

&#x20;   pub role: String,       // system / user / assistant / tool

&#x20;   pub content: String,

&#x20;   pub timestamp: u64,

}
```

**ContextManager（上下文管理器）**：



```
/// 上下文管理器：管理每个Agent的独立上下文

pub struct ContextManager {

&#x20;   contexts: std::collections::HashMap\<String, Context>,

&#x20;   storage\_path: std::path::PathBuf,

}

impl ContextManager {

&#x20;   /// 创建新上下文（每个Agent开始工作时创建）

&#x20;   pub fn 创建上下文(\&mut self, role: AgentRole, task\_id: Option\<u64>) -> String;

&#x20;  &#x20;

&#x20;   /// 获取上下文

&#x20;   pub fn 获取上下文(\&self, context\_id: \&str) -> Option<\&Context>;

&#x20;  &#x20;

&#x20;   /// 添加消息

&#x20;   pub fn 添加消息(\&mut self, context\_id: \&str, message: ContextMessage) -> Result<(), ContextError>;

&#x20;  &#x20;

&#x20;   /// 记录工具调用

&#x20;   pub fn 记录工具调用(\&mut self, context\_id: \&str, call: ToolCallRecord) -> Result<(), ContextError>;

&#x20;  &#x20;

&#x20;   /// 清理上下文（任务完成后清理，不保留在共享空间）

&#x20;   pub fn 清理上下文(\&mut self, context\_id: \&str) -> Result<(), ContextError>;

&#x20;  &#x20;

&#x20;   /// 持久化

&#x20;   pub fn 保存(\&self) -> Result<(), ContextError>;

}
```

**核心原则**：



* 上下文是每个 Agent 独立的执行过程，**不共享**

* 任务完成后，上下文可以清理或归档，但**不传递给下一层级**

* 下一层级只从任务看板获取任务元信息，创建自己的新上下文



***

## 四、实现路线图（分阶段执行）

### 阶段一：数据结构扩展（预计 30 分钟）



1. **扩展 TaskStatus**：在 `状态枚举.rs` 中新增洪荒五层状态，实现 `可流转到` 方法

2. **扩展 Task**：在 `任务数据.rs` 中新增洪荒五层元信息字段

3. **新增支撑结构**：AgentRole、TaskScene、TaskPriority、StatusChange、各阶段文档结构

4. **编译验证**：`cargo check` 通过

### 阶段二：任务看板实现（预计 45 分钟）



1. **新增任务看板 - 殿模块**：按九根六层结构创建目录

2. **实现 TaskBoard**：核心结构、发布 / 承接 / 提交 / 查询方法

3. **实现 JSONL 持久化**：保存 / 加载方法

4. **实现状态流转校验**：承接和提交时校验状态机

5. **单元测试**：覆盖核心方法

### 阶段三：上下文管理实现（预计 30 分钟）



1. **扩展执行语境 - 殿**：新增上下文管理结构

2. **实现 ContextManager**：创建 / 获取 / 添加消息 / 记录工具调用 / 清理

3. **实现上下文持久化**：保存 / 加载

4. **单元测试**：覆盖核心方法

### 阶段四：集成与验证（预计 30 分钟）



1. **集成测试**：模拟完整五层流转（道祖发布→圣人设计→大罗金仙实现→准圣验收→道祖终审）

2. **文档生成**：为新增模块生成文档注释

3. **完整编译**：`cargo build --release` 通过

4. **完整测试**：`cargo test` 全部通过



***

## 五、代码规范与约束

### 5.1 命名规范



* 遵循项目现有中文命名规范（九根六层：包→域→府→殿→阁→园→文件）

* 结构体和枚举用中文，方法用中文（如 `新建`、`发布任务`、`承接任务`）

* 模块文件用 `模块.rs` 作为入口

### 5.2 错误处理



* 定义统一的错误类型 `TaskBoardError` 和 `ContextError`

* 使用 `thiserror` 库（项目已有依赖）

* 所有可能失败的方法返回 `Result<T, Error>`

### 5.3 序列化



* 使用 `serde` + `serde_json`（项目已有依赖）

* 所有数据结构派生 `Serialize, Deserialize`

* JSON 字段名用中文（与结构体字段一致）

### 5.4 兼容性



* **不删除**现有字段和状态（Task 的 id/title/description/status/created\_at 保留）

* **不修改**现有公共 API 的签名

* 新增字段用 `Option` 包裹，确保反序列化兼容

* 新增状态不影响现有状态的流转

### 5.5 测试要求



* 每个公共方法至少一个单元测试

* 状态流转的合法和非法路径都要测试

* 集成测试覆盖完整五层流转

* 测试用中文命名（如 `测试_道祖发布任务`、`测试_圣人承接设计`）



***

## 六、验收标准

### 6.1 编译与测试



* [ ] `cargo check` 无错误无警告

* [ ] `cargo build --release` 成功

* [ ] `cargo test` 全部通过

* [ ] 测试覆盖率：核心模块 ≥ 80%

### 6.2 功能验收



* [ ] TaskStatus 包含洪荒五层所有状态，状态流转校验正确

* [ ] Task 包含所有洪荒五层元信息字段

* [ ] TaskBoard 支持发布 / 承接 / 提交 / 查询 / 更新产物 / 保存 / 加载

* [ ] ContextManager 支持创建 / 获取 / 添加消息 / 记录工具调用 / 清理 / 保存

* [ ] JSONL 持久化正常，重启后数据不丢失

### 6.3 集成验收



* [ ] 模拟完整五层流转：道祖发布需求 → 圣人承接设计 → 圣人提交设计 → 大罗金仙承接实现 → 大罗金仙提交实现 → 准圣承接验收 → 准圣提交验收（通过）→ 道祖承接终审 → 道祖提交终审（通过）→ 任务已完成

* [ ] 模拟验收不通过循环：准圣验收不通过 → 待修复 → 大罗金仙承接修复 → 大罗金仙提交 → 准圣再次验收 → 通过

* [ ] 上下文隔离验证：圣人的上下文不包含道祖的思考过程，大罗金仙的上下文不包含圣人的思考过程

* [ ] 任务看板作为唯一通道：层级间所有信息传递都通过任务看板，不通过上下文传话

### 6.4 文档验收



* [ ] 所有公共结构体、枚举、方法有文档注释

* [ ] 新增模块有 `模块.rs` 入口和模块说明

* [ ] 状态机流转图在文档注释中说明

* [ ] 示例代码：展示如何使用 TaskBoard 完成一次完整流转



***

## 七、执行顺序（严格按此顺序）



1. **先读取现有文件**：

* `太初/任务管理-域/任务核心-府/任务模型-殿/任务定义-阁/任务-数据-园/任务数据.rs`

* `太初/任务管理-域/任务核心-府/任务模型-殿/状态定义-阁/状态-枚举-园/状态枚举.rs`

* `太初/任务管理-域/任务核心-府/任务仓库-殿/仓库实现-阁/仓库-模块-园/仓库模块.rs`

* `鸿蒙/基础能力-域/项目认知-府/执行语境-殿/语境管理-阁/语境-数据-园/语境数据.rs`

1. **阶段一：数据结构扩展**（按 3.1、3.2、3.3 节）

2. **阶段二：任务看板实现**（按 3.4 节）

3. **阶段三：上下文管理实现**（按 3.5 节）

4. **阶段四：集成与验证**（按第四节）

5. **最终验收**（按第六节逐项检查）



***

## 八、注意事项



1. **不要破坏现有代码**：所有扩展都是新增，不修改现有公共 API

2. **先编译再继续**：每个阶段完成后运行 `cargo check`，确认无错误再进入下一阶段

3. **中文命名一致性**：严格遵循项目现有中文命名规范

4. **错误处理要完整**：不要用 `unwrap()`，所有错误都要传播或处理

5. **测试要充分**：特别是状态流转的边界情况

6. **持久化要可靠**：JSONL 文件写入要原子化（先写临时文件再重命名），防止数据损坏

7. **上下文隔离要严格**：确保不同层级的上下文完全隔离，不能互相访问



***

**执行完成后，输出：**



1. 新增 / 修改的文件清单

2. 编译和测试结果

3. 集成测试的完整流转日志

4. 验收标准逐项检查结果

5. 已知限制和后续优化建议