// 状态共享 - 府 —— 跨府运行时状态共享（按类型ID索引）。
//
// 本质：进程内运行时状态容器——各府经本府读取/写入共享状态，不直接传参。
// 与 Service Definition 的区别：Service Definition 是服务接口注册（静态，启动时注册一次），
// 状态共享是运行时状态（动态，随时可写可读）。
// 与事件总线-府的分工：状态共享是"当前状态快照"（读最新值），事件总线是"事件通知"（通知变化发生）。
// 二殿：类型-定义-殿（状态键/状态值）· 状态-存取-殿（状态共享/读取/写入）。
// 跨府引用止步本入口，只认根符号，不深链到殿/阁/园。

#[path = "类型-定义-殿/模块.rs"]
pub mod 类型_定义_殿;

#[path = "状态-存取-殿/模块.rs"]
pub mod 状态_存取_殿;

pub use 状态_存取_殿::*;
pub use 类型_定义_殿::*;
