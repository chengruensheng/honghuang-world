//! 循环 - 驱动 - 园：鸿钧主循环（想法 → 要求 → 设计 → 实现 → 验收 → 定档）。
//!
//! D2 §2.10.1 按职责拆分为四个子模块（见同目录 `世界运行/`）：
//! - `主循环`：主政一轮 / 运行一轮 / 执行一条待执行任务线 / 打回撤销 / 临时天道守卫
//! - `现状拼装`：构造现状 / 落点护栏 / 取测试区摘录 / 红线文件清单
//! - `状态推进`：状态目录 / 读改写队列 / 推进要求状态 / 推进并警 / 任务线入队领取回填 / 指标告警
//! - `持久化`：持久化列表（泛型）/ 持久化要求们 / 持久化任务线们
//!
//! 子模块经 `pub use` re-export 至本模块，对外保持原 API 不变（crate 根经
//! `模块.rs` 的 `pub use 世界运行::*;` 继续透出 `主政一轮`/`主政回执`/`落对话记录` 等）。
//!
//! 注：父模块 `模块.rs` 用 `#[path = "世界运行.rs"]` 定位本文件，导致子模块默认查找
//! 路径不指向 `世界运行/` 目录；且模块名为非 ASCII 标识符，须显式 `#[path]` 指定文件。

#[path = "世界运行/主循环.rs"]
mod 主循环;
#[path = "世界运行/持久化.rs"]
mod 持久化;
#[path = "世界运行/状态推进.rs"]
mod 状态推进;
#[path = "世界运行/现状拼装.rs"]
mod 现状拼装;

pub use 主循环::*;
pub use 状态推进::*;
