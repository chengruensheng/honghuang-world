#[cfg(test)]
mod tests {
    use super::super::阶段提示::{阶段提示, 任务快照};
    use super::super::解析与净化::{提取json, 剥离杂质标签, 解析并构造};
    use super::super::构建核验::核验结论;
    use hm_cognition::AgentRole;
    use tc_task::{Task, TaskStatus};

    /// 供解析测试用的「永不通过」核验器（本组用例不触发核验）
    fn 空核验() -> 核验结论 {
        核验结论 { 通过: true, 摘要: String::new(), 原始输出: String::new() }
    }

    /// 设计层判定契约无解时，必须**如实上报终态**而非进入实现层（缺陷 15-1/15-2 的修复回归）。
    /// 否则下游要么把契约偷偷降级后自报通过（造假），要么拒绝实现后被验收反复打回至卡死。
    #[test]
    fn 圣人_声明无解_进已确认无解终态() {
        let 答复 = r#"{"边界定义":{"输入边界":"任意 u64"},"安全区域":[],"契约":[],"修改文件":[],"新建文件":[],"依赖":[],"无解声明":{"契约名":"精确取半","判定依据":"奇数无法被 2 整除，x/2*2≠x","类型":"数学无解"}}"#;
        let 产出 = 解析并构造(&AgentRole::圣人, TaskStatus::圣人设计中, 答复, &空核验, None).expect("应解析成功");
        assert_eq!(产出.下一状态, TaskStatus::已确认无解, "声明无解应直接进终态，不得进入实现层");
    }

    /// 契约可满足（无解声明为 null）时，必须正常进入实现层——不得因字段存在就误判无解。
    #[test]
    fn 圣人_未声明无解_正常进实现层() {
        let 答复 = r#"{"边界定义":{"输入边界":"u64"},"安全区域":[],"契约":[],"修改文件":[],"新建文件":[],"依赖":[],"无解声明":null}"#;
        let 产出 = 解析并构造(&AgentRole::圣人, TaskStatus::圣人设计中, 答复, &空核验, None).expect("应解析成功");
        assert_eq!(产出.下一状态, TaskStatus::待大罗金仙实现, "契约可满足应正常进入实现层");
    }

    /// 「无解声明」字段缺省（旧格式/模型未输出）时按可满足处理，向后兼容既有任务数据。
    #[test]
    fn 圣人_无解声明缺省_按可满足处理() {
        let 答复 = r#"{"边界定义":{},"安全区域":[],"契约":[],"修改文件":[],"新建文件":[],"依赖":[]}"#;
        let 产出 = 解析并构造(&AgentRole::圣人, TaskStatus::圣人设计中, 答复, &空核验, None).expect("应解析成功");
        assert_eq!(产出.下一状态, TaskStatus::待大罗金仙实现, "缺省无解声明应向后兼容为可满足");
    }

    #[test]
    fn 阶段提示_圣人_含无解声明要求() {
        let 任务 = Task::新建(1, "无解任务".to_string(), "描述".to_string(), 0);
        let 快照 = 任务快照(&任务);
        let 提示 = 阶段提示(&AgentRole::圣人, &快照);
        assert!(提示.contains("无解声明"), "圣人提示应告知可声明无解，实际: {提示}");
        assert!(提示.contains("不可能被满足"), "圣人提示应说明何时声明无解，实际: {提示}");
        assert!(提示.contains("虚假声明无解"), "圣人提示应警示虚假声明，实际: {提示}");
        assert!(提示.contains("不算声明"), "圣人提示应明确「写进边界定义文字里不算声明」，实际: {提示}");
    }

    /// 兜底识别：设计层把无解判定写进「边界定义」自然语言却漏填结构化字段时，
    /// 系统必须按强特征词兜底认定为无解（实测无损压缩任务即此形态：写下完整鸽巢原理证明却填 null）。
    #[test]
    fn 圣人_文本声明无解但字段为空_兜底进终态() {
        let 答复 = r#"{"边界定义":{"输出边界":"此契约在信息论上不可能被同时满足：鸽巢原理严格证明长度 n 的输入空间 2^(8n) 严格大于长度小于 n 的输出空间，必存在不可单射的输入"},"安全区域":[],"契约":[],"修改文件":[],"新建文件":[],"依赖":[],"无解声明":null}"#;
        let 产出 = 解析并构造(&AgentRole::圣人, TaskStatus::圣人设计中, 答复, &空核验, None).expect("应解析成功");
        assert_eq!(产出.下一状态, TaskStatus::已确认无解, "自由文本含强无解特征应兜底进终态");
    }

    /// 兜底不得误伤正例：正常可解需求的设计文档（无强无解特征词）必须照常进入实现层。
    /// 依据：全量历史设计文档扫描——5 个正例任务对特征词零命中。
    #[test]
    fn 圣人_文本无无解特征_不误判() {
        let 答复 = r#"{"边界定义":{"输入边界":"非负整数 n","输出边界":"返回第 n 个斐波那契数；对 n=0 返回 0，n=1 返回 1，n>=2 递推"},"安全区域":[],"契约":[{"契约名":"dp_fib","方法":[{"名称":"dp_fib","签名":"fn dp_fib(n: u64) -> u64","描述":"返回第 n 个斐波那契数"}],"描述":"标准动态规划实现"}],"修改文件":[],"新建文件":["src/lib.rs"],"依赖":[],"无解声明":null}"#;
        let 产出 = 解析并构造(&AgentRole::圣人, TaskStatus::圣人设计中, 答复, &空核验, None).expect("应解析成功");
        assert_eq!(产出.下一状态, TaskStatus::待大罗金仙实现, "正常可解需求不得被兜底误判为无解");
    }

    /// 落盘核验门：实现层宣称「新建/修改」的文件必须真实存在于工作区，否则当场判非法。
    /// 依据（2026-09-14 实证）：#51 实现层整轮零写操作，却输出完整实现文档并自检「通过: true」。
    #[test]
    fn 大罗金仙_宣称落盘的文件不存在_判非法() {
        let 临时 = std::env::temp_dir().join("hm_落盘核验_缺失");
        std::fs::create_dir_all(&临时).expect("建临时工作区");
        let 答复 = r#"{"代码变更":[{"文件路径":"编造.rs","变更类型":"新建","摘要":"无"}],"工具调用":[{"工具名":"write_file","参数":"{}","结果摘要":"写入成功","时间戳":1}],"自检":{"通过":true,"边界合规":true,"契约合规":true},"created_at":1}"#;
        let 错误 = match 解析并构造(&AgentRole::大罗金仙, TaskStatus::待大罗金仙实现, 答复, &空核验, 临时.to_str()) {
            Ok(_) => panic!("宣称新建的文件不存在应判非法"),
            Err(e) => e,
        };
        assert!(
            错误.to_string().contains("编造交付") && 错误.to_string().contains("编造.rs"),
            "错误须点明编造交付并列出缺失文件，实际: {错误}"
        );
    }

    /// 落盘核验门的正例：宣称的文件真实存在时必须放行（不得误伤真干活的一轮）
    #[test]
    fn 大罗金仙_宣称的文件真实存在_放行() {
        let 临时 = std::env::temp_dir().join("hm_落盘核验_存在");
        std::fs::create_dir_all(临时.join("子目录")).expect("建临时工作区");
        std::fs::write(临时.join("子目录").join("真实.rs"), "// 真").expect("写文件");
        let 答复 = r#"{"代码变更":[{"文件路径":"子目录/真实.rs","变更类型":"新建","摘要":"真"}],"工具调用":[{"工具名":"write_file","参数":"{}","结果摘要":"写入成功","时间戳":1}],"自检":{"通过":true,"边界合规":true,"契约合规":true},"created_at":1}"#;
        let 产出 = 解析并构造(&AgentRole::大罗金仙, TaskStatus::待大罗金仙实现, 答复, &空核验, 临时.to_str())
            .expect("文件已存在应放行");
        assert_eq!(产出.下一状态, TaskStatus::待准圣验收);
    }

    /// 落盘核验门：声称有代码变更却没有任何工具调用，自相矛盾，同样判非法
    #[test]
    fn 大罗金仙_有代码变更但零工具调用_判非法() {
        let 临时 = std::env::temp_dir().join("hm_落盘核验_零调用");
        std::fs::create_dir_all(&临时).expect("建临时工作区");
        let 答复 = r#"{"代码变更":[{"文件路径":"x.rs","变更类型":"新建","摘要":"无"}],"工具调用":[],"自检":{"通过":true,"边界合规":true,"契约合规":true},"created_at":1}"#;
        let 错误 = match 解析并构造(&AgentRole::大罗金仙, TaskStatus::待大罗金仙实现, 答复, &空核验, 临时.to_str()) {
            Ok(_) => panic!("零工具调用应判非法"),
            Err(e) => e,
        };
        assert!(
            错误.to_string().contains("「工具调用」数组为空"),
            "错误须点明无任何工具调用，实际: {错误}"
        );
    }

    #[test]
    fn 阶段提示_太乙金仙_含清理纪律() {
        let 任务 = Task::新建(1, "清理纪律任务".to_string(), "描述".to_string(), 0);
        let 快照 = 任务快照(&任务);
        let 提示 = 阶段提示(&AgentRole::太乙金仙, &快照);
        assert!(提示.contains("按名找文件"), "太乙金仙提示应先扫描残留，实际: {提示}");
        assert!(提示.contains("删除文件"), "太乙金仙提示应要求用删除文件工具清理，实际: {提示}");
        assert!(提示.contains("{\"模式\":\"**/*.bak\"}"), "太乙金仙提示应给出显式模式参数示例（防空参连败），实际: {提示}");
        assert!(提示.contains("重新扫描"), "太乙金仙提示应要求删除后复核为空，实际: {提示}");
        assert!(提示.contains("不得在工具失败时直接宣告清理完成"), "太乙金仙提示应禁止失败即宣告，实际: {提示}");
    }

    #[test]
    fn 提取json_无杂质_原样返回() {
        let 输入 = r#"{"a":1}"#;
        assert_eq!(提取json(输入).as_deref(), Some(r#"{"a":1}"#));
    }

    #[test]
    fn 提取json_后附代码块标记_正确截断() {
        let 输入 = "以下是设计文档：\n```json\n{\"边界定义\":{\"输入\":\"n\"},\"输出\":{}}\n```\n完毕";
        let 提取 = 提取json(输入).expect("应提取到 JSON");
        // 截取到配平的 }，不含尾随 ``` 标记
        assert_eq!(提取, r#"{"边界定义":{"输入":"n"},"输出":{}}"#);
    }

    #[test]
    fn 提取json_字符串内含括号_不误判() {
        // 字符串值里含 { 与 }，括号配平应跳过字符串内容
        let 输入 = r#"{"描述":"斐波那契 F(n) 用 {} 表示边界","值":1}"#;
        assert_eq!(
            提取json(输入).as_deref(),
            Some(r#"{"描述":"斐波那契 F(n) 用 {} 表示边界","值":1}"#)
        );
    }

    #[test]
    fn 提取json_嵌套对象_取最外层() {
        let 输入 = r#"{"轮次":[{"通过":true,"问题":["a","b"]}],"最终结果":true}"#;
        let 提取 = 提取json(输入).expect("应提取到嵌套 JSON");
        assert_eq!(提取, r#"{"轮次":[{"通过":true,"问题":["a","b"]}],"最终结果":true}"#);
    }

    #[test]
    fn 提取json_无括号_返回空() {
        assert!(提取json("没有 JSON 内容").is_none());
    }

    #[test]
    fn 提取json_只有左括号未闭合_返回空() {
        assert!(提取json("内容 { 未闭合").is_none());
    }

    #[test]
    fn 剥离杂质_think标签_完整剥离() {
        let 输入 = r#"<think>内部推理{"key":"val"}</think>

{"代码变更":[],"自检":{}}"#;
        let 净化 = 剥离杂质标签(输入);
        assert!(!净化.contains("内部推理"), "think 内容应被剥离");
        assert!(!净化.contains("<think>"), "think 标签应被剥离");
        let json = 提取json(&净化).expect("应提取到正文 JSON");
        assert!(json.contains("代码变更"), "应提取到正文 JSON 而非 think 内碎片");
    }

    #[test]
    fn 剥离杂质_think大小写不敏感() {
        let 输入 = r#"<Think>思考过程</Think>{"结果":true}"#;
        let 净化 = 剥离杂质标签(输入);
        let json = 提取json(&净化).expect("大写 Think 也应剥离");
        assert_eq!(json, r#"{"结果":true}"#);
    }

    #[test]
    fn 剥离杂质_代码块围栏_提取内容() {
        let 输入 = "以下是文档：\n```json\n{\"边界定义\":{\"输入\":\"n\"}}\n```\n完毕";
        let 净化 = 剥离杂质标签(输入);
        let json = 提取json(&净化).expect("应从代码块中提取 JSON");
        assert_eq!(json, r#"{"边界定义":{"输入":"n"}}"#);
    }

    #[test]
    fn 剥离杂质_无杂质_原样保留() {
        let 输入 = r#"{"直接":"json","值":42}"#;
        assert_eq!(剥离杂质标签(输入), 输入);
    }

    #[test]
    fn 剥离杂质_think未闭合_跳过标签保留后续() {
        let 输入 = r#"<think>未闭合的思考{"正文":true}"#;
        let 净化 = 剥离杂质标签(输入);
        // 未闭合时跳过  标记本身，后续内容当正文
        let json = 提取json(&净化).expect("未闭合 think 后仍应提取 JSON");
        assert_eq!(json, r#"{"正文":true}"#);
    }
}
