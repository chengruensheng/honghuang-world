# 智能体 · 天庭治理-府 + 道术施展-府

> 定位：**多智能体架构（世界之身）**——四层组织 + 角色执行。
> 落位：`鸿蒙 / 基础设施-域 / 天庭治理-府`（lib = `tianting_fu`）+ `道术施展-府`（lib = `daoshu_fu`）。
> 本质：**组织编排管组织，任务执行管角色如何执行。**
> 状态：最终命名定稿 · 活文档（后续新增角色/工作流并入对应小节）。

---

## 一、两府分工

| 府 | 职责 | 一句话 |
|:--|:--|:--|
| **天庭治理-府** | 组织编排（管组织） | 想法 → 要求 → 设计 → 验收 → 版本 → 进化 |
| **道术施展-府** | 任务执行（管角色如何执行） | 每个角色按自己的道术跑 L1-L4 工作流 |

## 二、天庭治理-府（组织编排）

四层神仙组织 + 两阶段生长：

| 层 | 角色 | 职责 |
|:--|:--|:--|
| 顶层 | 界主（天意） | 发布想法 |
| 天层 | 天道（巡世）+ 鸿钧（主政） | 发现 / 化要求 / 派遣 / 验收 / 定档 |
| 中层 | 女娲 + 四圣 | 初稿设计 + 评审 |
| 底层 | 大罗金仙 + 准圣 | 写代码 / 六维验收 |

八殿（实际落地）：

| 殿 | 职责 |
|:--|:--|
| 类型-定义-殿 | 核心类型契约：八态状态机 / 世界状态 / 要求书（声名-集中） |
| 天层-主控-殿 | 鸿钧回路（主政-落笔：化要求/确认设计/验收裁决/定档）+ **鸿钧-对话-阁（界主对话循环：对话-循环/意图-判别/汇报-拼装）** + 天道回路（巡世-扫描） |
| 智慧-主控-殿 | 中层设计（模板-生成：按类别套设计方案）+ **圣人-商讨-阁（商讨-编排/评审-汇总：女娲初稿+四圣评审，分级单轮/多轮）** |
| 队列-调度-殿 | 要求 / 设计队列（落盘 jsonl）+ 八态状态推进 |
| 团队-调度-殿 | 拆解项 → 道术施展-府 执行任务（派发-落单） |
| 版本-存档-殿 | 源码快照 + 版本记录 + 回退（快照-落库） |
| 进化-主控-殿 | 进化环：失败归因 → 调整级别（仅乙阶段；当前只归因留痕，不触发调整） |
| 驱动-入口-殿 | 世界运行主循环：想法 → 要求 → 设计 → 实现 → 验收 → 定档（循环-驱动；多任务线调度） |

> 注：入口注释曾写"七殿"，实际已落 8 殿（驱动-入口-殿未计入早期注释）；本表为当前事实。

**鸿钧主控化（界主对话模型，2026-08-17 入稿）**：鸿钧 = 界主的对话伙伴 + 任务总控——闲聊/发布任务/澄清/追问/并发发新任务都在对话中发生；圣人商讨分级（简单单轮/复杂多轮）；关键节点自动汇报、追问可展开详情；消息可见性：界主发言非@仅鸿钧可见、@点名直达，角色发言全员可见。详见 多智能体架构设计.md §1.5.5。

**进化环边界（经/权）**：进化环只可自动调整「权」格位；触及「经」格位必须走 ⑤级结构级 + 界主确认。

## 三、道术施展-府（任务执行）

- 角色工作流引擎（agent_fu 升级）。
- 每个层级角色按自己的"道术"执行：天层 / 智慧 / 手脚 各角色 L1-L4 工作流。
- 铁律：只执行，不越级组织；大罗金仙分道执行，不跨道硬扛。

五殿（实际落地）：

| 殿 | 职责 |
|:--|:--|
| 类型-定义-殿 | 执行域契约：工作流级别 / 执行角色卡 / 执行任务 / 执行回执 |
| 角色-卡册-殿 | 角色卡登记落册（身份 → 道/职司/模型池/契约） |
| 工作流-编排-殿 | L1-L4 四档执行步骤定义（分步-驱动） |
| 任务-调度-殿 | 执行主链路：提示渲染 → 工具循环（function calling）→ 构建自证 → 失败续跑（派发-落单） |
| 手脚-施展-殿 | 智能体手脚架工具集：文件读写改删 / 目录浏览 / 内容检索 / 命令执行（沙箱隔离） |

> 手脚-施展-殿 10 个工具（写/读/改/删文件、列目录、找文件、搜内容、运行命令、读格位、查格位历史）；其中「读格位」「查格位历史」直连识海承载-府记忆，不属文件/命令类手脚。

## 四、依赖关系

```
天庭治理-府（组织） ──调度执行──▶ 道术施展-府（执行）
       │ 读投影 / 写回填                 │ 读投影
       ▼                                 ▼
              识海承载-府（记忆）
```

- 天庭治理-府 → 识海承载-府：读投影 / 回填记忆。
- 天庭治理-府 → 道术施展-府：团队-调度-殿交设计、收产物。
- 道术施展-府 → 识海承载-府：角色执行读投影。
- 三者都是府（lib crate），跨府引用止步 lib 根，不深链。
