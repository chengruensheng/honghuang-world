// 面板组件.js —— 右栏属性面板（依赖导航核心，委托当前视图渲染属性）

import { 订阅切换 } from '../../../运行支撑-殿/路由导航-阁/导航-核心-园/导航核心.js';

/** 挂载属性面板，随视图切换渲染对应视图的属性内容 */
export function 挂载面板(容器) {
  订阅切换((视图) => {
    if (视图 && typeof 视图.属性 === 'function') {
      视图.属性(容器);
    } else {
      容器.innerHTML = '';
    }
  });
}