# 层级结构设计

> 定位：洪荒世界层级结构（维度/域/府/殿/阁/园）的完整规范——每层怎么拆、装什么、边界怎么走、园怎么分类。
> 用途：可迁移到其他项目作层级架构参考；也是本项目的重构蓝图与 AI 生成约束。
> 状态：定稿 · 2026-08-13 · 界主拍板 · 2026-08-20 入稿「九是维度，府是插件」。

---

## 一、设计总纲

1. **六层完整保留**：维度 → 域 → 府 → 殿 → 阁 → 园，不压缩、不跳层。
2. **能力只在园**：实际代码与数据只落在园；中间层只放「模块桥接」（`模块.rs`，仅 `pub mod` 声明子层，不写实现、不写编排）。
3. **鸿蒙为地基**：所有通用能力集中鸿蒙，其余维度只留领域能力。
4. **活文档**：本稿随世界生长——后续新增殿/阁/园，先在本稿对应章节落笔再落码；实现须能在本稿溯源，溯不到源视为补丁。
5. **九是维度，府是插件**（2026-08-20 界主拍板）：九根是「物以类聚」的维度分类（本体 5 根 + 工程 4 根），府是可挂载可替换的**插件单元**。府暴露 Service Definition（插件接口），跨府引用经插件注册表查找，鸿蒙是插件注册表 + 事件总线 + 状态共享的提供者。殿/阁/园作为插件内部组织保留，不扁平化。详见 §三边界模型、§五鸿蒙地基模型、§六依赖与跨层约束。

---

## 二、六层结构

| 层 | 回答 | 职责 | 承载文件 |
|:--|:--|:--|:--|
| 维度 | 世界由什么构成 | 本体范畴 + 工程需求，9 根 | 无 |
| 域 | 靠什么活着 | 存在方式，语义分组 | 无（纯目录） |
| 府 | 什么能力 | 编译单位（lib crate），唯一引用止步点 | 入口.rs（lib 根聚合） |
| 殿 | 哪些模块 | 模块划分 | 模块.rs（桥接） |
| 阁 | 什么功能 | 功能分组 | 模块.rs（桥接） |
| 园 | 怎么实现 / 存哪 | 实现与数据落点 | 模块.rs（桥接）+ 实现文件 |

九根（维度）：
- 本体维度（5 根 · 创世链，回答"世界由什么构成"——时间 / 空间 / 存在等范畴）：太初=存在 · 混沌=可能性 · 乾坤=空间（容器）· 道韵=法则 · 量劫=时间（劫数）
- 工程需求维度（4 根 · 保障链，工程必需支撑）：鸿蒙=地基 · 证道=测试 · 道果树=构建 · 传承殿=传承

### 2.1 切分纪律（拒绝单链）

六层每一层都要承担真实切分，避免「单链下沉、末端堆积」的无效平铺：

1. **拒绝单链**：府 → 殿 → 阁 的中间层不得只有一个子节点（一个府只 1 殿、一个殿只 1 阁 = 该层形同虚设）。中间层应切出 ≥2 个子节点；确因语义无法再拆的，说明切分粒度失衡，需合并上层或细化下层，而非保留单链。
2. **均衡切分**：同一层兄弟节点的子节点数大致相当，避免「一端堆满、一端单支」。
3. **准确切分**：切分依据模块 / 功能的真实语义边界，不硬凑数、不偷懒合并。园层例外——园是最终落点，允许 1~N 个园。

---

## 三、边界模型

**府 = 插件单元（lib crate + Service Definition），也是唯一的引用止步点。**

府作为插件单元（2026-08-20 入稿）：
- 每个府是一个 lib crate，暴露 Service Definition（插件接口，等价于原 lib 根透出的符号）。
- 跨府引用经插件注册表查找目标府的 Service Definition，止步于 Service Definition（等价于原「止步 lib 根」），禁深链点到园内具体文件。
- 鸿蒙是插件注册表 + 事件总线 + 状态共享的提供者（§五）。
- 府可运行时挂载/替换/组合（插件化），新增能力只需写一个府插件，不必走完整六层。
- 殿/阁/园作为插件内部组织保留，不扁平化——插件内部仍需分层组织代码。

**Service Definition 机制（2026-08-21 入稿）**：

Service Definition = 府在 lib 根定义的服务 trait（如 `shihai_fu::识海服务` trait），包含该府对外暴露的方法签名。服务 trait 就是 Service Definition 的具体形式，等价于原 lib 根透出的符号。

机制：
1. 插件上下文维护服务注册表 `HashMap<TypeId, Arc<dyn Any + Send + Sync>>`，按类型 ID 索引服务实例。
2. 每个府在 `应用()` 方法中创建服务实例（`Arc<dyn 服务trait>`），注册到插件上下文。
3. 跨府调用方 `use 目标府::服务trait`（止步 lib 根），经插件上下文查找服务实例，调用 trait 方法。
4. 注册：`ctx.注册服务(服务实例)`；查找：`ctx.查找服务::<Arc<dyn 服务trait>>()` → `Option<Arc<dyn 服务trait>>`。

渐进式改造：本阶段只加 Service Definition 机制（插件上下文加服务注册表 + 注册/查找方法），不删现有 `use shihai_fu::` 跨府引用；后续逐步把跨府引用改为经插件上下文查找服务实例。

文件落点（三态）：
- **域 = 纯目录**：不在编译树内，不建任何文件。
- **府 = `入口.rs`**：`Cargo.toml` 的 `lib.path` 指向它，只 `pub mod` 各殿 + `pub use` 透出符号，不写实现、不写编排。
- **殿 / 阁 / 园 = `模块.rs` 桥接**：每层一个，只 `pub mod` 子层 + `pub use` 透出，不写实现、不写编排。园内实现文件才是真正承载代码/数据的地方。

引用规则：
- 跨府：经插件注册表查找目标府的 Service Definition，`use {府lib}::符号` 止步 lib crate 根（Service Definition 暴露点）。
- 同府内跨园：经共同上级 `模块.rs` 透出的符号中转，不深链点到对方园文件。
- 跨维：经鸿蒙（插件注册表 + 事件总线 + 状态共享），不直调。
- 领域维度之间：不直连，经鸿蒙中转。

---

## 四、园的分类

园是原子承载单元，按内容性质分六类：

| 类型 | 命名 | 装什么 | 示例 |
|:--|:--|:--|:--|
| 代码园 | `XX-园` | 实现代码（.rs 等） | 原子-写入-园 |
| 存储园 | `XX-数据` | 数据文件（.json / .jsonl / .db / .csv 等） | 任务-状态-数据 |
| 配置园 | `XX-配置` | 配置文件（.toml / .yaml / .ini 等） | 密钥-注入-配置 |
| 模板园 | `XX-模板` | 生成样板（.hbs / .j2 / .tmpl 等） | 邮件-渲染-模板 |
| 脚本园 | `XX-脚本` | 自动化脚本（.sh / .ps1 / .bat 等） | 浏览器-校验-脚本 |
| 资源园 | `XX-资源` | 静态资源（图片 / 字体 / 图标等） | 图标-库-资源 |

---

## 五、鸿蒙地基模型

**鸿蒙 = 盘古 = 唯一地基，承载所有通用能力。**

通用能力（归鸿蒙）：
- 文件读写、网络访问、日志、密钥/配置、缓存、限流/熔断、事件总线、状态共享、模型连接、翻译、段库/上下文。

领域能力（留各维度）：
- 规则求值、命运蓝图、因果管理、界面呈现、工作台、任务分解等带领域语义的逻辑。

分类判据：一个能力去掉"领域名词"后仍完整成立 → 通用能力，归鸿蒙；否则 → 领域能力，留原维度。

鸿蒙内部仍按 域→府→殿→阁→园 分层，对外统一为"地基"语义，不抢其他维度职能。

鸿蒙承载"运行时通用能力"；测试（证道）/ 构建（道果树）/ 文档（传承殿）是工程配套维度，不进运行时、不归鸿蒙。

鸿蒙作为插件基础设施提供者（2026-08-20 入稿）：
- **插件注册表**：府插件的注册、查找、卸载——跨府引用的统一入口。
- **事件总线**：跨维事件中转——原「跨维经鸿蒙」的落地机制。
- **状态共享**：跨维状态读写——原「跨维经鸿蒙状态共享」的落地机制。

> 鸿蒙既是「通用能力地基」，又是「插件基础设施提供者」——两者一致：插件注册表/事件总线/状态共享本身就是通用能力，归鸿蒙天经地义。

---

## 六、依赖与跨层约束

1. **单向向下**：维度 → 域 → 府 → 殿 → 阁 → 园，下层永不反向引用上层。
2. **跨府经插件注册表**（2026-08-20 入稿）：引用经插件注册表查找目标府的 Service Definition，止步于 Service Definition（等价于原「止步 lib 根」），禁深链点到园内具体文件。
3. **跨维经鸿蒙**：9 根之间一切调用/事件/状态经鸿蒙（插件注册表 + 事件总线 + 状态共享），禁跨维直调。
4. **领域维度之间不直连**：经鸿蒙事件总线 / 状态共享中转。
5. **同府跨园经共同上级**：同府内两个园不直连，经共同上级 `模块.rs` 透出的符号中转，不深链点到对方园文件。

### 6.1 跨府引用（正反例 · 插件化）

场景：鸿蒙内部，`流水线编排-府` 调用 `文件访问-府` 的原子写入能力（两府各自独立 lib crate，均为插件）。

`文件访问-府`（lib = `infra_file`）内部均衡切分：

```text
文件访问-府/
├── 入口.rs                    ← lib 根（Service Definition 暴露点，引用止步点）
├── 文件-读写-殿/              ← 模块 1
│   ├── 写入-落盘-阁/          ← 功能 1
│   │   └── 原子-写入-园/      ← 原子写入在这里
│   └── 读取-加载-阁/          ← 功能 2
└── 文件-校验-殿/              ← 模块 2
    ├── 格式-校验-阁/
    └── 完整性-校验-阁/
```

```rust
// ✓ 正例：经插件上下文查对方 Service Definition，止步于 lib 根透出的服务 trait
use infra_file::文件服务;  // infra_file = 文件访问府的 Service Definition（服务 trait）
use std::sync::Arc;

pub fn 落盘结果(ctx: &插件上下文, 路径: &str, 内容: &str) -> Result<(), 错误> {
    let 文件 = ctx.查找服务::<Arc<dyn 文件服务>>()
        .ok_or("文件访问府服务未注册")?;
    文件.原子写入(路径, 内容)?;   // 跨府调用经插件上下文查找服务实例
    Ok(())
}
```

```rust
// ✗ 反例：深链点到对方园内具体文件（写入只是文件访问-府的一条支链）
use infra_file::文件_读写_殿::写入_落盘_阁::原子_写入_园::原子写入;  // 禁止
```

> 关键：`文件服务` trait 能在 `infra_file::` 根拿到，靠「园 → 阁 → 殿 → 府」逐层 `pub use` 透出。跨府方经插件上下文只认 Service Definition（服务 trait），不认中间路径。当前阶段（直接 `use infra_file::原子写入`）与目标阶段（经 `查找服务::<Arc<dyn 文件服务>>()` 查实例）止步点相同——都是 lib 根。

### 6.2 跨维度引用（正反例）

场景：`乾坤`（界面）记录一次用户操作异常到 `量劫`（日志/违规登记）。

```rust
// ✗ 反例：跨维直调量劫的府
use anomaly_log::记录日志;   // 禁止：乾坤 → 量劫 直连
```

```rust
// ✓ 正例：经鸿蒙事件总线中转（唯一通道）
use collab_event_bus::发布事件;

pub fn 处理超时(追踪id: &str) -> Result<(), 错误> {
    发布事件(事件::异常发生 {
        追踪id,
        描述: "用户操作超时",
    })?;   // 鸿蒙把事件路由给量劫的日志记录府
    Ok(())
}
```

```rust
// ✓ 另一种正例：经鸿蒙状态共享读写跨维状态
use collab_state_share::写入状态;

pub fn 记录异常(记录: 异常记录) -> Result<(), 错误> {
    写入状态("异常/最新", 记录)?;   // 量劫从鸿蒙状态共享读取
    Ok(())
}
```

> 三条铁律：跨府经插件上下文只认 Service Definition（`{府lib}::服务trait`），路径含 `殿/阁/园` 即深链违规；跨维永不 `use` 对方维度的府，一律经鸿蒙；鸿蒙是中立中介（插件注册表 + 事件总线 + 状态共享，只转发事件/存共享状态/查插件，不替量劫干活）。

---

## 七、命名规范

### 7.1 段数与后缀

- 域 / 府：2 段（`配置管理-府`）；殿 / 阁 / 园：3 段（`配置-读写-殿`）。
- 园 = 2 段名字 + 1 段类型后缀（共 3 段）：代码园 `-园`、存储园 `-数据`、配置园 `-配置`、模板园 `-模板`、脚本园 `-脚本`、资源园 `-资源`。

### 7.2 每层词义（层层递进）

各层回答不同的问题，命名体现语义递进，词不重复：

| 层 | 回答 | 命名语义 | 示例 |
|:--|:--|:--|:--|
| 域 | 靠什么活着 | 领域 | 环境适应 |
| 府 | 什么能力 | 能力 | 配置管理 |
| 殿 | 拆成哪些模块 | 模块 | 配置-读写 |
| 阁 | 做什么 | 功能 | 密钥-加载 |
| 园 | 怎么做 | 实现手段 | 原子-写入 |

### 7.3 判据 A · 园用实现手段词

代码园（`-园`）名必须含一个「实现手段词」，且该词**不得出现在本链路任何上层名字中**。其余类型园（数据 / 配置 / 模板 / 脚本 / 资源）不适用本判据。

实现手段词表：流式 / 多轮 / 原子 / 批量 / 并行 / 缓存 / 降级 / 兜底 / 分块 / 增量 …

### 7.4 判据 B · 禁换序

下层名**禁止**是上层名的纯词序重排；下层必须「新增一个词」或「把某词替换为更具体的词」。

- 换位置 = 违规；加新信息 = 合规。

### 7.5 标识符

- 全中文，代码标识符中文（类型 UpperCamelCase，函数/变量 snake_case）。
- 目录名用连字符（`文件-读写-殿`），Rust 模块名用下划线（`文件_读写_殿`），由 `#[path]` 桥接。
- 英文豁免（厂商标识 / 库名 / 工具标识）按既有白名单执行。

### 7.6 域名语义可辨

域的回答是「靠什么活着」，域名必须一眼看出具体领域；严禁用「恒观」这类看不出究竟是什么的词做域名。

---

## 八、落地规范

1. 内容落园：代码 → 代码园；数据 → 存储园；配置 → 配置园；模板 → 模板园；脚本 → 脚本园；静态资源 → 资源园。
2. 府 `Cargo.toml`：`lib.path` 指向 `入口.rs`，入口只 `pub mod` + `pub use`。
3. 域纯目录无文件；殿 / 阁 / 园各放 `模块.rs` 桥接，只 `pub mod` 子层，不写实现、不写编排。
4. 通用能力入鸿蒙，领域能力留原维度（按 §五 判据）。
5. 督察判据与本设计对齐。
6. **园内多实现文件**：一个代码园可承载多个实现文件（先例：`命令-沙箱-园` = 命令沙箱.rs + 沙箱护栏.rs）。拆分纪律：
   - 按职责切（schema / 执行 / 编排 / 解析 / 收集 等语义边界），不硬凑；
   - 园 `模块.rs` 逐一 `#[path]` 声明各文件并 `pub use` 透出，上层殿/阁/入口桥接不变；
   - 园内文件间共享的符号标 `pub(crate)`（非对外接口不升 pub）；对外跨府符号保持 `pub` 从园模块透出；
   - 模块名须避开同文件导出的 pub 符号名（E0255 重名），必要时加后缀区分；
   - 单一职责内聚的园（如 `命令-沙箱-园`）不拆，避免把同件事切成碎片。
   - 已按本规范拆分的园：类型-声明-园、扫描-落格位-园、工具-循环-园、派发-落单-园、主政-落笔-园、增量-检测-园。

---

*层级结构设计 · 定稿 · 2026-08-13 · 界主拍板*
