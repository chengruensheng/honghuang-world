# 项目心智模型 · 详细设计稿

> 定位：为任意一个软件项目建立一个「记忆」——动态更新、全部都在、需要时回想、不需要时专注现在。
> 用途：可迁移到任何新项目；是本项目的重构蓝图与 AI 生成约束。
> 状态：详细设计稿 · 待评审 · 活文档（后续新增格位/记忆规则并入对应小节）。

---

## 0. 一句话本质

给项目造一个**记忆体**，而不是数据库。核心原则：**代码负责记"事实"，LLM 负责补"语义"，人兜底填"根"。**

---

## 1. 三个环节

```
编码（谁来记）  →  存储（全都在）  →  检索（按需回想）
```

| 环节 | 回答 | 关键动作 |
|:--|:--|:--|
| 编码 | 谁来记 | 代码扫描 / LLM 按种子探索 / 人类录入 |
| 存储 | 记在哪 | 六范畴分格位，每格位独立，防污染 |
| 检索 | 怎么想起 | 顺序三档排布 + 按需投影 |

---

## 2. 数据结构

### 2.1 格位（Bucket）—— 心智模型的基本单元

```
格位 {
  名字        // 唯一标识，自解释（如「初心·使命」「结构」），不设编号
  范畴        // 六范畴之一
  种子提示词   // 一句本质描述，LLM 按它去项目里找内容（见 §6.2）
  来源        // 主要填充源：代码 / LLM / 人类
  固化度      // 经 = 不可改 / 权 = 可迭代
  共享度      // 共享 = 复用 / 私有 = 隔离
  顺序档位    // 最前 / 中间 / 最后（对齐注意力，见 §7.1）
  推荐位置    // 降级链第一级的建议查找处（见 §7 降级链）
  存储        // 独立存储文件，文件名 = 格位名（如「初心·使命.jsonl」）
}
```

### 2.2 记录（Entry）—— 格位里的一条内容

```
记录 {
  格位名        // 属于哪个格位
  内容        // 记下的内容
  证据        // 来源证据：从「XX」第 N 段 /「XX」符号 /「人」 推断
  时间戳      // 写入时间
  来源        // 代码 / LLM / 人类
  前记录      // 版本链（只增不改，可追溯）
  坐标        // 可选：微观坐标（层·对象·属性），见 §5
}
```

### 2.3 坐标（Coordinate）—— 微观定位

```
坐标 {
  层          // 项目 / 模块 / 文件 / 符号 / 代码
  对象        // 如「函数」「接口」「源文件」
  属性        // 如「签名」「依赖」「语义」
}
```

### 2.4 心智模型整体

```
心智模型 {
  元信息      // 工作区 + 工具清单（走配置，不占格位名额）
  格位们        // 36 日常 / 72 全量
  坐标网格    // 五级颗粒度（64 / 128 / 256 / 512）—— 已废弃，见 §5
  索引        // 名字→格位、坐标→记录 的定位索引 —— 坐标索引已废弃；依赖图索引保留
  会话记录    // 最近上下文的完整记录（工作记忆，见 §7.4）
}
```

### 2.5 元信息（不占格位名额）

心智模型有两项元信息，是系统运行的"根配置"，不是项目内容，因此不占 36/72 名额：

```
工作区 {
  根路径      // 项目的绝对根目录（坐标原点）
  范围        // 哪些目录属于项目
  排除项      // 版本库 / 依赖 / 构建产物 / 临时目录 等
}

工具清单 {
  工具们      // 系统可用工具的列表（硬编码配置）
}
```

- **工作区**：来源 = 代码探测 + 人类指定；固化 = 经；共享 = 共享。拼装时作为**最前**的固定前缀注入，模型第一眼就知道「我在哪、边界到哪」。它也是扫描范围、路径解析、工作目录抉择的基准。
- **工具清单**：硬编码配置，按角色过滤可见工具。

---

## 3. 36 格位详细表（日常主用）

每个格位一行，含种子提示词与全部属性。**这是填充与拼装的核心依据。**

| 格位名 | 范畴 | 种子提示词（本质） | 来源 | 固化 | 共享 | 顺序 |
|:--|:--|:--|:--|:--|:--|:--|
| 初心·使命 | 目标 | 项目的根本目的、为什么存在、承担什么职责 | 人类 | 经 | 共享 | 最前 |
| 铁律·总纲 | 规则 | 不可破的硬约束与最高准则 | 人类 | 经 | 共享 | 最前 |
| 价值观·原则 | 自我 | 诚实不编造等操守与行为原则 | 人类 | 经 | 共享 | 最前 |
| 底线 | 自我 | 安全红线、绝不能做的事 | 人类 | 经 | 私有 | 最前 |
| 身份 | 自我 | 角色定义（我是谁） | 人类 | 经 | 共享 | 最前 |
| 标准 | 规则 | 命名 / 格式 / 红线 | 人类 | 经 | 共享 | 最前 |
| 架构 | 规则 | 技术架构原则 | 人类+代码 | 经 | 共享 | 最前 |
| 任务 | 目标 | 当前任务指令 | 人类+LLM | 权 | 私有 | 最前 |
| 状态 | 世界 | 当前运行状态 | 代码+LLM | 权 | 共享 | 最前 |
| 目标 | 目标 | 当前阶段目标与长远目标 | 人类+LLM | 权 | 共享 | 最后 |
| 验收标准 | 目标 | 完成的判据 | 人类 | 权 | 共享 | 最后 |
| 阶段·进度 | 世界 | 当前阶段与任务进度 | 代码+LLM | 权 | 共享 | 最后 |
| 阻塞 | 世界 | 当前阻塞与风险 | LLM | 权 | 共享 | 最后 |
| 验收结果 | 世界 | 本轮测试 / 验收结果 | 代码 | 权 | 共享 | 最后 |
| 承诺 | 自我 | 承诺事项 | LLM | 权 | 私有 | 最后 |
| 方向 | 目标 | 长期演进路径 | 人类 | 经 | 共享 | 中间 |
| 需求 | 目标 | 原始需求与迭代需求 | 人类+LLM | 权 | 共享 | 中间 |
| 期望·意向 | 目标 | 阶段性期望与当前意图 | LLM | 权 | 私有 | 中间 |
| 能力 | 自我 | 能力边界 + 可用工具 | 代码 | 权 | 私有 | 中间 |
| 权限 | 自我 | 操作权限范围 | 人类 | 经 | 私有 | 中间 |
| 自省·觉察 | 自我 | 自我检查清单与觉察 | LLM | 权 | 私有 | 中间 |
| 细则·解读 | 规则 | 可操作规范与解读 | LLM | 权 | 共享 | 中间 |
| 世界观 | 规则 | 项目世界观 | 人类 | 经 | 共享 | 中间 |
| 传承·决策 | 规则 | 历史决策与经验沉淀 | LLM | 权 | 共享 | 中间 |
| 环节规则 | 程序 | 各环节执行规则 | LLM | 权 | 共享 | 中间 |
| 例外·临时 | 程序 | 例外豁免与临时约束 | LLM | 权 | 共享 | 中间 |
| 环境·依赖 | 世界 | 平台 / 运行时 / 外部库 | 代码 | 权 | 共享 | 中间 |
| 结构 | 世界 | 目录 / 模块组织（含 pub API 签名清单，定档扫描写入，实体键=API·{lib名}） | 代码 | 权 | 共享 | 中间 |
| 文件 | 世界 | 文件清单与入口 | 代码 | 权 | 共享 | 中间 |
| 数据 | 世界 | 数据资源 | 代码 | 权 | 共享 | 中间 |
| 调用 | 世界 | 工具调用记录与返回 | 代码 | 权 | 共享 | 中间 |
| 事件 | 经历 | 已发生的事实与事件流 | 代码+LLM | 经 | 共享 | 中间 |
| 变更 | 经历 | 代码与配置变更 | 代码 | 经 | 共享 | 中间 |
| 产物 | 经历 | 产出物清单与构建产物 | 代码 | 经 | 共享 | 中间 |
| 教训 | 经历 | 错误与规避 | LLM | 权 | 共享 | 中间 |
| 理解·记忆 | 经历 | 抽象认知与零散记忆 | LLM | 权 | 私有 | 中间 |

> 工具清单不占 36 名额（硬编码配置，走配置不落格位）。


## 4. 72 格位（复杂全量）= 36 格位的细分

36 格位在复杂项目下拆细，覆盖同一语义的多个侧面。按六范畴：

**① 目标记忆（13）**：初心、使命、方向、需求、目标、验收标准、任务、期望、意向、需求·原始、需求·迭代、目标·阶段、目标·长期

**② 规则记忆（13）**：铁律、总纲、标准、细则、解读、世界观、传承、决策、架构、标准·命名、标准·格式、决策·选型、传承·经验

**③ 自我记忆（9）**：价值观、原则、底线、身份、能力、权限、自省、觉察、承诺

**④ 程序记忆（7）**：阶段规则、任务规则、执行规则、校验规则、触发规则、例外、临时规则

**⑤ 世界记忆（16）**：环境、依赖、结构、文件、数据、状态、调用、阶段、进度、阻塞、验收结果、工具、结构·目录、结构·模块、文件·清单、文件·入口

**⑥ 经历记忆（14）**：事件、变更、产物、教训、理解、记忆、事件·关键、事件·流、变更·代码、变更·配置、产物·清单、产物·构建、理解·认知、理解·碎片

---

## 5. 五级颗粒度坐标网格（64 / 128 / 256 / 512）

> ⚠️ **已废弃**（2026-08-16 清理）：本章节的坐标网格机制已从代码移除。
> 定位需求已被「格位六范畴 + 依赖图（符号档案 + 结构树）」覆盖。
> 废弃原因：①功能重复；②半成品（五级颗粒度只用到文件层）；③死代码（坐标字段写了没人读）。
> 原设计保留如下，仅作历史参考。

微观信息按「对象 × 属性」定位，构成「索引」的主体。

**模块层 64 = 8 × 8**
- 对象：模块、领域、目录、接口、构建、配置、部署、测试
- 属性：规则、目标、约束、实际、依赖、接口、用途、历史

**文件层 128 = 16 × 8**
- 对象：源文件、文档、样式、数据、资源、模板、配置、测试、脚本、组件、页面、服务、模型、视图、入口、库
- 属性：同上 8 属性

**符号层 256 = 16 × 16**
- 对象：函数、类、方法、属性、变量、常量、类型、接口、事件、钩子、状态、引用、导入、导出、装饰、泛型
- 属性：签名、类型、参数、返回、语义、用例、错误、边界、调用者、被调用、变更、测试、注释、复杂度、风险、待议

**代码层 512 = 32 × 16**
- 对象：语句、表达式、调用、赋值、返回、条件、循环、匹配、解构、闭包、宏、泛型、模式、字面量、运算符、索引、链式、异步、迭代、集合、转换、比较、逻辑、算术、位运算、引用、可变、所有权、生命周期、注解、绑定、标注
- 属性：同上 16 属性

**归并规则**：微观坐标的「对象·属性」最终归并回宏观语义格位——`结构`、`文件`、`变更`（三个世界/经历格位）。

---

## 6. 编码：谁来记（三个填充源）

### 6.1 代码填充（世界记忆 + 经历记忆的事实部分）

**触发**：初始化 + 代码变更时增量扫描。

**扫什么**：文件树、函数签名（含参数/返回）、接口/契约、依赖、调用关系、目录结构。

**落到哪**：`环境·依赖`、`结构`、`文件`、`数据`、`调用`、`事件`、`变更`、`产物`。（原「以及坐标网格各层」已随 §5 废弃）

**铁律**：这些是事实，代码说了算，**LLM 不得改写**。

### 6.2 LLM 填充（语义记忆）

**触发**：初始化播种 + 代码变更后语义失效 + 定期补充。

**流程**：
1. 取格位的**种子提示词**（本质描述）。
2. LLM 按种子去项目里**主动找**符合本质的内容。
3. 三级降级：**推荐文件 → 代码 → 人类**。
4. 找到后归纳为一条记录，**必须带证据**。

**为什么通用**：种子描述的是"本质"（如"根本目的"），不是"路径"（如 README）。所以任何项目都成立。

### 6.3 人类填充（根）

**触发**：LLM 三级降级到头仍找不到；或价值观/底线等纯主观内容。

**落格位**：价值观·原则、底线、身份、铁律·总纲、标准 等来源=人类的格位。

**标记**：来源=人类，固化度=经。

---

### 6.4 人类格位临时代偿（LLM 探索项目生成临时内容，2026-08-17 入稿）

**背景**：来源=人类的格位（初心·使命 / 铁律·总纲 / 标准 / 细则·解读 / 架构 / 身份 / 价值观·原则 等，固化=经）在人类尚未录入时，投影里只有格位名+种子、正文为空，实现层对「本项目测试写在哪、宏怎么导入、库根导出哪些符号、模块三件套怎么写」等**项目通则**零感知（实测：补测试任务 24 轮未收敛，四类编译错误全是"通则缺位"）。

**机制**：人类格位在人类未填时，用 **LLM 探索项目生成临时内容**落格位，让投影先有真实项目实例，人类后续录入覆盖为经：

| 项 | 约定 |
|:--|:--|
| 触发 | 主政读背景前检查该格位是否为空；空 → 补一次临时代偿 |
| 探索素材 | 搜集文档（.md/.txt）+ 代码印证（.rs 清单）+ 依赖图结构树/符号清单——实证优先，非空想 |
| 生成 | 复用 播种-归纳 提示（种子 → 素材 → LLM 归纳本质），来源=LLM |
| 标记 | 来源=LLM、固化度=**权**、证据=探索实证（如「从多智能体架构设计.md §4.2 + 依赖图 128 符号 归纳」） |
| 生命周期 | 跨任务复用（写格位即长效）；代码变更致证据失效按既有失效机制重采；**人类录入 → 覆盖为 经，临时代偿自动让位**（来源可信度 代码>人类>LLM，人类权威最高） |
| 不改根 | 临时代偿只改善「投影有内容」，不改变人类格位的"根"地位——人类一天未录，一天是权；录了就换人 |

**为什么可以临时**：格位表的"来源=人类"指**最终权威**，不是"只能人类写"。LLM 按种子+实证探索生成的衍生内容作为占位，忠实地把项目现状先喂给执行层；人类补录后以经格位覆盖，来源可信度排序不变。

**落位**：识海承载-府 播种-归纳-园 新增 `临时代偿`（判空 + 带图谱实证的归纳）；天庭治理-府 世界运行.rs 主政读背景前调用。

**测试性能优化（2026-08-21 入稿）**：`临时代偿_人类已录格位零成本跳过` 单测此前仅预写「初心·使命」一条人类记录，其余人类格位（铁律·总纲 / 价值观·原则 / 底线 / 身份 / 标准 / 架构 / 方向 / 权限 / 世界观 等）仍会递归搜集 `std::env::temp_dir()` 文档并尝试调用模型（空配置致网络超时），单测耗时 146 秒。改为预写**全部**来源=人类的格位记录后，跳过检查全部命中，毫秒级完成（146 秒 → 0.38 秒），且断言更严格（所有人类格位均跳过而非仅一条）。

### 6.5 可操作规则格位注入（规则从格位读，2026-08-17 入稿）

**背景**：可操作规则（如「全中文输出」「命名的定红线」「构建验证纪律」）散落在各提示词模板里硬编码，改规则要重编号令、且各环节各自写死易漂移。

**机制（双轨，2026-08-17 确认）：**
- **主机制·元数据层化固定规则段**：`元数据层化` 拼完首屏后追加「【固定规则·高价值信息】」段，遍历**范畴=规则**的中间档格位（细则·解读 / 环节规则 / 铁律·总纲 / 标准 等）读链头注入。共用背景的角色（执行/提审/需求拆分）自动带上——契合"格位每次加载固定高价值信息、因太重才分 36/72 档"的本意。
- **补充机制·注入规则**：`注入规则(存储, 格位名)` 读规则格位链头格式化注入，供独立构造提示的角色（终裁 / 设计 / 归因）按需接入。

| 项 | 约定 |
|:--|:--|
| 写入 | 界主/鸿钧可按需把规则写入 `细则·解读`；亦可以临时代偿(LLM 探索)预填 |
| 读取 | 主机制：元数据层化遍历规则格位；补充：各独立提示拼装前调 `注入规则` |
| 生效 | 改格位即全局生效，**不需重编号令**——规则与代码解耦 |
| 预算 | 固定规则段独立控制（单格位 300、段总量 800 字符），防挤占首屏不失控 |

**为什么对**：规则是"可变行为约束"，属权重位（权=可迭代），不该硬编码进"代码"（固化）。规则随背景作为固定高价值信息加载，正是"代码记事实、LLM补语义、人填根/规则"的体现。

**落位**：识海承载-府 三档拼装.rs `元数据层化` 固定规则段 + 播种-归纳-园 `注入规则`；独立提示角色（终裁/模板生成/要求化/归因）已接入 `注入规则`（2026-08-21落码，五处：终裁单准圣审验、终裁鸿钧终裁、要求化解析想法、模板生成设计/改稿、教训归因归因，复用 细则·解读 格位链头）。

### 6.6 结构图位 pub API 签名扫描（代码填充·防 API 脑补，2026-08-22 入稿）

**背景**：世界自转中 LLM 生成代码时脑补不存在的 API 导致编译失败。现有防幻觉提示词有硬编码例子（`mingling_fu::沙箱_命令::原子入池`），需泛化；现有查库根导出只覆盖涉及路径对应的单个 crate 的 pub 符号，不够全。路线 C：自动扫描 workspace 所有 pub API 写入「结构」格位，执行时注入现状。

**触发**：代码定档后（定档环节）。

**动作**：遍历 workspace 所有 crate，用依赖图的扫描能力提取每个 crate 的库根 pub 符号签名，写入「结构」格位。每个 crate 一条记录，实体键 = API·{lib名}，新写入覆盖旧记录（链头 = 最新）。

**落到哪**：「结构」格位（范畴=世界，来源=代码，固化度=权）。

**铁律**：API 签名是事实，代码说了算，**LLM 不得改写**——与 §6.1 代码填充同纪律。

**落位**：识海承载-府 铭记-殿（编码）新增 pub API 扫描写入「结构」格位；天庭治理-府 定档环节触发扫描写入。

---

## 7. 检索：怎么想起

### 7.1 顺序三档（对齐注意力）

工作区元信息作为**固定前缀**，永远排在最前（先于一切格位）。

| 档 | 位置 | 装什么 | 注意力效应 |
|:--|:--|:--|:--|
| 最前 | 开头 | 核心：初心/铁律/价值观/身份/任务/状态等 | 首因（第一眼建立世界观） |
| 最后 | 结尾 | 当前：目标/验收标准/进度/阻塞/验收结果 | 近因（做结论时最新鲜） |
| 中间 | 中部 | 普通背景：结构/文件/历史/调用等 | 易被忽略，可压缩 |

### 7.2 预算裁剪

预算不足时，**从中间往两头裁**，永远保住首（核心）和尾（当前）。

### 7.3 投影（按需取用）

1. **定位**：用索引（名字→格位、坐标→记录）拿到精准坐标，不盲翻。
2. **取用**：简单任务只给最前+最后；复杂任务才补中间。
3. **更新**：执行产物回填进对应格位，保持心智模型鲜活。

### 7.4 缓存（避免重复看同一内容）

**诉求**：不变的内容不重复读；最近的内容完整留；拼装结果按需复用。

**三级缓存**（按内容稳定性）：

| 缓存级 | 内容 | 失效条件 |
|:--|:--|:--|
| 永久缓存 | 固化度 = 经 的格位 | 代码变更 / 人类修改 |
| 版本缓存 | 固化度 = 权 的格位 | 版本号 / 时间戳变化 |
| 会话缓存 | 最近上下文的完整记录 | 会话结束 / 新任务开始 |

**拼装时的取用顺序**：
1. 经的格位 → 永久缓存直接注入，**不重新读**（它们不变）。
2. 权的格位 → 版本缓存，版本未变即复用。
3. 最近上下文 → 会话缓存取**完整记录**，不截断、不重拼。

**拼装结果缓存**：
- 一次投影结果按「输入指纹」（各格位版本号 + 任务）缓存。
- 指纹未变 → 直接复用，**不重拼**。
- 指纹变了 → 只重拼变化的部分（增量），不全量。

**会话记录（最近上下文）**：
- 保留最近 N 条上下文的**完整记录**（对话、任务状态、执行产物），不截断。
- 新任务开始时，把上一会话的会话记录**归档进经历记忆格位**，再开启新的会话缓存。

> 效果：拼装从「每次全量重拼」变成「增量更新」，只有真正变化的部分才重算。

### 7.5 结构图位 pub API 签名注入（执行现状拼装时注入，2026-08-22 入稿）

**触发**：执行现状拼装时（现状拼装.rs）。

**动作**：读取「结构」格位中实体键以「API·」开头的所有记录，格式化注入现状。注入格式示例：

```
【接口·契约·可用API清单】
[mingling_fu] 库根=入口.rs
  pub fn 投递想法(内容: &str) -> String
  pub mod 沙箱_命令
[shihai_fu] 库根=lib.rs
  pub fn 元数据层化(...) -> String
```

**预算**：独立预算（约 3000 字符），超限时按涉及路径相关 crate 优先保留。

**防幻觉效果**：LLM 只能引用注入清单中的 API，不在清单中的需先创建实现——与 §9 证据与信任同纪律，把"脑补 API"从软约束变成有清单可对照的硬约束。

**落位**：识海承载-府 现状拼装.rs 读取「结构」格位中实体键以「API·」开头的记录注入；执行提示.rs 防幻觉提示词泛化为"你当前要修改的 crate 中不存在的 API，不得直接引用。如需使用未在【接口·契约·可用API清单】中列出的 API，必须先创建其实现。"

---

## 8. 生命周期

| 阶段 | 何时 | 做什么 |
|:--|:--|:--|
| 建立 | 项目初始化 | ① 确定工作区 → ② 代码扫描建世界记忆 → ③ LLM 按种子+降级播种语义 → ④ 人类填根 |
| 更新 | 持续运行 | 代码变更→增量扫描；执行/决策→回填经历记忆；LLM 定期补语义 |
| 取用 | 每次任务 | 定位 → 顺序三档投影 → 输出上下文 → 回填产物 |

---

## 9. 证据与信任（防幻觉）

- LLM 填充的每条记录**必须带证据**：`从「XX」第 N 段 /「XX」符号 推断`。
- 代码一变，凡证据指向已变更位置的记录，标记**失效**，触发重新采集。
- 来源可信度排序：**代码 > 人类 > LLM**。

---

## 10. 一页总结

```
项目心智模型 = 记忆

编码（谁来记）        存储（全都在）          检索（怎么想起）
───────────────────────────────────────────────────────
代码 → 世界/事实      六范畴 36格位(72全量)      顺序三档（首因+近因）
LLM → 语义(种子+降级) 名字标识·公私隔离        按需投影，专注现在
人类 → 根            三维度(来源/固化/共享)    预算从中间裁

降级链：推荐文件 → 代码 → 人类
防幻觉：LLM 填充必带证据，来源排序 代码>人类>LLM
```

---

## 附录 E · 待确认 / 待修正清单

| 项 | 内容 | 状态 |
|:--|:--|:--|
| 承载类型映射 | 微观坐标归并到宏观语义格位时，承载类型号（26/27/32）与语义格位名字（结构/文件/变更，实际为 35/36/47）不一致 | 已废弃（坐标网格机制已于 2026-08-16 移除，§5 标注废弃，依赖图索引替代） |
| 顺序档位划分 | §3 表的三档划分（最前 9 / 最后 6 / 中间 21） | 已确认（识海承载-府 三档-拼装 按此落地，上下文.md §三 与格位清单一致） |
| 来源可信度排序 | §9 的「代码 > 人类 > LLM」 | 已确认（上下文.md §二 落定：来源可信度 代码>人类>LLM，记录必带证据） |

---

*项目心智模型 · 详细设计稿 · 待评审*
