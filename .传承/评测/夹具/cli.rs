// 外部判据夹具：由评测方提供，不属于被测产物，直接断言行为。
// 注入位置：产物 crate 的 tests/ 目录；通过条件：本夹具全部测试通过。
// {{crate}} 占位符在注入前被替换为产物 crate 名（连字符转下划线）。

#[test]
fn 外部判据_解析等于号形式() {
    assert_eq!({{crate}}::解析参数("--值=42"), Some(42));
}

#[test]
fn 外部判据_解析空格形式() {
    assert_eq!({{crate}}::解析参数("-v 42"), Some(42));
}

#[test]
fn 外部判据_非法输入返回空() {
    assert_eq!({{crate}}::解析参数("乱写"), None);
}

#[test]
fn 外部判据_负数值() {
    assert_eq!({{crate}}::解析参数("--值=-7"), Some(-7));
}
