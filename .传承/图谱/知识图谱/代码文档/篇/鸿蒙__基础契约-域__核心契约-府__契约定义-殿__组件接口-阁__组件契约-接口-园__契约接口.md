# 鸿蒙/基础契约-域/核心契约-府/契约定义-殿/组件接口-阁/组件契约-接口-园/契约接口.rs

> **坐标**：鸿蒙 / 基础契约-域 / 核心契约-府 / 契约定义-殿 / 组件接口-阁 / 组件契约-接口-园
> **来源语言**：rust ｜ 定义 6 ｜ 出边 5 ｜ 入边 54 ｜ 悬空 0
> 由「代码文档」府依符号索引生成；事实源：`.传承/图谱/知识图谱/符号索引.json`

## 一、本文件定义

共 6 个符号。

| 种类 | 名 | 可见性 | 行 |
|---|---|---|---|
| 模块 | `契约接口` | 私有 | 1 |
| 特征 | `Component` | pub | 4 |
| 特征 | `Initializable` | pub | 9 |
| 特征 | `Shutdown` | pub | 14 |
| 函数 | `当前时间戳` | pub | 19 |
| 函数 | `原子写入文件` | pub | 29 |

## 二、对外依赖

共 5 条。

| 类型 | 本文件符号 | 目标 | 置信度 | 行 |
|---|---|---|---|---|
| 包含 | `契约接口` | `Component` | 中 | 4 |
| 包含 | `契约接口` | `Initializable` | 中 | 9 |
| 包含 | `契约接口` | `Shutdown` | 中 | 14 |
| 包含 | `契约接口` | `当前时间戳` | 中 | 19 |
| 包含 | `契约接口` | `原子写入文件` | 中 | 29 |

## 三、被引用

共 54 条。

| 类型 | 本文件符号 | 来源 | 置信度 |
|---|---|---|---|
| 包含 | `Component` | `契约接口` | 中 |
| 包含 | `Initializable` | `契约接口` | 中 |
| 包含 | `Shutdown` | `契约接口` | 中 |
| 包含 | `当前时间戳` | `契约接口` | 中 |
| 包含 | `原子写入文件` | `契约接口` | 中 |
| 包含 | `契约接口` | `组件契约-接口-园` | 中 |
| 调用 | `当前时间戳` | `写入` | 高 |
| 调用 | `原子写入文件` | `保存` | 高 |
| 调用 | `当前时间戳` | `创建` | 高 |
| 调用 | `原子写入文件` | `保存` | 高 |
| 调用 | `当前时间戳` | `生成` | 高 |
| 调用 | `当前时间戳` | `发布任务` | 高 |
| 调用 | `当前时间戳` | `承接任务` | 高 |
| 调用 | `当前时间戳` | `定向回退` | 高 |
| 调用 | `当前时间戳` | `标记回退` | 高 |
| 调用 | `当前时间戳` | `更新需求文档` | 高 |
| 调用 | `当前时间戳` | `更新设计文档` | 高 |
| 调用 | `当前时间戳` | `更新实现文档` | 高 |
| 调用 | `当前时间戳` | `更新验收文档` | 高 |
| 调用 | `当前时间戳` | `更新终审文档` | 高 |
| 调用 | `当前时间戳` | `更新审核记录` | 高 |
| 调用 | `当前时间戳` | `执行推进` | 高 |
| 调用 | `当前时间戳` | `完成当前层级记录` | 高 |
| 调用 | `当前时间戳` | `发布` | 高 |
| 调用 | `原子写入文件` | `保存` | 高 |
| 调用 | `当前时间戳` | `记日志` | 高 |
| 调用 | `当前时间戳` | `新` | 高 |
| 调用 | `当前时间戳` | `进入冷却期` | 高 |
| 调用 | `当前时间戳` | `够用` | 高 |
| 调用 | `当前时间戳` | `统计` | 高 |
| 调用 | `当前时间戳` | `新` | 高 |
| 调用 | `当前时间戳` | `纠正` | 高 |
| 调用 | `当前时间戳` | `晋升到经历教训` | 高 |
| 调用 | `当前时间戳` | `创建上下文` | 高 |
| 调用 | `当前时间戳` | `添加消息` | 高 |
| 调用 | `当前时间戳` | `记录工具调用` | 高 |
| 调用 | `当前时间戳` | `追加` | 高 |
| 调用 | `当前时间戳` | `装配看板驱动台` | 高 |
| 调用 | `原子写入文件` | `保存` | 高 |
| 调用 | `当前时间戳` | `开启` | 高 |
| 调用 | `原子写入文件` | `保存` | 高 |
| 调用 | `当前时间戳` | `执行召回` | 高 |
| 调用 | `当前时间戳` | `解除召回` | 高 |
| 调用 | `当前时间戳` | `解析并构造` | 高 |
| 调用 | `当前时间戳` | `看板澄清` | 高 |
| 调用 | `当前时间戳` | `看板审核` | 高 |
| 调用 | `当前时间戳` | `看板发布` | 高 |
| 调用 | `当前时间戳` | `发布看板任务` | 高 |
| 调用 | `当前时间戳` | `发布对齐任务` | 高 |
| 调用 | `当前时间戳` | `执行` | 高 |
| 调用 | `当前时间戳` | `创建会话` | 高 |
| 调用 | `当前时间戳` | `写入检查点` | 高 |
| 调用 | `当前时间戳` | `收尾定向驱动` | 高 |
| 调用 | `当前时间戳` | `启动驱动` | 高 |

## 四、悬空引用

无悬空引用。



---

> **事实校验**：通过（叙述中的符号引用均可溯源）
