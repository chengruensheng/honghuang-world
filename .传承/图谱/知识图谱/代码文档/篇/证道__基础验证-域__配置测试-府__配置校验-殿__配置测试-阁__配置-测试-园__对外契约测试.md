# 证道/基础验证-域/配置测试-府/配置校验-殿/配置测试-阁/配置-测试-园/对外契约测试.rs

> **坐标**：证道 / 基础验证-域 / 配置测试-府 / 配置校验-殿 / 配置测试-阁 / 配置-测试-园
> **来源语言**：rust ｜ 定义 7 ｜ 出边 6 ｜ 入边 7 ｜ 悬空 0
> 由「代码文档」府依符号索引生成；事实源：`.传承/图谱/知识图谱/符号索引.json`

## 一、本文件定义

共 7 个符号。

| 种类 | 名 | 可见性 | 行 |
|---|---|---|---|
| 模块 | `对外契约测试` | 私有 | 1 |
| 模块 | `tests` | 私有 | 2 |
| 函数 | `对外配置默认值正确` | 私有 | 7 |
| 函数 | `对外契约解析覆盖字段` | 私有 | 16 |
| 函数 | `对外契约缺字段用默认` | 私有 | 26 |
| 函数 | `从目录向上查找命中祖先文件` | 私有 | 35 |
| 函数 | `从目录向上查找未命中` | 私有 | 51 |

## 二、对外依赖

共 6 条。

| 类型 | 本文件符号 | 目标 | 置信度 | 行 |
|---|---|---|---|---|
| 包含 | `对外契约测试` | `tests` | 中 | 2 |
| 包含 | `tests` | `对外配置默认值正确` | 中 | 7 |
| 包含 | `tests` | `对外契约解析覆盖字段` | 中 | 16 |
| 包含 | `tests` | `对外契约缺字段用默认` | 中 | 26 |
| 包含 | `tests` | `从目录向上查找命中祖先文件` | 中 | 35 |
| 包含 | `tests` | `从目录向上查找未命中` | 中 | 51 |

## 三、被引用

共 7 条。

| 类型 | 本文件符号 | 来源 | 置信度 |
|---|---|---|---|
| 包含 | `tests` | `对外契约测试` | 中 |
| 包含 | `对外配置默认值正确` | `tests` | 中 |
| 包含 | `对外契约解析覆盖字段` | `tests` | 中 |
| 包含 | `对外契约缺字段用默认` | `tests` | 中 |
| 包含 | `从目录向上查找命中祖先文件` | `tests` | 中 |
| 包含 | `从目录向上查找未命中` | `tests` | 中 |
| 包含 | `对外契约测试` | `配置-测试-园` | 中 |

## 四、悬空引用

无悬空引用。



---

> **事实校验**：通过（叙述中的符号引用均可溯源）
