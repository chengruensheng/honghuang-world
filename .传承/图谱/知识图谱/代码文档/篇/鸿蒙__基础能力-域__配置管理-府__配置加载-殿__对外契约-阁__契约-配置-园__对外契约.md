# 鸿蒙/基础能力-域/配置管理-府/配置加载-殿/对外契约-阁/契约-配置-园/对外契约.rs

> **坐标**：鸿蒙 / 基础能力-域 / 配置管理-府 / 配置加载-殿 / 对外契约-阁 / 契约-配置-园
> **来源语言**：rust ｜ 定义 11 ｜ 出边 13 ｜ 入边 14 ｜ 悬空 0
> 由「代码文档」府依符号索引生成；事实源：`.传承/图谱/知识图谱/符号索引.json`

## 一、本文件定义

共 11 个符号。

| 种类 | 名 | 可见性 | 行 |
|---|---|---|---|
| 模块 | `对外契约` | 私有 | 1 |
| 常量 | `对外契约文件名` | pub | 9 |
| 结构体 | `对外契约` | pub | 13 |
| 结构体 | `对外配置` | pub | 20 |
| 函数 | `默认并发上限` | 私有 | 32 |
| 实现块 | `impl Default for 对外配置` | 私有 | 34 |
| 函数 | `default` | 私有 | 35 |
| 常量 | `默认对外契约` | 私有 | 45 |
| 函数 | `解析或默认` | 私有 | 48 |
| 函数 | `从目录向上查找` | pub | 61 |
| 函数 | `对外契约配置` | pub | 75 |

## 二、对外依赖

共 13 条。

| 类型 | 本文件符号 | 目标 | 置信度 | 行 |
|---|---|---|---|---|
| 包含 | `对外契约` | `对外契约文件名` | 中 | 9 |
| 包含 | `对外契约` | `对外契约` | 中 | 13 |
| 包含 | `对外契约` | `对外配置` | 中 | 20 |
| 包含 | `对外契约` | `默认并发上限` | 中 | 32 |
| 包含 | `对外契约` | `impl Default for 对外配置` | 中 | 34 |
| 包含 | `impl Default for 对外配置` | `default` | 中 | 35 |
| 包含 | `对外契约` | `默认对外契约` | 中 | 45 |
| 包含 | `对外契约` | `解析或默认` | 中 | 48 |
| 包含 | `对外契约` | `从目录向上查找` | 中 | 61 |
| 包含 | `对外契约` | `对外契约配置` | 中 | 75 |
| 调用 | `default` | `默认并发上限` | 高 | 39 |
| 调用 | `对外契约配置` | `从目录向上查找` | 高 | 83 |
| 调用 | `对外契约配置` | `解析或默认` | 高 | 85 |

## 三、被引用

共 14 条。

| 类型 | 本文件符号 | 来源 | 置信度 |
|---|---|---|---|
| 包含 | `对外契约文件名` | `对外契约` | 中 |
| 包含 | `对外契约` | `对外契约` | 中 |
| 包含 | `对外配置` | `对外契约` | 中 |
| 包含 | `默认并发上限` | `对外契约` | 中 |
| 包含 | `impl Default for 对外配置` | `对外契约` | 中 |
| 包含 | `default` | `impl Default for 对外配置` | 中 |
| 包含 | `默认对外契约` | `对外契约` | 中 |
| 包含 | `解析或默认` | `对外契约` | 中 |
| 包含 | `从目录向上查找` | `对外契约` | 中 |
| 包含 | `对外契约配置` | `对外契约` | 中 |
| 调用 | `默认并发上限` | `default` | 高 |
| 调用 | `从目录向上查找` | `对外契约配置` | 高 |
| 调用 | `解析或默认` | `对外契约配置` | 高 |
| 调用 | `对外契约配置` | `启动` | 高 |

## 四、悬空引用

无悬空引用。



---

> **事实校验**：通过（叙述中的符号引用均可溯源）
