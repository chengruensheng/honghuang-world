# 太初/任务管理-域/任务核心-府/任务看板-殿/任务流转-阁/流转-逻辑-园/流转逻辑.rs

> **坐标**：太初 / 任务管理-域 / 任务核心-府 / 任务看板-殿 / 任务流转-阁 / 流转-逻辑-园
> **来源语言**：rust ｜ 定义 3 ｜ 出边 0 ｜ 入边 11 ｜ 悬空 0
> 由「代码文档」府依符号索引生成；事实源：`.传承/图谱/知识图谱/符号索引.json`

## 一、本文件定义

共 3 个符号。

| 种类 | 名 | 可见性 | 行 |
|---|---|---|---|
| 函数 | `状态层级标签` | pub | 13 |
| 函数 | `状态归属角色` | pub | 44 |
| 函数 | `承接后状态` | pub | 60 |

## 二、对外依赖

本文件未发出边。

## 三、被引用

共 11 条。

| 类型 | 本文件符号 | 来源 | 置信度 |
|---|---|---|---|
| 包含 | `状态层级标签` | `流转逻辑` | 中 |
| 包含 | `状态归属角色` | `流转逻辑` | 中 |
| 包含 | `承接后状态` | `流转逻辑` | 中 |
| 调用 | `状态归属角色` | `承接任务` | 高 |
| 调用 | `承接后状态` | `承接任务` | 高 |
| 调用 | `状态归属角色` | `提交任务` | 高 |
| 调用 | `状态层级标签` | `状态层级` | 高 |
| 调用 | `状态归属角色` | `执行一轮_恢复` | 高 |
| 调用 | `状态归属角色` | `恢复闸门` | 高 |
| 调用 | `状态层级标签` | `收尾定向驱动` | 高 |
| 调用 | `状态层级标签` | `启动驱动` | 高 |

## 四、悬空引用

无悬空引用。



---

> **事实校验**：通过（叙述中的符号引用均可溯源）
