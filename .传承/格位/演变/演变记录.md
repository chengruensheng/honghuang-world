---
层级: 天道
文档型: 治理档案
版本: 1.0
域: 格位
状态: 已定
更新: 2026-09-11
---

# 格位演变记录

> 定位：记录 `.传承/格位/36格位.json` 的创建与历次变更，供追溯格位定义如何演进。
> 不解决什么：不复制 36格位.json 的内容（唯一源为 `36格位.json` 本体，避免双份漂移）。

## 一、演变时间线

| 日期 | 变更 | 说明 |
|------|------|------|
| 2026-09-11 | 初建 | 从源 Rust（`格位数据.rs` + `角色数据.rs`）导出 30 实证格位 + 6 扩展位，版本 1.0 |
| 2026-09-11 | 证据路径同步 | 「执行-准圣」证据 `传承殿/端到端基准.ps1` → `.传承/门禁/端到端基准.ps1`（脚本已归集至门禁） |

## 二、变更规则

- 每次对 `36格位.json` 做实质修改（增删格位、改维度、改证据路径），在时间线追加一行。
- 旧定义不覆盖式重写，保留本表历史行即可；需要回看历史时以本表为索引。
