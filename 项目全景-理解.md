# 项目全景理解

> 本文档是对「洪荒 · 世界」的完整理解记录：每个府/殿/阁/园的作用、核心类型、格位与依赖图、命令清单、彩蛋、以及设计文档与代码的漂移点。
> 用途：后续开发、重构、接管时的总索引；随世界生长更新。
> 依据：7 份设计文档 + 全部关键代码（8 万行 / 1012 文件 / 8 crate）+ 运行产物（版本-v1 至 v13）。

---

## 一、一句话本质

用洪荒神话世界观建模的、会自己进化的多智能体软件开发系统。
界主发布想法 → 鸿钧化要求 → 圣人设计 → 大罗金仙实现 → 准圣六维验收 → 鸿钧终裁 → 版本存档 → 天道巡世进化。
世界自己写代码、自己验收、自己存档、自己进化。

---

## 二、世界观（九根维度 + 四层职业）

### 九根维度

| 类别 | 维度 | 对应 | 状态 |
|---|---|---|---|
| 本体五根（创世链） | 太初 | 存在 | 骨架立·未启用 |
| | 混沌 | 可能性 | 骨架立·未启用 |
| | 乾坤 | 空间（容器） | 呈现侧已启用 |
| | 道韵 | 法则 | 未启用 |
| | 量劫 | 时间（劫数） | 未启用 |
| 工程四根（保障链） | 鸿蒙 | 地基（盘古） | 已启用（核心） |
| | 证道 | 测试 | 已启用 |
| | 道果树 | 构建 | 构建产物目录在用 |
| | 传承殿 | 传承 | 骨架立·未启用 |

### 四层职业

| 位阶 | 角色 | 职司（道） | 职责 | 铁律 |
|---|---|---|---|---|
| 顶层 | 界主（人·天意） | — | 发布想法，不干预实现 | 只管想 |
| 天层 | 天道 | 巡世之神 | 长期巡逻世界（乙阶段） | 只发现不指挥 |
| 天层 | 鸿钧 | 道祖·主政 | 化要求/派遣/验收终裁/定档 | 不亲执行 |
| 中层 | 女娲+四圣 | 设计/评审 | 初稿设计 + 分角度评审 | 不担任执行者 |
| 底层 | 大罗金仙 | 代码/前端/数据库/监控 | 实现（写代码） | 按道分工不跨道 |
| 底层 | 六准圣 | 六维审验 | 各审一维，回呈鸿钧 | 只审验不终裁 |

**一句话：圣人定道，大罗行术，准圣验术，鸿钧终裁。**

---

## 三、六层结构规范

- 六层：维度 → 域 → 府 → 殿 → 阁 → 园，层层递进，能力只在园落地。
- 府 = 唯一编译单位（lib crate），唯一引用止步点。
- 域 = 纯目录；府 = `入口.rs`；殿/阁/园 = `模块.rs` 桥接（只 `pub mod` + `pub use`）。
- 三条铁律：跨府止步 lib 根；跨维经鸿蒙；单向向下。
- 园分六类：代码园 `-园`、存储园 `-数据`、配置园 `-配置`、模板园 `-模板`、脚本园 `-脚本`、资源园 `-资源`。
- 命名：全中文标识符，目录连字符、模块下划线（`#[path]` 桥接）；代码园名须含实现手段词（流式/原子/批量/并行/缓存/兜底/分块/增量）。

---

## 四、八府全景

### 1. 识海承载-府（shihai_fu）—— 世界之脑 · 记忆

> 本质：给项目造记忆体，不是数据库。代码记事实，LLM 补语义，人兜底填根。

#### 核心类型（格位体系）

**格位**（心智模型基本单元）：名字 / 范畴 / 种子提示词 / 来源 / 固化度 / 共享度 / 顺序档位 / token上限 / 推荐位置 / 存储。

**记录**（格位里一条内容，必带证据）：格位名 / 内容 / 证据 / 时间戳 / 来源 / 前记录 / 坐标 / 失效 / 实体键（同实体成链，链头=最新一条）。

**坐标**：层（项目/模块/文件/符号/代码）× 对象 × 属性。

**会话记录**：会话id / 内容 / 时间戳（一次任务执行的完整工作记忆）。

**工具清单**：10 个工具名（写/读/改/删文件、列举目录、寻找文件、搜索内容、运行命令、读格位、查格位历史）。

**关键枚举**：
- 范畴：目标 / 规则 / 自我 / 程序 / 世界 / 经历（六范畴）
- 固化度：经（不可改）/ 权（可迭代）
- 共享度：共享 / 私有（私有仅界主可见）
- 顺序档位：最前（首因）/ 中间 / 最后（近因）
- 来源：代码（可信度3）> 人类（2）> LLM（1）

**默认 token 上限**（格位独立计数）：最前人类 500 / 最前代码 300 / 最前 LLM 400 / 最后 300 / 中间人类 500 / 中间其他 400。

#### 36 格位完整清单（六范畴 × 顺序档位）

| 档位 | 格位名 | 范畴 | 来源 | 固化 | 共享 |
|---|---|---|---|---|---|
| 最前 | 初心·使命 | 目标 | 人类 | 经 | 共享 |
| 最前 | 铁律·总纲 | 规则 | 人类 | 经 | 共享 |
| 最前 | 价值观·原则 | 自我 | 人类 | 经 | 共享 |
| 最前 | 底线 | 自我 | 人类 | 经 | 私有 |
| 最前 | 身份 | 自我 | 人类 | 经 | 共享 |
| 最前 | 标准 | 规则 | 人类 | 经 | 共享 |
| 最前 | 架构 | 规则 | 人类+代码 | 经 | 共享 |
| 最前 | 任务 | 目标 | 人类+LLM | 权 | 私有 |
| 最前 | 状态 | 世界 | 代码+LLM | 权 | 共享 |
| 中间 | 方向 | 目标 | 人类 | 经 | 共享 |
| 中间 | 需求 | 目标 | 人类+LLM | 权 | 共享 |
| 中间 | 期望·意向 | 目标 | LLM | 权 | 私有 |
| 中间 | 能力 | 自我 | 代码 | 权 | 私有 |
| 中间 | 权限 | 自我 | 人类 | 经 | 私有 |
| 中间 | 自省·觉察 | 自我 | LLM | 权 | 私有 |
| 中间 | 细则·解读 | 规则 | LLM | 权 | 共享 |
| 中间 | 世界观 | 规则 | 人类 | 经 | 共享 |
| 中间 | 传承·决策 | 规则 | LLM | 权 | 共享 |
| 中间 | 环节规则 | 程序 | LLM | 权 | 共享 |
| 中间 | 例外·临时 | 程序 | LLM | 权 | 共享 |
| 中间 | 环境·依赖 | 世界 | 代码 | 权 | 共享 |
| 中间 | 结构 | 世界 | 代码 | 权 | 共享 |
| 中间 | 文件 | 世界 | 代码 | 权 | 共享 |
| 中间 | 数据 | 世界 | 代码 | 权 | 共享 |
| 中间 | 调用 | 世界 | 代码 | 权 | 共享 |
| 中间 | 事件 | 经历 | 代码+LLM | 经 | 共享 |
| 中间 | 变更 | 经历 | 代码 | 经 | 共享 |
| 中间 | 产物 | 经历 | 代码 | 经 | 共享 |
| 中间 | 教训 | 经历 | LLM | 权 | 共享 |
| 中间 | 理解·记忆 | 经历 | LLM | 权 | 私有 |
| 最后 | 目标 | 目标 | 人类+LLM | 权 | 共享 |
| 最后 | 验收标准 | 目标 | 人类 | 权 | 共享 |
| 最后 | 阶段·进度 | 世界 | 代码+LLM | 权 | 共享 |
| 最后 | 阻塞 | 世界 | LLM | 权 | 共享 |
| 最后 | 验收结果 | 世界 | 代码 | 权 | 共享 |
| 最后 | 承诺 | 自我 | LLM | 权 | 私有 |

#### 72 格位（复杂全量，六范畴细分）
- ① 目标记忆（13）：初心/使命/方向/需求/需求·原始/需求·迭代/目标/目标·阶段/目标·长期/验收标准/任务/期望/意向
- ② 规则记忆（13）：铁律/总纲/标准/标准·命名/标准·格式/细则/解读/世界观/传承/决策/决策·选型/传承·经验/架构
- ③ 自我记忆（9）：价值观/原则/底线/身份/能力/权限/自省/觉察/承诺
- ④ 程序记忆（7）：阶段规则/任务规则/执行规则/校验规则/触发规则/例外/临时规则
- ⑤ 世界记忆（16）：环境/依赖/结构/结构·目录/结构·模块/文件/文件·清单/文件·入口/数据/状态/调用/阶段/进度/阻塞/验收结果/工具
- ⑥ 经历记忆（14）：事件/事件·关键/事件·流/变更/变更·代码/变更·配置/产物/产物·清单/产物·构建/教训/理解/记忆/理解·认知/理解·碎片

#### 依赖图（图谱）体系

**符号档案**：项目 / 模块 / 文件 / 符号 / 代码（完整定义体）/ 签名（单行）/ 解释（语义）/ 波及（引用文件清单）。

**依赖图**：档案们（全项目符号集合）+ 结构树（完整目录层级，根为 crate 相对路径，向下为殿/阁/园）。

**结构节点**：名字 + 子节点（目录层级树，按层级逐段插入，复用同名节点）。

**查询方法**：
- 查符号（512→64 追溯）/ 查文件（64→512 定位）/ 查模块
- 查涉及文件（涉及路径→符号/文件匹配→波及文件，统一正反斜杠）
- 查相关文件（精确匹配，空则兜底命令接线文件）
- 补全同阁（目标文件所在阁的全部兄弟园）
- 下探（结构树关键词匹配渲染子树）

**命令接线文件**（依赖图常量，命令类任务联动补全）：兜底分发.rs / 缓存读取.rs / 入口执行.rs。

#### 坐标网格（五级颗粒度）—— 已废弃（2026-08-16 清理），见项目心智模型-设计稿 §5

| 层 | 网格大小 | 对象 | 属性 |
|---|---|---|---|
| 项目 | 无网格 | — | — |
| 模块 | 64（8×8） | 模块/领域/目录/接口/构建/配置/部署/测试 | 八属性（规则/目标/约束/实际/依赖/接口/用途/历史） |
| 文件 | 128（16×8） | 源文件/文档/样式/数据/资源/模板/配置/测试/脚本/组件/页面/服务/模型/视图/入口/库 | 八属性 |
| 符号 | 256（16×16） | 函数/类/方法/属性/变量/常量/类型/接口/事件/钩子/状态/引用/导入/导出/装饰/泛型 | 十六属性（签名/类型/参数/返回/语义/用例/错误/边界/调用者/被调用/变更/测试/注释/复杂度/风险/待议） |
| 代码 | 512（32×16） | 语句/表达式/调用/赋值/返回/条件/循环/匹配/解构/闭包/宏/泛型/模式/字面量/运算符/索引/链式/异步/迭代/集合/转换/比较/逻辑/算术/位运算/引用/可变/所有权/生命周期/注解/绑定/标注 | 十六属性 |

**索引**：名字→格位（HashMap）+ 坐标→记录（HashMap），建一次查多次。

**坐标索引**：坐标（层·对象·属性）→ 格位名，落盘 `.上下文/坐标索引.json`。

**微观坐标归并**：项目/模块→结构格位，文件→文件格位，符号/代码→变更格位。

#### 殿/阁/园作用

| 殿 | 阁 | 园 | 作用 |
|---|---|---|---|
| 铭记-殿（编码） | 代码-扫描-阁 | 扫描-执行-园 | 六类扫描入口，落格位快照（文件/结构/环境·依赖） |
| | | 收集-园 | 源文件/Cargo.toml 递归收集，路径归属 crate，`\`→`/` 归一 |
| | | 符号-解析-园 | 提取 pub 符号签名/上方注释/定义体/use 引用（花括号配平防误） |
| | | 批量-登记-园 | 变更登记：变更报告→事件格位（汇总）+变更格位（逐文件，实体键幂等） |
| | | 增量-检测-园 | 文件索引基线 + 增量变更检测（指纹=大小+修改时间，.tmp 半写保护） |
| | 地道-精炼-阁 | 教训-归因-园 | 失败→LLM 归因（为什么+规避）→教训格位；执行前读教训注入现状 |
| | | 机制-提炼-园 | 复杂变更（≥3文件）→LLM 归纳机制→理解·记忆格位 |
| | | 波及-追溯-园 | 变更文件→依赖图反查（出边波及+入边引用） |
| | 人类-录入-阁 | 录入-落根-园 | 人类录入根记录（格位须存在且固化度=经） |
| | 语义-播种-阁 | 播种-归纳-园 | 按种子降级链（推荐位置→代码→人类）归纳语义 + **临时代偿**（人类格位未录时 LLM 探索项目生成临时内容，人类可覆盖为经；设计稿 §6.4） |
| | | 播种-提示-模板 | 播种提示词模板（占位符 种子/素材/印证） |
| | | 符号-补解释-园 | 依赖图空解释符号→LLM 补语义写回（「阴」守护进程） |
| 纳藏-殿（存储） | 变更-存档-阁 | 回滚-垫-园 | 写操作前快照，任务失败单文件撤销（任务id分组，线程本地） |
| | 会话-记录-阁 | 会话-归档-园 | 会话缓存读写 + 归档到事件格位 |
| | 模型-存储-阁 | 格位-清单-园 | 36/72 格位定义 + 找格位 |
| | | 类型-声明-园 | 格位/记录/依赖图/会话记录/工具清单 类型 |
| | | 模型-落盘-园 | 工作区定位 + 心智模型 + 模型存储（写记录/读格位/读链头集）+ 证据失效标记 |
| | | 依赖-边-园 | 依赖图落盘/加载（.上下文/依赖图.json） |
| 回想-殿（检索） | 投影-拼装-阁 | 三档-拼装-园 | 元数据层化（首屏3000字符）+ 拼装投影（最前→最后→中间） |
| | 权限-过滤-阁 | 角色-过滤-园 | 角色范畴白名单 + 可见工具（私有仅界主） |
| | 预算-缓存-阁 | 分级-缓存-园 | 三级缓存（永久/版本/会话 + 投影指纹） |

**工作区定位**：环境变量 WORLD_WORKSPACE_ROOT → 向上探测锚点（.上下文/Cargo.toml/AGENTS.md）→ 当前目录。

**扫描排除项**：.git/.svn/.hg/.上下文/node_modules/vendor/target/临时文件夹 + 构建产物目录（读 .cargo/config.toml target-dir 首段）。

**角色范畴白名单**：界主/鸿钧=全量；女娲=目标/规则/世界；多宝=目标/规则/世界/程序；准圣=目标/规则/世界。

**可见工具**：界主/鸿钧/天道/多宝/白泽/龟灵/玄天=全量10个；其余=只读6个（读文件/列举目录/寻找文件/搜索内容/读格位/查格位历史）。

---

### 2. 天庭治理-府（tianting_fu）—— 世界之身 · 组织编排

#### 核心类型（14 枚举 + 16 结构）

**枚举**：阶段（甲/乙）、进入路径、要求来源（界主/天道巡世/鸿钧自主）、要求类别（功能/性能/美观/优化/维护/新能力/补基础）、要求状态（八态）、验收结论（通过/打回）、缺陷层（设计层/实现层）、调整级别（①-⑤）、优先级（低/中/高）、成熟度、想法状态（未处理/已化为要求/已打回/已解决）、构建状态、准圣维度（六维）、优先级。

**结构**：想法 / 约束 / 要求书 / 拆解项 / 设计方案 / 产物条目（含变化类型）/ 验收回执 / 版本记录 / 巡世候选 / 法则违逆 / 巡世报告 / 失败条目 / 进化记录 / 规模统计 / 项目档案 / 世界状态（12字段）/ 终裁回执 / 准圣意见。

**八态状态机**：待领 → 设计中 → 待确认 → 已确认 → 待实现 → 实现中 → 已验收 → 已存档（打回可回退）。

**世界状态 12 字段**：阶段 / v1已存档 / 进入路径 / 长期记忆 / 界主想法池 / 在途要求 / 验收历史 / 失败模式 / 版本历史 / 巡世候选池 / 项目档案 / 天道报告库。

#### 殿/阁/园作用

| 殿 | 阁 | 园 | 作用 |
|---|---|---|---|
| 类型-定义-殿 | 模型-声明-阁 | 声明-集中-园 | 上述全部类型契约 |
| 天层-主控-殿 | 鸿钧-主政-阁 | 需求-拆分-园 | LLM 判断拆/不拆，拆则最多4子要求，失败回退不拆 |
| | | 要求-提审-模板 | 解析想法提示词模板（涉及路径只能来自真实路径/符号） |
| | | 主政-落笔-园/要求化 | 化为要求 + 解析想法 + 净化涉及路径（6规则）+ 界主路径兜底提取 + 命令联动补全 |
| | | 主政-落笔-园/验收 | 确认设计（机械校验）+ 验收裁决 + 定档（写验收结果格位） |
| | | 主政-落笔-园/终裁 | 三段流程：机械门槛→六准圣→鸿钧终裁 |
| | | 主政-落笔-园/模块树 | 反孤儿校验（pub mod 链逐层向上） |
| | 天道-巡世-阁 | 巡世-扫描-园 | 扫描世界→巡世报告/候选池/违逆（规模启发：源文件>200产候选） |
| 智慧-主控-殿 | 设计-落笔-阁 | 模板-生成-园 | 模型设计（LLM）+ 模板兜底（类别→工作流级别） |
| 队列-调度-殿 | 队列-存取-阁 | 落盘-取队-园 | jsonl 队列（入队追加/取队删除/水位）+ 八态推进 |
| 团队-调度-殿 | 执行-派遣-阁 | 派发-落单-园 | 拆解项→执行任务，L1_qa/L2_script/L3_program/L4_complex 映射 |
| 版本-存档-殿 | 存档-落定-阁 | 快照-落库-园 | 增量快照（同名同字节跳过）+ 版本记录 + 世界状态原子写 |
| 进化-主控-殿 | 进化-归因-阁 | 归因-留痕-园 | 失败归因→调整级别（设计层调流程/次数≥2调分工/否则调提示词） |
| 驱动-入口-殿 | 世界-运行-阁 | 循环-驱动-园 | 主循环（见下） |

#### 主循环（世界运行.rs 完整逻辑）

1. **主政一轮**：LLM 需求拆分决策 → 不拆走单要求 / 拆则逐子要求串行走全链（每子独立验收/定档/打回撤销）。
2. **运行一轮**：
   - 元数据层化（3000字符）→ 依赖图加载 → 结构树补全提审背景
   - **人类格位临时代偿**（来源=人类的格位未录时，LLM 探索项目生成临时内容，人类可覆盖为经）
   - 解析想法 → 入池（待领）→ 依赖图滞后防御（涉及路径无匹配则重建）
   - 模型设计（模板兜底）→ 状态机推进（设计中→待确认→已确认→待实现）
   - 现状装配：查相关文件 + 补全同阁 + 结构下探，封顶 8000 字符
   - **图谱契约投影**（库根导出签名 / 核心类型契约 / 测试样例参照）
   - 教训注入（上轮失败经验）
   - **打回自动重投循环**（最大2次）：并发派遣 → 地道整理 → 产物去重+字节数回填 → 失败归因+失败沉淀 → 终裁 → 通过则定档已存档，打回则撤销产物+重投
3. **并发派遣**：`std::thread::scope` spawn 多个任务线程（大罗并行），地道整理并行（白嫖等待期）。
4. **打回撤销产物**：版本库降序回溯最近含该文件快照覆盖，全不含则删除。

---

### 3. 道术施展-府（daoshu_fu）—— 世界之身 · 任务执行

#### 核心类型

**执行角色**：身份 / 道 / 职司 / 模型池 / 契约（五字段）。

**工作流级别**：问答（L1_qa，作答）/ 脚本（L2_script，分析/落笔/自证）/ 程序（L3_program，分析/设计/实现/自证）/ 复杂（L4_complex，分析/设计/实现/审验/自证）。

**执行任务**：目标 / 工作流 / 角色们。

**执行状态**：运行中 / 成功 / 失败。

**产物条目**：路径 / 类别 / 字节数 / 变化类型（新增/修改/未变）。

**执行回执**：状态 / 产物们 / 说明 / 用量 / 轮数。

#### 殿/阁/园作用

| 殿 | 阁 | 园 | 作用 |
|---|---|---|---|
| 类型-定义-殿 | 模型-声明-阁 | 声明-集中-园 | 上述类型 |
| 角色-卡册-殿 | 角色-登记-阁 | 登记-落册-园 | 角色卡 HashMap（身份→执行角色），保存/加载 json |
| 工作流-编排-殿 | 流程-分步-阁 | 分步-驱动-园 | L1-L4 步骤清单 |
| 任务-调度-殿 | 任务-派遣-阁 | 工具-定义-园 | 10 个工具 schema |
| | | 工具-执行-园 | 工具执行分发 + 落盘护栏三层（空内容拒写/超长拒写/路径越界） |
| | | 工具-循环-园 | function calling 主循环 + 预算双轨（32轮/90万token）+ 会话内压缩三件套 |
| | | 执行-提示-模板 | 执行提示词模板（先读后写/构建验证/压缩三件套） |
| | | 派发-落单-园 | 派遣执行 + 执行基线指纹 + 构建自证（build+test）+ 失败重试 |
| | | 文本-解析-园 | `<<<文件:路径>>>...<<<结束>>>` 标记协议解析 |
| 手脚-施展-殿 | 文件-读写-阁 | 原子-读取/写入、增量-改写、批量-删除 | 4 个文件工具（写/改/删走回滚垫） |
| | 目录-浏览-阁 | 批量-列目、通配-找档 | 列举目录/寻找文件 |
| | 内容-检索-阁 | 流式-检索 | 搜索内容 |
| | 命令-执行-阁 | 命令-沙箱、进程-驱动 | 运行命令（沙箱护栏+超时强杀） |

**10 个工具**：写文件/读文件/改文件/删文件/列举目录/寻找文件/搜索内容/运行命令/读格位/查格位历史。

---

### 4. 模型连接-府（moxing_fu）—— LLM 调用

| 殿 | 园 | 作用 |
|---|---|---|
| 类型-定义-殿 | 声明-集中-园 | 对话消息/工具定义/工具调用/模型回复/用量（OpenAI 兼容 function calling） |
| 模型-调用-殿 | 模型-调用-园 | 调用模型 + 指数退避重试（5xx/429/529，1s/2s/4s/8s，最多3次）+ 输出上限分级（落盘65536/常规16384/精简4096）+ 三层护栏（工具参数兜底/think保留/空输出兜底） |

### 5. 日志记录-府（rizhi_fu）—— 技术日志

tracing 定制封装。技术日志走 rizhi_fu，业务流水走识海承载-府，两者勿混。各府经 lib 根调用。

### 6. 配置管理-府（peizhi_fu）—— 密钥/模型配置注入

`.env` → 结构化配置（GIT/DEEP SEEK/MINIMAX M3 密钥）。

### 7. 命令操作-府（mingling_fu，bin=号令）—— 操作台 CLI

五殿：命令-解析（入口/参数/分发）、权限-校验（令牌）、观览-查询（事项/世界/流水/记忆）、号令-下达（想法/要求/设计/验收/版本/记忆/巡世）、输出-呈现（文本/JSON）。

### 8. 单元测试-府（zhengdao_fu）—— 测试承载

六殿镜像被测六府，经被测府 lib 根黑盒验证。

---

## 五、核心机制清单（18 项）

| 机制 | 作用 |
|---|---|
| 两阶段制 | 甲=功能优先（关进化），乙=自动优化（开进化），切换点唯一=v1存档 |
| 八态状态机 | 要求书状态流转，非法迁移告警 |
| 需求拆分 | LLM 判断拆/不拆，拆则子要求逐个串行 |
| 打回撤销产物 | 按最近版本快照恢复涉及文件 |
| 打回自动重投 | 打回自动带意见重投（上限2次） |
| 机械前置全绿 | build+test 双绿才算过 |
| 现状装配 | 结构下探+符号摘要+文件全文，封顶8000字符 |
| 图谱契约投影 | 库根导出签名+核心类型契约+测试样例参照+补测试落位判据，并入现状尾部（§4.2 规则6） |
| 人类格位临时代偿 | 人类格位未录时 LLM 探索项目生成临时内容，人类可覆盖为经（§4.2 规则5） |
| 背景渐次披露 | 元数据层化（首屏3000字符）+按需读格位 |
| 工具循环预算双轨 | 轮数（32）+token（90万），任一耗尽熔断 |
| 会话内压缩 | 历史>15000摘要+工具结果微压缩+单条微压缩 |
| 回滚垫 | 写操作前快照，任务失败单文件撤销 |
| 执行基线指纹 | 任务前拍全量基线，结束diff补齐产物 |
| 失败模式沉淀 | 失败按要求id+阶段去重累加 |
| 版本存档 | 增量快照+版本记录+回退 |
| 进化环 | 采集→归因→分级调整（⑤级需界主确认） |
| 巡世环 | 天道扫描→候选池→自主消费→闭环 |
| 模型调用护栏 | 工具参数兜底/think保留/空输出兜底/退避重试 |
| 命令沙箱 | 禁kill/禁cargo run号令/超时10分钟/路径越界 |

---

## 六、33 条命令清单

### 写命令（11 条，需令牌 WORLD_AI_TOKEN 或 -t）

| 命令 | 作用 |
|---|---|
| 想法 投递 | 投递想法，驱动主政一轮 |
| 要求 化为 | 想法解析为要求书入队 |
| 设计 上呈 | 模板生成设计方案入队 |
| 设计 确认 | 确认设计（当前仅校验输出） |
| 验收 裁决 | 构造验收回执入历史 |
| 版本 存档 | 版本递增+增量快照+记录 |
| 版本 回退 | 恢复到指定版本 |
| 记忆 回填 | 写记录进格位 |
| 记忆 播种 | 扫描工作区源码写入识海 |
| 记忆 补语义 | LLM 补依赖图空解释符号 |
| 巡世 扫描 | 天道巡逻产出候选与违逆 |

### 读命令（21 条，免令牌）

想法列表/详情、要求列表/详情、设计列表/详情、验收历史/详情、版本列表/详情、记忆投影、世界状态、世界时间（十二时辰）、世界历法、世界检索、队列水位、流水列表/总览/跟踪/跟随、巡世报告。

### 帮助（1 条）

---

## 七、彩蛋与隐藏设计

| 彩蛋 | 位置 | 内容 |
|---|---|---|
| 十二时辰 | 流式时刻.rs | 世界时间输出子丑寅卯而非几点 |
| 「阴」守护进程 | 补符号解释.rs | LLM 补语义身份=阴（旁路观察者） |
| 「白嫖」等待期 | 机制提炼/世界运行 | 趁 LLM 思考空档做免费地道收尾 |
| 「磨蹭」「抽风」「空转」 | 工具循环.rs | 描述模型行为自嘲式笔记 |
| 老师判作业/蝼蚁 | 设计稿 §18.3 | 鸿钧串行「老师只有一个」、大罗并行「蝼蚁可多」 |
| 微信工作群 | 设计稿 §18.3 | 圣人设计「一人发稿，众人审」 |
| 号令自保护 | 沙箱护栏.rs | 禁模型运行/探查自身二进制「号令」 |
| 回滚垫 | 回滚垫.rs | 「曾存在=false」处理新增文件回滚 |
| WORLD_AI_TOKEN | 原子校验.rs | 界主（人）可读、AI 写命令需令牌 |
| bin=号令 | Cargo.toml | 操作台即「下达号令」 |
| 经/权 | 格位固化度 | 经=不可改、权=可迭代（道家概念） |
| 证道=测试 | zhengdao_fu | 用测试「证道」验证道术成色 |
| 实测翻车笔记 | 各文件注释 | 「4781残留盘面」「os error 32」真实教训 |

---

## 八、依赖关系

```
mingling_fu ──► tianting_fu ──► daoshu_fu ──► moxing_fu ──► peizhi_fu
      │   │   │       │              │
      │   │   │       └────► shihai_fu ──► moxing_fu
      │   │   └──► shihai_fu
      │   └──► moxing_fu / peizhi_fu
zhengdao_fu ──► {shihai_fu, tianting_fu, daoshu_fu, moxing_fu, peizhi_fu, rizhi_fu}
```

---

## 九、设计文档 vs 代码漂移（重要，重构前须对齐）

| 项 | 设计文档 §11.2 | 代码实际 | 状态 |
|---|---|---|---|
| 六准圣维度 | 红云业务正确性/镇元子数据完整性/鲲鹏性能并发/神农安全副作用/冥河异常兼容/轩辕用户体验 | 曾偏离为「完整性/健壮性/命名规范/模块接入/依赖合理/可测试性」 | **已修复**：终裁.rs 恢复业务六维，114 测试全绿 |
| 鸿钧终裁 | 「任一打回→打回」 | **6一致通过→通过；6一致打回→打回；争议→LLM综合判断** | 待定（争议LLM综合判断是合理改进，需回写设计稿） |

> 六维漂移的根因：AI 生成时把「工程规范（命名/接入/依赖）」误当「审验维度」硬编码。已按「本质/表现分离」修复：六准圣审本质（业务语义），工程规范下沉机械门槛、规则从格位读。

---

## 十、当前状态

- 8 crate、约 380 rs 文件、约 1.8 万行，`cargo check --workspace --all-targets` 零警告，全量测试（约 280 用例）全绿。
- 版本存档 v1 → v14（真实自进化历史）。
- 乙阶段（自动优化期）已进入。
- 模型：MiniMax-M3（thinking adaptive），OpenAI 兼容接口。
- 生产化收敛（2026-08-17）：鸿钧对话（对话 发言/历史）、任务线机制（世界 守护/驱动/中止）、事件流直播（世界 直播）、回滚垫前缀撤销、涉及路径护栏、落点护栏、构建自证沙箱化、指标与失败告警、成本分级。

---

*项目全景理解 · 随世界生长更新 · 依据代码与设计稿实读*
