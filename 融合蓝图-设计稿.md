# 项目心智模型 × 多智能体架构 · 融合蓝图

> 定位：把「项目心智模型（脑）」与「多智能体架构（身）」合成一个可落地的新项目设计。
> 依据：\`项目心智模型-设计稿.md\`（记忆/缓存）+ \`多智能体架构设计.md\`（组织/流程）+ \`层级结构-设计.md\`（六层规范）。
> 状态：融合蓝图 · 决策已全部拍板 · 活文档。
> 已拍板：**落位与命名（三府）**、**会话粒度 = 会话级**、**预算切分 = 最低预算 + 动态提升**、**进化环 vs 固化度（经/权边界）**、**会话与格位预算占比（分开计量）**。
> 活文档：后续新拍板的决策并入对应小节，随世界生长；实现须能溯源到本稿。

---

## 0. 一句话本质

两份文档不是并列拼合，而是**身与脑**：

- **多智能体架构 = 世界之身**：四层组织、流程、队列、版本存档、进化环——回答"谁干什么、怎么流转"。
- **项目心智模型 = 世界之脑**：编码/存储/检索、36 格位、三级缓存、防幻觉——回答"世界记住什么、怎么回想"。

融合动作：把「脑」装进「身」预留的三个坑里。

---

## 1. 融合总纲：三处填空

多智能体架构只给"记忆"留了黑盒（\`记忆-存取-殿\` + \`世界状态.长期记忆: String\`），心智模型恰好把黑盒打开。要填的坑有三个：

| 坑 | 多智能体现状 | 心智模型填什么 |
|:--|:--|:--|
| **存储坑** | \`长期记忆: String\` | 36 格位（72 全量）+ 依赖图索引 |
| **消费坑** | 每角色"读世界状态"（鸿钧②/中层领取/底层领取） | 检索三档投影 + 角色权限 + 预算 |
| **编码坑** | 天道扫描 / 接手建档 / 验收回执 / 进化归因 | 编码三源：代码记事实 / LLM 补语义 / 人填根 |

---

## 2. 六范畴 ↔ 天庭治理-府 映射

心智模型的六范畴，完整覆盖"世界是谁 + 在管什么 + 要干什么 + 干过什么"：

| 范畴 | 对应多智能体内容 | 谁写 | 写时机 |
|:--|:--|:--|:--|
| 目标 | 想法池、要求书（方向/验收标准） | 界主→鸿钧 | 接收想法/化要求 |
| 规则 | 六层规范、命名、契约（法则） | 界主(经) + 天道巡世(违逆) | 建立 + 巡世 |
| 自我 | 角色卡（身份/道/职司/LLM 池/契约） | 界主(经) | 建角色/进化⑤级 |
| 程序 | 状态机、流程、限流、队列 | 鸿钧/进化环 | 调整时 |
| 世界 | 项目档案（结构/规模/依赖/构建） | 天道扫描(代码) | 接手建档 + 每次变更 |
| 经历 | 验收历史、失败模式、版本历史、进化记录 | 代码+LLM | 每任务/每版本 |

关键点：六范畴 = **自我认知**（目标/规则/自我/程序）+ **对外观察**（世界）+ **历史书**（经历）。它同时是"世界的记忆"和"世界对项目的记忆"，统一了多智能体里"世界状态"和"项目档案"两套东西。

---

## 3. 检索投影 ↔ 角色

心智模型的三档投影，回答多智能体没回答的问题——**每个角色开工前到底"看见"什么**：

| 角色 | 投影侧重 | 档位 |
|:--|:--|:--|
| 鸿钧（主政） | 最前（身份/任务/状态）+ 最后（目标/验收/进度） | 全量 |
| 女娲（设计） | 目标 + 规则 + 世界（结构/文件） | 中间偏重 |
| 多宝（实现） | 任务 + 规则 + 世界（文件/数据/调用） | 中间偏重 |
| 准圣（验收） | 验收标准 + 世界（产物/变更） | 最后偏重 |

心智模型的 \`共享/私有\` 字段，正好对齐多智能体的角色权限（不同角色看到不同格位集合）。

---

## 4. 架构骨架 + 落位（三府）

**落位**：\`D:\\洪荒 - 世界\\鸿蒙\`（维度·地基）→ \`基础设施-域\` → 三个府。

\`\`\`
鸿蒙（维度 · 地基）
└── 基础设施-域
    ├── 识海承载-府 (shihai_fu)    脑 · 记忆（上下文）
    │   ├── 铭记-殿（编码：代码扫描 / LLM 播种 / 人类录入）
    │   ├── 纳藏-殿（存储：36 格位 / 依赖图索引 / 会话记录）
    │   └── 回想-殿（检索：三档投影 / 角色权限 / 预算 / 三级缓存）
    │
    ├── 天庭治理-府 (tianting_fu)   组织编排（管组织）
    │   ├── 天层-主控-殿（鸿钧回路 + 天道回路）
    │   ├── 智慧-主控-殿（中层设计）
    │   ├── 队列-调度-殿（要求 / 设计队列）
    │   ├── 团队-调度-殿（调度道术施展-府执行）
    │   ├── 版本-存档-殿（构建 + 快照 + 记录）
    │   └── 进化-主控-殿（进化环，仅乙阶段）
    │
    └── 道术施展-府 (daoshu_fu)     任务执行（管每个层级角色如何执行）
        └── 角色工作流引擎（agent_fu 升级：天层 / 智慧 / 手脚 各角色 L1-L4 工作流）
\`\`\`

**命名对照**：

| 位置 | 旧称 | 新称 | 依据 |
|:--|:--|:--|:--|
| 上下文 | 上下文承载-府 / 段库承载-府 | **识海承载-府** | 识海 = 记忆之海，承载 = 容器，对应"给项目造记忆体" |
| 组织编排 | 世界-府（world_fu） | **天庭治理-府** | 天庭 = 四层神仙职司，治理 = 主政/编排/验收/进化 |
| 任务执行 | 智能体编排-府（agent_fu） | **道术施展-府** | 道术 = 各角色的道+术，施展 = 执行，对应"角色如何执行" |

**依赖**：
- 天庭治理-府（组织）→ 识海承载-府（记忆）：每处"读世界状态"→ 回想-殿检索投影；验收/归档/进化 → 铭记-殿回填。
- 天庭治理-府（组织）→ 道术施展-府（执行）：团队-调度-殿把设计方案交给道术施展-府执行、收产物。
- 道术施展-府（执行）→ 识海承载-府（记忆）：每个角色执行时读投影（任务 + 规则 + 世界）。
- 三者都是府（插件单元 · lib crate + Service Definition），跨府引用经插件注册表止步 Service Definition，不深链（2026-08-20 入稿「九是维度，府是插件」）。

**三府实现`府插件`trait（2026-08-21 入稿）**：渐进式插件化——三府各实现`chajian_fu::府插件`trait（名称/注入/应用三方法），在世界启动时创建`插件上下文`并按依赖顺序批量注册。依赖关系：识海承载-府（无依赖）→ 道术施展-府（注入识海承载-府）→ 天庭治理-府（注入识海承载-府+道术施展-府）。本阶段不删现有`use shihai_fu::`跨府引用（lib crate 仍在，止步点不变），只加插件接口实现+注册代码；后续逐步把跨府引用改为经插件上下文查找。

**Service Definition 机制（2026-08-21 入稿）**：

插件上下文加服务注册表 + 注册/查找方法，让每个府能暴露和查找服务接口：

- **插件上下文加服务注册表**：`服务表: RwLock<HashMap<TypeId, Arc<dyn Any + Send + Sync>>>`，按类型 ID 索引服务实例。
- **注册服务**：`pub fn 注册服务<T: Any + Send + Sync>(&mut self, 服务: T) -> Result<(), String>`——用 `TypeId::of::<T>()` 作 key。
- **查找服务**：`pub fn 查找服务<T: Any + Send + Sync>(&self) -> Option<Arc<T>>`——用 `TypeId` 查找 + 向下转型。

三府的服务 trait（Service Definition 的具体形式，定义在各自 lib 根）：

| 府 | 服务 trait | 暴露方法 | 依据 |
|:--|:--|:--|:--|
| 识海承载-府 | `识海服务` | 回想（检索投影）、铐记（回填记忆） | §4 依赖：天庭/道术读记忆 |
| 道术施展-府 | `道术服务` | 执行任务（角色工作流引擎） | §4 依赖：天庭调度道术执行 |
| 天庭治理-府 | `天庭服务` | 调度要求、验收产物、归档版本 | §4 依赖：组织编排 |

每个府在 `应用()` 方法中创建服务实例（`Arc<dyn 服务trait>`），注册到插件上下文。跨府调用方 `use 目标府::服务trait`（止步 lib 根），经 `ctx.查找服务::<Arc<dyn 服务trait>>()` 查找服务实例，调用 trait 方法。

渐进式改造：本阶段只加 Service Definition 机制（插件上下文加服务注册表 + 注册/查找方法 + 三府定义服务 trait + 在 `应用()` 中注册），不删现有 `use shihai_fu::` 跨府引用；后续逐步把跨府引用改为经插件上下文查找服务实例。

**批量注册接入`应用()`（2026-08-21 入稿）**：

`批量注册()`对每个插件先调`应用(&mut 上下文)`注册服务到服务表，再`注册()`入插件注册表——这样`应用()`中注册的服务实际生效。按依赖顺序调用`应用()`，依赖插件的服务先注册，被依赖方可在`应用()`中经`ctx.查找服务()`查找依赖服务。`应用()`失败则插件不注册（初始化失败不应入注册表）。

**全局化插件上下文（2026-08-21 入稿）**：

用`std::sync::OnceLock<插件上下文>`存全局插件上下文，启动时初始化一次，之后只读查找：

- **初始化**：`OnceLock::get_or_init(|| { let mut ctx = 插件上下文::新(); 批量注册(&mut ctx, 三府插件); ctx })`——在`投递想法()`首次调用时初始化，注册三府插件+服务。
- **查找**：全局上下文经`查找服务::<Arc<dyn 服务trait>>()`查找服务实例——`查找()`和`查找服务()`只需`&self`（内部`RwLock`读锁），线程安全。
- **不提供注册**：初始化后不再注册插件/服务（所有插件+服务在初始化时注册完成），`注册()`和`注册服务()`不在全局上下文上调用。
- **跨府引用改造**：各府从`use 目标府::符号`改为`use 目标府::服务trait` + `全局上下文.查找服务::<Arc<dyn 服务trait>>()`查找服务实例，调用 trait 方法。

**服务trait真实实现方案（2026-08-21 入稿）**：

服务trait从占位实现替换为真实逻辑。核心三决策：

**决策一：服务实例持有重依赖**

每个服务实例struct持有其府的重依赖，在`应用()`中从环境变量与工作区构造。服务trait方法签名去掉实例已持有的依赖参数，只保留任务特定的参数。

| 服务实例 | 持有依赖 | 构造方式 |
|:--|:--|:--|
| `识海服务实例` | `模型存储` | `模型存储::在工作区(&工作区::定位())` |
| `道术服务实例` | `任务调度`（内含`模型配置`+`工作区根`） | `peizhi_fu::读模型配置(.env)` + `任务调度::新(配置, 根)` |
| `天庭服务实例` | `模型配置`、`模型存储`、`任务调度` | 同上 + `模型存储::在工作区()` |

`道术施展-府`加`peizhi_fu`依赖以读`.env`构造`模型配置`（`天庭治理-府`已有此依赖，模式一致）。各府独立读配置——`.env`只在启动时读一次，值相同。

**决策二：服务trait方法签名对齐真实逻辑**

三府服务trait方法签名基于真实逻辑函数签名，去掉实例持有的依赖参数：

`识海服务`（实例持有`模型存储`，方法不传`存储`）：
- `回想(角色: &str, 格位们: &[格位], 预算字符: usize, 调用方: 调用方层级) -> Result<String, String>`——包`元数据层化`。
- `铐记(格位名: &str, 内容: &str, 来源: &str, 录入者: &str) -> Result<(), String>`——包`存储.写记录(&记录::新(...))`。

`道术服务`（实例持有`任务调度`，方法不传`配置`/`工作区根`）：
- `执行任务(任务id: &str, 任务: &执行任务, 背景: &str, 现状: &str, 涉及路径: &[String], 设计方案: &str, 验收标准: &str) -> Result<执行回执, String>`——包`任务调度.派遣执行(...)`。

`天庭服务`（实例持有`模型配置`/`模型存储`/`任务调度`，方法不传这些）：
- `调度要求(想法: &想法) -> Result<主政回执, String>`——包`主政一轮(想法, &配置, &存储, &调度)`。
- `验收产物(要求id: &str) -> Result<验收回执, String>`——包验收逻辑。
- `归档版本(要求id: &str) -> Result<(), String>`——包归档逻辑。

**决策三：两阶段渐进改造**

- **阶段一（步骤8-10，服务trait真实实现）**：服务trait方法包现有逻辑函数，传入实例持有的依赖。现有调用链不改——`投递想法()`仍直接调`主政一轮()`，`主政一轮`仍直接调`任务调度.派遣执行()`。服务trait已定义且可调，但暂无调用方经服务trait调用。`天庭服务实例`此阶段直接持有`任务调度`（在`应用()`中构造），不经过`道术服务`。
- **阶段二（步骤11-14，改造跨府引用）**：调用方改为经服务trait调用——`投递想法()`调`天庭服务.调度要求()`，`主政一轮`内部调`道术服务.执行任务()`代替`调度.派遣执行()`，`识海服务.回想()`代替`元数据层化()`。服务实例从全局上下文查找（`取全局上下文().查找服务()`），`天庭ServiceInstance`不再持有`任务调度`。跨府函数调用消除，类型引用保留。

**跨府引用改造方案（阶段二，2026-08-21 入稿）**：

阶段二把5处核心业务逻辑调用改为经服务trait调用。135处跨府引用中仅此5处是核心业务逻辑调用，其余是工具函数和类型引用，保留。

**5处核心改造点**：

| # | 文件 | 行 | 改造前 | 改造后 | 所属步骤 |
|:--|:--|:--|:--|:--|:--|
| 1 | `世界运行.rs` | 303 | `shihai_fu::元数据层化(存储, "多宝", &全部格位(), 3000, 调用方层级::设计)` | `识海服务.回想("多宝", &全部格位(), 3000, 调用方层级::设计)` | 13 |
| 2 | `世界运行.rs` | 406 | `shihai_fu::元数据层化(存储, "多宝", &全部格位(), 3000, 调用方层级::执行)` | `识海服务.回想("多宝", &全部格位(), 3000, 调用方层级::执行)` | 13 |
| 3 | `世界运行.rs` | 619 | `调度.派遣执行(&任务id, 任务, &背景, &执行现状, &涉及路径, &设计方案文本, &验收标准)` | `道术服务.执行任务(&任务id, 任务, &背景, &执行现状, &涉及路径, &设计方案文本, &验收标准)` | 14 |
| 4 | `原子入池.rs` | 51 | `tianting_fu::主政一轮(&想法, &配置, &存储, &调度)` | `天庭服务.调度要求(&想法)` | 14 |
| 5 | `世界运行.rs` | 1477 | `crate::主政一轮(&想法, 配置, 存储, &调度)`（在`执行一条待执行任务线`内） | `天庭服务.调度要求(&想法)`（从全局上下文查找`天庭服务`，去掉`调度`构造） | 14 |

**服务实例获取方式**：从全局上下文查找——`取全局上下文().查找服务::<Arc<dyn 服务trait>>()`。全局上下文在`投递想法()`首次调用时初始化，之后只读查找，线程安全。

**函数签名改造**：

改造点1-3在`主政一轮`和`运行一轮`内部，这两个函数去掉`调度`参数（`道术服务`从全局上下文查找，不再经参数传入）：

| 函数 | 改造前签名 | 改造后签名 |
|:--|:--|:--|
| `主政一轮` | `(想法, 配置, 存储, 调度)` | `(想法, 配置, 存储)` |
| `运行一轮` | `(想法, 要求id, 配置, 存储, 调度)` | `(想法, 要求id, 配置, 存储)` |

`配置`和`存储`保留——`主政一轮`/`运行一轮`内还有大量工具函数调用（`临时代偿`、`扫描`、`机制归纳`、`登记变更`等）需要这两个参数，这些是工具函数不是服务trait方法，保留。

**`天庭服务实例`改造**：

从持有`任务调度`改为不持有（`主政一轮`不再需要`调度`参数）：

| 字段 | 改造前 | 改造后 |
|:--|:--|:--|
| `配置` | 保留 | 保留 |
| `存储` | 保留 | 保留 |
| `调度` | 保留 | **删除** |

`天庭插件.应用()`相应去掉`调度`构造。`天庭治理-府`仍依赖`daoshu_fu`（类型引用：`执行任务`、`执行回执`、`执行状态`等），但不再直接调`任务调度.派遣执行()`。

**`投递想法`改造**：

- 去掉`配置`和`调度`构造（`天庭服务.调度要求()`不需要传这些）
- 保留`存储`（用于写事件记录，`存储.写记录()`是工具函数调用，不是服务trait方法）
- `主政一轮`调用改为`天庭服务.调度要求(&想法)`

**改造顺序**（步骤12-14）：

1. **步骤12（道术→识海）**：空操作——`道术施展-府`内对`shihai_fu::`的引用全是工具函数和类型引用（`工作区`、`事件流`、`当前毫秒`、`模型存储`、`记录`、`回滚垫`、`当前任务`等），无核心业务逻辑调用。`元数据层化`仅在注释/提示文本中出现，非函数调用。`.写记录()`仅在测试代码中出现。
2. **步骤13（天庭→识海）**：改造`世界运行.rs` line 303、406——`shihai_fu::元数据层化(...)` → `识海服务.回想(...)`，从全局上下文查找`识海服务`。
3. **步骤14（天庭→道术 + 命令操作→天庭）**：改造`世界运行.rs` line 619——`调度.派遣执行(...)` → `道术服务.执行任务(...)`；改造`原子入池.rs` line 51——`tianting_fu::主政一轮(...)` → `天庭服务.调度要求(&想法)`；改造`世界运行.rs` line 1477——`crate::主政一轮(...)` → `天庭服务.调度要求(&想法)`（在`执行一条待执行任务线`内，去掉`调度`构造）；`主政一轮`/`运行一轮`签名去掉`调度`参数；`天庭服务实例`去掉`调度`字段；`天庭插件.应用()`去掉`调度`构造；`投递想法`去掉`配置`/`调度`构造。

**类型共享边界**：跨府类型仍经lib根共享——Service Definition替换的是函数调用（`元数据层化`→`识海服务.回想()`），不消除类型引用（`use shihai_fu::{格位, 调用方层级}`仍保留）。格位、调用方层级、执行任务、想法等类型是跨府共享的领域类型，经各府lib根导出。

**状态共享-府（2026-08-21 入稿）**：

跨府运行时状态共享——各府经状态共享-府读取/写入共享状态，不直接传参。与Service Definition的区别：Service Definition是服务接口注册（静态，启动时注册一次），状态共享是运行时状态（动态，随时可写可读）。

| 项 | 内容 |
|:--|:--|
| 府 | `鸿蒙/基础设施-域/状态共享-府`（lib = `zhuangtai_fu`） |
| 殿 | `类型-定义-殿`（状态键/状态值）+ `状态-存取-殿`（状态共享/读取/写入） |
| 依赖 | zhuangtai_fu ← daoshu_fu / tianting_fu / mingling_fu（消费方），无反向依赖 |

核心接口：

```rust
/// 状态共享——跨府运行时状态，按类型ID索引
pub struct 状态共享 {
    状态表: RwLock<HashMap<TypeId, Arc<dyn Any + Send + Sync>>>,
}

impl 状态共享 {
    pub fn 新() -> Self;
    /// 写入状态：按类型ID覆盖（后写覆盖先写）
    pub fn 写入<T: Any + Send + Sync>(&self, 状态: T) -> Result<(), String>;
    /// 读取状态：按类型ID查找+向下转型+Clone返回
    pub fn 读取<T: Any + Send + Sync + Clone>(&self) -> Option<T>;
    /// 移除状态：按类型ID移除
    pub fn 移除<T: Any + Send + Sync>(&self) -> Result<(), String>;
}
```

全局化：与插件上下文同理，用`OnceLock<状态共享>`存全局状态共享，启动时初始化，之后可读可写（`RwLock`保护，线程安全）。

与事件总线-府的分工：状态共享是"当前状态快照"（读最新值），事件总线是"事件通知"（通知变化发生）。状态共享适合"当前任务线id""当前想法id"等运行时状态；事件总线适合"任务完成""想法打回"等事件通知。

状态共享接入主循环（2026-08-21 入稿）：

| 项 | 内容 |
|:--|:--|
| 状态类型 | `当前想法id(pub struct(String))`、`当前要求id(pub struct(String))`——定义在 zhuangtai_fu 类型-定义-殿 状态键值-阁，供 tianting_fu 写入 / mingling_fu 读取 |
| 主循环写入 | `主政一轮` 开始时 `取全局状态().写入(当前想法id(想法.id.clone()))`；`运行一轮` 开始时 `写入(当前要求id(要求id.to_string()))`；结束时保留（供观览查询读最后处理状态，不移除） |
| 观览查询读取 | `呈现世界状态()` 末尾追加"当前想法/要求"段：从 `取全局状态()` 读取 `当前想法id` / `当前要求id`，未初始化或未写入时显示"（无）" |
| 初始化 | 启动时（`投递想法` 或 `执行一条待执行任务线` 入口）调 `zhuangtai_fu::初始化全局状态()`，与 `chajian_fu::初始化全局上下文()` 同处 |

---

## 5. 已拍板：会话粒度 = 会话级

> 界主拍板：工作记忆（会话记录）以**会话级**为颗粒度。

**会话的定义**：一次任务执行过程——从要求书入队，经设计、实现，到验收定档。

| 项 | 规则 |
|:--|:--|
| 会话起点 | 要求书入队 / 被领取 |
| 会话终点 | 验收定档（通过 / 打回终判） |
| 会话记录 | 该会话的完整工作记忆（要求书、设计方案、执行产物、验收回执），**不截断、不重拼** |
| 归档 | 会话终点 → 归档进经历格位（事件/变更/产物/教训）→ 清空会话缓存 |
| 并发 | 多要求并发 = 多会话并行，各会话独立工作记忆，**互不污染** |
| 颗粒度 | 以"会话"为最小单元：设计/实现/验收共用同一份会话记录，不拆到单个动作；也不合并到世界迭代周期 |

> 效果：一个任务 = 一个会话 = 一份完整现场。会话缓存按"任务"键隔离（与拼装输入指纹的"任务"维度一致）。

### 5.1 已拍板补充：界主对话 = 会话（鸿钧主控化）

> 界主拍板（2026-08-17）：鸿钧主控化后，**一次界主对话 = 一个会话**（会话级颗粒度的自然扩展，见 多智能体架构设计.md §1.5.5）。

| 项 | 规则 |
|:--|:--|
| 对话会话 | 界主与鸿钧的连续对话 = 一个会话（闲聊/发任务/澄清/追问同属一个会话现场） |
| 任务会话 | 对话中发布的每个任务 = 独立任务线，沿用既有"一次任务=一个会话"的工作记忆 |
| 关系 | 对话会话 ⊇ 任务线会话：对话会话记录对话流，任务线会话记录任务现场，互不污染 |
| 归档 | 对话会话归档进经历格位（理解·记忆）；任务线会话归档进事件/变更/产物/教训 |
| 消息可见性 | 界主发言非@仅鸿钧可见、@点名直达该角色；角色发言全员可见（透明工作群） |

---

## 6. 已拍板：预算切分 = 最低预算输出 + 上限动态提升

> 界主拍板：任务执行时**按最低预算输出**，预算上限**根据实际使用需求动态提升**。

### 6.1 预算三层结构

| 层 | 内容 | 性质 |
|:--|:--|:--|
| 工作区前缀 | 根路径 / 范围 / 排除项 | 固定注入，永不裁 |
| 长时记忆格位 | 36 格位（最前/中间/最后） | 按需投影，可裁剪 |
| 工作记忆 | 会话记录（本任务完整现场） | 完整保留，不截断 |

### 6.2 最低预算（每次上下文注入的默认起点）

| 部分 | 内容 | 效应 |
|:--|:--|:--|
| 工作区前缀 | 我在哪、边界到哪 | 建立坐标原点 |
| 最前档经格位 | 身份/铁律/价值观/标准/任务 | 首因（必保） |
| 最后档当前 | 目标/验收标准/进度/阻塞/验收结果 | 近因（必保） |
| 会话记录 | 本任务完整现场 | 工作记忆，完整 |

> 这是"最低预算输出"：token 最少，只给做任务必需的核心 + 当前 + 现场。

### 6.3 动态提升（按需扩容）

- **触发**：执行中需要更多上下文（查结构/文件/历史/数据契约/调用关系）。
- **动作**：用索引定位 → 按需投影补充**中间档**对应格位 → 预算上限随之提升。
- **上限**：**不预设硬顶**，随实际需求增长；软约束是"只补任务真正需要的格位，不全量铺开"。

> 实质：把心智模型原有的"预算不足从中间往两头裁"（自上而下裁剪）**反转为"从最低预算往上长"（自下而上生长）**——默认最小，需要才长，长到够用为止。

### 6.4 与三级缓存的关系

| 缓存级 | 内容 | 对预算的作用 |
|:--|:--|:--|
| 永久缓存 | 经格位 | 直接注入，不重读（本就该在最低预算里） |
| 版本缓存 | 权格位 | 版本未变即复用，变了才重拼 |
| 会话缓存 | 会话记录 | 完整保留，随会话走 |

---

## 7. 已拍板：进化环 vs 固化度（经/权边界）

> 界主拍板：进化环（乙阶段）只可自动调整「权」格位；触及「经」格位必须走 ⑤级结构级 + 界主确认。

1. 进化环的自动调整（①调提示词 / ②调分工 / ③调流程 / ④重组团队）**只能作用于固化度 = 权 的记录**。
2. 固化度 = **经** 的记录，进化环**不得自动改写**（含铁律·总纲、价值观·原则、底线、身份、标准、架构、方向、权限、世界观、事件、变更、产物等）。
3. 进化环判断需改动「经」格位 → 归为**⑤级结构级** → 出调整提案（改哪条 / 为什么 / 改前 vs 改后）→ **界主审批**：批准则生效留痕，否决则废弃。
4. 判定依据 = 记录字段 \`固化度\`；进化环每次调整前先查目标记录固化度，\`经\` 则强制走⑤级。

---

## 8. 已拍板：会话与格位的预算占比（分开计量）

> 界主拍板：会话记录（工作记忆）设**独立预算**，与 36 格位（长时记忆）**分开计量**，会话不挤占长时记忆预算。

1. 总上下文预算 = **长时记忆（36 格位）+ 工作记忆（会话记录）** 两块，**各自独立计量、互不挤占**。
2. 长时记忆：按 §6「最低预算输出 + 上限动态提升」执行。
3. 工作记忆（会话记录）：**完整保留、不截断、不重拼**；大小由会话内容自然界定（一个任务的要求书 + 设计 + 产物 + 验收），**不设与长时记忆共享的硬上限**，也不因长时记忆动态提升而被裁剪。
4. 会话终点 → 归档进经历格位 → 清空会话缓存（释放工作记忆），避免跨会话无限累积。

### 8.1 结构图位 pub API 签名注入预算（2026-08-22 入稿）

> 背景：世界自转中 LLM 生成代码时脑补不存在的 API 导致编译失败。复用「结构」格位（项目心智模型 §3 36 格位之一，不新增格位），自动扫描 workspace 所有 crate 的库根 pub API 签名写入「结构」格位（实体键=API·{lib名}），执行现状拼装时从「结构」格位读实体键以「API·」开头的记录注入，防 LLM 脑补 API。

| 项 | 约定 |
|:--|:--|
| 格位 | 结构（范畴=世界，来源=代码，固化度=权，共享度=共享，顺序档位=中间）；pub API 签名记录实体键=API·{lib名} |
| 扫描时机 | 代码定档后（定档环节）自动扫描 workspace 所有 crate 的库根 pub API 签名写入「结构」格位，项目无关——解析所有members不筛选命名后缀，从[lib] path获取库根文件 |
| 注入时机 | 执行现状拼装时（现状拼装.rs），读取「结构」格位中实体键以「API·」开头的记录格式化注入现状 |
| 注入格式 | 【接口·契约·可用API清单】按 crate 列出 pub 签名（库根 + pub fn/pub mod） |
| 注入预算 | **独立预算约 3000 字符**，不挤占会话记录与长时记忆预算；超限时按涉及路径相关 crate 优先保留 |
| 防幻觉 | LLM 只能引用注入清单中的 API，不在清单中的需先创建实现；执行提示.rs 硬编码例子泛化为通用描述 |
| LLM维护 | 落盘前阴面LLM对清单做一次维护/补充（利用阴拓展能力，补充自动扫描遗漏的语义信息）；自动扫描兜底+LLM维护增强，两者共存 |

> 落位：识海承载-府 现状拼装.rs 注入 + 铭记-殿 扫描写入；天庭治理-府 定档环节触发扫描。详见 多智能体架构设计.md §11.2 规则 19。

---

## 9. 落地顺序

1. **先建识海承载-府**（记忆底座：36 格位结构 + 编码三源 + 检索三档 + 三级缓存 + 会话记录）——一切角色都读它。
2. **再建天庭治理-府骨架**（组织编排：四层 + 队列 + 状态机），把每处"读世界状态"改成"识海承载-府回想-殿检索投影"。
3. **接道术施展-府**（任务执行：角色工作流引擎，复用 agent_fu 的 L1-L4）。
4. **打通编码回填**：验收回执→经历格位、天道扫描→世界格位、进化归因→教训格位、会话归档→经历格位。
5. **最后接版本存档/进化环**（乙阶段）。

---

## 10. 一页总结

\`\`\`
融合 = 身 + 脑

多智能体架构（身）            项目心智模型（脑）
───────────────────────────────────────────────────────
四层组织 + 流程 + 队列         编码三源（代码/LLM/人类）
版本存档 + 进化环              六范畴 36 格位（72 全量）
记忆黑盒 → 由脑填满            三档投影 + 索引 + 防幻觉

三府：识海承载-府（记忆）/ 天庭治理-府（组织）/ 道术施展-府（执行）
会话粒度：一次任务执行 = 一个会话，完整不截断，终点归档
预算切分：最低预算输出，上限按需动态提升（默认最小，按需生长）
经/权边界：进化环只调「权」格位，碰「经」格位走⑤级 + 界主确认
预算占比：会话记录独立预算，与 36 格位分开计量，互不挤占
防幻觉：LLM 填充必带证据，来源排序 代码 > 人类 > LLM
\`\`\`

> **2026-08-22 入稿**：复用「结构」格位（不新增格位，仍 36 格位），自动扫描 workspace 全部 crate 库根 pub API 签名写入「结构」格位（实体键=API·{lib名}），执行现状拼装时从「结构」格位读实体键以「API·」开头的记录注入，防 LLM 脑补不存在的 API。注入预算独立约 3000 字符，见 §8.1。

---


## 11. 监控界面 · 呈现侧·以府显示

> 定位：世界的"实时仪表盘"——把鸿蒙六府 + 命令操作府 + 观测探针府 + 配置管理府 + 单元测试府共 9 个 crate 的运行状态，**以府为单元**可视化为一张卡片墙。
> 落位：`乾坤 / 呈现-域 / 监控界面-府`（Rust lib crate，`入口.rs` 为 lib 根，`lib=jiankong_fu`），呈现侧府插件，**入 Cargo workspace**——与其他 Rust 府同栈编译，统一技术栈。
> 状态：入稿 · 2026-08-19（接 2026-08-17 鸿钧主控化后）。
> **二次修订 · 2026-08-19 同日**：形态由"9 卡片仪表盘 + 拉刷新"推翻为"**直播 + 白箱 + 让用户信任**"（界主裁定 §动机）。
> **三次修订 · 2026-08-21**：技术栈由 Python 改为 Rust（axum + tokio + SSE），界主裁定统一技术栈；落位从"不入 Cargo workspace"改为"入 Cargo workspace，lib=jiankong_fu"；数据来源从"直读 jsonl 文件"改为"经各府 lib 根符号读取"。形态设计（白箱六字段 / 双视图 / SSE 三源 / LOD 渲染 / 九府卡片）不变。
> 活文档：实现过程中如发现契约偏差，回归本稿对应小节修订；实现须能溯源到本稿。

---

### 一、本质与目标

| 项 | 内容 |
|:--|:--|
| 一句话 | 给世界一张仪表盘，让界主 / 鸿钧 / 任何 LLM 角色一眼看清"谁在干什么、卡在哪、有没有坏" |
| 用户 | 主要是**界主**（人在浏览器观察），其次是**鸿钧对话循环**内可能引用仪表盘数据做自我汇报 |
| 实时性 | **SSE 长连接直播**（服务端推送，每条事件即时到达）—— 监控是观察工具，不是控制工具，但要"看见过程"，故用 SSE 而非拉刷新 |
| 写约束 | **只读**：监控界面不写任何状态，调各府的**观览-查询-殿 / 观测探针府**的读入口，禁止越权调"号令-下达-殿"的写命令 |
| 视觉原则 | 一府一卡片；卡片分三态：**绿（无活跃 / 速率正常）/ 黄（有任务进行）/ 红（失败 / 超时 / 环境故障）** |

> **不重复实现的边界**：技术日志走 `rizhi_fu`（鸿蒙/基础设施-域/日志记录-府）；观测数据走 `jiance_fu`（鸿蒙/基础设施-域/观测探针-府）；监控界面只**消费**这两府的 lib 根符号，**不另造采集层**。这与 AGENTS.md 第 5 条"勿重复实现"一致。

---

### 二、落位与命名

#### 2.1 路径

```
乾坤（维度 · 容器）
└── 呈现-域（界面）
    ├── 命令操作-府（mingling_fu）    命令行交互
    └── 监控界面-府 (jiankong_fu)    仪表盘（本稿新增，与号令府平级；Rust lib crate，入 Cargo workspace）
        ├── 入口.rs                  lib 根（pub mod 各殿 + pub use 透出 Service Definition）
        ├── Cargo.toml               lib=jiankong_fu，入 workspace
        ├── 类型-定义-殿/            HTTP / JSON / 事件契约 / 白箱六字段类型
        ├── 数据-抓取-殿/            从各府 lib 根读数据（事件流 / 观测 / 状态）
        ├── 视图-装配-殿/            组装响应（快照 / SSE 流 / 回放 / 双视图）
        └── 输出-呈现-殿/            HTTP 路由（axum）+ 静态资源服务（include_str!/include_bytes! 内嵌）
```

> **为什么是府而不是命令操作-府的第六殿**：监控界面有自己的"长连接/轮询机制"+"静态资源分发"，与命令操作府的"解析+权限+下达+呈现输出文本"职责不同；放一个殿会让 殿内阁-园 树失衡（>4 阁），违背六层 §2.1 切分纪律。**新开一府**更清晰。

#### 2.2 命名（依层级结构-设计.md §7）

| 层 | 名字 | 依据 |
|:--|:--|:--|
| 府 | 监控界面-府 | 名字体现"呈现·监控"语义，区别于"操作" |
| lib crate | `jiankong_fu` | 入 Cargo workspace，与其他 Rust 府同栈编译 |
| 启动入口 | `入口.rs`（lib 根）+ `Cargo.toml`（`lib.path=入口.rs`） | 府=插件单元（lib crate + Service Definition），符合层级结构 §三 |
| 殿 | 类型-定义 / 数据-抓取 / 视图-装配 / 输出-呈现 | 沿用命令操作-府的殿命名语料 |
| 阁 | 各殿下属按功能划分 | 同 |
| 园 | 各阁下属按实现手段命名（轮询-园 / 卡片-渲染-园 / 静态-资源-园） | 同 |

---

### 三、分府视图契约 · 九张卡片

> **每张卡片的数据来源 = 那个府的"观览-查询-殿 + 观测探针"，** 不另造数据。
> 卡片字段必须 **可单测**——验收时按契约断言每张卡片都返回了 §3.x 列的字段。

#### 3.1 九张卡片总览

| # | 卡片 | 来源 | 关切字段 |
|:--|:--|:--|:--|
| 1 | 识海承载-府 | 鸿蒙/基础设施-域/识海承载-府 + 观测探针 | 36 格位写入条数 / 最近 N 次编码（铭记） / 最近 N 次归档（纳藏） / 三档拼装命中率（回想） |
| 2 | 天庭治理-府 | 鸿蒙/基础设施-域/天庭治理-府 | 八态状态机当前队列长度 / 进行中要求数 / 等待设计数 / 终裁待审数 / 鸿钧对话循环轮数 |
| 3 | 道术施展-府 | 鸿蒙/基础设施-域/道术施展-府 | 工具循环总轮数 / 当前 token 预算（占 90 万上限）/ 最近失败任务 / 派发落单回滚垫使用 |
| 4 | 模型连接-府 | 鸿蒙/基础设施-域/模型连接-府 | 最近 5 次模型调用的 token / 缓存命中率 / 平均耗时 / 5xx 重试次数 |
| 5 | 日志记录-府 | 鸿蒙/基础设施-域/日志记录-府 | 订阅构建状态 / 兜底构建文件大小 / 并行落地速率 / 流式渲染队列 |
| 6 | 配置管理-府 | 鸿蒙/世界配置-域/配置管理-府 | 已加载的 .env 项数 / 缺失告警 / 占位密钥提示 |
| 7 | 观测探针-府 | 鸿蒙/基础设施-域/观测探针-府 | 探针条目数 / 当前正在写盘的 span / 跨界异常计数 |
| 8 | 命令操作-府 | 乾坤/呈现-域/命令操作-府 | 当前鉴权令牌态 / 最近 10 条号令执行结果 / 解析失败率 |
| 9 | 单元测试-府 | 证道/鸿蒙-域/单元测试-府 | 最近一次 \`cargo test\` 结果（通过 / 失败用例清单） / 总用例数 / 跳转跳转时间 |

#### 3.2 卡片共有的 "骨架三段"

每张卡片渲染时必须有三段：

1. **心跳段**：最近一条活动时间 + 间隔（如"3 秒前活跃"）——来自日志记录-府的时间戳
2. **计数段**：上方表格里的关切字段
3. **最近动作段**：该府最近 5 条动作摘要（来源 = \`.上下文/观测/记录.jsonl\` 该府过滤）

#### 3.3 全局页脚（一项，不算府）

| 字段 | 来源 |
|:--|:--|
| 世界当前北京时间 | 命令操作-府的流式-时刻-园 |
| 本轮要求 id + 状态 | \`.上下文/状态/要求.jsonl\` |
| 本轮通过的轮数比 | 计算 \`.上下文/状态/验收.jsonl\` 最近 10 条 |
| 当前基线版本 | \`.上下文/状态/版本.jsonl\` 最后一条 |
| 网络状态 | \`.上下文/状态/网络-状态.json\` |

---

### 四、技术做法

#### 4.1 Rust 实现（**界主裁定 · 2026-08-21 三次修订**）

> **决策演进**：
> - 2026-08-19 一稿：用 Rust 实现（已作废的"原决策"）
> - 2026-08-19 二稿：改用 Python（界主裁定，理由：与临时 Python 监控废址衔接自然、零第三方依赖、与 Rust 仓解耦）
> - **2026-08-21 三稿（本稿，界主裁定）**：**改回 Rust 实现**。理由：
>   1. **统一技术栈**：与鸿蒙六府 + 命令操作府 + 观测探针府 + 配置管理府 + 单元测试府同栈，一份 `cargo build` 编完整个世界，不再"Python + Rust 双栈并行"
>   2. **直接复用各府 lib 根符号**：数据抓取从"读 jsonl 文件"升级为"经 `shihai_fu::` / `jiance_fu::` / `zhuangtai_fu::` 等 lib 根符号读取"，类型安全、零序列化损耗、零文件轮询
>   3. **编译单一二进制**：监控界面与命令操作府可同入一个 bin（或各自 bin），部署只一个可执行文件，无 Python 运行时依赖
>   4. **类型安全**：白箱六字段契约直接由 Rust 类型表达，编译期拦截字段缺失；Python 字典靠运行时断言
>   5. **符合层级结构 §三"府=插件单元（lib crate + Service Definition）"模型**：监控界面-府作为 Rust lib crate 入 Cargo workspace，是府插件化的自然形态；Python 辅佐层是历史包袱
>
> **仍要做的清理**：临时文件夹里旧的 `监控服务器.py 12 K` + `监控.html 20 K` **全弃**；不复用旧代码，也不复用旧 Python 语言。AGENTS.md 第 9 条：实现完成时把临时文件夹里的旧 Python 文件一并清空。

#### 4.2 Rust HTTP 服务（**界主裁定 · 2026-08-21 三次修订**）

| 层 | 技术 |
|:--|:--|
| 路由 | `axum`（Router + GET/POST handler），tokio 异步运行时 |
| SSE | `axum::response::sse::Sse` + `KeepAlive`，长连接推送事件流 |
| 数据 | 经各府 lib 根符号读取（`shihai_fu::` / `jiance_fu::` / `zhuangtai_fu::` / `rizhi_fu::` 等），止步 lib 根，不深链园内文件 |
| 序列化 | `serde` + `serde_json`，白箱六字段类型由 `类型-定义-殿` 定义 |
| 静态 | `include_str!` / `include_bytes!` 编译期内嵌 `index.html` / `style.css` / `app.js`（与 §八 同款三件套，HTML 不足 50 行 / CSS 不足 150 行 / JS 不足 200 行），无运行时文件 IO |
| 部署 | `cargo run -p mingling_fu -- 监控 启动`（命令操作府子命令）或独立 bin `cargo run -p jiankong_fu`；独立端口 **8080**，与命令操作府端口不冲突 |
| 进程 | tokio 多线程异步运行时；SSE 流靠 axum 的连接管理，无需手写线程池 |

#### 4.3 路由（**2026-08-19 二次推翻**）

> **形态转向**：从"拉刷新仪表盘"改为"**流式直播 + 白箱回放**"。理由：
> - 拉刷新 1.5s 间隔内的中间动作被丢失 → **直播不能丢事件**
> - 用户对"世界在跑"的不信任源于看不到过程 → 必须把过程摊开
> - 信任的另一面是事后能查 → 必须有可回放
>
> **7 端点设计**：

| 方法 | 路径 | 形态 | 用途 |
|:--|:--|:--|:--|
| GET | `/` | 静态 | index.html |
| GET | `/static/*` | 静态 | 资源 |
| GET | `/api/snapshot` | JSON | 当前快照（直播首帧 + 暂停时拉取用） |
| **GET** | **`/api/stream`** | **SSE 长连接** | **直播：服务端持续推送新事件** |
| **GET** | **`/api/replay?since=Ts&until=Ts&府=id`** | **SSE/NDJSON** | **回放：按时间段重放历史事件** |
| GET | `/api/rooms` | JSON | 9 张卡片清单（与 monitor.rooms.json 同步） |
| POST | `/api/settings` | JSON | 唯一允许的写：调间隔/主题/端口（复用命令操作府令牌） |

> **为什么用 SSE 不用 WebSocket**：
> - 监控是**单向**（服务端→浏览器），SSE 完美匹配
> - SSE 基于 HTTP/1.1 长连接，浏览器 `EventSource` 原生、自动重连
> - WebSocket 需要握手 + 二进制协议 + 心跳，SSE 不需要
> - axum 自带 `response::sse::Sse` 流式响应构造器，零手写协议帧

> **关键边界**：直播流一旦断开（断网/刷新），再连回时先 GET `/api/snapshot` 拿"当前"，再订阅 `/api/stream` 防漏——“先拿快照再订阅”对会话不丢事。

---

### 五、与现有机制的关系

#### 5.1 跨府引用（**2026-08-21 三次修订 · Python→Rust**）

- Rust 监控界面与各府同栈编译，**经各府 lib 根符号读取数据**（止步 lib 根，不深链园内文件），无 `subprocess` 跨进程调用
- 数据来源统一走各府 lib 根：识海承载-府 `shihai_fu::`（格位 / 编码 / 归档 / 命中率）、观测探针-府 `jiance_fu::`（观测记录 / span / 跨界异常）、状态相关 `zhuangtai_fu::`（要求 / 验收 / 版本 / 网络状态）、日志记录-府 `rizhi_fu::`（兜底构建 / 流式渲染队列）等
- 监控界面-府**不调用任何府的写入口**（号令下达/要求入队/验收裁决等）——这与权限模型一致：它是只读视图
- 与命令操作府共用鉴权**仅在 `POST /api/settings` 写自己配置时**复用令牌，跨府经插件上下文查 `mingling_fu::鉴权服务` Service Definition，不深链

#### 5.2 与日志系统边界

| 数据 | 归属 | 监控如何拿到 |
|:--|:--|:--|
| 技术日志 | `rizhi_fu`（鸿蒙/基础设施-域/日志记录-府） | 调 `rizhi_fu::读日志` lib 根入口 |
| 观测记录（业务流水） | `jiance_fu`（鸿蒙/基础设施-域/观测探针-府） + 识海承载-府 | 调 `jiance_fu::` lib 根入口读观测记录，按 `关联`/`域`/`角色`/`接口` 过滤 |
| LLM 调用统计 | `rizhi_fu` 日志 + 模型连接-府计数 | 调 `jiance_fu::` 提供的汇总入口 |

监控界面**不重复实现**采集层，统一经 lib 根符号消费已有的。

#### 5.3 与构建/版本机制

- 监控服务与命令操作府共用 `道果树/构建物-域` 目标目录
- 启动时 `监控界面-府` 读 **固定的 `monitor.rooms.json`**（一份小清单，列出 9 个府的名字、关切字段、最近动作路径），避免硬编码；该清单是监控界面-府自己的配置园资产，不写各府 Cargo.toml
- `monitor.rooms.json` 例：

\`\`\`json
{
  "rooms": [
    {"id":"shihai_fu","name":"识海承载-府","源":"鸿蒙/基础设施 - 域/识海承载-府","关切字段":["格位","编码","归档","三档命中率"]},
    ...9 条
  ]
}
\`\`\`
- 版本变更时（\`号令 版本 存档\`），监控界面应刷掉"基线版本"字段，**不改 UI**

---

### 六、入稿与实现边界

#### 6.1 入稿时间线

| 日期 | 入稿内容 |
|:--|:--|
| 2026-08-13 | 层级结构-设计.md（维度/域/府/殿/阁/园 六层定稿） |
| 2026-08-17 | 融合蓝图 §5.1 鸿钧主控化入稿 |
| 2026-08-17 | 项目心智模型 §6.4 人类格位临时代偿 / §6.5 可操作规则格位注入 入稿 |
| 2026-08-19 | **监控界面 · 呈现侧·以府显示** 入稿 → 第十府落位 |
| 2026-08-19 同日修订 | 形态由"9 卡片仪表盘 + 拉刷新"推翻为"直播 + 白箱 + 让用户信任" |
| 2026-08-19 二稿 | 改用 Python 实现（界主裁定，推翻 §11.4.1 的 Rust 决策） |
| 2026-08-21 | 测试性能优化：shihai_fu 单测 146 秒 → 0.38 秒；端到端 13 分 49 秒现状分析 |
| **2026-08-21 三稿（本稿）** | **改回 Rust 实现**（界主裁定，统一技术栈）；落位入 Cargo workspace，lib=jiankong_fu；数据来源改经各府 lib 根符号读取；观测探针-府路径从 `鸿蒙/` 迁至 `鸿蒙/基础设施-域/` |

#### 6.2 实现约束

1. **先入稿、再落码**（AGENTS.md 第 8 条）：本稿既为依据，本稿之后才落 `监控界面-府` 五个目录与具体代码
2. **日志埋点必带**（AGENTS.md 第 6 条）：监控界面-府初始化时经 `rizhi_fu` lib 根记 INFO；卡片抓取失败经 `rizhi_fu` 记 WARN
3. **不污染真实盘面**（AGENTS.md 第 4 条）：Rust 实现代码落点 `乾坤/呈现-域/监控界面-府/`（`入口.rs` + `Cargo.toml` + 四殿），入 Cargo workspace；静态资源 `index.html / style.css / app.js` 经 `include_str!` / `include_bytes!` 编译期内嵌，不落盘到生产代码目录。`临时文件夹/` 仅允许旧废 Python 文件一次性归档，不可作为新生产代码落点
4. **测试在 `证道/单元测试-府` 镜像为新殿**（`监控界面-验证-殿`），不内联到监控界面-府自身
5. **九张卡片各自单测**：卡片渲染纯函数化，输入各府 lib 根返回的类型化数据，输出 `JSON`，断言字段齐全

#### 6.3 验收（接六维准圣）

| 维度 | 验收要点 |
|:--|:--|
| 业务正确性 | 9 卡片缩略字段齐全；直播推送每条事件符合 §9.3 六字段契约（缺一即红角拒收） |
| 数据完整性 | 任一源府 lib 根追加事件 → 直播流 100ms 内出现；回放按时间游标正确返回 |
| 性能并发 | 直播 SSE 单流 ≤ 200ms 延迟（lib 根内存读）；10 路并发 EventSource 不互相干扰 |
| 安全副作用 | 不写任何状态文件；只起一个 HTTP 服务进程；POST /api/settings 复用令牌才能写监控自己的配置 |
| 异常兼容 | 任一源府 lib 根读失败 → 直播只标"该庭暂无事件"，不阻塞他庭；SSE 断了 1 秒内自动重连 |
| 用户体验 | 直播行 6 字段齐顶对齐 + 数字 tabular-nums；红黄绿三态 + 错误有时间戳；移动端至少能浏览 |

---

### 八、界面布局 · 直播 + 白箱

> **形态定位**：仪表盘是骨架，**直播是主角**。仪表盘缩为顶部状态条，主体是实时事件流与回放游标。
> 借鉴 DSH 的"暗色 + 状态点"骨架；舍去 LLM 对话的"对话流"元素（监控不与 LLM 对话）。
> 视觉原则：**暗色 + 状态点 + 时刻轴贯穿 + 数字齐顶 + 中文为主**。
> 落地：`乾坤/呈现-域/监控界面-府/输出-呈现-殿/静态-资源-阁/` 下三件套 `index.html` + `style.css` + `app.js`，经 `include_str!` / `include_bytes!` 编译期内嵌到二进制。
>
> **核心一句话**：任何时刻打开浏览器，**看见世界正在做什么**——不光是"进行中"，是"它怎么做的、做到哪了、谁在干、用了多少 token、卡在哪"。

#### 8.1 总布局 · 直播为主 · 卡片缩略 · 顶部状态条

\`\`\`
┌────────────────────────────────────────────────────────────────────────────┐
│ 顶部状态条                                                                  │
│ 洪荒 · 直播 │ 阶段:v15乙 │ 当前要求:要求-38 │ ⏰ 13:42:18 │ token:56.7万/90万 │
│ 🟢🟢🟢🟡🟡🟡🟢🟢🟢        ← 一行 9 卡片缩略（状态点）                                  │
├────────────────────────────────────────────────────────────────────────────┤
│ 段头导航  [直播]·[回放]·[9 卡片]·[流水线]·[依赖图]·[设置]                            │
├────────────────────────────────────────────────────────────────────────────┤
│ 当前任务线 · 要求-38                                                        │
│ ┌──────────────────────────────────────────────────────────────────────┐  │
│ │ 要求-38:补齐解析调用单元测试(命令-解析-验证-殿)                          │  │
│ │ 阶段 实现中  轮数 7/32  累计 token 56.7万/90万  状态 🟢 正常推进         │  │
│ │ 涉及的庭: 道术施展(执行) · 单元测试(验证) · 鸿钧主控(派遣)                  │  │
│ └──────────────────────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ 实时事件流（直播 / SSE 推送 · 时间倒序 · 最近 100 条）                    │
│                                                                            │
│  13:42:18  鸿钧   化要求-38         token:1.2k  ↓0.2s                     │
│              └─ 拆分为子要求 #38.1/#38.2                                   │
│  13:42:15  道术   工具循环-7        token:8.9k  ↑2.1s                    │
│              └─ 写文件 鸿蒙/.../单元解析.rs +421 行                       │
│  13:42:12  识海   铭记·写入格位       token:0.3k  ↓0.1s                    │
│              └─ 格位"调用" +1 条 (人工)                                  │
│  13:42:08  鸿钧   关键节点·开始实现  ↓                                  │
│  ...                                                                       │
│                                                                            │
│  [暂停] [跳至首事件] [清屏(仅本地)] [导出该时段 NDJSON]                       │
├────────────────────────────────────────────────────────────────────────────┤
│ 页脚  基线:v15 │ 通过率:5/10 │ 总事件:12,489 │ 流速:14 ev/s │ 网:🟢 │ ⓘ     │
└────────────────────────────────────────────────────────────────────────────┘
\`\`\`

- **9 张卡片排成 3×3 网格**——一屏内三行三列全可见，不用滚动（视口≥1280×800）
- **顶部栏**两行：第 1 行 \`世界名 · 阶段 · 当前要求 · 北京时间\`；第 2 行 \`主视图切换\`
- **页脚一行**：7 字段塞满宽度，每字段等宽对齐

#### 8.2 段头导航六视图（**2026-08-19 二次推翻**：默认落"直播"）

| # | 视图 | 主区显示 | 何时切 |
|:--|:--|:--|:--|
| 1 | **直播** | 当前任务线详情 + 实时事件流 SSE 推送 | **默认** —— 想看世界此刻在干啥 |
| 2 | **回放** | 时间游标 + 历史事件 NDJSON 流式回灌 | 想看"3 任务前那一轮到底死在哪" |
| 3 | **9 卡片** | 9 张卡片缩略 3×3（精简成缩略图） | 想一眼看 9 庭状态 |
| 4 | **流水线** | 8 态列分组的 要求时间线 | 想看任务流转 |
| 5 | **依赖图** | 9 府 SVG 力导向图 | 想看府间耦合 |
| 6 | **设置** | 拉刷新间隔 / 端口 / 主题 / 暂停后重连 | 用户偏好 |

> 视图 1 直播 + 视图 2 回放 = 一对；视图 3-5 是补充视角（从不同角度观察）；视图 6 是配置。
> **首要的是直播**——其他视图是"补充"不喧宾夺主。

#### 8.3 卡片栅格对齐（视觉收口）

- **卡片等宽** \`grid-template-columns: repeat(3, 1fr);\`
- **卡片等高** \`grid-auto-rows: minmax(260px, auto);\`
- **数字字段等宽右对齐** \`font-variant-numeric: tabular-nums; text-align: right;\` —— 让列对得齐
- **分隔线** 用 \`───────\` 水平细线（中文 box-drawing），不靠颜色堆砌
- **状态点** 直径 10 px 圆点 + 颜色变量：\`--ok:#4ade80 / --warn:#facc15 / --err:#f87171 / --idle:#94a3b8\`

#### 8.4 响应式（不要求全端，但至少三档）

| 视口 | 列数 | 备注 |
|:--|:--|:--|
| ≥1280 | 3 列 | 默认，3×3 |
| 768–1279 | 2 列 | 折叠为 5 行（9+1） |
| <768 | 1 列 | 流式 9 条 |

> 仪表盘优先桌面浏览，不必极端压缩，但保留基本响应式（不能把窄屏用户挡在门外）。

#### 8.5 卡片交互（点 [详情↗] 后展开）

点卡片右下的 \`[详情↗]\` 后，从 3×3 网格模式切到 **单府详情模式**：

\`\`\`
┌─────────────────────────────────────────────┐
│ ← 返回全府  识海承载-府 · shihai_fu     🔴  │ ← 仅当状态=红时显示 🔴
├─────────────────────────────────────────────┤
│ 完整计数表                                    │
│ ┌──────────────┬──────────────┐              │
│ │ 36 格位写入  │      25      │              │
│ │ 最近编码数   │       8      │              │
│ │ 最近归档数   │      12      │              │
│ │ 三档命中率   │    92.3%     │              │
│ │ 当前会话 id  │ 要求-38-0    │              │
│ └──────────────┴──────────────┘              │
├─────────────────────────────────────────────┤
│ 最近 20 条动作（按时间倒序，每条 1 行）        │
│ 13:42:15  铭记  写入格位"调用" 189 KB        │
│ 13:42:12  纳藏  追加事件流 队列 +3            │
│ 13:42:08  回想  投影拼装 命中 5 缓存           │
│ ...                                          │
├─────────────────────────────────────────────┤
│ 该府依赖边（→ 写到 谁的 lib 根）              │
│ · shihai_fu → tianting_fu   (回填/投影)      │
│ · shihai_fu → daoshu_fu     (回填/投影)      │
└─────────────────────────────────────────────┘
\`\`\`

- **右上"返回"** 按钮回 3×3，不写栈（状态简单）
- **全计数表** 用 HTML \`<table>\` 不靠 div 假装
- **动作流** 时间倒序、最新在最上，每行 24 px 高
- **依赖边** 只读静态展示，不引导跳转（监控是只读工具）

#### 8.6 与 DSH 界面的对照（解释性透明）

| DSH（本对话界面）特征 | 监控界面采纳 / 弃用 | 理由 |
|:--|:--|:--|
| 左侧导航栏 | ❌ **弃** | 9 卡片已自描述，不需要竖向导航；如要"管道线"视图用顶部段头 |
| 中间对话流 | ❌ **弃** | 监控不是 LLM 对话，没必要流式渲染（流式是 LLM 工具特性） |
| 右侧详情面板 | 🟡 **变形** | 卡片点开后整页变详情（不叠加浮窗，浮窗在移动端易被裁） |
| 暗色主题 | ✅ **采** | 监控常亮屏，暗色护眼 |
| 等宽字体用于标识符 | ✅ **采** | 中文加英文标识符同宽排版 |
| 工具调用块（代码块深色背景） | ❌ **弃** | 监控不展示源码；动作摘要够用 |
| 实时打字指示器 | ❌ **弃** | 监控拉刷新不流式，不需打字机效果 |
| 折叠 / 展开 | 🟡 **轻采** | 详情展开用页面级切换，不内嵌折叠树 |

> 仪表盘借鉴"暗色 + 卡片栅格 + 状态点"三大要素；舍去"对话 + 流式 + 折叠树"三大要素。**只取骨架，不抄皮肤**。

---

### 九、显示方式 · 流式直播 + 白箱回放

> **形态转向（2026-08-19）**：从"拉刷新仪表盘"改为"**流式直播 + 可任意时段回放**"。本节重写 §九。

#### 9.1 数据流（直播主链路）

```
浏览器 (app.js)
  EventSource("/api/stream")    ──▶  HTTP long-poll (text/event-stream)
  fetch("/api/snapshot")        ──▶  当前快照（首帧 + 重连保护）
  fetch("/api/replay?since=…")  ──▶  历史时段重放（NDJSON/SSE）
            │
            ▼
jiankong_fu (Rust lib crate · axum + tokio)
  路由分发   ─▶  /api/stream 维护 last_offset 指针 + 各府 lib 根事件订阅
  数据-抓取殿 ─▶  jiance_fu::读观测() · shihai_fu::读格位() · zhuangtai_fu::读状态() · rizhi_fu::读日志()
  视图-装配殿 ─▶  把原始事件打包成统一事件结构（见 §9.3）
  输出-呈现殿 ─▶  axum::response::sse::Sse 长连接，每条事件 data: {...}\n\n
            │
            ▼  lib 根符号读（不写、不深链园内）
shihai_fu lib 根  ──  识海承载-府格位 / 编码 / 归档 / 命中率
jiance_fu  lib 根  ──  观测探针-府观测记录 / span / 跨界异常
zhuangtai_fu lib 根 ──  状态相关：要求 / 验收 / 版本 / 网络状态
rizhi_fu   lib 根  ──  日志记录-府兜底构建 / 流式渲染队列
```

#### 9.2 五事件渲染规约

| 序 | 事件 | 触发 | 动作 |
|:--|:--|:--|:--|
| 1 | \`init\` | 浏览器加载 index.html | 先 \`GET /api/snapshot\` 拉首帧与 \`last_offset\`，再 \`new EventSource(\"/api/stream\")\" 订阅 |
| 2 | \`tick_event\` | SSE 推送 \`data: {事件}\` | DOM 直接 \`prependChild\` 到 \`#eventStream\`，不重渲染 |
| 3 | \`focus_card\` | 用户点 9 卡片缩略中某颗 | 改 hash \`#府=id\"，跳单府详情（复用 snapshot 内存） |
| 4 | \`switch_view\` | 顶部段头切换 6 个视图 | 渲染所选视图；切到 \`回放\` 自动调 \`/api/replay\" |
| 5 | \`replay\` | 用户调时间游标 / 点 [跳至首事件] | \`GET /api/replay?since=Ts&until=Ts&府=id\"，NDJSON 流式回灌 |
| 6 | \`pause\` | 用户点 [暂停] | \`es.close()\` 断订阅（不杀服务端连接）；再点 [恢复] 重连 |
| 7 | \`error\` | SSE / fetch 失败 | 红角标 + \"直播中断，上次同步于 [时刻]\"，不阻塞他视图 |
#### 9.3 事件结构契约 · 白箱六字段（**2026-08-19 二次推翻**）
每条进入直播的事件必须是以下结构（缺一即白箱泄漏 = 视为非法事件，服务端拒推）：

\`\`\`json
{
  "ts": 1787038400123,            // 时刻 ms
  "源": "鸿蒙/道术施展-府",        // 来源庭（含维度+域+府）
  "动作": "工具循环-7",            // 动作名（动词 + 步骤号）
  "影响": [                       // 影响清单（白箱核心）
    {"类型": "格位", "名": "调用", "字节": 189000},
    {"类型": "文件", "路径": "证道/.../单元解析.rs", "变化": "+421"}
  ],
  "token": {"提示词": 1234, "输出": 567, "缓存": 89, "总计": 1890},
  "耗时ms": 2103,                 // 该动作的执行时长
  "证据": "..."                    // 字符串：原始 token / 文件 hash / 命令行 等
}
\`\`\`

- **6 字段必填**：\`ts / 源 / 动作 / 影响 / token / 耗时ms\`；\`证据\` 可选但建议带
- **缺一即白箱泄漏**——服务端拒推此事件，客户端视为"直播黑洞"，立刻标注红角
- **两端契约**：jiankong_fu 的视图-装配-殿负责把各府 lib 根返回的类型化数据打包成这一结构；app.js 的渲染函数按这一结构渲染行

#### 9.3.1 卡片渲染契约（仍按纯函数）

\`\`\`
fn 渲染卡片(原始数据: 卡原始数据) -> 卡渲染结果 {
    匹配原始数据.状态 {
        活跃(_) => 加状态点(绿, "活跃"),
        进行(_) => 加状态点(黄, "进行中"),
        失败(_) => 加状态点(红, "失败"),
        _        => 加状态点(灰, "待机")
    }
    // 三段拼接: 心跳 / 计数 / 最近动作
}
\`\`\`

- 输入 = 9 卡片缩略用的 \\\`卡原始数据\\\`；输出 = \\\`卡渲染结果\\\`（JSON 字段顺序固定，断言友好）
- **不依赖**当前时间 / 随机数 / 全局状态（便于单测断言）
- 卡片不是主战场，**主战场是 §9.3 的事件结构**——保证白箱是这条，不是卡片

#### 9.4 状态色映射（前端 CSS 变量，主题可切）

\`\`\`css
:root {
  --ok: #4ade80;      /* 绿：无活跃 / 速率正常 */
  --warn: #facc15;    /* 黄：有任务进行 */
  --err: #f87171;     /* 红：失败 / 超时 */
  --idle: #94a3b8;    /* 灰：待机 */
}
[data-theme="light"] {
  --ok: #16a34a; --warn: #ca8a04; --err: #dc2626; --idle: #64748b;
}
\`\`\`

> 亮暗主题切换不影响 HTML 结构，只换 CSS 变量——\`app.js\` 读 cookie 决定初始值。

#### 9.5 字体与排版（中文为主）

| 元素 | 字体 |
|:--|:--|
| 中文 / 通用 | 系统默认（\`PingFang SC\` / \`Microsoft YaHei\` 备用） |
| 数字 / 时间戳 | \`JetBrains Mono\` 备用，系统等宽回退 |
| 英文标识符（lib 名 / 命令） | 等宽，颜色为 \`#38bdf8\`（亮）/ \`#7dd3fc\`（暗），与一般文本区分 |

- **数字字段全部 \`tabular-nums\`** —— \`3 \` 与 \`12\` 等宽对齐
- **路径名**用反引号包裹呈 \`monospace\`：\`鸿蒙/.../识海-铭记-殿\`

#### 9.6 静态资源清单（\`输出-呈现-殿/静态-资源-园/\`）

\`\`\`
monitor/
├── index.html       ← 单页 HTML，<head> 引 style.css，<body> 留 #app 挂载点
├── style.css        ← 暗色默认 + 主题变量 + 栅格 + 卡片样式
├── app.js           ← 视图组件 + fetch + 渲染 + 状态机
└── favicon.ico      ← 可选：项目 logo（界主决定）
\`\`\`

- HTML/CSS/JS 三件套最小，**不引第三方库**（无 jQuery、无 React）—— 避免拖入依赖
- 前端逻辑 < 200 行 JS，CSS < 150 行，HTML < 50 行 —— **小到能贴全文 review**

---

### 十、视图组件契约 · 顶部段头六视图

> §8.2 已列六视图，本节给六视图各自的契约。直播与回放为主，其他视图是补充。

#### 10.1 全府（默认）

- **数据**：9 张卡片 + 全局页脚
- **拉取**：\`GET /api/cards\`
- **响应 JSON 形**：
  \`\`\`json
  {
    "cards": [
      {"id":"shihai_fu","name":"识海承载-府","状态":"活跃","心跳秒数":3,"计数":{"格位":25,"编码":8,"归档":12},"最近动作":["铭记","纳藏"]},
      ...9 条
    ],
    "footer": {"基线":"v15","通过率":"5/10","想法":96,"要求":39,"验收":72,"网络":"OK"}
  }
  \`\`\`

#### 10.2 流水线（任务流转时间线）

- **数据**：\`要求.jsonl\` 按状态分组后渲染
- **拉取**：\`GET /api/pipeline\`
- **UI**：8 列（要求态机的 8 态），每列下列出当前在该态的 要求 id
- **不展示**：要求方向文本（一屏放不下，悬停 tooltip 提示）

#### 10.3 网络（府依赖图）

- **数据**：\`依赖图.json\` 的简化版
- **拉取**：\`GET /api/deps\`
- **UI**：SVG 力导向图，节点 = 府，边 = \`use {lib}::符号\` 引用
- **不展示**：循环依赖（第一次发现时只标红，不强制打回）

#### 10.4 日志

- **数据**：\`兜底构建.log\` tail + \`观测.jsonl\` tail
- **拉取**：\`GET /api/log?since=N\&府=X\`
- **UI**：两栏：左日志条目，右动作条目；可过滤 \`府\`

#### 10.5 设置

- **拉取**：\`GET /api/settings\`（返回当前默认值）
- **写**：\`POST /api/settings\`（仅三个字段：间隔秒 / 端口 / 主题）—— **这是监控界面-府唯一允许的写**（写自己的配置，不动世界状态）
- **注**：写入口经 \`权限-校验\`（命令操作府的令牌），与命令操作府共用鉴权

---

### 十一、可访问性与维护性约束

| 维度 | 要求 |
|:--|:--|
| 键盘可达 | \`Tab\` 顺序：顶部栏 → 段头 → 卡片栅格 → 页脚；回车触发 \`[详情↗]\` |
| 屏幕阅读 | \`role="region"\` + \`aria-label="府 #X 卡片"\` |
| 国际化 | 所有可视字符串放 \`i18n.zh.json\`，先仅做中文，结构预留 \`en\` 切换（**未来扩展点**，本期不做） |
| 关闭脚本 | \`<noscript>\` 时显示静态卡片视图（只显示第 1 帧的快照，标"实时性失效"） |
| 浏览器兼容 | 目标 Chrome 120+ / Edge 120+；**不**支持 IE / 老 Safari |
| 移动端可看 | < 768 视口 → 单列流式，但触控点 ≥ 44 px（苹果 HIG） |
### 十二、收束 · 一句话总结

```
监控界面 = 乾坤呈现-域 · 第十府 · Rust lib crate（jiankong_fu，入 Cargo workspace）
本质 = 世界执行期间的直播 + 白箱 + 让用户信任
主视图 = 当前任务线 + SSE 实时事件流（六字段契约）
辅助 = 时间游标回放 / 9 卡片缩略 / 8 态流水线 / 府依赖图 / 设置
技术 = axum + tokio + axum::response::sse + serde_json · include_str!/include_bytes! 内嵌静态资源 · SSE 非 WebSocket
边界 = 只读 · 经各府 lib 根符号读不写 · 旧的临时 Python 全弃
```
---

### 十三、任务为中心 · 会话为单元（**2026-08-19 第三次推翻**）

> **界主裁定**：第三章 直播不再按事件时间平铺，而按"**任务 → 会话 → 动作 → 详情**"四级树组织。
> 理由：信任的根基是"看见一个任务从入队到定档的完整轨迹"，而不是"看见一万条混在一起的事件流"。
> 这把"事件时间流"折叠成"任务会话的时间线"——按任务可分组、可逐动作下钻、可回放任何时段。

#### 13.1 形态从"流"到"树"

| 旧形态（§11 §8.1） | **新形态（本节）** |
|:--|:--|
| 顶部状态条 + 1 视图（事件流） | **顶部状态条 + 任务列表 + 当前任务会话树** |
| 默认视图：时间倒序的扁平事件流 | **默认视图：按要求 id 倒序的任务列表，点任务展开其会话** |
| 每事件 5 列（ts/源/动作/token/耗时） | **任务卡片 = 顶部摘要（id/方向/状态/阶段）；展开会话 = 按时序的动作行；点动作 = 全量详情** |

#### 13.2 任务列表（左侧导航栏或顶部段头）

按 `要求.jsonl` 倒序列出最近 N 个（默认 50）任务，每行 6 字段：

| 字段 | 来源 |
|:--|:--|
| id | `要求.id` |
| 状态 | `要求.状态`（待领 / 待实现 / 实现中 / 已通过 / 已打回） |
| 阶段 | `要求.阶段`（甲 / 乙） |
| 类别 | `要求.类别` |
| 最近动作 ts | 最近一次该任务的会话动作时间戳 |
| 方向 | `要求.方向.前 60 字` |

任务列表支持：
- **筛选** by 状态 / 阶段 / 类别（多选）
- **搜索** by id / 方向
- **排序** by id / 最近动作 ts

#### 13.3 会话树（点任务后展开）

会话是该任务从入队到定档的**完整动作链**，按时间正序：

```
[要求-38] 补齐解析调用单元测试                      (实现中)
├─ 08-18 08:11:39 [入队]   鸿钧 化要求            ts=...  [▶ 全量]
├─ 08-18 08:11:46 [设计]   女娲 模板生成          ts=...  [▶ 全量]
├─ 08-18 08:12:11 [实现]   道术 工具循环-7        ts=...  [▶ 全量]
├─ 08-18 08:14:55 [验收]   准圣-业务正确性        ts=...  [▶ 全量]
├─ 08-18 08:15:01 [验收]   准圣-数据完整性        ts=...  [▶ 全量]
└─ 08-18 08:16:30 [结果]   验收 结论=通过          ts=...  [▶ 全量]
```

**会话树动作节点 6 字段**（与 §9.3 兼容）：

| 字段 | 来源 |
|:--|:--|
| ts | 事件六字段之 ts |
| 类型 | 入队 / 设计 / 实现 / 验收 / 结果 / 定档 |
| 庭 | 事件六字段之 源 |
| 动作 | 事件六字段之 动作 |
| token | 事件六字段之 token.总计 |
| 全量 | 折叠的来源文件原始行 |

#### 13.4 动作全量（点 [▶ 全量] 后展开）

每个动作的全量 = 该动作**对应的来源 jsonl 行的完整 JSON 对象**（不裁剪）。

| 动作类型 | 全量来源 | 全量形 |
|:--|:--|:--|
| 入队 | 状态/要求.jsonl 中该 id 的完整行 | 完整要求（id/方向/验收标准/约束/状态/阶段...） |
| 设计 | 状态/设计.jsonl / 验收全文 | 模板生成的方案 |
| 实现 | 观测/记录.jsonl 中该任务时间窗内的全部行 + 产物文件路径 | 实现动作链 + 文件变化清单 |
| 验收 | 状态/验收.jsonl 中该 id 的整行 | 准圣意见们 + 评分 + 用量 + 终裁依据 |
| 结果 | 同验收 | 同上 |
| 定档 | 状态/版本.jsonl 中含该要求验收编号的版本 | 版本号 + 改了什么 + 阶段 |

#### 13.b 深直播：思考 + 工具调用（**2026-08-19 第三次推翻追加**）


> 界主追加质问：每个动作"全量"展开后还要继续下钻——**看见 LLM 的思考（含 system prompt / 思考 / 响应）与工具调用（每次读/写文件、跑命令的全量参数 + 返回）**。
> 数据已具备：
> - `.上下文/状态/任务线.jsonl` 是**桥**：每条任务线带 `要求id` 把任务↔执行全过程串起来
> - `.上下文/观测/记录.jsonl` 含**所有 LLM 调用**（载荷.接口=模型连接-府::调用模型），载荷.关联.要求 即任务 id
> - `.上下文/格位/调用.jsonl` 含**工具调用统计累计**
> - `.上下文/状态/对话.jsonl` 是**鸿钧对话循环**（界主↔鸿钧）
> 当前 v2 没接上这三源；本节入稿后立即补装。

##### 13.b.1 动作层级（四层）

    会话 → 高层动作 → LLM 调用 → 工具调用
       入队    鸿钧/女娲/道术/准圣 大模型对话   读/写/改/删/列/找/搜/跑

| 层级 | 形 | 来源 | 例 |
|:--|:--|:--|:--|
| L1 会话 | 任务根 | 要求.jsonl | 要求-38 |
| L2 高层动作 | 入队/设计/实现/验收/结果/定档 | 要求/验收/版本.jsonl + 任务线 | 鸿钧 化要求-38 |
| L3 LLM 调用 | 单次模型连接 | 观测/记录.jsonl 接口=模型连接-府::调用模型 关联.要求=要求id | prompt 含 system_prompt+history+user + response |
| L4 工具调用 | 单次工具执行 | 观测/记录.jsonl 载荷含命令/参数/返回 | 读 单元解析.rs +421 行 |

##### 13.b.2 后端装配规则（jiankong_fu 视图-装配-殿，伪代码示意）

    def assemble_event_from_llm(line):
        d = json.loads(line)
        link = d.get("关联", {})
        tid = link.get("要求")
        cont = d.get("载荷", {}).get("内容", "")
        msg = json.loads(cont) if cont else {}
        msgs = msg.get("messages", [])
        last_user = next((m for m in reversed(msgs) if m.get("role") == "user"), {}).get("content", "")
        return {
            ts: int(d.get("时间戳", 0)),
            源: "模型连接-府/LLM调用",
            动作: f"LLM · {len(msgs)} 消息 · {msg.get('model','?')}",
            影响: [{类型: llm调用, 模型, 消息数, 尾部前60: last_user[:60]}],
            _role_kind: "llm",
            _task_id: tid,
            _raw: line,
        }

    def assemble_event_from_tool(line):
        # 类似：从载荷.接口 抽出 道术施展-府::工具调用 + 关联.要求 join
        # 返回 _role_kind=tool

##### 13.b.3 前端 UI 树形（二级展开）

    [要求-38] 补齐解析调用单元测试               (实现中)
    ├─ 08-18 08:11:39 [入队] 鸿钧               [全量 ▾]
    ├─ 08-18 08:11:46 [设计] 女娲               [全量 ▾]
    ├─ 08-18 08:12:11 [实现] 道术               [全量 ▾]
    │    ┌── 实现 子树 ──────────────────────────────────────────┐
    │    │ 08-18 08:12:11 [LLM] 拆解子要求       [prompt/响应 ▾]│
    │    │ 08-18 08:12:13 [LLM] 模板设计        [prompt/响应 ▾]│
    │    │ 08-18 08:13:25 [Tool] 读 文件        工具+返回 ▾  │
    │    │ 08-18 08:13:30 [Tool] 写 文件        工具+返回 ▾  │
    │    │ ...                                                  │
    │    └────────────────────────────────────────────────────────┘
    ├─ 08-18 08:14:55 [验收] 准圣               [全量 ▾]

- 点 L2 动作 → 下钻子树显示 L3/L4
- 点 L3 [LLM] → 抽屉显示完整 prompt（含 system_prompt 与 历史）+ response
- 点 L4 [Tool] → 抽屉显示工具名 + 参数 + 返回值 + 耗时

##### 13.b.4 新增端点

GET /api/sessions/{id}/thoughts    该会话所有 LLM 调用（升序 ts），含 prompt 与 response
GET /api/sessions/{id}/tools       该会话所有工具调用（升序 ts），含参数与返回

##### 13.b.5 验收

业务正确性 — L2 动作点开能下钻 L3/L4；L3 抽屉能看见 LLM prompt（含 system_prompt 与多轮） + response
数据完整性 — 加入后不丢事件；关联.要求 的任务都能 join 进对应会话
性能并发 — /api/sessions/{id}/thoughts 返 50 条 LLM 调用 < 500ms
安全副作用 — 只读，不写
异常兼容 — 某任务无 LLM 调用时 thoughts 端点返空数组而非 500

---

#### 13.c 步骤流（**2026-08-19 第四次推翻**）

> 界主追加：前面 §13 任务树 + §13.b LLM/Tool 树形不够——我们要的是**叙事的步骤流**。
> 任务不是「事件堆」，而是**一段对话**：LLM 思考 → 选工具 → 工具执行 → 工具返回 → LLM 思考 → ...→ 验收。
> 监控界面要像**指挥中心屏幕**——一眼能看出「现在走到哪一步了」。

##### 13.c.1 步骤 = LLM 思考 + Tool 调用的会话

一个 LLM 至下一个 LLM 之间构成一个「步骤」（step）。每个步骤含：

| 步骤组件 | 描述 | 来源 |
|:--|:--|:--|
| 步骤号 | 1, 2, 3... | 装配计算 |
| 步骤标题 | LL**思考目的**（一句话） | 从 LLM 消息末 user 内容提 60 字 |
| 步骤开始 ts | 该步骤第一条事件 ts | 视图中第 1 条 LLM 的 ts |
| 步骤耗时 | 步骤结束 - 开始 | 步骤内最后 ts - 步骤内第一 ts |
| 步骤 token | 步骤内所有 LLM 调用 token 累加 | 步骤内 LLM 事件 sum |
| 内部组件 | LLM 思考 + 工具调用列表 | 步骤内 LLM/Tool 事件 |

##### 13.c.2 步骤流视图（前端）

    [要求-40] 事件总线-府说明文档
    ├─ 步骤 1 · 查 god 文件位置        (3 min · 12 tok · 5 工具)
    │   ├─ LLM 思考: 找 鸿蒙/基础设施/事件总线-府 路径
    │   ├─ 工具调用: 寻找文件 (根=".上下文/状态/项目档案", 模式="lib")
    │   ├─ 工具返回: 找到 9 个文件
    │   ├─ LLM 决定: 选文件 cargo.toml
    │   ├─ 工具调用: 读文件 (路径=...)
    │   └─ 工具返回: 27 行
    ├─ 步骤 2 · 读 Cargo.toml 解析依赖    (1 min · 8 tok · 2 工具)
    │   ├─ LLM 思考: 解析依赖...
    │   ├─ 工具调用: ...
    │   └─ 工具返回: ...
    ├─ 步骤 3 · 写 事件总线-府-说明.md      (5 min · 25 tok · 1 工具)
    │   ├─ LLM 思考: 文档结构...
    │   ├─ 工具调用: 写文件 (路径=..., 内容=...)
    │   └─ 工具返回: 写成功
    └─ 步骤 4 · 验证 + 提交验收           (1 min · 6 tok)
        ├─ LLM 思考: 检查 cargo build...
        └─ LLM 决定: 提交要求-40 验收

- 步骤 1 是当前正在跑的（高亮 + 流动指示）
- 步骤 2+ 是已完成的（✓ 轻息）
- 点任一步 → 展开子 LLM/Tool
- 不用「全量 JSON 塞眼睛」——每条文字简短

##### 13.c.3 后端装配（jiankong_fu 视图-装配-殿，伪代码示意）

在 §13.b 装配基础上加 `build_steps(actions)`：

    def build_steps(actions):
        steps = []
        cur = None
        for a in actions:  # 升序 ts 遍历
            t = a.get('层', 'l2')
            if t == 'llm':
                if cur:
                    steps.append(cur)
                cur = {'步骤号': len(steps)+1, '标题': a['全量']['载荷']['parsed']['messages'][-1]['content'][:60], '开始 ts': a['ts'], '组件': [a]}
            elif t in ('tool', 'l2'):
                if cur:
                    cur['组件'].append(a)
                    cur['结束 ts'] = a['ts']
        if cur: steps.append(cur)
        return steps

端点:GET `/api/sessions/{id}/steps` 返步骤流列表。

##### 13.c.4 验收

业务正确性 — 步骤流与时间线一致;每个步骤标题简短(≤60 字);步骤耗时与 token 累加正确
用户可读性 — 步骤流像「记者直播字幕」,每步可独立展开;不再「全量 JSON 塞眼睛」

jiankong_fu 视图-装配-殿 在 STATE_TX 装配器之上加一层**会话索引**（伪代码示意）：

```python
def build_sessions():
    by_id = {}
    for d in require_lines:
        sid = d['id']
        by_id[sid] = {'id': sid, '方向': d.get('方向',''), '状态': d.get('状态',''), '阶段': d.get('阶段',''), '类别': d.get('类别',''), '动作们': []}
    for ev in EVENT_BUS:
        m = re.match(r'要求 (要求-\d+)', ev.get('动作',''))
        if m and m.group(1) in by_id:
            by_id[m.group(1)]['动作们'].append(ev)
    return by_id
```

接口：GET `/api/tasks` 返回任务列表；GET `/api/sessions/{id}` 返回单个会话全量。

#### 13.d 分裂流（**2026-08-21 第五次演进**）

> 界主追加：平时单页流（像聊天界面），并行时动态分裂多列，每条任务线一列。
> 修正：分裂单位是**任务线**不是角色——角色是纵向流水线，任务线才是横向真并行。
> 与 §13.c 关系：**叠加不推翻**——步骤流是列内内容形态，分裂流是列间拓扑形态。
> 此节同时收敛 §13「默认视角是什么」的悬案：单页流为常态、分列为异常，默认视角=单列步骤流。

##### 13.d.1 分裂单位 = 任务线

| 候选单位 | 取/舍 | 理由 |
|:--|:--|:--|
| 角色 | 舍 | 鸿钧→女娲→大罗→准圣是纵向流水线，串行时单页流天然按序出现；按角色分屏鸿钧列永远主列，其他列断续亮灭=伪并行 |
| 任务线 | **取** | 任务线是横向真并行（多个大罗金仙同时执行）；分裂那一刻=世界真正并发那一刻；数据结构上任务线id是天然分流键 |

**不变量**：鸿钧在任何时刻只有一个（单一意志线）。按任务线分屏尊重此不变量——鸿钧的事件永远在主列，分裂出的列都是大罗/准圣的并行任务线。

##### 13.d.2 三态布局

```
串行段（单页流，占满）          并行段（自动分列）           汇流（合拢）

 步骤2 · 女娲设计        ┌─线A·大罗实现─┬─线B·大罗实现─┐  步骤5 · 准圣审验
 ▸ 思考→工具→返回       │ ▸ 读文件 ✓  │ ▸ 写文件 ✓  │  ▸ 六维意见·2矛盾
 步骤3 · 派遣 ──────────┤ ▸ 写文件 ✓  │ ▸ 思考中…   │  ▸ 鸿钧终裁
 步骤4 · 分裂 ●─────────┤ ▸ 跑命令…   │              │
                       └─────────────┴──────────────┘  合流点 ●
```

| 布局态 | 触发条件 | 视觉形态 |
|:--|:--|:--|
| 串行段 | 当前活跃任务线 ≤ 1 | 单列步骤流占满（§13.c 形态） |
| 并行段 | 活跃任务线 ≥ 2 | CSS Grid 自动分 N 列，每列头=任务线id+角色+八态着色 |
| 汇流 | 并行段结束（验收/终裁） | 各列合拢为「汇合面板」，展示 reducer 清洗后的矛盾清单 |

- 分裂点/合流点用「●」在主流时间轴上打标
- **布局本身是健康信号**：界主扫一眼分裂宽度就知道世界当前并发度

##### 13.d.3 不值得分屏的特例

| 并行场景 | 处理 | 理由 |
|:--|:--|:--|
| 六准圣审验（六维并行） | 汇合面板（不分六列） | 每份产出=意见+评分（短文本）；§14.20 reducer 已清洗成矛盾清单；展示「准圣3 与准圣5 对 XX 矛盾」比看六路原声有用 |
| 阴阳双脑（§14.18 旁路） | 主列+右侧窄栏常驻 | 阳干活阴打杂=主从关系非对等分屏；阴的产出（推演三指令、格位整理）用侧栏滚动 |

##### 13.d.4 多列时间对齐（最大技术难点）

回放时拖动游标，两条任务线事件密度不同（A 列 3 事件，B 列 40 事件），游标扫过去各列怎么同步？

**方案**：借鉴 Git graph——**每列独立稀疏刻度 + 一条全局游标线横穿**，不做逐帧对齐。
- 每列按自身事件密度渲染刻度（稀疏采样，≤20 刻度/列）
- 全局游标=一条横穿所有列的竖线，拖动时各列按自己事件最近匹配高亮
- 不追求逐帧对齐（各列事件 ts 不一定有对应），只保证游标 ts 语义一致

##### 13.d.5 降级形态（§13 定稿前过渡）

分屏渲染依赖多列时间对齐（§13.d.4），在 §13 定稿前先用降级形态：
- **并行块内联单列**：并行事件用并排小卡内联在单列步骤流里（不真正分屏）
- 视觉：`[线A ▸读文件✓ | 线B ▸写文件✓]` 内联卡片，不分裂列
- 数据通路不变（任务线id 分流键），仅前端渲染降级——定稿后切真分屏只需换渲染层

##### 13.d.6 数据贯通

**分流键**：任务线id（观测记录的关联字段已带任务线id）。

**后端装配**（jiankong_fu 视图-装配-殿，伪代码示意）：

    def build_topology(events):
        # 按任务线id分组，检测分裂/合流点
        segments = []  # 串行段 / 并行段 / 汇流段
        active_lines = set()  # 当前活跃任务线
        for ev in events:  # 升序 ts
            line_id = ev.get('任务线id', '主线')
            if line_id not in active_lines and len(active_lines) >= 1:
                segments.append({'类型': '分裂', 'ts': ev['ts'], '线': line_id})
            active_lines.add(line_id)
            # 任务线结束时移出 active_lines，active 归 1 时合流
        return segments

**API 契约**：

| 端点 | 用途 | 返回 |
|:--|:--|:--|
| GET `/api/topology` | 拓扑段列表（串行/并行/汇流） | `[{类型, ts, 线[], 步骤[]}]` |
| GET `/api/lines/{id}/steps` | 单任务线步骤流 | `[{步骤号, 标题, ts, 组件[]}]`（§13.c 形态） |
| GET `/api/replay?since=&until=` | 回放（扩展） | 带 `任务线id` 分流键的事件列表 |
| SSE `/api/events/stream` | 直播（扩展） | 事件载荷带 `任务线id` 字段 |

**前端数据流**：

    SSE事件 → 取 任务线id → 查活跃列 →
      无活跃列/单列 → 追加主列步骤流
      多列 → 追加对应列步骤流
      分裂事件 → 触发分列渲染
      合流事件 → 触发合拢→汇合面板

##### 13.d.7 前端状态机扩展（app.js）

    const state = {
        ...§13.7 既有字段,
        topology: [],           // 拓扑段列表
        activeLines: new Set(), // 活跃任务线id
        columns: {},            // {线id: {角色, 八态, 步骤[]}}
        replayCursor: null,     // 全局回放游标 ts
    };
    // SSE 事件带 任务线id → 按 §13.d.6 数据流分发

##### 13.d.8 验收

| 维度 | 验收要点 |
|:--|:--|
| 业务正确性 | 串行段单列/并行段分N列/汇流合拢；分裂单位=任务线非角色；鸿钧事件永远主列 |
| 数据完整性 | 事件任务线id 与列映射一致；分裂/合流点 ts 与拓扑段边界对齐 |
| 降级兼容 | 并行块内联单列形态可用；定稿后切真分屏仅换渲染层 |
| 时间对齐 | 回放游标横穿所有列；各列按自身密度稀疏刻度；不逐帧对齐 |
| 用户体验 | 一眼知并发度（布局=健康信号）；六准圣→汇合面板非六列；阴阳→主列+侧栏 |

---

*分裂流 · 任务线为分流键 · 单页流常态分列异常 · 融合蓝图 §13.d · 入稿 2026-08-21*

---

#### 13.e 渲染降噪与摘要提炼

> 起因：界主反馈"信息大部分是噪音，看不到内部世界在做什么"。
> 审视步骤卡片发现 80% 画面是噪音：tk 0（token 全 0）、0ms（耗时全 0）、空工具调用组件项、
> 原始证据含 `<think>` 标签与 JSON 片段、孤立步骤号无关联要求。
> 本节定下渲染层降噪规则与摘要提炼机制，让界主一眼看到"做了什么 → 结果是什么"。

##### 13.e.1 噪音判定

| 噪音类型 | 判定 | 处理 |
|:--|:--|:--|
| 零 token | `token.总计 === 0` | 不渲染 token 条与数值 |
| 零耗时 | `耗时ms === 0` | 不渲染耗时标签 |
| 空工具事件 | 证据 ≤ 2 字 且 token=0 且 耗时=0 | 装配时跳过（已实现） |
| 原始证据 | 含 `<think>`、`</think>`、`{`、`}` 等标记 | 摘要提炼后展示，原始证据收进详情区 |
| 无内容组件 | 组件项证据为空 且 token=0 且 耗时=0 | 不渲染该组件项 |

##### 13.e.2 摘要提炼函数

从原始证据提取一行可读摘要，规则按域分派：

| 域 | 提取规则 | 示例 |
|:--|:--|:--|
| 工具调用/读文件 | 提取文件路径 + 结果行数 | `读取 config.toml → 42行` |
| 工具调用/写文件 | 提取文件路径 + 写入状态 | `写入 入口.rs → 完成` |
| 工具调用/跑命令 | 提取命令名 + 退出码 | `cargo build → 退出0` |
| 提示词 | 提取前 40 字可读文本（去标记） | `"请分析以下代码…"` |
| 思考 | 提取 `<think>` 后首句 | `需要先读取配置文件` |
| 回复 | 提取前 60 字可读文本 | `配置文件内容如下…` |
| 返回 | 提取结果状态 + 关键数值 | `成功 · 42行` |
| 其他 | 去标记后前 50 字 | — |

去标记规则：删 `<think>`/`</think>`、截 JSON 大括号段、去首尾空白、合并连续空白。

##### 13.e.3 卡片头新格式

```
[图标] 动作摘要 → 结果摘要          [tk N] [耗时]  [▽]
```

- 动作摘要：`取步骤标题` 结果 + 摘要提炼（证据前 40 字可读化）
- 结果摘要：从证据尾部提取结果关键信息（行数/退出码/成功失败）
- token 条与数值：仅 `token.总计 > 0` 时渲染
- 耗时标签：仅 `耗时ms > 0` 时渲染
- 箭头：点击展开详情区

##### 13.e.4 详情区（默认收起）

展开后显示：
1. 原始证据全文（等宽字体，`pre-wrap`，最高 400px 滚动）
2. 组件项列表（仅渲染有证据或非零 token/耗时的组件）
   - 组件项格式：`[图标] 组件名 → 摘要    [tk N] [耗时]`
   - 组件项 token/耗时同样条件显示

##### 13.e.5 步骤关联要求

若事件携带 `任务线id`，卡片头左侧追加任务线标签（如 `线3`），颜色用域着色。
若步骤流中同一任务线连续多步，只在首步显示标签，后续步骤省略以减少重复。

##### 13.e.6 验收

| 维度 | 验收要点 |
|:--|:--|
| 降噪 | 零 token/零耗时不渲染；空组件不渲染；原始标记不出现在卡片头 |
| 可读 | 卡片头一行显示"动作 → 结果"；摘要无 `<think>`/JSON 片段 |
| 信息保留 | 原始证据在详情区完整可查；非零 token/耗时保留显示 |
| 关联 | 步骤关联到任务线；同线连续步骤不重复标签 |
| 交互 | 点击卡片头展开/收起；默认最近 5 步展开 |

---

*渲染降噪 · 摘要提炼 · 卡片头一行可读 · 融合蓝图 §13.e · 入稿 2026-08-22*

---

#### 13.f 轨迹表格白箱 · 对齐 dsh（**2026-08-23 第六次演进**）

> 起因：界主审视 `E:\trae\deepseek-harness`（下称 dsh）的监控界面后裁定——洪荒监控界面的白箱展示要对齐 dsh 的"AI 执行过程 DevTools"形态。
> dsh 给用户看的是**轨迹表格（事件账本）**：每一步推理、每一次工具调用、每一个 token 消耗、每一毫秒耗时都像 Chrome Network 面板一样逐行展示，点任意行展开看完整原文。
> 与 §13 系列关系：**叠加不推翻**。§13（含 §13.b/c/d/e）是**叙事视角**——按任务→会话→步骤树形组织，讲"一个任务从入队到定档的故事"；本节是**账本视角**——按轮次→事件行表格组织，记"世界运行的事件流水"。两视角在顶部段头并列可切，共享同一份 §9.3 白箱六字段事件流，不重复采集。
> 与 §9.3 关系：白箱六字段（`ts/源/动作/影响/token/耗时ms/证据`）是**后端契约**，轨迹表格是**前端展示**。本节所有事件行由白箱六字段派生，缺一即拒渲染（沿用 §9.3 不变量）。

##### 13.f.1 形态定位 · 两视角并列

| 视角 | 形态 | 组织轴 | 对应既有节 |
|:--|:--|:--|:--|
| 叙事视角（默认） | 任务→会话→步骤树 | 按 `要求id` 分组、按时间正序下钻 | §13 / §13.b / §13.c / §13.d / §13.e |
| **账本视角（本节新增）** | **轮次→事件行表格** | **按 Turn 分组、按事件序号逐行** | **本节** |

- 顶部段头（§8.2）在原六视图外**追加第七视图「轨迹」**，落点 `输出-呈现-殿/静态-资源-园/monitor/trajectory.html`（独立子页，独立 bundle，不污染 §9.6 主界面三件套的"不引第三方库"约束）
- 两视角共享 SSE 主链路（§9.1）与回放端点（§9.2 第 5 事件），切视角不重连
- 默认视角仍为叙事视角（§13.d 拍板：单页流常态）；账本视角是"想看原始事件账本时切过去"的 DevTools 入口

##### 13.f.2 轨迹表格（Trajectory Table）

表格是账本视角的主体，形态对齐 dsh 与 Chrome Network 面板：逐行事件、按轮次分组、可折叠、可虚拟化、流式追加、可向上加载历史。

###### 7 种事件类型

每行事件有一个类型标签，共 7 种，由 §9.3 白箱六字段的 `源`+`动作` 派生（派生规则见 §13.f.8）：

| 类型标签 | 含义 | 颜色（沿用 §9.4 CSS 变量域） |
|:--|:--|:--|
| `system` | 系统提示词 | 域色·基础设施 |
| `user` | 用户消息（界主输入） | 域色·呈现 |
| `context` | 上下文注入（格位回想/历史灌入） | 域色·基础设施 |
| `compacted` | 压缩记录（上下文压缩后的摘要标记） | `--warn` |
| `message` | 助手回复（LLM 输出） | 域色·道术 |
| `tool` | 工具调用 | 域色·道术 |
| `subtool` | 子工具（工具内嵌套调用的子工具，如"跑命令"内含"读环境变量"） | 域色·道术 浅化 |

###### 按轮次（Turn）分组

- 每个轮次（Turn）= 一次完整的 LLM 调用周期（含该周期的 system/user/context 注入 → message 回复 → tool/subtool 调用链）
- 轮次与 §13.c 的"步骤"同粒度：一个步骤即一个轮次（§13.c 步骤以 LLM 调用为界，本节轮次同此）
- 每组有 **sticky 头**：`Turn N · 角色名 · 起始 ts · 累计 token · 累计耗时`，滚动时组头悬停
- 每组**可折叠**：点 sticky 头收起/展开该轮次所有事件行

###### 行格式

每行展示（从左到右）：

```
#序号  [类型标签]  摘要文本  [input tok][output tok][think tok]  耗时
```

| 列 | 内容 | 来源 |
|:--|:--|:--|
| `#序号` | 全局事件序号（从 1 起，跨轮次连续） | 装配时按 ts 升序编号 |
| `[类型标签]` | 7 种之一，§13.f.8 派生 | `源`+`动作` 派生 |
| 摘要文本 | 一行可读摘要（≤80 字，去标记） | §13.e.2 摘要提炼函数 |
| `[input tok]` | 输入 token | `token.提示词` |
| `[output tok]` | 输出 token | `token.输出` |
| `[think tok]` | 思考 token（推理链消耗） | `token.推理`（§13.f.7 扩展字段） |
| 耗时 | `耗时ms` 格式化（如 `2.1s` / `412ms`） | `耗时ms` |

- 零 token / 零耗时沿用 §13.e.1 降噪规则：不渲染对应标签
- 摘要文本沿用 §13.e.2 提炼规则：去 ` Shields`/`<`标签`>`/JSON 片段，留可读首句

###### 虚拟化

- 行数 > 100 时启用虚拟化，仅渲染可视区 ± 缓冲行
- 方案二选一（实现时定）：
  - **A**：`@tanstack/react-virtual`（独立 bundle，仅轨迹子页加载，不入主界面三件套）
  - **B**：纯 JS `IntersectionObserver` + 行高固定 + 回收不可视行 DOM（零依赖，与 §9.6 精神一致）
- 不变量：虚拟化不丢事件、不乱序；滚动到任意行能正确还原序号与 sticky 组头

###### 流式更新

- SSE 推送新事件（§9.2 第 2 事件 `tick_event`）时，表格末尾**追加新行**，不重渲染整表
- 新行入场视觉：底部淡入 + 短暂高亮（≤300ms），不打断用户当前滚动/选中
- 若新事件属于新轮次，先插入新轮次的 sticky 组头再插入行

###### 历史加载

- 向上滚动到顶部时，`GET /api/trajectory?before={最早 ts}&limit=200` 加载更早记录
- 加载后**在顶部插入**，序号重编（因序号按 ts 升序连续）；插入时保留用户当前滚动位置（不跳屏）
- 加载到无更多记录时，顶部显示"已到最早事件"占位

##### 13.f.3 详情面板（Details Panel）

点任意行展开该事件的完整原文，形态对齐 dsh：右侧抽屉或行内展开区，字段按事件类型选择性展示。

| 字段 | 含义 | 适用类型 | 来源 |
|:--|:--|:--|:--|
| `inputDetail` | 完整请求/消息原文 | system/user/context/compacted/message/tool | 事件 `证据` 或载荷原文 |
| `promptDetail` | 完整系统提示词 + 工具目录 | message/tool | LLM 调用的 `messages[0]` + 工具 schema 列表 |
| `previousPromptDetail` | 被替换的旧提示状态（用于 diff 对比） | context/compacted | 压缩/注入前的旧 messages 快照 |
| `outputDetail` | 完整助手/工具结果原文 | message/tool/subtool | LLM response 或工具返回值 |
| `thinkingDetail` | 完整推理过程（思考链） | message | `token.推理` 对应的原始思考文本 |
| `sourceBlocks` | 原始消息块（按模型顺序） | message | LLM 调用的 `messages` 数组原文 |
| `outputBlocks` | 工具结果块 | tool/subtool | 工具调用的返回结构（含分块） |
| `schemaDetail` | 工具 schema 定义 | tool/subtool | 工具注册时的 JSON schema |
| `assistantMetrics` | TTFT、解码吞吐量、时间指标 | message | 模型连接-府观测（首 token 时延、解码 tok/s） |
| `result` | 工具结果摘要 | tool/subtool | §13.e.2 提炼的工具结果摘要 |
| `isError` | 是否失败 | 全类型 | 事件 `动作` 含失败标记 或 `影响` 含错误项 |
| `provider/model` | 模型来源 | message | 模型连接-府调用的 `provider`+`model` 字段 |
| `retry/maxRetries` | 重试信息 | message/tool | 模型连接-府/道术施展-府的重试计数 |

- 字段**按事件类型选择性展示**：如 `system` 行只展示 `inputDetail`；`message` 行展示 `inputDetail/promptDetail/outputDetail/thinkingDetail/sourceBlocks/assistantMetrics/provider/model/retry`；`tool` 行展示 `inputDetail/outputDetail/outputBlocks/schemaDetail/result/isError/耗时`
- `previousPromptDetail` 存在时，`promptDetail` 与之做 **diff 对比**展示（新增行绿、删除行红、不变行灰），让界主看清"上下文压缩/注入改了什么"
- 原文区等宽字体、`pre-wrap`、最高 60vh 滚动（沿用 §13.e.4 详情区约束）
- `assistantMetrics` 展示为指标小卡：`TTFT 1.2s · 解码 45 tok/s · 总 2.1s`

##### 13.f.4 时间线（Timeline）

表格上方有一条 Chrome-Network 风格的时间线概览，用于一眼看清事件密度与耗时分布。

###### 4 种时间模式

| 模式 | 横轴 | 用途 |
|:--|:--|:--|
| `sequence` | 事件序号 | 看事件数量分布（默认） |
| `duration` | 每事件耗时 | 看哪步慢 |
| `time` | 实际时间（ts） | 看真实发生时刻 |
| `actual` | 实际耗时（扣除等待/并行的净执行时间） | 看真实计算开销 |

- 模式切换在时间线右上角，4 个单选按钮
- 每事件在时间线上一个色块：宽度=该模式下的值映射、颜色=§13.f.2 类型色、高度=固定
- 悬停色块显示 tooltip：`TTFT · 解码耗时 · 持续时间 · 类型 · 序号`

###### 拖拽选时间范围筛选

- 在时间线上拖拽选一段范围，表格仅显示该 ts 区间内的事件行
- 拖拽时实时高亮所选范围两端游标；松手后表格过滤刷新
- 清除筛选：点时间线空白处或点"清除范围"按钮

###### 与 §13.d.4 多列时间对齐的关系

- 账本视角是**单列**事件账本（不分任务线分屏），时间线也是单条
- §13.d 分裂流的"多列时间对齐"是叙事视角的关切，不引入本节
- 回放时（§9.2 第 5 事件 `replay`）账本视角时间线游标与叙事视角游标共享同一 `since/until` 语义

##### 13.f.5 搜索

- 搜索框在表格上方，输入后**全文索引**匹配所有事件的 `证据`+`动作`+`影响` 文本
- 节流 3 秒：输入停止 3 秒后才触发查询（避免逐键打后端）
- 匹配高亮：命中字符在表格行摘要与详情面板原文中用 `--warn` 色背景标记
- 搜索结果仍按轮次分组展示，非命中行**淡化**（不删除，保留上下文），命中行正常亮度
- 端点：`GET /api/trajectory/search?q={关键词}&since=&until=`，返命中事件 id 列表 + 高亮区间

##### 13.f.6 折叠

| 折叠操作 | 效果 |
|:--|:--|
| 按轮次折叠/展开 | 收起/展开某 Turn 的所有事件行（点 sticky 头） |
| 按助手消息折叠/展开 | 收起/展开某 `message` 行之后到下一 `message` 行之前的所有 `tool/subtool` 行（把"一次回复的工具调用链"折叠成一行） |
| 全部折叠 | 所有轮次收起，只剩 sticky 头列表 |
| 全部展开 | 所有轮次展开到事件行 |

- 折叠状态记入 URL hash（如 `#trajectory?collapse=turn:3,5;msg:7`），刷新可还原

##### 13.f.2a trajectory 子页落地（2026-08-23 第九次演进·入稿）

> 起因：界主亲测监控界面 narrative 视图后裁定——trajectory 视图需以独立子页落地，对标 Chrome Network 面板形态。
> 落位：jiankong_fu 路由 /trajectory.html · 独立 bundle · include_str!/include_bytes! 内嵌。

形态定位：
- 主体：表格行（每事件一行），不是卡片
- 列布局：#序号 · 类型标签 · 时间 · 角色 · 摘要 · 输入 · 输出 · 缓存 · 写 · 思考 · 总计 · 耗时
- sticky 头：列名永远可见
- Turn 分组：每组一个 sticky 段头（Turn N · 任务线id · 起始ts · 累计token · 累计耗时），可折叠
- 思考原文：message 行右侧 ▶ 思考 (N字) 折叠按钮
- 工具结果：tool/subtool 行点击展开调用参数和返回
- 搜索/过滤：顶栏搜索框（按动作/角色/影响项过滤）

7 种事件类型派生（沿用 §13.f.8 算法）：
- system / user / context / compacted / message / tool / subtool

URL hash 状态：#trajectory?collapse=turn:N;msg:M · 刷新可还原折叠状态。

依据：融合蓝图 §13.f.2 + §13.f.6 + §13.f.7a + §13.f.8 + AGENTS 第 6/8 条。
- 折叠不丢数据，仅改渲染；展开立即还原

##### 13.f.7 token 累计展示

- 表格底部 sticky 汇总条：`input累计 / output累计 / cacheRead累计 / cacheWrite累计 / reasoning累计 / 总计`
- 逐行累加：每渲染一行，把该行 token 各分量加到汇总条
- `token` 字段在 §9.3 基础上扩展分量（向后兼容，原 `提示词/输出/缓存/总计` 保留）：

```json
"token": {
  "提示词": 1234,      // input（含 cacheRead 命中部分）
  "输出": 567,         // output
  "缓存读": 89,        // cacheRead
  "缓存写": 12,        // cacheWrite
  "推理": 234,         // reasoning（思考链消耗）
  "总计": 2136
}
```

- `cacheRead` = 命中缓存的输入 token（不计费或低费）；`cacheWrite` = 写入缓存的 token
- `reasoning` 对应 `thinkingDetail` 的 token 消耗，与 `输出` 分开计量（思考链不算输出）
- 汇总条数字 `tabular-nums`（§9.5），等宽对齐

##### 13.f.7a 思考链文本提取与入库契约（2026-08-23 第八次演进·入稿）

> 起因：界主要求看到 agent 的思考、结构化回复、工具调用查看了什么、明白了什么。当前 token 五分量只计量 reasoning_tokens（数量），但思考文本本身未被显式捕获与呈现。
> 落位：moxing_fu 模型调用 + jiance_fu 记回复 + jiankong_fu 白箱事件 + 监控界面-府 §13.f 轨迹表格呈现。

数据流四步：
1. moxing_fu 提取思考链：调用模型/调用模型带工具 在解析回复后立即调 提取思考链(content) -> Option<String>，从 content 中剥离 <think>...</think> 块（字符串字面量感知的平衡扫描）。
2. moxing_fu 入库：调用模型 把 (思考, 回复) 双参传给 jiance_fu::记回复(思考, 回复, ...)。
3. jiance_fu 标准化：记回复 把 思考 段落独立存放于载荷 附加.思考。
4. jiankong_fu 还原：三源归并 解析观测记录时，把 附加.思考 提取为白箱事件的新字段 思考链（Option<String>）。

契约表：
| 项 | 约定 |
|:--|:--|
| moxing_fu::提取思考链 | 输入 &str，输出 Option<String>；None=无思考块 |
| jiance_fu::记回复 载荷 | 思考非空时入 附加.思考；为空不入（节省字节） |
| jiankong_fu::白箱事件 思考链 | Option<String>，serde skip_serializing_if |
| 三源归并 还原 | 仅在回复思考/回复内容域提取，防误读 |

依据：融合蓝图 §13.f.7 + §13.f.8 派生规则 + AGENTS 第 5/6/8 条。

##### 13.f.8 与白箱六字段契约的映射 · 7 种事件类型派生

7 种事件类型由 §9.3 白箱六字段的 `源`+`动作` 派生，派生规则在 `jiankong_fu` 视图-装配-殿 实现（伪代码示意）：

```python
def 派生事件类型(事件):
    源 = 事件["源"]
    动作 = 事件["动作"]
    载荷 = 解析载荷(事件["证据"])

    if "提示词" in 源 or "提示词" in 动作:
        角色 = 载荷.get("角色")
        if 角色 == "系统":  return "system"
        if 角色 == "界主" or 角色 == "user":  return "user"
        if 载荷.get("注入类"):  return "context"
        return "user"  # 兜底

    if "压缩" in 动作 or 载荷.get("压缩标记"):  return "compacted"

    if "回复" in 动作 or "思考" in 动作 or "模型连接" in 源:
        return "message"

    if "工具调用" in 动作 or "道术施展" in 源:
        if 载荷.get("子工具标记"):  return "subtool"
        return "tool"

    return "message"  # 兜底，避免漏派生
```

| 事件类型 | 派生条件（白箱六字段视角） |
|:--|:--|
| `system` | `源` 含 `观测域::提示词` 且 载荷`角色=系统` |
| `user` | `源` 含 `观测域::提示词` 且 载荷`角色=界主` |
| `context` | `源` 含 `观测域::提示词` 且 载荷带`注入类`（格位回想/历史灌入） |
| `compacted` | `动作` 含`压缩`标记 或 载荷带`压缩标记` |
| `message` | `源` 含 `观测域::回复思考` 或 `模型连接-府` |
| `tool` | `源` 含 `观测域::工具调用` 或 `道术施展-府`，且无子工具标记 |
| `subtool` | `源` 含 `观测域::工具调用` 且 载荷带`子工具标记` |

- 派生必须**穷尽**：任意合法 §9.3 事件都能派生到唯一类型；派生失败 = 白箱泄漏，拒推（沿用 §9.3 不变量）
- 派生函数可单测：对每种类型造一个六字段样例，断言派生结果

##### 13.f.9 LOD 三级展开（保留 §13.e，对齐）

详情面板的展开深度沿用 §13.e 的 LOD 三级，不另造：

| 层 | 内容 | 默认态 |
|:--|:--|:--|
| L0 一行重点 | `前缀 + ts + 源 + 动作 + 耗时 + token.总计` | 始终可见（即表格行） |
| L1 重点字段 | `证据前 80 字 + 影响[] 全字段` | 默认折叠（点行展开） |
| L2 全量载荷 | 原始 JSON pretty（§13.f.3 详情面板字段全集） | 默认折叠（点"看全量"展开） |

- L0 = 表格行；L1 = 点行后的摘要详情；L2 = 详情面板完整原文
- 与 §13.e.3/e.4 的卡片头/详情区同源，仅展示容器从"步骤卡片"换成"表格行+抽屉"

##### 13.f.10 洪荒特色 · 多视图切换与星系视觉（**2026-08-23 第七次演进**）

> 起因：界主审视当前 Rust 正式版（jiankong_fu）界面后裁定——"当前与之前的设计差距好多，之前是星图、图谱等设计，6+版本，现在除了时序就没有其他"。
> v6+融合版（`临时文件夹/门面v6+`）已有三界面：私聊屏（一对一对话）、全员屏（星系放射图）、副屏（世界星图）。Rust 正式版只做了账本视角（轨迹表格），洪荒视觉语料未补。
> 本次入稿：在账本视角基础上**追加多视图切换**，白箱展示（时序）+ 洪荒视觉（星系/星图/私聊）两者兼得，符合融合蓝图"对齐 dsh 白箱 + 补齐洪荒特色"的双重目标。
> 风格基调：Linear Aesthetic（深色精密 · 微边框 · 呼吸感 · 辉光 · shimmer），与洪荒现有设计语言（深墨近黑 `--底 #0d0f0e`、洪荒青绿 `--活 #13d4a4`、中文 CSS 变量）同向，不另立色系。

###### 13.f.10.1 多视图切换机制

顶栏增加视图切换标签组（与既有工具按钮并列），四视图可切：

| 视图 | 标签 | 主体 | 数据源 |
|:--|:--|:--|:--|
| 时序 | `时序` | 轨迹表格（§13.f.2，**默认**） | `/api/events/recent` + SSE `/api/events/stream` |
| 星系 | `星系` | 星系放射图（§13.f.10.2） | `/api/topology`（角色在位）+ `/api/snapshot`（运转态） |
| 星图 | `星图` | 世界星图（§13.f.10.3） | `/api/snapshot`（想法/要求/事件/格位） |
| 私聊 | `私聊` | 一对一对话（§13.f.10.4） | `/api/tasks`（会话列表）+ 后端消息投递（待定） |

- 切视图**不重连 SSE**：四视图共享同一信源链路，切时仅切主体区，顶栏/状态行/深空背景不变
- 视图状态各自独立：时序的折叠/搜索/选中、星系的 tooltip、星图的副导航、私聊的会话选择互不干扰
- 当前视图记入 URL hash（如 `#view=星系`），刷新可还原
- 窄屏（< 900px）时视图标签可溢出滚动，不换行

###### 13.f.10.2 星系放射图（全员屏）

SVG 星系，鸿钧为金色恒星居中，其他角色为行星沿轨道运转，体现"道祖主政、万星拱卫"的洪荒角色体系。

| 层 | 内容 | SVG 元素 |
|:--|:--|:--|
| 轨道层 | 4 圈轨道圆（半径 130/200/270/340），主轨道高亮 | `<circle>` 虚线 |
| 中心层 | 鸿钧金色恒星：外晕(r80)+中晕(r55)+旋转光环(r48/r42 虚线)+核心(r32 径向渐变 `#fff7cf→#ffb703`) | `<circle>` + `<radialGradient>` |
| 行星层 | 每角色一行星：球体(r18 径向渐变)+光晕+名标签，按轨道分布 | `<circle>` + `<text>` |
| 连线层 | 鸿钧→各行星协作连线（渐变 `#ffd60a→#48cae4`），运转中实线、待命虚线 | `<line>` + `<linearGradient>` |
| 粒子层 | 每行星 3 粒子沿轨道流动，示"道气流转" | `<circle>` 小点 + rAF 动画 |

- 角色色取 §13.f.10.5 角色色彩变量（`--色-鸿钧` 金身、`--色-女娲` 粉霞、`--色-多宝` 多宝紫 等）
- 行星运转速度按轨道递减（`[0.00018, -0.00014, 0.00011, -0.00009]` rad/ms，正逆交替示"天道流转不拘一向"）
- 悬停行星显示 tooltip：`角色名 · 职能 · 状态`；点击行星右栏展开该角色详情
- 运转中行星：亮色径向渐变 + 光晕；待命行星：暗色 + 无光晕
- rAF 动画帧随视图切换启停（切离星系视图时 `cancelAnimationFrame`，不空耗）

###### 13.f.10.3 世界星图（副屏）

世界星图 = 指标卡行 + 星图舞台（SVG）+ 想法星座 + 要求推进 + 事件流星 + 格位星云，以"格位为星云、事件为流星、想法为星座"的意象组织世界全貌。

| 区 | 内容 | 数据 |
|:--|:--|:--|
| 指标卡行 | 4 卡：想法数 / 要求数 / 智能体数 / 最近事件数，每卡含主数+副标 | `/api/snapshot` 汇总 |
| 世界星图 SVG | 格位星云团（24 格位为发光团）+ 想法星座（连线成星座）+ 事件流星（动画划过） | 格位/想法/事件 |
| 想法星座 | 按状态分组（待化/已化为要求/已消散），每想法一星点，组内连线成星座 | `/api/snapshot` 想法列表 |
| 要求推进 | 按状态分组（待实现/实现中/已定档/已废弃），每要求一条，显示推进进度 | `/api/snapshot` 要求列表 |
| 事件流星 | 最近事件以流星意象列出（时间+摘要+源色），新事件顶部划入 | `/api/events/recent` |
| 格位星云 | 24 格位以星云团意象列出（格位名+负载+状态色），点击下探 | `/api/snapshot` 格位列表 |

- 世界星图 SVG 用 `viewBox="0 0 800 460"`，格位星云团位置按格位索引分散布局，团内核 + 散点 + 名签
- 事件流星动画：新事件从右上划入左下，带拖尾渐变，3s 内淡出
- 想法星座连线：同状态想法用细线连接成星座图形，星座名取该组状态名

###### 13.f.10.3b 函数级调用图谱·星空视图（2026-08-23 第十次演进·入稿）

> 起因：界主亲测需要把函数级调用图谱（依赖图）作为星空视图呈现——每个函数是一颗星，调用关系是星辰间的连线，体现"代码的星辰大海"。
> 落位：jiankong_fu 新增 /api/starmap 端点 + starmap.html 子页 + SVG 星空渲染。
> 数据源：shihai_fu::依赖图::加载自工作区()（.上下文/依赖图.json）。

形态定位：
- 主体：SVG 全屏画布（深色 #0d0f0e）
- 节点 = 函数/类型（圆点 + 文字标签）
- 边 = 调用关系（曲线）
- 节点位置 = 力导向布局（force-directed）
- 节点大小 = 调用入度（被调次数越多越大）
- 节点颜色 = 按 crate 分组
- 交互：hover 高亮节点 + 关联边，点击节点弹出代码摘要

API 契约：

| 端点 | 方法 | 返回 |
|:--|:--|:--|
| /api/starmap | GET | { 节点们: [...], 边们: [...] } |

节点字段（精简版）：

```
{
  "id": "模块::文件::符号",
  "名字": "提取思考链",
  "文件": "鸿蒙/基础设施 - 域/模型连接-府/...",
  "crate": "moxing_fu",
  "大小": 5,    // 入度
  "类型": "fn" // fn | type
}
```

边字段：

```
{ "源": "节点id", "目标": "节点id" }
```

前端渲染（starmap.js）：
- 力导向：Fruchterman-Reingold 算法（O(n²) 简化版），200 节点 < 100ms
- 节点：circle r="大小" + 颜色按 crate 分组 + 文字标签
- 边：path SVG 曲线 + 透明度按距离
- 性能：> 200 节点启用聚合（按 crate 折叠）；孤立节点（dead code）过滤

依据：融合蓝图 §13.f.10.3b + §11 + AGENTS 第 6/8 条。

###### 13.f.10.4 私聊屏（一对一对话）

与单个角色一对一对话界面，左栏会话列表 + 中栏对话流 + 右栏角色详情。

| 区 | 内容 |
|:--|:--|
| 左栏·会话列表 | 5 角色（鸿钧/女娲/多宝/红云/玄天）会话项，头像+名+职能+状态点 |
| 中栏·对话头 | 当前角色大头像 + 名 + 职能 + 运转态 |
| 中栏·对话流 | 消息气泡（界主右、角色左），含思考链折叠、工具调用展开 |
| 中栏·输入栏 | textarea + 发送钮，Enter 发送 / Shift+Enter 换行 |
| 右栏·角色详情 | 选中角色的职能、近期事件、在位状态 |

- 消息投递：`POST /api/chat/{角色}`（端点待定，本次先做前端壳，后端投递后续补）
- 对话流沿用 §13.e.4 详情区约束（等宽字体、pre-wrap、最高 60vh）
- 发送中浮层："鸿钧思考中…" + 光晕脉冲

###### 13.f.10.5 深空背景星点层 + Linear Aesthetic 风格

全局背景层，四视图共享，营造"洪荒未判、深空无垠"的氛围。

| 元素 | 实现 |
|:--|:--|
| 星点画布 | `<canvas>` 全屏 fixed，200 星点随机分布，rAF 缓慢闪烁（opacity 0.2↔0.8 呼吸） |
| 星云雾 | 2 层 `radial-gradient` 椭圆雾（青绿 + 暗紫），`position: fixed` 低 opacity |
| 微边框 | 所有卡片/面板边框用 `rgba(255,255,255,0.06-0.1)` 1px，近不可见但分层 |
| 辉光 | 活跃元素（选中行/运转行星/活角色）加 `box-shadow: 0 0 8px var(--活)` 软辉 |
| shimmer | 卡片 hover 时 45° 光线扫过（CSS linear-gradient + transform 动画） |
| 呼吸 | 状态点/光晕用 `animation` 2-3s 周期 opacity/scale 微变，示"世界活着" |

- 星点画布随视图切换不重创（全局单例），rAF 帧率限 30fps 避免高刷耗
- Linear Aesthetic 反模式禁用：不用纯黑 `#000`（用 `--底 #0d0f0e`）、不用纯白字（用 `--字 #e8ece9`）、不大幅 hover 位移（≤4px）、不彩色滥用（主色单色系，活色仅用于交互高亮）

###### 13.f.10.6 角色色彩体系（17 角色，已就位）

沿用 §13.f.2 既有 CSS 变量（`--色-鸿钧` 至 `--色-轩辕`），17 角色色取角色本性：

| 层 | 角色 | 色 |
|:--|:--|:--|
| 天层 | 鸿钧（道祖·主政） | `#e8b84a` 金身 |
| 天层 | 天道（巡世·世界之眼） | `#c8d4e0` 银白 |
| 中层·圣人 | 女娲/老子/元始/通天/后土 | 粉霞/青灰/天青/诛仙红/大地黄 |
| 底层·大罗金仙 | 多宝/白泽/龟灵/玄天 | 多宝紫/神兽白/玄龟青/玄天青 |
| 底层·六准圣 | 红云/镇元子/鲲鹏/神农/冥河/轩辕 | 红云赤/地仙绿/北冥蓝/百草翠/血河暗红/黄帝金 |

- 星系放射图行星色、私聊屏头像色、诸圣列表色点均取此体系，不另造色

###### 13.f.10.7 验收补充

| 维度 | 验收要点 |
|:--|:--|
| 视图切换 | 四标签可切；切不重连 SSE；视图状态各自独立；URL hash 可还原 |
| 星系放射图 | 鸿钧居中金色；行星按轨道运转；悬停 tooltip；点击右栏下探；切离停 rAF |
| 世界星图 | 指标卡数准；格位星云/想法星座/事件流星/要求推进渲染；流星动画 |
| 私聊屏 | 会话列表 5 角色；对话流渲染；输入栏可输入（发送后端待定先做壳） |
| 深空背景 | 星点闪烁不卡；星云雾低 opacity 不抢主；四视图共享不重创 |
| Linear Aesthetic | 微边框近不可见；辉光适度；shimmer 流畅；呼吸不突兀；反模式禁用 |

##### 13.f.11 API 契约（新增端点）

| 端点 | 用途 | 返回 |
|:--|:--|:--|
| `GET /api/trajectory?since=&until=&before=&limit=&turn=` | 轨迹事件列表（支持时间窗/向上翻页/按轮次过滤） | `[{序号, 轮次, 类型, 摘要, token, 耗时ms, 事件id}]` |
| `GET /api/trajectory/event/{id}` | 单事件详情面板全量（§13.f.3 字段全集） | `{inputDetail, promptDetail, ...}` 按类型选择性返回 |
| `GET /api/trajectory/search?q=&since=&until=` | 全文搜索（§13.f.5） | `[{事件id, 高亮区间[]}]` |
| `GET /api/trajectory/timeline?mode=sequence|duration|time|actual&since=&until=` | 时间线色块数据（§13.f.4） | `[{序号, ts, 值, 类型}]` |
| `SSE /api/trajectory/stream` | 轨迹流式追加（复用 §9.1 主链路，payload 加 `序号/轮次/类型`） | 同 §9.2 第 2 事件扩展 |

- 与 §13.8 既有端点**并存**：叙事视角用 §13.8 端点，账本视角用本节端点，共享 §9.3 事件源
- `before` 参数用于 §13.f.2 历史加载（向上翻页）：返 `ts < before` 的最近 `limit` 条
- 所有端点**只读**（沿用 §11 §一 写约束）

##### 13.f.12 前端状态机扩展（trajectory.js）

```javascript
const state = {
    events: [],           // 事件行列表（升序 ts）
    turns: new Map(),     // 轮次 → 事件序号区间
    expandedRow: null,    // 当前展开详情面板的事件id
    collapse: { turn: new Set(), msg: new Set() },  // 折叠状态
    timelineMode: 'sequence',
    searchQuery: '',
    searchHits: new Set(),
    timeRange: null,      // {since, until} 时间线拖拽筛选
    tokenSum: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, reasoning: 0 },
};
// SSE 事件 → 派生类型(§13.f.8) → 追加行 + 更新 tokenSum
// 点行 → 展开 L1/L2 → 拉 /api/trajectory/event/{id}
// 向上滚到顶 → 拉 /api/trajectory?before=... → 顶部插入 + 序号重编
```

##### 13.f.13 验收

| 维度 | 验收要点 |
|:--|:--|
| 业务正确性 | 7 种事件类型派生穷尽无遗漏；按轮次分组 sticky 头正确；详情面板按类型选择性展示字段 |
| 数据完整性 | 虚拟化不丢事件不乱序；历史加载后序号连续；SSE 追加与历史加载不冲突 |
| 契约对齐 | 所有事件行由 §9.3 白箱六字段派生；缺字段事件拒渲染；派生函数单测通过 |
| 性能并发 | 1000 行虚拟化滚动 60fps；`/api/trajectory` 200 条 < 300ms；搜索节流 3s |
| 时间线 | 4 种模式切换正确；拖拽筛选与表格联动；悬停 tooltip 显示 TTFT/解码/持续 |
| 搜索高亮 | 命中字符 `--warn` 背景；非命中行淡化不删除；清空搜索恢复全行 |
| 折叠 | 按轮次/按消息/全部折叠展开正确；折叠状态入 URL hash 可还原 |
| token 累计 | 逐行累加正确；汇总条 5 分量 + 总计；`reasoning` 与 `输出` 分开计量 |
| LOD 三级 | L0 始终可见/L1 默认折叠/L2 默认折叠；与 §13.e 同源不另造 |
| 两视角并列 | 叙事/账本可切；切视角不重连 SSE；共享 §9.3 事件源 |
| 安全副作用 | 所有新端点只读；不写任何文件 |
| 异常兼容 | 空轨迹返空表非 500；派生失败事件标红角不阻塞其他行 |

---

*轨迹表格白箱 · 对齐 dsh · 账本视角与叙事视角并列 · 融合蓝图 §13.f · 入稿 2026-08-23*

---

#### 13.6 SSE 推送变化

新事件来时服务端立即算它属于哪个会话，只推三种类型：

```
{type: 'event',           任务id, ev}
{type: 'task-new',        任务id, 摘要}
{type: 'task-status-change', 任务id, 新状态}
```

前端按 type 分发。

#### 13.7 前端状态机（app.js 改写）

```javascript
const state = {
    tasks: [],
    selectedTaskId: null,
    sessions: {},
    expanded: {},
    detailOpen: null,
};
// 左 30% 任务列表 + 右 70% 会话树 + 全量抽屉
// 默认 selectedTaskId = 最新"实现中"任务，无则最新待领
// SSE 推 type=event 且 ev 属于当前任务 → 自动追加到树末尾
```

#### 13.8 与既有形态的兼容

**保留**：所有 §11 原 7 端点 + 事件六字段契约。**新增**：3 个端点。

| 端点 | 状态 |
|:--|:--|
| GET /, /static/* | 保留 |
| GET /api/snapshot | 保留 |
| GET /api/stream | 扩展 payload |
| GET /api/replay | 保留 |
| GET /api/rooms, /api/settings, /api/health | 保留 |
| **新增** GET /api/tasks | 任务列表 |
| **新增** GET /api/sessions/{id} | 单个会话全量 |
| **新增** GET /api/search?q= | 跨任务搜索（可选 v2） |

#### 13.9 验收（六维准圣）

| 维度 | 验收要点 |
|:--|:--|
| 业务正确性 | 任务列表 50 条 / 点任务展开完整会话 / 动作按时序 / 全量展开后能看原始 jsonl |
| 数据完整性 | SSE type=event 推送后前端对应任务的会话树自动追加 |
| 性能并发 | GET /api/tasks < 100ms；GET /api/sessions/{id} < 500ms |
| 安全副作用 | 不写任何文件；只读 |
| 异常兼容 | 任务 id 不存在返 404；要求.jsonl 缺字段时树仍渲染，缺字段标 ? |
| 用户体验 | 一屏可见完整任务列表 + 当前任务会话；点动作 [▶ 全量] 弹出抽屉（不跳页） |

---

*任务为中心 · 会话为单元 · 融合蓝图 §13 · 入稿 2026-08-19*

---

## 十四、dsh 手法对齐纲领 · 主轴 Rust · 严格入稿

> 定位：以 `E:\trae\deepseek-harness` 的运行时质量为对照，把洪荒 v14 的工程纪律对齐上去。
> 状态：入稿 · 界主 2026-08-19 拍板。
> 依据：`临时文件夹/dsh-对齐评估-草稿.md`（预备稿全文，路线图详细表见此稿）。
> 技术栈：**主轴 Rust**，实现细节可换（trace/lint/CI 工具等），仓体与世界观不动。

### 14.1 拍板（不可逆决策）

| # | 决策 | 内容 |
|---|---|---|
| 1 | 起点 | **v14 继续渐进改造**（D1-A）—— 保留真实自进化历史，每阶段一版存档 |
| 2 | 优先 | **运行时质量优先**（D2-A）—— 路线 0→1→2→3→4→1→6→5 |
| 3 | 纪律 | **严格入稿**（D3-A）—— AGENTS.md 第 8 条贯彻，每改一处先写设计稿 |
| 4 | 技术栈 | **主轴 Rust**，实现细节可换（trace/lint/CI 工具等），仓体与世界观不动 |

### 14.2 不变量（任何阶段不得破坏）

1. 鸿钧单一意志线（决策与终裁唯一）
2. 八态状态机（要求书状态迁移合法）
3. 识海记忆契约：36 格位 / 依赖图 / 回滚垫 / 三档投影
4. 日志统一经 `日志记录-府`（rizhi_fu） lib 根
5. 跨府只走 lib 根符号，跨府止步
6. 先入稿、再落码；结构级进化须界主确认（⑤级）
7. 故事化命名与中文标识符
8. 六层（维度/域/府/殿/阁/园）切分纪律

### 14.3 六阶段路线图（精炼）

| 阶段 | 主题 | 工期估 | 关键产出 | 对应现有设计稿 |
|---|---|---|---|---|
| 0 | 基线冻结 | 1 周 | v15 基线存档 + clippy 零警告 + 字节快照 | §6 落地顺序 + §5 版本存档 |
| 1 | 质量基础设施 | 1 周 | lefthook + check.sh + 4 个 contract test | 新增子章 |
| 2 | 类型化事件总线 | 3 周 | 新增 `事件总线-府` + 工具循环四段 waterfall | §18.2 P1 续 |
| 3 | 角色插件化 | 2 周 | 角色册注册器 + effect disposer | §18.1 P3 |
| 4 | Profile 装配 | 1 周 | profile + patch + 装配-检查 | §18.4 阶段门 |
| 5 | API Gateway 双端 | 2 周起 | `API 网关-府` + CLI 拆 thin | §18 不变量不动 |
| 6 | 文档与生态 | 2 周起 | 子系统文档 + 模块图 + cookbook | 传承殿现状 |

### 14.4 阶段门（机械验证 · 版本存档定档）

| 门 | 从 → 到 | 机械验证 |
|---|---|---|
| G1 | 阶段 0 → 1 | v15 基线存档 + clippy 零警告 |
| G2 | 阶段 1 → 2 | `./scripts/check.sh` 一键全绿 + 4 contract test 固化 |
| G3 | 阶段 2 → 3 | 工具流水线时序图与 dsh `tool-execution-pipeline.zh.md` 同构 |
| G4 | 阶段 3 → 4 | 动态注册/卸载角色不影响在途任务 |
| G5 | 阶段 4 → 6 | 可只启甲阶段最小集 + patch 替换生效 |
| G6 | 阶段 6 → 5（可选） | 文档体系独立可发布后开产品化 |

### 14.5 风险与边界

| 风险 | 缓解 |
|---|---|
| 阶段 2 改动面广 | 阶段 1 先把 contract test 固化，破一处立即定位 |
| 角色插件化与既有冲突 | 先做语义等价层，渐进替换 |
| v14 老债拖累 | 阶段 0 先清理（clippy 零警告），不留债 |
| 测试覆盖 | 关键路径 100%，其他 60%+ |

### 14.6 详细预备稿

| 项 | 路径 |
|---|---|
| 全量路线表（每阶段每项动作落位+验证） | `临时文件夹/dsh-对齐评估-草稿.md` |
| 差距定位矩阵 | 同上 §一 |
| 风险评估 | 同上 §五 |

### 14.7 与现有设计稿的关联

- §6「落地顺序」 → 阶段 0-1 路线与之衔接
- §7「进化环 vs 固化度」 → 阶段 3 角色插件化须遵守经权边界
- §18「实现方式对齐」 → 阶段 2-6 是 §18.1-18.5 内容的具体化
- 融合蓝图 §5.1「鸿钧主控化」 → 不动，作为已落地基线
- §13「任务为中心 · 会话为单元」 → 阶段 6 文档化后与监控界面联动

### 14.8 阶段 1 细化 · 质量基础设施（2026-08-19 入稿）

> 依据：界主拍板「开始阶段 1」+ AGENTS.md 第 10 条（根目录禁 scripts/，脚本按六层落位）。

#### 14.8.1 落位决策

| 产出 | 落位 | 说明 |
|---|---|---|
| 检查总线（检查-全部.ps1） | `道果树/质量门禁-域/门禁-府/` | 不入 Cargo workspace（PowerShell 辅佐层；与监控界面-府不同——监控界面-府已于 2026-08-21 改入 workspace，见 §11） |
| 重复检测（查重复.ps1） | 同上 | 同上 |
| 依赖图生成（生成依赖图.ps1） | 同上 | 同上 |
| 公共函数（公共-函数.ps1） | 同上 | 同上 |
| pre-commit 钩子 | `.git/hooks/pre-commit` | git 强制名（工具豁免）；**不引 lefthook**（外部依赖与主轴 Rust 相悖） |

> 全部文件名中文（git 强制的 pre-commit 钩子名豁免，同 Cargo.toml 先例）。

#### 14.8.2 检查总线四件套

| # | 件 | 命令 | 判定 |
|---|---|---|---|
| 1 | 格式 | `cargo fmt --check` | 零差异 |
| 2 | 静态 | `cargo clippy --workspace --all-targets -- -D warnings` | 零警告 |
| 3 | 测试 | `cargo test --workspace --lib` | 零失败 |
| 4 | 重复 | `查重复.ps1` | 重复率 ≤ 阈值 |

**重复阈值策略（先基线后收紧）**：阶段 1 默认 15%（当前基线 10.6%），每阶段按重复报告消减 ≥2 个百分点，阶段 6 前收至 5%。

**当前重复基线（2026-08-19 实测）**：387 .rs 文件 / 20565 代码行 / 重复行 2180 / **10.6%**。已知重复热点（作 backlog）：
- 目录递归扫描逻辑（巡世扫描.rs × 增量检测.rs）
- 日期计算（流式历法.rs × 流式时刻.rs）
- 工具调用名统计（工具循环测试.rs × 工具循环.rs）
- 文件操作样板（多个 手脚-施展-殿 文件）

#### 14.8.3 依赖图生成

`生成依赖图.ps1` 解析全部 .rs 的 `lib::` 路径限定引用（含 use 导入与全路径调用，过滤 std/第三方），聚合府间依赖边，输出 mermaid。**实测 28 条边**，与设计稿 §8 依赖关系一致：
`命令操作→天庭治理→道术施展→模型连接→配置管理` + `识海/日志/观测探针` 横切 + `单元测试→六府`。

#### 14.8.4 contract test 固化（4 项防回归）

| # | 契约 | 现状 | 缺口处理 |
|---|---|---|---|
| 1 | 六维准圣审验 + 鸿钧终裁 | 已有（主政落笔测试/终裁测试） | 保持 |
| 2 | 事件流 append-only 追加/重放 | 已有 5 个（追加落流.rs 内联） | 保持 |
| 3 | 回滚垫写前备份/撤销 | 已有（回滚垫测试 + 文件工具测试） | 保持 |
| 4 | **打回自动重投** | **缺** | **新增**：`世界运行.rs` 文件尾内联 `#[cfg(test)] mod 重投测试`（补测试优先内联判据），测：`汇总打回意见` 内容契约（终裁依据+验收意见+准圣改进建议）、`最大重试次数=2` 常量、重试 id 后缀格式 |

> 补测试落位判据（设计稿 §4.2 规则 6）：涉及路径内已有被测文件 → 测试内联文件尾，不新建独立测试文件。

#### 14.8.5 阶段 1 门（G2 验收）

- `pwsh 检查-全部.ps1` 四件套一键全绿
- 4 个 contract test 固化（含新增重投测试）
- 依赖图生成脚本产出与 §8 一致
- 脚本定档入仓 `道果树/质量门禁-域/门禁-府/`

### 14.9 阶段 2 细化 · 类型化事件总线（2026-08-19 入稿）

> 对齐 dsh 事件域核心（`docs/architecture.zh.md` §事件 / `tool-execution-pipeline.zh.md`）。
> 目标：把工具循环内嵌护栏、验收命令式循环、重投 if/continue 改为**可横切挂入的扩展点**，
> 与 dsh `pre-execute → guards → execute → post-execute → result` 时序图同构。
> 约束：事件总线 = 进程内实时分发（扩展点）；事件流（追加落流.rs）= 持久事实源（jsonl）。两者分工不混淆。

#### 14.9.1 新府落位

| 项 | 内容 |
|---|---|
| 府 | `鸿蒙/基础设施-域/事件总线-府`（lib = `shijian_fu`） |
| 殿 | `类型-定义-殿`（分发模式/守卫裁决/事件枚举）+ `分发-执行-殿`（注册表/四模式分发/流水线） |
| 依赖 | shijian_fu ← daoshu_fu / tianting_fu / mingling_fu（消费方），无反向依赖 |

#### 14.9.2 两种设施

**A. 命名事件总线（emit / 通知域）**——可观测扩展点，载荷类型擦除（Any）：

```rust
pub struct 事件总线 { /* RwLock<HashMap<String, Vec<注册项>>> */ }
impl 事件总线 {
    pub fn 注册(&self, 事件: &str, 模式: 分发模式, 监听: 回调) -> 注销句柄;
    pub fn 通知(&self, 事件: &str, 载荷: &mut dyn std::any::Any) -> Result<(), String>;
    pub fn 串行(&self, 事件: &str, 载荷: &mut dyn std::any::Any) -> Result<(), String>;
}
// 回调签名：Fn(&mut dyn Any) -> Result<(), String> + Send + Sync；注册返回句柄，drop 自动注销（对齐 Cordis disposer）
// 分发模式：通知（emit，观察不中止）/ 串行（serial，按序 Err 中止）
// 并行（parallel）：Rust 用 std::thread::scope 直接表达（&mut 载荷不可跨线程共享），事件总线不造并行抽象
```

**B. 类型化流水线（waterfall / 扩展域）**——工具调用等强类型场景，无 Any 开销：

```rust
pub struct 流水线<T> { /* Vec<Box<dyn Fn(&mut T) -> Result<(), String> + Send + Sync>> */ }
impl<T> 流水线<T> {
    pub fn 注册(&mut self, 监听: impl Fn(&mut T) -> Result<(), String> + Send + Sync + 'static) -> 注销句柄;
    pub fn 执行(&self, 载荷: &mut T) -> Result<(), String>;  // 短路：任一 Err 即中止（waterfall 语义）
}
```

#### 14.9.3 工具流水线四段（对齐 dsh tool-execution-pipeline）

```
工具调用 → 预执行(pre-execute) → 守卫们(guards) → 执行(execute) → 后执行(post-execute) → 落定(result)
```

| 段 | dsh 对位 | 洪荒实现 | 职责 |
|---|---|---|---|
| 预执行 | `tools/pre-execute` waterfall | `流水线<工具请求>` | 审批/令牌/沙箱准备；任一拒绝 → 工具体跳过 |
| 守卫们 | monotonic guards | `Vec<Box<dyn 守卫>>` | deny-or-abstain，identity 保护；拒绝即止 |
| 执行 | `tools/execute` | `执行器`（现 `执行工具` 映射） | 真实工具函数体 |
| 后执行 | `tools/post-execute` waterfall | `流水线<工具结果>` | 结果改写/补上下文/观测留痕 |
| 落定 | `tools/result` | 冻结 `工具结果` | 单一模型可见结果 |

```rust
pub struct 工具请求 { 调用名: String, 参数: serde_json::Value, 工作区根: PathBuf, 涉及路径: Vec<String> }
pub struct 工具结果 { 文本: String, 写入文件们: Vec<(String, u64)> }
pub enum 裁决 { 放行, 拒绝(String), 弃权 }
pub trait 守卫 { fn 裁决(&self, 请求: &工具请求) -> 裁决; }

pub struct 工具流水线 {
    预执行: 流水线<工具请求>,
    守卫们: Vec<Box<dyn 守卫 + Send + Sync>>,
    执行: Box<dyn Fn(&工具请求) -> Result<工具结果, String> + Send + Sync>,
    后执行: 流水线<工具结果>,
}
```

**改造迁移**（现状 → 事件化，2026-08-21 确认 4/5 已落码）：
- 工具护栏（工具执行.rs `工具护栏`）→ 预执行 流水线注册项（写/改/删落盘校验 + 运行命令护栏）✅ 已落码（`工具护栏监听` + `预执行注册`）
- 路径越界/根内越界（`校验路径范围`/`校验落盘`）→ 守卫们（单调 deny，identity=路径）✅ 已落码（`涉及路径守卫` + `加守卫`）
- 沙箱视图（命令沙箱）→ 执行器内保留（execute 段职责）✅ 已落码（`执行器` 映射）
- 事件流留痕（工具循环.rs post 留痕）→ **保留在主循环**（2026-08-21 决策：后执行监听签名 `Fn(&mut 工具结果)` 拿不到 `工具请求` 上下文，且流水线是 OnceLock 静态单例而事件流是每次调用传入的 `Option<&事件流>`，后执行监听无法访问事件流引用；事件流留痕依赖任务id/轮次/事件流引用等调用上下文，不适宜移到静态流水线的后执行段。后执行流水线留给后续阶段做结果改写/spill 落盘等不依赖调用上下文的后处理）
- 工具循环.rs 主循环调用点：`执行工具(&调用, ...)` → `流水线.执行(&工具请求)` ✅ 已落码（工具执行.rs `执行工具` 内部走 `流水线().执行(&请求)`）

#### 14.9.4 事件化接入点（阶段 2 后半）

| # | 现状 | 事件化 | 分发模式 |
|---|---|---|---|
| 2.4a | 命令级令牌校验（原子校验.rs） | 保持（CLI 边界本就命令级）；工具级审批走预执行 | — |
| 2.4b | 验收裁决（终裁.rs 三段：机械门槛→六准圣→终裁） | `验收/裁决` 串行事件：机械门槛 → 六准圣并行（LLM 六次独立，parallel）→ 鸿钧终裁 | 串行 + 并行 |
| 2.4c | 打回自动重投（世界运行.rs if/continue） | `重投/循环` 串行事件：打回 → 撤销产物 → 注入意见 → 重投 | 串行 |

> 2.4b/2.4c 为阶段 2 后半（先 2.2 事件总线核心 + 2.3 工具流水线落地验证，再接入）。

**落码决策（2026-08-21）：** 2.4b/2.4c 已加事件总线通知（`通知`/emit，可观测扩展点），**不做串行事件改造**。

- **原因：** `事件总线::串行()` 的 Err 中止语义不适合终裁三段。机械门槛打回是"正常打回"（返回打回回执），不是"错误"；若改用串行事件，打回会被当作 Err 中止整条流水线，与终裁三段"打回即正常返回"的语义冲突。
- **保留方案：** 命令式控制流 + 通知（emit）。控制流照旧 `if/continue`，通知仅提供可观测扩展点，不改变控制语义。后续若需串行事件，须先为事件总线引入"打回非错误"的分发模式（如 `串行软中止`），再迁。
- **落码位置：** `终裁.rs` 行 1167-1178（`验收/裁决` 通知）、`世界运行.rs` 行 819-829（`重投/循环` 通知）。

#### 14.9.5 阶段 2 门（G3 验收）

- 工具流水线时序图与 dsh `tool-execution-pipeline.zh.md` 同构（预执行→守卫→执行→后执行→落定）
- 事件总线-府 单测覆盖 通知/并行/串行 + 流水线短路/注销
- 工具循环真实链路回归：`cargo build + test` 全绿 + 检查总线四件套全绿
- 事件流（持久事实源）不受影响：追加/重放 5 测试继续通过

### 14.10 阶段 3 细化 · 角色插件化（2026-08-19 入稿）

> 对齐 Cordis 插件生命周期（docs/cordis-primer.zh.md）：插件 = 对象 + `inject` 依赖声明 + `apply(ctx)` 副作用（可逆注册）。
> 现状：`登记落册.rs` 已有 登记/卸载/缺失模型池（依赖校验），缺 生命周期状态机 + 副作用注册/撤销 + 在途保护。
> 目标：角色册升级为注册器——登记→就绪→生效→卸载 四态，副作用可逆，变更不影响在途任务。

#### 14.10.1 生命周期状态机

```
登记(已登记) → 依赖校验通过 → 就绪(已就绪) → 生效(副作用已注册) → 卸载(副作用已撤销+卡移除)
                   │                                     │
                   └─ 依赖缺失 → 停在 已登记（可补依赖后重试）  └─ 在途任务未清 → 拒绝卸载（软保护）
```

| 态 | Cordis 对位 | 含义 |
|---|---|---|
| 已登记 | 插件已加载 | 角色卡入册，未校验依赖 |
| 已就绪 | inject 激活 | 声明依赖（模型池）可用 |
| 已生效 | apply() 执行 | 副作用（提示词片段/工具 schema）已注册 |
| 已卸载 | teardown | 副作用已撤销，卡移除 |

#### 14.10.2 类型设计（登记落册.rs 升级）

```rust
pub enum 角色状态 { 已登记, 已就绪, 已生效, 已卸载 }

/// 角色副作用：生效时注册、卸载时撤销（可逆，对齐 Cordis disposer）。
pub struct 角色副作用 {
    注册: Box<dyn Fn() -> Result<(), String> + Send + Sync>,
    撤销: Box<dyn Fn() -> Result<(), String> + Send + Sync>,
}

pub struct 登记条目 {
    角色: 执行角色,
    状态: 角色状态,
    副作用: Option<角色副作用>,
}
```

角色册 升级接口：

| 接口 | 说明 |
|---|---|
| `登记(角色)` | 入册，状态=已登记 |
| `就绪(身份, 可用模型池)` | 依赖校验过 → 已就绪；缺失 → 留在 已登记 返回缺失池 |
| `生效(身份, 副作用)` | 注册副作用 → 已生效（重复生效报错） |
| `卸载(身份)` | 在途未清 → 拒绝；否则撤销副作用 → 移除 |
| `在途登记/在途清除/在途数(身份)` | 派发落单 在任务开始/结束时登记（软保护） |
| `取状态(身份)` / `全部条目()` | 观测 |

#### 14.10.3 接入点（阶段 3 后半）

| # | 接入 | 落位 |
|---|---|---|
| 3.3a | 派发落单：任务派遣时 `在途登记`，回执落定时 `在途清除` | 派发落单.rs |
| 3.3b | 角色卡 schema 化：契约 String → 结构化（保留 契约 语义字符串 + 新增 声明们: Vec<String> 可选） | 类型-定义-殿 声明集中.rs |
| 3.3c | 模型 Provider 热替换：模型连接-府 Provider 注册表（当前 trait 已存在，补 register/unregister） | 模型连接-府 模型调用.rs |

#### 14.10.4 阶段 3 门（G4 验收）

- 动态注册/卸载角色不影响在途任务（在途保护测试）
- 生命周期状态机 4 态流转单测（登记→就绪→生效→卸载）
- 副作用注册/撤销可逆（撤销后监听消失）
- 检查总线四件套全绿 + 全 workspace 测试通过

### 14.11 阶段 4 细化 · Profile 装配（2026-08-19 入稿）

> 对齐 dsh profile/bundle 装配（docs/architecture.zh.md §Profile 与组合包）：`--dump-config` 可自检实际启动树，
> 各层可 patch 替换。Rust 静态编译下「替换实现」经 阶段 3 注册表（Provider/角色册/事件监听）表达——
> 阶段 4 不做新机制，做「装配配置 + 自检 + 应用」。

#### 14.11.1 装配配置

`.上下文/装配.json`（默认不存在则按全默认装配）：

```json
{
  "阶段": "乙",
  "启用扩展": ["巡世", "进化", "观测"],
  "模型提供者": "http",
  "角色册路径": "鸿蒙/基础设施 - 域/道术施展-府/角色-卡册-殿/角色-登记-阁/登记-落册-园/角色册.json"
}
```

| 字段 | 说明 | 默认 |
|---|---|---|
| 阶段 | 甲/乙（乙 = 开 巡世/进化） | 乙 |
| 启用扩展 | 巡世/进化/观测 开关（扩展点注册） | 全开 |
| 模型提供者 | 全局提供者注册表 的键 | http |
| 角色册路径 | 角色册 json 落位（阶段 3 全局角色册 装载源） | 默认路径 |

#### 14.11.2 读取与应用

| 接口 | 落位 | 说明 |
|---|---|---|
| `读装配()` | 配置管理-府（lib 根） | 读 装配.json，缺省返回默认装配 |
| `应用装配(装配)` | 天庭治理-府 世界运行.rs | 启动时：按 阶段/启用扩展 决定注册 巡世/进化 监听；按 模型提供者 取 Provider；按 角色册路径 装载 全局角色册 |

#### 14.11.3 装配-检查 命令（对齐 dsh --dump-config）

`号令 装配 检查` 输出：装配配置 + 实际注册状态（府树、Provider 已注册表、角色册条目与生命周期状态、事件总线监听数）。
落位：命令操作-府 观览-查询-殿（读命令，免令牌）。

#### 14.11.4 patch 语义（Rust 静态编译下的对位）

dsh 的 patch 替换（替换某 bundle 的 config）在洪荒的等价物 = **注册表替换**（阶段 3 已提供）：
- 换模型 → `全局提供者注册表().替换("http", 新提供者)`（装配配置 模型提供者 字段指向）
- 换角色卡 → 装载不同 角色册.json（装配配置 角色册路径）
- 换行为 → 事件总线 注册/注销 监听（预执行/后执行/验收/重投 扩展点）
阶段 4 不新增 patch 文件机制，文档化上述等价路径即可。

#### 14.11.5 阶段 4 门（G5 验收）

- `号令 装配 检查` 可输出实际装配树（含 Provider/角色册/事件监听状态）
- 装配配置可切换 模型提供者（http ↔ 模拟）且 应用装配 生效
- 甲阶段装配（无 巡世/进化）可启动（扩展点不注册）
- 检查总线四件套全绿 + 全 workspace 测试通过

### 14.12 阶段 6 细化 · 子系统文档落位（2026-08-19 入稿）

> 界主拍板：阶段 6 只做两件事——① 阶段 2/3 新机制补子系统文档 ② cookbook；阶段 5 砍掉。
> 落位约束（实测发现）：执行器「源码维度白名单」只允许写入含源码的维度目录——传承殿（无源码）拒写，
> 故**子系统文档写进对应府目录内**（府 = 源码维度可写），传承殿 只放总索引（索引由界主/世界经 记忆 写入）。

#### 14.12.1 子系统文档落位

| 文档 | 落位 | 内容 |
|---|---|---|
| 事件总线-府 说明 | `鸿蒙/基础设施 - 域/事件总线-府/事件总线-府-说明.md` | 事件总线/流水线/工具流水线四段/守卫/注销句柄 + dsh 对照 |
| 角色册-注册器 说明 | `道术施展-府/角色-卡册-殿/角色-登记-阁/登记-落册-园/登记落册-说明.md` | 生命周期四态/副作用/在途保护 |
| 装配-配置 说明 | `配置管理-府/装配-配置-殿/装配-配置-说明.md` | 装配.json 字段/装配检查 |
| cookbook 总纲 | `传承殿/落稿留痕-阁/cookbook.md` | 新增府/角色/工具的步骤（世界经 记忆 写入） |

> cookbook 落 传承殿 时同样受白名单限制——由世界经「记忆 回填」（格位写入，非文件工具）落位，或由界主直接录入。

#### 14.12.2 阶段 6 门（G6 验收）

- 事件总线-府 说明文档由**世界真实任务**产出并通过验收（文档存在 + 覆盖四段/守卫/注销/dsh 对照）
- 角色册/装配 说明文档同上（已落盘：`登记落册-说明.md`、`装配-配置-说明.md` 均已存在，2026-08-21 核验）
- cookbook 落位可用（记忆 或 文件二选一）

### 14.13 缺陷修复入稿 · 涉及路径净化支持非 .rs 产物（2026-08-19）

> **发现**（2026-08-19 真实任务实测）：投递「写 事件总线-府 说明文档（.md）」被拒——
> 涉及路径净化规则 ③ 只认 `.rs` 文件或结构目录，`.md` 被剔除 → 涉及路径空 → 落点护栏拒绝。
> 后果：**世界无法处理写文档/配置/脚本类任务**（产物非 .rs）。此缺陷直接阻断阶段 6 文档任务。

#### 14.13.1 修复

| 位置 | 现状 | 修复 |
|---|---|---|
| 净化涉及路径（要求化.rs） | 文件仅认 `.rs` 结尾 | 认**合法产物扩展名表**（.rs/.md/.json/.toml/.yaml/.yml/.txt/.py/.sh/.ps1/.bat/.html/.css/.js），结构目录不变 |
| 兜底提取路径（要求化.rs） | 只扫 `.rs` 片段 | 扫 `.` + 合法扩展名 片段 |
| 测试 | — | 补：.md 路径保留 / 非法扩展名（.xyz）剔除 / 兜底提取 .md |

> 扩展名表为常量集中声明（防漂移）；不覆盖"无扩展名文件"（无扩展名 = 非路径特征，仍按符号名/剔除）。

#### 14.13.2 门

- 写文档任务（.md 产物）重投后可过落点护栏（涉及路径非空）
- 新增测试全绿 + 检查总线全绿

### 14.14 缺陷修复入稿 · 打回撤销保护未存档改动 + 设计打回自动重审（2026-08-19）

> **发现**（2026-08-19 放权测试轮 2）：要求-42（世界自修缺陷 2）执行层误删阶段 2 事件埋点（终裁.rs 25 行），
> 验收打回后**撤销未恢复**——打回撤销的「版本快照兜底」按版本库 v16 快照恢复，而阶段 2 埋点在 v16 之后
> 且未版本存档 → 未存档改动被抹掉（现场已 git checkout 恢复）。
> 根因：`打回撤销产物`（世界运行.rs）在 回滚垫撤销数=0 或失败时回退到「版本库最近快照」恢复——
> 版本快照是定档时点状态，含其后的任何未存档改动都会在恢复时被覆盖。

#### 14.14.1 修复 1 · 打回撤销只走回滚垫，移除版本快照兜底

| 项 | 内容 |
|---|---|
| 原则 | **撤销 = 回到任务写前状态**（回滚垫 = 任务写前备份）；版本快照是定档时点，**永不作为恢复源**（防抹掉 v16 后未存档改动） |
| 改动 | `打回撤销产物`：回滚垫撤销数=0 或失败 → 不再走版本快照；改为逐产物核对「执行-基线」——基线有该文件且当前≠基线（任务期间改过但回滚垫无记录）→ warn 留痕「该文件未走回滚垫，打回未恢复，保留当前盘面」；基线无记录 → 忽略 |
| 副作用 | 任务期间绕过回滚垫的改动可能残留盘面（旧问题）——但残留可后续清理，**绝不抹掉未存档成果**（两害相权取轻） |
| 门 | 新测试：构造"回滚垫无记录 + 版本库有旧快照"场景 → 打回撤销不恢复快照内容、盘面保持当前 |

#### 14.14.2 修复 2 · 设计打回自动重审 + 工作流单值约束

| 项 | 内容 |
|---|---|
| 背景 | 缺陷 3：LLM 设计把 拆解项.工作流 填成多值（`L2_script|L3_program|L4_complex`）→ 机械校验打回 → 「想法 投递」直接失败，无自动重审 |
| 改动 A | 模板生成.rs 模型设计提示：`拆解项.工作流 必须且只能取一个值：L1_qa/L2_script/L3_program/L4_complex，禁止列表/竖线` |
| 改动 B | 验收.rs 设计确认打回 → 若打回原因是「可修的设计缺陷」（工作流非法/拆解空/自评空）→ 回 智慧-主控-殿 自动重审（上限 2 次，附打回明细），而非终止投递 |
| 门 | 新测试：设计含多值工作流 → 打回重审 → 重审后通过（或重试耗尽落失败） |

#### 14.14.3 门（G6.5）

- 检查总线四件套全绿 + 全 workspace 测试通过
- 放权安全性：世界自主任务打回后，未存档改动（如阶段 2 埋点）不被抹掉

### 14.15 缺陷修复入稿 · 涉及路径扩展名表配置化（2026-08-19）

> 界主裁定方案 B：缺陷 1 修复（§14.13 合法文件扩展名表）的硬编码重构为**装配配置声明**（数据驱动）——
> 扩展名表从 `.上下文/装配.json` 的 `合法产物扩展名` 字段读（默认 13 种），新增文件类型不改代码只改配置。
> 依据：设计稿「机制通用化」方向（§4.2 不再写死本项目专名、§6.2 随数据驱动）。

#### 14.15.1 改动

| 位置 | 现状 | 改动 |
|---|---|---|
| 装配配置（配置管理-府 声明集中.rs） | 无此字段 | 加 `合法产物扩展名: Vec<String>`，`#[serde(default = "默认扩展名")]`，Default = 13 种 |
| 读装配 | — | 旧装配.json 无此字段 → serde default 兜底（向前兼容） |
| 要求化.rs（天庭治理-府） | `const 合法文件扩展名` 硬编码 | 删除常量；净化涉及路径/兜底提取 改经 `peizhi_fu::读装配().合法产物扩展名` 读取 |
| 依赖 | tianting_fu 无 peizhi_fu 依赖 | Cargo.toml 加 `peizhi_fu = { path = "../../世界配置 - 域/配置管理-府" }`（无循环：peizhi_fu 不依赖 tianting_fu） |

#### 14.15.2 兼容与性能

- 旧装配.json（无 `合法产物扩展名` 字段）→ serde default 兜底，行为不变
- 读装配 每次解析想法时调用（文件小、频率低，可接受）；不做缓存（界主改配置即生效）

#### 14.15.3 门

- 默认配置下 13 种扩展名行为不变（现有 净化/兜底 测试全过）
- 装配.json 配置自定义扩展名（如 `.vue`）→ 净化放行该类型（新测试）
- 检查总线四件套全绿

### 14.16 缺陷 2 修复 · 产物内容摘要支持非 .rs（2026-08-19 界主亲自修）

> 世界 5 次修复失败（设计模板→并行中断→网络→改文件匹配→打磨空转），且缺陷 2 是验收质量核心件
> （准圣凭字节猜谜，直接影响产品化质量），界主收口亲自修。依据 §14.13 修复方向。

#### 14.16.1 改动（终裁.rs 产物内容摘要 函数内）

| 产物类型 | 摘要提取 |
|---|---|
| .rs（原有） | pub 符号签名 + 测试函数名（不变） |
| .md（新增） | 标题行（`#` 开头章节名，上限 10 条） |
| .json（新增） | 顶层键名（上限 10 个） |
| .toml/.yaml/.yml/.txt（新增） | 截断首 500 字符 |
| 二进制/未知（新增） | 跳过（read_to_string 失败即跳过） |

> **§14.16 修正（2026-08-19 实测）**：`.md` 骨架初版用 `!行.starts_with("##")` 过滤二级标题——
> 实测文档用 `##`/`###` 章节（装配-配置-说明.md），骨架只剩主标题，准圣仍"凭字节猜"。
> 修正：取**所有 `#` 开头行**（主标题+章节+小节，上限 10 条），准圣可核对章节覆盖。

- 扩展名判定经 装配配置.合法产物扩展名（§14.15 数据驱动，排除 .rs）
- 单文件摘要与总摘要上限沿用（20 条/2000 字符）

#### 14.16.2 测试

- 更新 产物内容摘要_非rs与空文件不崩：.md 产物 → 摘要含章节标题；空文件 → 跳过
- 新增：.json 产物 → 摘要含顶层键
- 检查总线四件套全绿

---

*dsh 手法对齐纲领 · 融合蓝图 §14 · 入稿 2026-08-19*

### 14.18 阴 · 旁路大脑设计（2026-08-19 入稿，界主蓝图还原）

> **起源**：洪荒最初设计中的「阴/阳」双脑系统——阳（执行）与阴（旁路）并发，共享一套提示词；
> 阴不执行任务操作（不跑工具循环），专职打杂与推理：整理/写格位/推演/检索，供阳与后续任务使用。
> **现状差距**：权限隔离（阳只读格位）✅ 已落地；机械并发（白嫖等待期）✅ 已有；
> 语义型阴（独立上下文+并发整理+事前推演）❌ 仅碎片存在（补解释/归因/提炼），未成主循环。

#### 14.18.1 阴的完整职责

| 职责 | 说明 | 现状 |
|---|---|---|
| 写格位 | 语义沉淀：教训/机制/理解·记忆 | ✅ 雏形（归因/提炼），缺独立上下文 |
| 补语义 | 依赖图空解释符号补一句 | ✅ 补符号解释（身份=阴） |
| **推演（核心新增）** | **事前影响面**：计划改动路径 → 图谱推演 → 三类边界指令给阳 | ❌ 现状是事后 推演波及 |
| 检索 | 历史提示词/格位检索供下一步 | ❌ 缺 |
| 并发 | 阳干活时阴旁路（白嫖等待期挂语义整理） | ⚠️ 机械型有，语义型缺 |

#### 14.18.2 推演三指令（本节核心）

**输入**：要求书涉及路径（计划改动）→ 依赖图前向（改了什么流出）+ 后向（谁引用被改）+ 联动接线
**输出**：三类边界指令块（注入执行现状，阴生成/机械兜底）：

| 指令 | 语义 | 例子 |
|---|---|---|
| **不能碰** | 涉及路径外但受波及的关键文件——只读参考，禁止写（改了连锁破坏） | 入口.rs/模块.rs 接线、Cargo.toml、被波及的他人文件 |
| **可以** | 涉及路径内目标文件——可写 | 任务要求改的 .rs/.md |
| **不能漏** | 联动文件——改 A 必须检查/同步 B | 模块链接线、调用方（入边）、测试镜像、命令接线文件 |

> 与现有 推演波及（波及追溯.rs）的关系：复用其出边/入边反查逻辑，**前置到计划阶段**，输入从"变更报告"改为"涉及路径"，输出从"波及清单"升级为"三类指令"。

#### 14.18.3 阴的运行形态

- **独立上下文**：元数据层化 3000 字符 + 阳的阶段动作摘要 + 事件流尾巴（不挤占阳的窗口）
- **触发**：挂白嫖等待期（阳 LLM 等待间隙）并发跑；每任务 2-3 次（设计/实现前推演、实现后整理）
- **产出**：推演指令注入下一段执行现状；整理结果写格位（教训/机制/理解·记忆）
- **身份**：观测角色 新增 `阴`（旁路），与 `归因`（世界级维护）区分

#### 14.18.4 门（G6.6）

- 推演三指令：计划改 `甲.rs`（波及乙、被丙引用）→ 指令含「可以：甲」「不能漏：乙/丙」「不能碰：涉及路径外关键文件」
- 阴独立上下文不与阳挤占（各自窗口可测）
- 检查总线四件套全绿 + 全 workspace 测试通过

### 14.19 规则五分类 · 铁律·总纲 分级（2026-08-19 入稿，界主定义）

> **界主定义**：铁律·总纲 格位内规则分**五种级别**；其他规则格位（标准/细则/世界观/传承/环节/例外）**不受影响**。
> **优先级**：临时天道 > 天道项目 > 大道项目 > 天道全局 > 大道全局（临时 > 项目 > 全局）。
> **去重**：不同级别**无重复**——同一条规则只属于一个级别（写入时校验）。

#### 14.19.1 五种级别（界主定义）

| 级别 | 定义 | 例子（界主给） |
|---|---|---|
| **大道全局规则** | 所有世界通用的根本法则（最底层，不动摇） | （未给例，推断：普适铁律） |
| **天道全局规则** | 本世界运行通用的法则 | （未给例，推断：机制级当前生效） |
| **大道项目规则** | 本项目根本法则·全项目通用（宪法级） | **本项目中文 / 维度结构 / 层级规范——全项目通用** |
| **天道项目规则** | 本项目场景/语言生效的法则 | **语言/场景生效的规则**（如当前对话语言约束） |
| **临时天道规则** | 任务级特定规则（一次性，任务结束失效） | 单任务特殊约束 |

> 界主口诀：**大道 = 全局生效（本项目宪法）· 天道 = 语言/场景生效 · 临时天道 = 任务级特定**。

#### 14.19.1a 纯配置化约束（界主裁定，2026-08-19）

**五种级别的规则条文零硬编码**——全部存在 铁律·总纲 格位（界主/鸿钧/LLM 写入），代码只实现机制（分级/注入/优先级/去重/生命周期），不写死任何规则内容。

| 原则 | 内容 |
|---|---|
| 条文来源 | 铁律·总纲 格位记录（带 规则级别 字段），界主/鸿钧录入 |
| 代码职责 | 仅：按级别分层注入 + 冲突优先级 + 去重校验 + 临时天道生命周期 |
| 改规则 | 改格位即生效，不改代码（与 §4.2 规则 7 同理念） |
| 安全硬边界 | **例外**：路径越界/红线守卫 仍是代码硬编码（安全不可配置，防执行层自改守卫）——行为规则可配，安全不可配 |

#### 14.19.2 实现设计

| 项 | 内容 |
|---|---|
| 类型 | 识海承载-府 类型-声明-园 加 `规则级别` 枚举（五档） |
| 记录结构 | 铁律·总纲 的记录加 `级别: 规则级别` 字段（serde default=大道项目，向后兼容旧记录） |
| 写入校验 | 写入铁律·总纲 时须带级别；**去重校验**——同内容规则不得跨级别重复（写入时报错/警告） |
| 注入 | 三档拼装的「固定规则段」（§4.2 规则 7）升级：按级别分层注入，**冲突时 临时>项目>全局**（同题规则取高优先级）；条文全从格位读 |
| 临时天道生命周期 | 任务开始：界主/鸿钧 写入临时天道（带级别）→ 注入该任务背景；任务结束（验收定档/打回终判）：**清理或标记失效**（防止泄漏到后续任务） |
| 其他格位 | 不动（标准/细则/世界观等维持现行为） |

#### 14.19.3 门（G6.7）

- 铁律·总纲 记录可带五级别之一；旧记录（无级别）反序列化兜底为 大道项目
- 注入：同题规则 临时 > 项目 > 全局（去重后只注入一条）
- 临时天道：任务结束清理（测试：写入→任务模拟结束→读不到）
- 检查总线四件套全绿

### 14.20 数据清洗 reducer（纯代码·对齐 Gipp 实战）（2026-08-21 入稿）

> 起源：X 用户 Gipp 实战复盘——40 个 Haiku worker 产出原样堆给 Sonnet，加纯代码 reducer（去重/剔残缺/分组/标矛盾）后 token 砍 87%、成本砍 86%、延迟砍 78%。
> 核心思想：**最贵的模型不该干数据清洗的活，清洗该还给纯代码**。reducer 只做机械判定，不调 LLM。

#### 14.20.1 reducer 四步

| 步 | 语义 | 纯代码实现 |
|---|---|---|
| 去重 | 同内容只留最新 | `HashMap<内容指纹, 记录>` 取最后写入 |
| 剔残缺 | 格式不整/截断/失效剔除 | 结构校验 + 失效标记过滤 |
| 分组 | 按维度分组（实体键/问题类型/府） | `HashMap<分组键, Vec<记录>>` |
| 标矛盾 | 多源矛盾标出供上层聚焦 | 同键不同值的记录对标 |

#### 14.20.2 三个应用点

| # | 应用 | 现状缺陷 | reducer 落位 |
|---|---|---|---|
| 1 | 格位 jsonl 清洗 | 失效记录只读取跳过不物理清理，jsonl 持续增长 | 识海承载-府 新增 `清洗格位()`：去重（同内容留最新）+ 剔失效（物理清理）+ 分组（按实体键留链头）+ 标矛盾 |
| 2 | 图谱陈旧边清理 | 删除文件后依赖图边不清理（无陈旧边清理） | 增量检测识别删除 → 同步清理依赖图该文件的符号和边 |
| 3 | 六准圣产出 reducer | 6 准圣产出原样堆给鸿钧终裁，无清洗 | 终裁.rs 汇合前加纯代码 reducer：去重（重复意见只留一条）+ 剔残缺 + 分组（按问题类型）+ 标矛盾（准圣间矛盾标出让鸿钧聚焦裁决） |

> 应用点 1/2 优先（数据基础设施），应用点 3 后续（需改终裁汇合逻辑，风险较高）。

#### 14.20.3 预算观（哪些活该还给代码）

| 代码干（机械可判定） | LLM 干（需语义理解） |
|---|---|
| 去重、剔残缺、分组、标矛盾、路径归一、格式校验、指纹比对、增量检测 | 推理、决策、设计、审验、归因、提炼 |

> 判据：**机械可判定的归代码，需语义理解的归 LLM**。reducer 不调 LLM、不联网、零 token 成本。

#### 14.20.4 Lost in the Middle（U 形曲线）

斯坦福研究：上下文中间信息易被忽略。注入规约：
- 最重要信息放前（临时天道最前——已做）
- 中间段压缩（历史摘要——已做）
- 末尾放最新上下文（已做）
- **reducer 产出的精简结果放前段，原始多源产出不直接注入**（新增）

#### 14.20.4a 显式规则注入落码（2026-08-21 入稿）

§14.20.3 预算观 与 §14.20.4 U 形曲线 作为**显式可操作规则**落 `细则·解读` 格位（一条记录，`规则级别=大道项目`），经现有注入链生效：

| 注入路径 | 机制 | 生效表现 |
|---|---|---|
| `shihai_fu::注入规则(存储, "细则·解读")`（五处接入） | 读格位链头最新非失效记录注入提示词首行 | 新记录时间戳最新成链头，注入「【可操作规则·细则·解读】{预算观+U形曲线}」 |
| `读审验标准()`（终裁准圣提示词【审验标准清单】段） | 取含「审验标准」的记录 | 新记录不含该字样，仍取原十二轮审验标准清单——审验不破坏 |
| 三档拼装固定规则段 | 读全量记录去重注入 | 审验标准清单 + 预算观 + U 形曲线 共存扩展注入（预算内） |

> 落码方式 1（不改代码）：新增格位记录即扩展注入内容，与 §4.2 规则 7「改格位即生效、不改代码」同理念。记录 `证据` 字段溯源至本节。

#### 14.20.5 门（G6.8）

- 格位 jsonl 清洗：失效记录物理清理 + 同内容去重 + 实体键分组留链头（测试：写入失效+重复记录 → 清洗后 jsonl 只含有效链头）
- 图谱陈旧边清理：删除文件后依赖图不含该文件的符号和边（测试：建图 → 删文件 → 清洗 → 查该文件边为空）
- 六准圣产出 reducer：6 准圣产出经 reducer 后 token 数下降 + 矛盾被标出（后续）
- 检查总线四件套全绿

#### 14.20.6 实现落位（2026-08-21 入稿）

| 应用点 | 落位 | 关键签名 |
|---|---|---|
| 格位 jsonl 清洗 | 模型-落盘-园/模型落盘.rs `清洗格位()` | `fn 清洗格位(&self, 格位名: &str) -> Result<清洗报告, String>` |
| 图谱陈旧边清理 | 类型-声明-园/依赖图.rs `清理文件()` + 增量-检测-园 地道整理 串接 | `fn 清理文件(&mut self, 路径: &str) -> usize` |

**清洗报告**：

| 字段 | 语义 |
|---|---|
| 原条数 | 清洗前 jsonl 总条数 |
| 剔除失效数 | `失效=true` 物理剔除条数 |
| 去重数 | 同内容指纹（内容+证据+实体键）只留最新，剔除的重复条数 |
| 分组留链头数 | 按实体键分组后保留的链头条数（写入 jsonl 的最终条数） |
| 矛盾清单 | 同实体键不同内容指纹的记录对标，供上层聚焦 |

**清洗格位() reducer 四步**：
1. 去重：`HashMap<内容指纹, 记录>`，内容指纹 = (内容, 证据, 实体键)，同指纹取时间戳最新
2. 剔失效：过滤 `失效=true`（物理清理，重写 jsonl 不含失效记录）
3. 分组留链头：按实体键分组，每组留时间戳最新一条（链头 = 实体当前状态）
4. 标矛盾：同实体键不同内容指纹的记录对标，产出矛盾清单（不剔除，只标记供上层）

**清理文件(路径)**：移除该文件的符号档案 + 从其他档案.波及 中移除该文件（入边清理）。返回移除档案条数。
**地道整理 串接**：识别删除后，加载依赖图 → 对每个删除文件调 `清理文件` → 保存。全量重建（任务后扫描）天然排除已删文件，为兜底；地道整理的即时清理缩短「删除→图同步」窗口，避免旧图含陈旧边被查相关文件误召回。

---

### 14.22 抗模型更新设计原则 · 别给模型打工（2026-08-23 入稿）

> **触发**：界主发来 https://mp.weixin.qq.com/s/IWuCOv3bmvnK0r0gzO_LZw
> **冲击**：DeepSeek 上线 V4-Flash-Vision-Exp 多模态模型（单图 384 token、600 图上限、同价）—— 「给纯文本 AI 安眼睛」的桥接插件一夜归零。
> **核心论点**：「**插件是给模型缺陷打的补丁。模型的缺陷是临时的，你打的补丁就是一次性的。**」
> **立场**：本项目设计**不受波及**（18 项主体设计中 15 项完全不受波及，3 处为能力增强方向），但需把「抗模型更新」的设计原则**固化入稿**，作为后续设计的红线。

#### 14.22.1 抗模型更新判据 · 给后续设计的红线

| 反模式（要避） | 正例（本项目应做） | 判据 |
|:--|:--|:--|
| ❌ 给模型做「**记忆桥接**」（绕过模型上下文窗口的中间层） | ✅ 提升**项目级**记忆（识海承载-府的 36 格位 = 项目自己的元信息库，不是模型桥接） | 「这个中间层消失后，**模型自己**会做吗？」会 → 桥接是临时的 |
| ❌ 给模型做「**工具桥接**」（绕过 function calling 的中间层） | ✅ 走标准 function calling + 5 段瀑布（pre-execute → execute → post-execute → finalizeContent → result） | 同上 |
| ❌ 给模型做「**格式桥接**」（绕过 JSON schema 约束的 prompt hack） | ✅ 强类型 schema 约束（zod / Rust struct）+ 工具 schema 约束 | 同上 |
| ❌ 给模型做「**角色桥接**」（一个 prompt 模拟多角色） | ✅ 真多角色（独立上下文 + 独立工具 + 独立权限） | 同上 |
| ❌ 给模型做「**视觉桥接**」（OCR + 截图理解 + 多模态 hack） | ✅ 走多模态模型原生能力（如 V4-Flash-Vision-Exp） | 同上 |
| ❌ 给模型做「**推理桥接**」（Chain-of-Thought 模板化、Tree-of-Thought 工具化） | ✅ 模型原生 reasoning（如 thinking adaptive） | 同上 |

#### 14.22.2 一句话原则

> **「替模型补缺陷的工作一律不接；替模型提供脚手架的工作放心接。」**
>
> 脚手架（认知架构 + 工具循环 + 持久化 + 可观测）是模型能力变强后**自然消化**的——这是好事，因为消化意味着模型升级时你**零代码改动即获新能力**。
>
> 缺陷补丁（视觉桥接、记忆桥接、推理桥接、角色桥接）是模型能力变强后**必然失效**的——这是坏事，因为失效意味着你**几百行封装一夜归零**。

#### 14.22.3 本项目主体设计的抗模型更新能力矩阵

| 设计点 | 是否属于「缺陷补丁」 | 模型升级后行为 |
|:--|:--|:--|
| 九维六层结构 | ❌ 工程结构哲学 | 不变 |
| 多智能体四层组织 | ❌ 组织编排模型 | 不变 |
| 八态状态机 | ❌ 流程控制 | 不变 |
| 36/72 格位 + 六范畴 | ❌ 记忆架构 | 不变 |
| 依赖图（符号档案 + 结构树） | ❌ 工程元信息 | 不变 |
| 回滚垫（事务级撤销） | ❌ 写入安全 | 不变 |
| 白箱可观测（五哨兵域） | ❌ 调试基础设施 | 不变 |
| 失败归因 + 教训沉淀 | ❌ 经验复用 | 不变 |
| 进化环（⑤ 级调整） | ❌ 自我演化 | 不变 |
| 两阶段制（甲/乙） | ❌ 工程节奏 | 不变 |
| 洪荒审判 CLI | ❌ 命名/层级审计 | 不变 |
| Service Definition + 插件注册表 | ⚠️ **半**缺陷补丁（为多 provider 替换设计） | 模型升级 → provider 直接换 → **零代码改动** |
| 10 个工具 | ❌ 手脚工具集 | 不变（写文件/跑命令等仍是必需品） |
| 工具循环 32 轮 / 90 万 token 双轨 | ⚠️ **轻**补丁（应对上下文窗口压力） | 窗口变大 → 轮数可压缩（**收益**而非损失） |
| 会话内压缩三件套 | ⚠️ **轻**补丁（应对上下文窗口压力） | 同上（**收益**而非损失） |
| 派生项目心智模型 | ❌ 认知架构 | 不变 |
| 回滚垫前缀撤销 / 涉及路径护栏 / 沙箱 | ❌ 安全/构建 | 不变 |
| 监控界面-府 | ❌ 可观测 UI | 不变 |

**结论**：18 项中 **15 项（83%）完全不受波及**；3 项为「能力增强方向」（换更好模型即获更强能力，非补丁失效）。

#### 14.22.4 触发器：发现「缺陷补丁」时立刻停

| 触发场景 | 应当的动作 |
|:--|:--|
| 设计文档中出现「让模型能 X」类描述（X = 看图、记忆、推理、规划、调工具...） | **立即警觉**：这是「缺陷补丁」候选 |
| 实现中写「自定义 schema parser 让模型能输出指定格式」 | 同上 |
| 测试中有「通过中间 prompt 让模型实现多角色」 | 同上 |
| 设计稿出现「模型上下文不够怎么办」 | 优先扩项目级记忆（识海承载-府），不为模型做桥接 |

#### 14.22.5 验收门（G6.10）

- **门 G6.10.1**：任何新要求书的设计方案若包含「给模型做桥接」类描述，必须有「**为何不能走原生能力**」的论证（否则打回）
- **门 G6.10.2**：本节列出的 18 项设计点**禁止以「补丁失效」为由推翻**——它们是抗模型更新的护城河
- **门 G6.10.3**：每年大版本（V4 → V5 → V6 等）模型升级后，本节末做一次**设计抗性回检**（由天道巡世触发）

#### 14.22.6 与其他节的关系

- §4「架构骨架」：本节是 §4 的护城河——三府结构能抗模型更新是因为它们不与模型能力耦合
- §14.20「reducer 数据清洗」：reducer 思想本身抗模型更新（纯代码机械判定，不调 LLM）
- §14.18「阴 · 旁路大脑」：阴的设计是组织编排抗模型更新，不是「让阳跑得更快」
- §15「界主外行 + 工程化路线」：工程化（CI / 验证脚本 / 双语 / 测试 / 沙箱）全是抗模型更新的护城河——模型怎么升级都不影响工程基础设施

---

*dsh 手法对齐纲领 · 融合蓝图 §14 · 入稿 2026-08-21*

---

## 十五、界主外行 · 工程化由世界自己造（2026-08-22 入稿）

> **界主原话**：「本项目没有工程化，是因为我根本不会，不知道差哪些，因为我是门外汉，也没有做过项目。」
>
> 这不是缺陷，是**事实**——本项目从一开始就不是「人类工程师在 IDE 里手写」的产物，而是「**世界自己写、自己验收、自己存档**」的实验。所以：
>
> - **工程化的「外行差距」应当由世界自己补齐**，而不是由界主承担——界主只负责发想法与拍板
> - 哪些工程化要补、补到什么程度、补的节奏——**全部以「要求书 → 设计 → 实现 → 验收 → 定档」走主循环**
> - 这一章是**新一类要求书的总源头**——后续 CI、verify-脚本、双语、postmortem、cargo-deny、cookbook、错误码统一化等等，**全部从这一章派生**

### 15.1 界主责任 vs 世界责任（明确分工）

| 维度 | 界主职责 | 世界职责 |
|:--|:--|:--|
| 想什么 | 发想法（业务方向、体验目标） | 接收并入队 |
| 拍什么 | 拍板（拍板=验收定档） | 提请拍板（送达产物+理由） |
| 怎么造 | **不造** | 怎么造（设计 + 实现 + 验收 + 进化） |
| 怎么验 | **不验** | 六准圣验 + 鸿钧终裁 |
| 怎么存档 | **不管** | 增量快照 + 版本记录 + 回退 |
| 怎么工程化 | **不懂、不背** | **CI / lint / test / doc / 双语 / 沙箱 / 错误码 / cookbook / postmortem — 全包** |
| 双语化 | （看不懂英文文档也无所谓，**不要为了界主双语化**） | 双语化是面向**未来 AI 协作者与外部开发者**，不是为界主——世界自己判定哪些文档值得双语，按 §15.4 节奏走 |
| 命名规范 | （看不懂） | 洪荒审判 CLI 守门（七规范自动审计） |
| 依赖治理 | （看不懂 Cargo.toml） | cargo-deny + lockfile 锁定 + deny.toml 配置 |
| 沙箱 | （没用过沙箱） | 命令沙箱护栏（已落地） + 跨平台扩展（按 §15.5 优先级） |

> 关键句：**界主是用户，不是工程师；世界是工程师，不是用户**——两者各司其职，不互相越界。

### 15.2 门外汉协议（界主发想法的最低门槛）

界主不需要懂工程化，但发想法时**必须包含 6 字段**（其他细节由世界反问补全）：

| 字段 | 界主怎么填 | 世界怎么用 |
|:--|:--|:--|
| **想做什么** | 一句话（如「让项目能上 GitHub」） | 拆解为要求书 |
| **为什么** | 一句话（动机：怕丢、给别人看、协作方便） | 排优先级 |
| **何时验收** | 一句话（「我说能用就行」） | 翻译为验收标准 |
| **谁会用** | 谁（自己 / 别人 / AI 协作者） | 决定双语/cookbook/对外发布策略 |
| **遇到问题** | （界主可以什么都不填） | 世界自检+自补 |
| **预算** | （界主可以什么都不填） | 世界按任务规模自定 |

> 协议落地：号令-下达-殿「想法 投递」命令的提示词模板加上这 6 字段填空（其余字段可空——空着就交给世界反问或自补）。

### 15.3 工程化差距清单（按本项目顺序）

> 来源：与 `E:\trae\deepseek-harness` 的质量对比（见 `传承殿/落稿留痕-阁/痕迹追溯-园/与deepseek-harness质量对比-20260822.md`）。
> 本节是差距清单的**正式入稿版**，后续每次工程化要求的提出都必须能溯源到本节。

| 序 | 差距项 | 优先级 | 世界任务书（派生） | 验收门 |
|:--|:--|:--|:--|:--|
| 1 | **0 个 CI workflow** | **P0** | 要求书：「为洪荒世界建立 CI/CD：ci.yml（cargo check --workspace + cargo test --workspace + cargo clippy）、e2e.yml（号令下达 → 主政 → 设计 → 实现 → 验收 → 定档 端到端）、docs.yml（设计稿字数/链接）」 | `.github/workflows/` 下 3 个 yml，PR 触发，cargo 全绿 |
| 2 | **0 个 verify-*/check-*/gen-* 脚本** | **P0** | 要求书：「写 10 个工程化门禁脚本：verify-命名一致 / check-无硬编码绝对路径 / check-无空 catch / check-无 IDE 配置残留 / gen-依赖图可视化 / gen-模块图 / check-文档预算 / verify-AGENTS 一致性 / verify-Cargo.toml 依赖治理 / check-无重复代码」 | `scripts/*.ps1` 或 `scripts/*.sh` 10 个，本地一键跑通 |
| 3 | **0 份双语文档** | **P1** | 要求书：「6 份核心设计稿（层级结构-设计 / 多智能体架构设计 / 项目心智模型-设计稿 / 融合蓝图-设计稿 / 上下文 / 智能体.md）+ README.md + AGENTS.md 做双语 i18n 三件套（*.md + *.zh.md + *.i18n.yaml）；加 verify-translation-pairing 门禁脚本」 | 10 份文档三件套齐全 + 门禁脚本可跑 |
| 4 | **postmortem 模板 = 1 篇（缺陷-记录）** | **P1** | 要求书：「把 `传承殿/落稿留痕-阁/缺陷-记录-20260819.md` 改写为正式 postmortem 模板（Executive summary + 根因 + gate 补丁 + lesson learned），并发 1 篇新 postmortem 走完流程」 | postmortem 模板 + 至少 2 篇 postmortem（含旧一篇重写） |
| 5 | **THIRD_PARTY_NOTICES.md = 缺失** | **P1** | 要求书：「引入 cargo-about 或手维护一份 THIRD_PARTY_NOTICES.md（按依赖逐项 MIT/Apache-2.0/BSD 等），加 verify-third-party-notices 门禁」 | 顶层 `THIRD_PARTY_NOTICES.md` 存在 + 门禁可跑 |
| 6 | **依赖治理 = 无** | **P1** | 要求书：「引入 cargo-deny（`deny.toml` 配置 license/advisory/sources/bans），接 lefthook pre-commit 自动跑」 | `deny.toml` + `cargo deny check` 全绿 |
| 7 | **AGENTS.md 命令清单 = 残** | **P1** | 要求书：「AGENTS.md 加 lint / format / test / verify 命令清单，任何协作者一键跑通（cargo check / cargo test / cargo clippy / cargo fmt / cargo deny / 洪荒审判 CLI / lefthook）」 | `AGENTS.md` 含一键命令段，README 指向 |
| 8 | **测试规模 47 → ≥ 200** | **P2** | 要求书：「每府对外契约固定 + 共享契约测试（八态状态机契约 / 投影契约 / 持久化契约 / 工具循环契约 / 事件流契约 / 回滚垫契约）+ 集成 e2e（号令下达 → 主政 → 设计 → 实现 → 验收 → 定档 跑通示例想法）」 | 全 workspace 测试 ≥ 200 用例，cargo test 全绿 |
| 9 | **错误码 = 字符串** | **P2** | 要求书：「所有 `Err(String)` 改造为 `Err(thiserror::Error + From)`，错误码统一为 `FS_NOT_FOUND / SANDBOX_DENIED / QUEUE_EMPTY` 等稳定 kebab-case 错误码；每个错误码对应中文消息 + 文档文档入口」 | `errors` 模块 + 全部府错误码化 + 文档 |
| 10 | **沙箱 = 仅 Rust 进程内** | **P2** | 要求书：「跨平台沙箱：Linux bwrap 封装（与 dsh 同思路但简化）+ Windows ACL 强化（当前已是 Windows）+ macOS Seatbelt 可选」 | 沙箱-平台-园 / 沙箱-护栏-园 双实现 |
| 11 | **监控界面-府 = 临时只读工具** | **P2** | 要求书：「把监控界面-府升级为正式 UI（非临时只读工具）：含任务列表 / 会话树 / 时间游标 / 6 视图 / 主题切换」 | jiankong_fu 完整实现 + UI 截图 |
| 12 | **release train = 无** | **P2** | 要求书：「release train：tag + 自动 pack（cargo package）+ 自动 changelog（从版本历史生成）+ 顶层 `release.yml`」 | `.github/workflows/release.yml` + tag `vN` 触发自动发布 |
| 13 | **Cookbook = 0 篇** | **P3** | 要求书：「cookbook 体系：adding-a-{府,殿,阁,园,命令,工具,角色,格位} 8 篇，每篇英中双语（仿 dsh 风格）」 | `docs/cookbook/` 8 篇，每篇三件套 |
| 14 | **Hooks 桥接 = 无** | **P3** | 要求书：「hooks 桥：claude-code / codex hook 协议适配（订阅 agent.hook-protocol，写入 session.md 末尾的 hook/invoked / hook/result 事件）」 | hooks-claude-code / hooks-codex 两个府（lib crate） |
| 15 | **Code Mode = 无** | **P3** | 要求书：「Code Mode seam（仿 dsh `ctx.codeRuntime`）：模型写一段代码调工具，dworker 线程隔离执行」 | code-mode-府（lib crate） |
| 16 | **VitePress 文档站 = 无** | **P3** | 要求书：「VitePress 文档站：双语导航 + docs/ 全量投影 + 自动 build deploy」 | docs/ 站点可访问 + 双语导航 |

### 15.4 推进节奏（甲乙阶段各自范围）

| 阶段 | 范围 | 理由 |
|:--|:--|:--|
| **甲（v1 之前）** | 只做 **P0 + P1**（共 7 项）—— CI、verify-脚本、双语、postmortem、THIRD_PARTY_NOTICES、cargo-deny、AGENTS 命令清单 | 甲阶段一切为功能让路；这 7 项是**任何开源项目想上 GitHub 的最低工程门槛** |
| **乙（v1 之后）** | 做 **P2**（共 5 项）—— 测试规模、错误码化、跨平台沙箱、UI 升级、release train | 乙阶段是「自优化期」，界主不提需求世界也持续产出要求流——这 5 项都是「补基础」类别 |
| **未来（v1 之后自选）** | 做 **P3**（共 4 项）—— Cookbook、Hooks 桥、Code Mode、文档站 | 取决于「世界自己进化到什么时候」，不强制时间表 |

### 15.5 自补机制（世界如何自己发现差距）

不需要每次靠对比报告，本项目自带**三种自补触发器**：

| 触发器 | 位置 | 何时提要求 |
|:--|:--|:--|
| **天道巡世**（乙阶段） | `天庭治理-府 / 天道-巡世-阁 / 巡世-扫描-园` | 扫描世界→发现「6 准圣维度红云（业务正确性）/ 镇元子（数据完整性）/ 鲲鹏（性能并发）/ 神农（安全副作用）/ 冥河（异常兼容）/ 轩辕（用户体验）」不合格项 → 自动派生要求书 → 鸿钧主政化要求 → 派发 |
| **失败归因** | `进化-主控-殿 / 进化-归因-阁 / 归因-留痕-园` | 任何任务失败归因留痕后，若「同类失败 ≥ 2 次」或「属于工程基础设施」类别 → 自动派生「补基础」要求书 |
| **鸿钧自主** | `天庭治理-府 / 天庭-主控-殿 / 鸿钧-主政-阁 / 主政-落笔-园` | 鸿钧在主政一轮中自主判断「世界缺乏 X 能力」→ 化为「补基础」要求书（要求类别=补基础，按 `要求来源=鸿钧自主` 标识） |

> 三种触发器都直接产出**符合 §15.3 表结构的要求书**，无需界主发起。界主只管拍板，不需发起。

### 15.6 验收门（G6.9）

- **门 G6.9.1**：本节列出的 16 项差距，**每一项都对应一张要求书**（在 `要求.jsonl` 中可查），状态机走完即视作补齐
- **门 G6.9.2**：每张工程化要求书的验收标准 = 「可一键复现」（任何协作者克隆仓库 → 跑 `bash 一键全验.sh` → 全绿即合格）
- **门 G6.9.3**：本节末尾每月 22 号（入稿日）自动出**差距清单增量报告**（天道巡世触发），由「与deepseek-harness质量对比」文档接续
- **门 G6.9.4**：本节触发的要求书**禁止走界主对话直接拍板**——必须走六准圣验收 + 鸿钧终裁（防界主不懂而误判通过）
- **门 G6.9.5**：本节所有要求书**必须有 lessons learned 段落**——完成后写入 `传承殿/落稿留痕-阁/`，供世界未来回顾

### 15.7 一键全验脚本（要求书产物）

工程化要求书的最终产物 = 一个能跑通的脚本：

```bash
# 一键全验.sh —— 任何协作者克隆仓库后跑这个，全绿即合格
#!/usr/bin/env bash
set -e

echo "=== 1. 编译 ==="
cargo check --workspace --all-targets

echo "=== 2. 测试 ==="
cargo test --workspace

echo "=== 3. Lint ==="
cargo clippy --workspace --all-targets -- -D warnings

echo "=== 4. 格式 ==="
cargo fmt --all -- --check

echo "=== 5. 命名规范 ==="
.trae/skills/洪荒审判/产物/三件套-审判.exe 全量 "d:/洪荒 - 世界"

echo "=== 6. 依赖治理 ==="
cargo deny check

echo "=== 7. 文档预算 ==="
bash scripts/check-文档预算.sh

echo "=== 8. 双语对齐 ==="
bash scripts/verify-translation-pairing.sh

echo "=== 9. 第三方声明 ==="
bash scripts/verify-third-party-notices.sh

echo "=== 10. e2e 端到端 ==="
bash scripts/e2e-号令下达-到-定档.sh

echo ""
echo "全绿 ✓"
```

> 此脚本是工程化的**终极判据**——任何一项不过都算工程化未到位。

### 15.8 界主唯一需要做的事

1. **每月 22 号前后**看一次「差距清单增量报告」（天道巡世自动出）
2. **任何看起来「世界应该会」的工程能力缺失** → 发一张想法（不必懂细节，世界自补）
3. **遇到别人问你项目质量** → 直接说「还在自补中，按 dsh 路线追赶」（不必解释）

> 界主不需要懂 CI、verify-脚本、双语、postmortem——**这些是世界的事，不是界主的事**。

---

*界主外行 · 工程化由世界自己造 · 融合蓝图 §15 · 入稿 2026-08-22 · 界主原话入稿*


