# 维护文档

> **定位**：记录项目当前真实状态、结构、命令与维护指南，供后续维护与续接会话快速对齐。
>
> **状态**：v1.25 · 2026-09-05
>
> **上次全量体检**：2026-09-05（五行引擎全部闭环后）
>
> **本次更新**：2026-09-05 —— 对话视图接真实智能体执行（HTTP 受理台 + 独立事件接口），测试数 104→112。历史变更明细见 [《修订记录.md》](修订记录.md)。

---

## 一、项目定位

「洪荒·世界」——一个"世界观与工程一体两面、五行相生驱动演化"的系统。九根六层骨架承载，五行引擎（太初→量劫→乾坤→道韵→混沌）闭环驱动。

技术栈：**Rust**（workspace，edition 2021）。

## 二、当前状态

**五行引擎全部实现并验收通过** ✅

| 五行 | 属性 | 根 | 引擎 | crate | 状态 |
|------|------|----|----|-------|------|
| 木 | 生长、生发 | 太初 | 任务引擎 | tc-task | ✅ 已验收 |
| 火 | 转化、焚炼、重生 | 量劫 | 迭代引擎 | lj-iteration | ✅ 已验收 |
| 土 | 承载、稳定 | 乾坤 | 记忆引擎 | qk-memory | ✅ 已验收 |
| 金 | 收敛、规范 | 道韵 | 规则引擎 | dy-rule | ✅ 已验收 |
| 水 | 流动、无定形 | 混沌 | 事件引擎 | hd-event | ✅ 已验收 |

鸿蒙基础设施（hm-error / hm-contract / hm-config / hm-log / hm-bootstrap）✅ 已验收。

五行相生联动（hm-signal / hm-signal-bus / hm-linkage）✅ 已验收，五引擎由信号总线闭环。

启动接入 ✅ 已验收：`hm-bootstrap` 并入 `hm-linkage`，启动即装配五行并完成相生闭环自检。

府运行期注入 ✅ 已验收：领域契约（hm-domain-contract）+ 运行时容器（hm-container），装配层 `Arc<dyn Trait>` 解耦，测试路径可注入 mock。

五行相克 ✅ 已验收（相克语义重构后）：相克 = 负反馈约束增长而非删除已有——金克木（待受理任务过盛拒绝创建）、木克土（记忆过盛标注待归档）、水克火（进行中迭代过盛拒绝开启）、火克金（规则过盛拒绝新增）、土克水（事件去重，唯一保留删除语义），均经容量上限注入引擎实现。

LLM 内容生成地基 ✅ 已验收：内容契约（hm-content-contract `内容生成器` trait）+ 内容生成（hm-content `对话生成器`，MiniMax chat/completions，无 key fail-loud）+ 证道 mock 测试（zd-content-test）。

信号总线（三种实现）✅ 已验收：`内存信号总线`（装配层默认，同步分发）、`落盘信号总线`（发布即追加落盘 + JSONL 重放 + 历史）、`异步信号总线`（发布入队 + 后台线程 + `排空`/`停止`）；`信号` 补 serde；证道 zd-signal-test 9 例覆盖三种总线及并发无丢失。

执行器（三工具）✅ 已验收：执行契约（hm-execute-contract `执行器` trait）+ 本地执行器（hm-execute `本地执行器`，工作区沙箱内读写文件/运行命令，拒绝越界路径；命令执行带超时（超时 `taskkill /T` 杀进程树）与输出大小限制（默认 64 KB）、写文件覆盖前自动备份 `.bak`、运行命令前危险命令黑名单拦截）+ 证道 zd-execute-test 10 例（读写一致、越界拒绝、命令输出、命令失败、组件名、超时、输出截断、覆盖备份、危险命令拦截、关键词不误伤）。

自主开发智能体（阶段三）✅ 已验收：内容契约扩展 `工具对话器` trait（function calling 数据模型），对话生成器实现 `工具对话器`（拼装 tools + 解析 tool_calls），自主开发府 hm-agent 实现 `智能体` 循环（LLM 决策→工具调用→执行器执行→结果回填→再决策，最大轮数保护 + 用户中断机制 + 实时日志），执行器超时/输出上限走配置（`executor_timeout_secs`/`executor_max_output_bytes`）、中断走 `中断句柄`，证道 zd-agent-test 11 例（mock 对话器 + mock 执行器，验证最终答复、载荷传递、多轮循环、超轮数/中断错误路径、未知工具/缺参数回填错误并继续循环、参数键中英兼容、事件回调按序发出且内容非空）；沙箱项目真实开发闭环 + 本项目自主开发均已实跑验证（详见修订记录）。

三态认知底座骨架 ✅ 已验收：项目认知-府（hm-cognition）承载「世界态/心智态/过程态」三态数据骨架——世界态 `图谱`（模块/符号/依赖边，`查依赖`/`被谁依赖`/`查符号`）、心智态 `维度` 枚举（内部/外在/规则/执行/目标/经历六主轴 + 内/外判定 + 三对正交）+ `格位`（维度/格位名/摘要/可信度/证据引用/目标内容）+ `心智地图`（按 6 维度组织，维度+格位名唯一，同名拒绝 + 单点修正）+ `目标内容`（初心/现况/愿景/偏移，仅目标维度持有）、过程态 `语境消息`+`过程上下文`（消息角色/追加/消息数）；证道 zd-cognition-test 14 例强断言测试覆盖三态增删查改、维度内外/正交、目标内容归属与序列化往返。本骨架为 MVP 数据层，持久化（写穿落盘）、上下文压缩、crate 扫描器、语言适配器、注入装配（推/流/拉）留待后续迭代。

世界入口前端府 ✅ 已交付：乾坤根「界面呈现-域/世界入口-府」纯手写组件化前端——原生 ES Modules 零依赖零构建，参考 Vue 组件化/响应式存储/hash 路由理念（对应 SFC/Pinia/Router，技术不同理念一致）；域/府/殿/阁/园五层全中文命名、依赖单向（视图层→布局层→运行支撑层）、府根 `入口.js` 层层装配（对应 crate 根 `模块.rs`）；三栏驾驶舱（侧边栏导航 + 主视图 + 属性面板）含对话（真实智能体受理 + 事件回放）、五行引擎（相生环 + 卡片点击详情含相生/被生链）、项目图谱（节点点击结构化解释）、格位地图（6×6 格位点击字段）、运行日志（实时流转）五视图；数据服务层接真实 axum API。前端无 cargo 测试，不进 crate 清单。

数据服务府（hm-http）✅ 已验收：鸿蒙基础能力-域新增「数据服务-府」，axum 同源托管前端静态文件 + 任务/迭代/记忆/规则/事件/认知三态/日志读写 API（GET 只读 11 个 + POST 创建任务/记日志）；日志管理-府新增「运行日志记录器」（内存环形缓冲）；联动装配接入认知三态实例 + 信号转日志 + `装配带持久化`（五引擎启动加载历史数据 + 变更自动落盘）；配置新增 `http.port`（默认 8321）/`persistence.dir`；证道 zd-http-test 16 例强断言。

对话接真实执行 ✅ 已验收：执行契约-府新增「开发执行契约」（`开发事件` 枚举 + `开发事件类型` + `开发执行契约` trait），自主开发-府 hm-agent `智能体` 实现事件回调（思考/工具调用/工具结果/任务答复四类事件，内容截断 200 字符）与 `开发执行契约`（`执行开发任务`/`中断句柄`）；数据服务-府新增「开发执行台」（`装配`/`预留`/`释放`/`启动执行`/`中断`/`记录事件`/`事件增量`/`当前状态`，后台线程执行 + 事件环形流上限 500 + 最近结果，结束含 panic 必清运行态）与「任务受理」编排（受理即建任务入仓库、标题前缀「对话任务: 」，状态联动待受理→进行中→已完成/已取消）；新增 `POST /api/dev/agent`（空 400/运行中 409/未上线 503）、`POST /api/dev/agent/stop`、`GET /api/dev/events?since=N` 三路由；启动入口 `装配开发受理台` 将真实智能体（对话生成器 + 本地执行器）装配到执行台，`run_dev_agent` 语义改为「智能体上线开关」（true 装配 HTTP 受理台，`dev_task` 非空自动受理），LLM key 缺失仅告警不阻断；世界入口前端对话视图删除 8 段 setTimeout 模拟执行流，改为真实 `POST /api/dev/agent` 受理 + `GET /api/dev/events` 500ms 增量轮询 + 「中断执行」按钮。

测试全绿：**112 个测试全部通过**（配置 4、任务 6、迭代 8、记忆 6、规则 6、事件 6、联动 13、内容 3、信号 9、执行 10、智能体 11、认知 14、数据服务 16）。

## 三、九根结构总览

| 根 | 定位 | 域（≤3 硬约束） |
|----|------|----------------|
| 太初 | 木·任务（领域代码） | 任务管理-域 |
| 量劫 | 火·迭代（领域代码） | 迭代管理-域 |
| 乾坤 | 土·记忆（领域代码） | 记忆管理-域、界面呈现-域 |
| 道韵 | 金·规则（领域代码） | 规则管理-域 |
| 混沌 | 水·事件（领域代码） | 事件管理-域 |
| 鸿蒙 | 跨域共享底层 | 基础契约-域、基础能力-域、系统装配-域 |
| 证道 | 测试与门禁 | 基础验证-域 |
| 道果 | 构建物 + 临时物 | （空，.gitignore 全忽略） |
| 传承殿 | 非代码内容 | 已定契约-域、落地设计-域、设计原则-域 |

## 四、crate 清单（35 个）

**领域/基础 crate（22 个）**：

| crate | 路径 | 职责 |
|-------|------|------|
| hm-error | 鸿蒙/基础契约-域/错误定义-府 | Error/Result（thiserror） |
| hm-contract | 鸿蒙/基础契约-域/核心契约-府 | Component/Initializable/Shutdown 契约 |
| hm-signal | 鸿蒙/基础契约-域/信号契约-府 | 信号 + 信号总线 trait |
| hm-domain-contract | 鸿蒙/基础契约-域/领域契约-府 | 五引擎领域契约 trait（泛型解耦领域模型） |
| hm-content-contract | 鸿蒙/基础契约-域/内容契约-府 | 内容生成器 + 工具对话器契约（LLM 内容生成 / function calling） |
| hm-execute-contract | 鸿蒙/基础契约-域/执行契约-府 | 执行器契约（读文件/写文件/运行命令）+ 开发执行契约（开发事件 + 开发执行契约） |
| hm-config | 鸿蒙/基础能力-域/配置管理-府 | 配置解析 |
| hm-log | 鸿蒙/基础能力-域/日志管理-府 | 日志记录 |
| hm-signal-bus | 鸿蒙/基础能力-域/信号总线-府 | 内存/落盘信号总线（同步分发 + 追加落盘） |
| hm-content | 鸿蒙/基础能力-域/内容生成-府 | 对话生成器（MiniMax chat/completions + 工具对话器实现） |
| hm-execute | 鸿蒙/基础能力-域/执行工具-府 | 本地执行器（工作区沙箱读写文件 + 运行命令） |
| hm-cognition | 鸿蒙/基础能力-域/项目认知-府 | 三态认知底座（世界态图谱 + 心智态格位/心智地图 + 过程态语境） |
| hm-http | 鸿蒙/基础能力-域/数据服务-府 | axum 数据服务（同源托管前端 + 五引擎/认知/日志读写 API） |
| hm-bootstrap | 鸿蒙/系统装配-域/启动入口-府 | 启动装配（含自主开发智能体开发入口） |
| hm-linkage | 鸿蒙/系统装配-域/联动装配-府 | 五行相生联动装配（Composition Root） |
| hm-container | 鸿蒙/系统装配-域/组件容器-府 | 运行时组件容器（注册/初始化/关闭） |
| hm-agent | 鸿蒙/系统装配-域/自主开发-府 | 自主开发智能体循环（LLM 决策 + 工具调用 + 执行器） |
| tc-task | 太初/任务管理-域/任务核心-府 | Task 状态机 + TaskStore |
| lj-iteration | 量劫/迭代管理-域/迭代核心-府 | Version + Iteration + IterationLog |
| qk-memory | 乾坤/记忆管理-域/记忆核心-府 | Memory + MemoryStore（持久化） |
| dy-rule | 道韵/规则管理-域/规则核心-府 | Rule + RuleSet（收敛唯一 + 评估） |
| hd-event | 混沌/事件管理-域/事件核心-府 | Event + EventBus（订阅分发 + 持久化） |

**证道测试 crate（13 个）**：zd-config-test、zd-task-test、zd-iteration-test、zd-memory-test、zd-rule-test、zd-event-test、zd-linkage-test、zd-content-test、zd-signal-test、zd-execute-test、zd-agent-test、zd-cognition-test、zd-http-test。

## 五、构建与测试命令

```powershell
# 构建全部
cargo build

# 若 rustc 报 ICE（no entry found for key，Windows 增量编译缓存损坏），清缓存重编译：
cargo clean
# 或禁用增量编译（rustc 1.97 Windows 下增量缓存易损坏，推荐）：
$env:CARGO_INCREMENTAL=0; cargo build

# 测试全部（PowerShell 下 2>&1 会报 NativeCommandError，属误报，以 exit code 与 test result 为准）
cargo test

# 查看依赖树（确认单向无环）
cargo tree
```

## 六、命名与组织规则速查

- **层命名**：域=`4字-域`、府=`4字-府`（含 Cargo.toml）、殿=`2字-2字-殿`、阁=`2字-2字-阁`、园=`名字-类型-园`（园是文件夹）。
- **代码落园**：实现代码只落园层，crate 根是府级 `模块.rs`（`[lib] path` 指向），`#[path]` 层层汇总（连字符转下划线），**全项目无 `src` 目录**。
- **命名唯一性**：同级不重复，下级≠直接上级（域≠府≠殿≠阁≠园）。
- **数量上限**：域 ≤ 3；殿 ≤ 6 阁；阁 ≤ 4 园。
- **规模**：单文件 ≤ 500 行（建议 ≤ 350）；单函数 ≤ 50 行。
- **证道只测府级**：测试全在证道，只测 crate 公共 API，内部无 `#[cfg(test)]`。
- **crate 命名**：`根拼音缩写-英文语义`（鸿蒙=hm、太初=tc、量劫=lj、乾坤=qk、道韵=dy、混沌=hd、证道=zd）。
- **依赖方向**：单向无环；五行引擎只依赖鸿蒙（+ serde/toml），证道依赖对应引擎；hm-linkage 依赖五引擎 + 信号总线（唯一汇聚 Composition Root）。

## 七、修订记录

历史变更与排查的详细记录（含七·一 ~ 七·十三各阶段）已迁至 **[《修订记录.md》](修订记录.md)**，此处只保留结论（见第二节「当前状态」）。

## 八、后续方向（待续）

1. **三态认知底座深化**：三态持久化（写穿落盘）、过程上下文阈值压缩、图谱 crate 扫描器、语言适配器、注入装配（推/流/拉接入 hm-agent）、36 格位本质清单——MVP 数据骨架已就位，待迭代补全。
2. **分布式总线**：异步信号队列与信号持久化已实现（见二节三种总线）；分布式总线仅在出现跨进程/多实例部署前提时再评估，当前单进程内无需求。
3. **内容演化层**：让五行真正产生业务内容（实际任务/记忆/规则数据），从结构闭环走向语义演化。
4. **道果整理**：target 构建缓存定期 `cargo clean`。
5. **世界入口打包桌面**：真实数据闭环已打通，对话已接真实执行，前端数据服务层已接 axum API；剩余 Tauri 2.0 壳打包桌面（前端零改动）。

---

> 文档状态：v1.25
> 创建日期：2026-09-04
