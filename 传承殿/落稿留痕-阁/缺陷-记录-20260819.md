# 真实任务缺陷记录（2026-08-19 · 放权测试）

> 位置修正：本记录由 临时文件夹 迁至 传承殿/落稿留痕-阁（git 跟踪）——
> 2026-08-19 实测：临时文件夹被世界执行层清空（缺陷 6），验收记录丢失，教训 = 重要记录不得放临时区。
> 依据：AGENTS.md 第 9 条（临时文件夹一次性，不入 git）。

## 缺陷 1 · 涉及路径净化只认 .rs（已修 §14.13）

- 现象：投递「写 .md 文档」被拒——`.md` 被净化剔除 → 涉及路径空 → 落点护栏拒绝
- 根因：`要求化.rs` 净化规则只认 `.rs` 文件/结构目录；兜底提取只扫 `.rs`
- 修复：合法产物扩展名表（13 种）常量集中声明 + 净化/兜底共用 + 2 测试
- 状态：✅ 已修（28344ba），重投成功

## 缺陷 2 · 产物内容摘要提取器只支持 .rs（已修 §14.16，界主收口）

- 现象：六准圣验收非 .rs 产物时「内容摘要为空，只能凭字节数审验」
- 根因：`终裁.rs` 摘要提取器只对 .rs 提取 pub 符号/测试名
- 修复：非 .rs 文本产物（.md/.json/.toml/.yaml/.yml/.txt）提取结构骨架（.md 标题行 / .json 顶层键 / 其余截断首 500）；二进制/未知跳过；扩展名判定经 装配配置（§14.15 数据驱动）
- 世界 5 次修复失败（设计模板→并行中断→网络→改文件匹配→打磨空转）→ 界主收口亲自修
- 状态：✅ 已修（6b83a05），71 测试通过（.md 骨架 / .json 顶层键 / 空文件跳过）

## 缺陷 3 · 设计工作流填多值 + 设计打回不重审（已修 §14.14）

- 根因：模板生成.rs 自己的 JSON 示例就用了多值 `L2_script|L3_program|L4_complex`（LLM 照抄）
- 修复：示例改单值 + 显式约束"工作流 必须且只能取一个值"；世界运行.rs 设计打回自动重审（上限 2 次）
- 状态：✅ 已修（852c47c）

## 缺陷 4 · 打回撤销按版本快照恢复会抹掉未存档改动（已修 §14.14）

- 现象：要求-42 执行层误删阶段 2 事件埋点（终裁.rs 25 行），打回撤销未恢复（版本库 v16 快照抹掉 v16 后未存档改动）
- 修复：打回撤销只走回滚垫（任务写前状态），移除版本快照兜底；无记录时核对执行-基线，不一致 warn 留痕保留盘面
- 状态：✅ 已修（852c47c），2 个新契约测试

## 缺陷 5 · 多 CLI 进程并行投递不安全（观察）

- 现象：并行投 3 个 想法（3 个 号令 进程）→ 只有 1 个要求被创建，其余进程竞争卡住
- 分析：状态文件（要求/想法.jsonl）读改写无跨进程锁；cargo build 跨进程无构建锁
- 结论：正确并行 = 世界内部（大罗并发/准圣并行）+ 任务线守护（对话发任务异步消费）；不起多个 CLI 进程
- 状态：⏳ 观察

## 缺陷 6 · 执行层清空临时文件夹（观察）

- 现象：临时文件夹内容被换（验收记录/验证脚本丢失，换成世界采集的文档副本）
- 根因：临时文件夹在扫描排除项内但世界执行层可写/可清；AGENTS.md 第 9 条本就说它一次性
- 教训：重要记录（缺陷/验证）必须放 git 跟踪位置（传承殿），不放临时文件夹
- 状态：✅ 记录迁移完成（本文档即证明）

## 观察点

- 执行层写文档误套"模块接线"规则（被涉及路径守卫拦截，守卫生效实证）
- grep/wc 命令在 Windows 不可用（§9.1 只翻译了 cat/ls）
- 装配 检查 的事件监听字段跨进程恒 0（进程模型事实）

---

## 补充记录（2026-08-19 放权轮 2 收尾）

- **任务 A（缺陷 2 修复）三次失败**：① 设计多值工作流打回（缺陷 3，已修）；② 并行时被中断；③ 串行重投网络故障（os error 10060，MiniMax API 连接超时，非代码问题）——待网络稳定重投
- **任务 B（角色册文档）并行时意外完成产物**：`登记落册-说明.md`（226 行，质量好）已入仓（8611211）——但任务未走完验收流程，产物由界主验收留存
- **新打回撤销逻辑实测**：要求-44 打回后终裁.rs 改动归零（回滚垫恢复写前）✅ 缺陷 4 修复验证生效
- **并行结论落地**：多 CLI 进程并行不可取（缺陷 5）；正确提速 = 网络稳定 + 串行任务 + 世界内部并发

## 补充记录（2026-08-19 放权轮 3 收尾）

- **任务 A（缺陷 2 修复）四次失败**：① 设计多值工作流（缺陷 3，已修）；② 并行中断；③ 网络故障 os error 10060；④ 改文件旧文匹配反复失败（8 轮不收敛）+ 写文件行尾污染——待修复 改文件 匹配与行尾后重投
- **观察点 7（新）**：执行层 写文件/改文件 后文件行尾改变（终裁.rs 1173/1173 纯行尾 diff，git diff --ignore-all-space 归零）——修复方向：写文件工具保持原文件行尾，或 git core.autocrlf 归一
- **观察点 8（新）**：监控界面-府 server.py 被意外修改（删核心函数 读事件流增量 的半成品，21+/93-），来源不明（疑似并行任务期间某进程越界）——已 git checkout 恢复；执行层工具涉及路径守卫对非涉及文件的防护待核查
- **缺陷 4 修复二次实证**：要求-45 打回后终裁.rs 内容与 HEAD 一致（埋点保住），新打回撤销逻辑稳定生效 ✅
- **并行结论最终落地**：跨进程并行（缺陷 5）不可取；世界内部并发 + 任务线守护为正确路径

## 质量检验结论（2026-08-19 · 2 个真实任务）

- **任务 A（装配配置说明文档）✅ 成功**：要求-47 已存档，六准圣通过——缺陷 1（.md 路径）/ 缺陷 2（骨架摘要）修复后首个文档任务成功；并发现观察点 10（骨架过滤 ## 致准圣只拿主标题）已修
- **任务 B（补读取装配测试）❌ 失败**：要求-48 打回——观察点 11（空转检测误判测试迭代），盘面干净（撤销生效）
- **守卫生效实证**：任务 A 中执行层越界写 `命令操作-府-说明.md` 被涉及路径守卫拦截，工作区零污染

## 观察点 11 · 空转检测对"补测试迭代"误判（已修 6be533b）

- 现象：要求-48 打回 `已写入后连续 6 轮无新增文件且未收敛，判定打磨空转`——"写入测试→跑 cargo test→失败→改同一测试文件"的正常迭代被误杀
- 根因：收尾纪律（§4.3 规则 6）判据 = 连续 N 轮无**新增文件**；补测试任务反复修改同一文件是正常迭代，无法与真空转区分
- 修复（6be533b）：判据改为"本轮有写入动作（写/改/删，即使是改同一文件）**或**新增文件"都算实质进展；只有既无写入动作、又无新增文件的纯读/命令/思考轮才累计（防真空转）。同文件反复改写超终止线（7 次）仍是独立防打磨机制，不受影响（补测试迭代通常 3-5 次在警告线内）
- 验证：要求-58（补测试任务）对同一文件连续 2 次 改文件（改→测→再改 迭代）成功通过，产物为规范测试用例 `超时毫秒为0应被拒绝`
- 状态：✅ 已修（6be533b），376 测试全绿

## 改进 · 写文件/改文件空操作优化（34577b0）

- 背景：要求-56 实证——执行层"确认现状"式重写同内容文件仍走一次写工具，浪费轮次 + 产生回滚垫备份/产物记录噪音
- 实现：写文件 目标已存在且内容相同 → 返回"内容未变化"提示不重写（mtime 不变）；改文件 替换结果与原文相同（旧文==新文）→ 跳过改写
- 测试：写文件 内容相同跳过/内容不同正常写入；改文件 替换结果相同跳过（共 4 新测试）
- 验证：要求-59（补测试）正常路径不受影响（改文件一次到位），产物为规范测试 `渲染落盘提示_注入非空规则段`
- 状态：✅ 已落地（34577b0），380 测试全绿

## 缺陷 9 · §14.19 临时天道共享键致旧规则复活注入（已修 75bdb2d）

- 现象：真实任务复验投递任务2（带【任务规则】）后，铁律·总纲 格位链 = [任务1规则(未失效), 任务1失效标记(失效), 任务2规则(未失效)]——键最新=任务2规则=未失效 → 失效键们为空 → **任务1 旧规则重新注入任务2 背景**（清理语义失效）
- 根因：守卫用固定实体键"临时天道"，任务2 新记录覆盖键最新判定，任务1 失效标记失效
- 修复：**每任务独立实体键**（`临时天道-<想法id>`），守卫持有实体键，drop 写同键失效标记；注入层"键最新失效→整键跳过"逻辑不变
- 测试：阶段3 场景（任务1键规则+失效 / 任务2键新规则）验证旧规则不注入、新规则注入
- 状态：✅ 已修（75bdb2d）；任务3 实测独立键 `临时天道-想法-xxx` 生效

## 缺陷 10 · §14.19 注入时机太晚致本任务背景读不到自己规则（已修 ceee85d 后复验）

- 现象：临时天道守卫原在 运行一轮 405 行注入，但执行背景在 383 行已生成——**本任务首屏背景不含自己的任务规则**（违背 §14.19"注入该任务背景"），拆分场景下规则只对第一个子要求生效
- 修复：注入移到 主政一轮 开头（所有背景生成之前），守卫生命周期 = 想法级全部子要求跑完
- 状态：✅ 已修（含在 §14.19 主提交 ceee85d）；真实任务三连投（要求-50/51/52）全通过

## 缺陷 11 · 监控界面 model 源读不到数据（已修 d673b9d）

- 现象：监控界面 SSE 三源中 model 源恒空——server.py 订阅 `临时文件夹/模型流水-观测.log`（旧块格式，23:48 停更），真实模型观测在 `.上下文/观测/记录.jsonl`（jiance_fu 六域，持续写入）
- 修复：model 源改指观测文件；recent/SSE 增量逐行经 `_tx_obs_dispatch` 装配（提示词/回复思考→LLM，工具调用/工具返回→Tool）；`_llm_event` 兼容回复思考域非 JSON（assistant 消息含 think 全文）；`_tool_event` 取 载荷.附加.工具；删除废弃 `_parse_model_log` 与 debug_recent.log 调试残留
- 附带修复：`follow_target_recent` model 分支误传 dict 给 `_tx_obs_dispatch`（内部 json.loads 失败）→ 改传原始行
- 验证：recent 返回 LLM 152+工具 48 条；SSE 首帧推送正常；health OK
- 状态：✅ 已修（d673b9d）

## §14.19 真实任务复验结论（2026-08-19 三连投）

- **要求-50（注释说明）** ✅：临时天道写入（级别=临时天道）→ 任务结束失效标记 → 产物只改目标文件（规则约束生效，无越界）
- **要求-51（注释说明）** ✅：任务2 复验共享键缺陷（旧版号令投递），验证产物落地 + 失效标记
- **要求-52/53/54（注释说明）** ✅：新版独立键生效（`临时天道-想法-xxx`）；无任务规则想法不受影响
- 共 4 个真实任务全部 6 准圣通过；缺陷 9/10 在复验中抓出并修复

## 缺陷 12 · 准圣验收"凭字节猜"（已修 562efd2）

- 现象：观测日志准圣 think 原文 *"I can't directly read the file content... 187 字节增量与一行中文注释体量吻合，判定通过"*——六准圣审验不读产物真实内容，靠字节数+符号签名推断
- 根因：准圣是纯 LLM 调用（无工具循环），提示词虽已注入机械摘要（符号签名/骨架），但无真实内容；提示词"可据此核对实现细节，不必假设无法读取文件"是误导（准圣实际无法读取）
- 修复：新增 `产物原文摘录`——读产物真实文件内容（预算内：单文件 1200 字符/总 3000 字符，长文件头 800+尾 300+中间省略标注），注入准圣与鸿钧终裁提示词；措辞改为"请以真实内容为准核对，勿仅凭字节数/符号推断"
- 测试：真实内容注入（头尾可见+截断标注）/ 空产物与缺失文件不崩占位
- 验证：要求-55 实测准圣 think 引用真实注释（"The top //! comment block contains: 三档 - 拼装 - 园"），不再说 can't read
- 状态：✅ 已修（562efd2），375 测试全绿

## 缺陷 13 · 执行层注释/文档重复堆叠（已修 21e0836）

- 现象：三档拼装.rs 顶部注释被 3 个真实任务各加一行意思相近的说明（固定规则段按级别注入被重复描述 3 遍），最终堆成 5 行说明——执行层不知"该语义顶部已存在"
- 根因：执行提示模板无注释去重约束；任务描述"追加一行说明"时执行层照做不查已有
- 修复：落盘提示模板加 **5.6 条款**——写注释/文档前先 读文件 查目标位置已有语义；同语义已存在则改/合并既有行或跳过，重复堆叠属质量问题触发验收扣分；存量清理（合并顶部 6-8 行）
- 验证：要求-56（任务要求追加与既有注释同语义的说明）执行层 think 引用既有第 6-7 行判断"已存在"，未堆新行，git 零改动
- 状态：✅ 已修（21e0836），375 测试全绿

## 缺陷 14 · 执行层在 doc comment 区插 `//` 普通注释（已修 a86e063）

- 现象：要求-60（核查类任务）执行层在 元数据层化 函数 doc comment（`//!` 区）与 pub fn 之间插入 `//` 普通注释，且语义与既有说明重叠——风格破坏 + 重复（5.6 去重约束未完全治住风格问题）
- 根因：5.6 只约束"同语义去重"，未约束注释风格（doc 区 `//!` vs 普通 `//` 混用）
- 修复：5.6 条款加『注释风格纪律』——doc comment 区只允许 `//!` 行、函数 doc 必须紧跟 pub fn（之间不得插入任何行）、追加 doc 内容必须用 `//!`；清理残留
- 状态：✅ 已修（a86e063）

## 改进 · 空操作写文件不入产物清单（273fe40）

- 背景：要求-56 遗留——空操作跳过虽不写盘，但执行器仍无条件 push 进 写入文件们 → 验收仍显示"产物 1 件"但 git 零改动（产物记录噪音）
- 实现：写文件/改文件 返回 Result<bool>（true=真写入）；执行器按返回值决定是否计入 写入文件们——空操作返回"跳过写入/跳过改写：内容与现状相同（未改盘面）"
- 测试：写文件 内容相同跳过/内容不同正常写入；改文件 替换结果相同跳过
- 状态：✅ 已落地（273fe40），380 测试全绿

## 改进 · 六准圣审验并发化（3571499）

- 背景：六准圣审验原为串行 for 循环 6 次 LLM 调用（每任务验收阶段耗时大户）；世界内部并发是正确提速路径（缺陷 5 结论：跨进程并行不可取，线程级并发可行）
- 实现：`std::thread::scope` 并发 spawn 6 线程；各线程内先 `进入观测(验收, 要求id)`（线程本地上下文，模型调用的 记请求/记回复 按此关联要求id 与角色）；要求书/产物们/模型配置 纯读共享（Sync）
- 验证：要求-57 通过，观测日志 6 条验收提示词 + 12 条回复思考全部关联要求-57，六准圣维度齐全（鲲鹏/镇元子/冥河/红云 等）
- 状态：✅ 已落地（3571499），375 测试全绿

## 缺陷 15 · 空操作假写绕过防打磨终止线（已修 a11b796）

- 现象：要求-61（参数摘要补测试）执行层对同一文件连续 19 次 改文件 但产物 0 件——每次都是"空操作跳过"（写回与现状相同内容），空操作返回 Ok 被误认为写入成功，最终 2 轮未收敛打回
- 根因：`路径写入次数` 只在真写入（push 进 写入文件们）时累加；空操作不 push → 防打磨终止线（7 次）完全失效，19 次假写未被拦截（空操作优化引入的副作用）
- 修复：新增 `尝试写入次数`（写/改工具被调用即计，无论成功还是空操作）；防打磨判定用 尝试数+写入数 之和；`工具结果` 加 `尝试写入们` 字段承载尝试路径；工具循环在调用前解析写/改路径计入尝试
- 验证：要求-62（同款任务）真写成功（产物 1 件，参数摘要补测试规范落盘），对比要求-61 假写 0 产物打回
- 状态：✅ 已修（a11b796），382 测试全绿

## 改进 · 验收耗时实测（c17b4b3）

- 背景：验收.jsonl 的 耗时秒 恒 0（终裁裁决调用处硬编码 0.0），无法量化验收性能（六准圣并发化收益无法对比）
- 实现：终裁调用前记 Instant 起点，结束后回填 `回执.验收.耗时秒` 并 info 留痕
- 验证：要求-62 实测 耗时秒=17.13 秒（非 0）
- 状态：✅ 已落地（c17b4b3）

---

*传承殿/落稿留痕-阁 · 缺陷记录 · 2026-08-19*

*传承殿/落稿留痕-阁 · 缺陷记录 · 2026-08-19*
