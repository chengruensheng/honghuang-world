# 后续实施提示词集（一次性可复制）

> 生成时间：2026-09-07
> 背景：v1.47~v1.61 大量已验证代码未 git 提交；端到端基准脚本已写好但未实跑；文档测试数与版本口径反复不一致；需要转入普通平台闭环。
> 用法：按顺序执行 ①→⑦，每个提示词自包含，可单独复制给任何 AI 执行。前三个是"欠账与事实源"，后四个是"普通平台产品化主线"。
>
> **历史留档补注（2026-09-11）**：本提示词集为 2026-09-07 一次性生成的历史留档。文中涉及「前端测试数 / 前端 / 界面 / 双视图 / 现有前端改动边界」等要求，因 2026-09-11「前端一刀切」重构（前端层整体移除，项目转为纯后端）已不适用；如需复用请统一改为「客户端 / 纯后端」口径。

---

## 一、git 收口（最紧急，需你在场确认后执行）

```
对 "f:\洪荒 - 世界" 做 git 状态审计并给出收口方案。要求：
1. 先运行 git status 和 git log --oneline -20，列出自 v1.46 之后全部未提交改动（按目录/模块归类，标注是否已验证）。
2. 按版本分批给出提交方案，建议提交信息与版本对应，例如 "v1.61 过程事件呈现公共化（396+27 全绿）"。
3. 明确每批包含的文件、提交信息、注意事项（如超大文件、敏感信息、.gitignore 缺失）。
4. 只输出计划和命令，不实际执行 git commit / push，等我确认后再执行。
```

---

## 二、端到端基准实跑 —— 拿到平台是否真的能用的第一手数据

```
验证 "f:\洪荒 - 世界" 的端到端基准脚本实际可用。要求：
1. 找到 "传承殿/端到端基准.ps1" 并用 read_file 完整读懂它：它启动什么、依赖什么环境变量、输出哪三个指标（无人干预率/测试通过率/返工率）、在哪读 LLM 配置。
2. 检查前置条件：后端如何启动、LLM 配置从哪读、需要哪些 API key。
3. 不要真跑（可能消耗额度），给出"要真实执行它需要用户提供什么 + 执行步骤 + 每步预期输出"。
4. 指出脚本当前会卡住或失败的已知问题，并给出最小修复。
```

---

## 三、版本事实源 —— 治本"文档测试数反复不一致"

```
在 "f:\洪荒 - 世界" 建立自动生成的发布事实源，解决文档里版本/测试数/门禁结果手工改来改去的问题。要求：
1. 设计一个单一版本事实源（建议一个数据文件或脚本），自动输出：版本号、git commit、分支、构建时间、Rust 测试数、前端测试数、门禁结果、E2E 三指标、遗留项。
2. 前端和文档改为读取同一事实源，不手工抄写。
3. 给出方案：涉及哪些现有文件、新增哪些、每步验收标准。
4. 只做方案与最小实现，不提交 git。
```

---

## 四、人机验收闭环

```
在 "f:\洪荒 - 世界" 建立自动验证与人工业务验收分离的闭环。要求：
1. 定义验收状态机：待自动验证 → 自动验证中 → 自动验证通过 → 待人工验收 → 人工验收通过 / 人工驳回 / 重新设计 / 重新实现 / 已交付。
2. 用户驳回时不是重来，而是选择原因（需求不清/设计不符/实现错误/测试不足/交互不符/产出不完整/扩大范围/缩小范围），并定向回到正确阶段。
3. 结合现有 v1.57 交付验收包，把验收结果持久化。
4. 说明改动文件清单、是否影响现有 API/前端、验收标准。
```

---

## 五、失败恢复操作化

```
在 "f:\洪荒 - 世界" 把"失败恢复"做成普通用户能操作的能力。要求：
1. 结合阶段2定向回退与阶段3依赖召回，梳理失败恢复的完整用户路径：看到失败 → 了解原因 → 选择恢复方式（仅回退当前/回退并召回下游/仅标记/取消）。
2. 在现有过程工作台上把回退/召回做成可见操作（显示原因、追溯链、影响范围、回退前后差异、下游阻塞状态）。
3. 构造 A→B→C 依赖链场景作为验收：A 回退后界面能看到 B/C 为何被召回、何时恢复。
4. 给改动清单与验收标准，不提交 git。
```

---

## 六、项目化 / 工作区化

```
把 "f:\洪荒 - 世界" 从单一工程控制台升级为可承载多项目的平台。要求：
1. 新增项目列表、工作区创建/选择、仓库连接、分支变更集、项目级任务搜索/筛选、历史交付与知识沉淀。
2. 设计"陌生用户第一次使用"路径：启动 → 创建项目 → 选工作区 → 连仓库 → 输任务 → 看过程 → 看 diff → 看测试 → 验收 → 交付，全程不要求先理解五行/道祖。
3. 评估现有前端/后端改动边界，给出最小可落地的步骤。
4. 给改动清单、验收标准，不提交 git。
```

---

## 七、世界观增强层（最后做，不挡普通用户）

```
在 "f:\洪荒 - 世界" 把世界观做成"高级审计层"，与普通视图共享同一份真实数据。要求：
1. 同一任务提供普通视图（需求分析/实现中/测试失败/等待验收/已交付）与洪荒视图（太初·任务生发/量劫·迭代修正/道韵·规则约束/混沌·事件传播/乾坤·记忆沉淀）。
2. 每个世界观概念必须可追溯到：概念 → 契约 → 代码实现 → API/信号 → 运行事件 → 文件产物 → 测试证据，禁止静态说明与真实行为漂移。
3. 给出双视图实现方案与映射表，说明何时用洪荒视图。
4. 列改动清单与验收标准，不提交 git。
```

---

## 执行顺序

```text
① git 收口（今天，需你在场）
② 端到端基准实跑（拿三指标）
③ 版本事实源（治本）
── 以上地基，以下产品主线 ──
④ 人机验收闭环
⑤ 失败恢复操作化
⑥ 项目化/工作区化
⑦ 世界观增强层
```
