# 治理量劫 v1.55 · P0 执行提示词（开工批次）

> 前置说明：本批提示词基于 2026-09-07 代码取证生成。开工前必须先跑「状态取证」，确认哪些已落地，严禁重复实现。
> 用途：T1/T2/T3 逐任务复制粘贴给 AI 助手执行，或发布到看板。

## 〇、开工前状态取证（先跑，10 分钟）

```powershell
cd "F:\洪荒 - 世界"
git log --oneline -3          # 预期：仍停在 e8a3437 v1.46
git status --short | Measure-Object -Line   # 预期：约 78 个未提交文件
Get-Content "传承殿\维护文档.md" -Encoding UTF8 | Select-Object -First 6   # 预期：状态 v1.55
cargo test --workspace --no-fail-fast 2>&1 | Select-String "test result"   # 预期：318 全绿
```

取证结论速查表：

| 治理项 | 当前状态（2026-09-07 实测） |
|---|---|
| T1 git 断链止血 | 🔴 未做：git 停在 v1.46，78 个文件未提交（v1.47~v1.55 共 9 版） |
| T2a 回喂重试（LLM 解析失败回喂修正，上限 2） | ✅ 已落地：驱动模块.rs 解析并构造带重试；测试 驱动测试.rs:405 |
| T2b deny_unknown_fields + 必填 | ✅ 已落地：设计/实现/验收文档.rs |
| T2c response_format: json_object | 🔴 未做：池模块.rs 请求体无此字段（0 命中） |
| T3 文档测试数对齐 | ✅ 已完成：维护文档 318 = 实测 318 |
| pre-commit hook / CI | 🔴 未做 |

---

## T1 · git 断链止血（P0 第一刀，30 分钟）

【背景】最后提交 e8a3437 v1.46（2026-09-06 12:28），此后 v1.47~v1.55 共 9 版、78 个文件只存在于工作区：+2473/−2253 修改、19+ 未跟踪、无远程仓库、无 CI。单机单副本，磁盘故障即全损。

【目标】九版成果一次归档提交 + 远程备份 + 提交纪律落地。

【执行步骤】
1. 清理暂存区外的构建产物：
   - 检查 .gitignore 是否已排除 `target/`、`测试结果*.txt`（若本会话生成的 测试结果.txt/测试结果2.txt 出现在未跟踪列表，删掉或加入 .gitignore）。
   - 关键安全核对：`git status --short` 输出中**绝不允许出现 .env / .env.local**（已确认在 .gitignore，仍须复核一次）。
2. 一次性归档提交：
   - `git add -A`
   - `git commit -m "v1.47~v1.55 治理量劫·止血归档：道祖接待/认知0A-0B-0C/错误处理/会话回放/开箱即用/端到端打通/Resume-Fork/测试回补318全绿"`
3. 建私有远程并推送（任选）：Gitee 私有仓库 或 自建 Git 服务；`git remote add origin <地址> && git push -u origin master`（分支名以现状为准）。
4. 整目录备份：将 `F:\洪荒 - 世界` 完整复制一份到第二块盘/外部介质（排除 target/ 以省空间，含 .git）。
5. 提交纪律落地：pre-commit hook 挂门禁——
   - 门禁脚本位置：`.codeartsdoer\skills\ai-defect-governance\scripts\detect_all.ps1`（13 条 detect_*.ps1 的汇总入口）。
   - 新建 `.git\hooks\pre-commit`（PowerShell 启动器）：提交前运行 detect_all.ps1，任一检测失败则 `exit 1` 拒绝提交。
   - 若 detect_all.ps1 存在会改工作区文件的问题，先核实其行为再挂（只读检测才可进 hook）。
6. 建立「版本即提交」约定：写入《维护文档》——每 vX.Y 与维护文档/修订记录同 commit；文档状态行更新后必须当日提交。

【验收标准】
- [ ] `git log --oneline` 出现归档提交，v1.46 与 v1.55 之间无断档
- [ ] `git status --short` 为空（或仅剩被忽略的产物）
- [ ] `git push` 成功，远程可 clone 验证
- [ ] 备份目录存在且可打开
- [ ] pre-commit 生效验证：故意制造一次违规（如提交含绝对路径的文件）被 hook 拒绝，随后恢复
- [ ] 维护文档新增「版本即提交」约定条目

【禁止事项】
- 禁止把 .env/.env.local/密钥文件加入暂存（提交前必须肉眼核对 status）
- 禁止修改任何源码文件来完成本任务（纯 git 与脚本操作）
- 禁止使用 `git push -f` 覆盖远程
- 禁止删除历史提交或改写已有 commit

【完成定义】验收 6 项全过 + 修订记录新增「v1.55·git 止血」条目。

---

## T2 · LLM 结构化输出收尾（P0 第二刀，半天）

【背景】回喂重试（解析失败→错误原文回喂→上限 2 次）与 deny_unknown_fields 已落地（驱动模块.rs 解析并构造带重试、设计/实现/验收文档.rs）。唯一缺口：请求侧未开启 json_object 模式，部分供应商的 JSON 输出稳定性未利用。

【目标】补上 response_format 能力（默认关闭、按供应商能力开启），不改已落地的重试逻辑。

【执行步骤】
1. 定位：`鸿蒙\基础能力-域\内容生成-府\生成实现-殿\对话生成-阁\池-模块-园\池模块.rs` —— `impl 内容生成器 for LLM池` 的 `生成` 与 `impl 工具对话器 for LLM池` 的 `对话` 中，`造体` 闭包构造请求体 `json!({ "model": ..., "messages": ... })`。
2. 改造：
   - 在 池内供应商 结构新增字段 `启用json模式: bool`（默认 false；配置解析处可从 `[llm.供应商]` 增加可选键读取，缺省 false，向后兼容）。
   - `造体` 闭包中：`if 供应商.启用json模式 { body["response_format"] = json!({"type": "json_object"}) }`。
   - **只对确认支持的供应商开启**（OpenAI 兼容 `/chat/completions` 支持 json_object；其他如 Anthropic/自建网关以文档为准），默认一律 false；tools 模式与 json_object 冲突的供应商禁止同时开启（需在配置注释中写明该限制）。
3. 测试：
   - 新增 1-2 个测试：启用 json_mode 的供应商构造请求体含 `response_format`；未启用的不含。
   - 确认既有 `驱动测试.rs:405 解析失败回喂重试成功` 继续通过（重试链路未被破坏）。
4. 文档：维护文档 LLM 池一节补充 json_mode 配置说明与供应商支持矩阵。

【验收标准】
- [ ] 池模块.rs 新增字段与请求体注入，默认行为零变化（未开启供应商请求体与之前逐字节一致）
- [ ] 新测试绿 + 全量 `cargo test --workspace --no-fail-fast` EXIT=0（318）
- [ ] 配置示例（default.toml）含 json_mode 注释说明
- [ ] 维护文档更新

【禁止事项】
- 禁止删除或改写已落地的 解析并构造带重试 与 deny_unknown_fields
- 禁止对未知支持的供应商强行开启 json_object
- 禁止改动 38 个 API 端点契约

【完成定义】验收 4 项全过 + 修订记录留痕。

---

## T3 · 文档测试数对齐（已完成，仅验证）

【现状】维护文档 v1.55 已声明「318 个测试全部通过（配置 7、任务 25、迭代 8、记忆 6、规则 6、事件 6、联动 16、内容 28、信号 9、执行 25、智能体 46、认知 66、数据服务 70）」，与 2026-09-07 实测 318 一致。本项无代码改动。

【验证动作（1 分钟）】跑 `cargo test --workspace --no-fail-fast`，核对 `test result` 求和 = 318 且分项与文档一致。一致即完成；不一致则修正文档数字（以实测为准），禁止为凑数添加无意义测试。

【验收】文档数字 = 实测数字。

---

## 附：完成后的收尾（T1~T3 全过后执行）

1. 更新《维护文档》状态行：v1.55 → v1.56，记录 T1 归档提交、T2 json_mode、T3 验证结论。
2. 更新《修订记录》：新增「v1.56 · 治理量劫·P0 止血完成」条目。
3. 提交本次全部文档与代码（遵守「版本即提交」）。
4. 输出一句状态回执：git 连续、318 全绿、远程可拉取、json_mode 可配。
